# Changelog

Формат: одна секция на релиз, изменения сгруппированы по смыслу.
Подробные пользовательские заметки — в [docs/release_notes/](docs/release_notes/).

Ломающие изменения помечены **!** — они меняют формат данных или поведение,
на которое можно было полагаться.

## Unreleased

Черновик пользовательских заметок — [docs/release_notes/upcoming.md](docs/release_notes/upcoming.md).

## v2.0.0

Подробные заметки: [docs/release_notes/2-0-0.md](docs/release_notes/2-0-0.md).

### Возможности (6)

- feat(contract)!: SPEC 133 — registry-driven link mapper (`core/config/linkmap`): 14 схем ссылок, Xray JSON, wg-quick `.conf` и эмит share-ссылок идут по таблицам `contract/registry`; 32 рукописных парсера/эмиттера сняты; `source_kinds` — виды источника таблицей; контракт 1.1.46, корпус 1 448 кейсов, 84 кода предупреждений
- feat(config): страховка «ядро отвергло узел» (SPEC 132) — негодный узел отключается с причиной, конфиг пересобирается, VPN поднимается без него; локально, в демоне, на шаге Final, при записи конфига удалённой машины; баннер не восстанавливается после перезапуска
- feat(config): предупреждения узла с именованными кодами — `uri_param_unknown`, `json_field_unknown`, `password_empty` вместо молчаливой потери полей
- feat(ui): фильтр списка Servers — regex по имени, протокол, транспорт, источник, порог пинга
- feat(ui): окно Core показывает статус Tailscale tailnet при работающем ядре
- feat(config): порт Shadowsocks по умолчанию 8388 (был 443), канон схемы

### Исправления (15)

- fix(config): hysteria2 `port+mport` — `merge: append` исполняется, диапазоны портов не теряются (`eb090912`)
- fix(config): двойная base64-обёртка подписки читается, а не даёт ноль узлов (`eb090912`)
- fix(config): `type=splithttp` в ссылке сохраняет транспорт XHTTP
- fix(config): tuic с пустым паролем — узел + предупреждение `password_empty`
- fix(config): share-ссылки — `security=tls` явно, `allowInsecure`, пробел как `%20`, keep-alive и поля xhttp в ссылке
- fix(config): Xray-подписки с `dialerProxy` → `fragment` через freedom — узел сохраняется с `tls.fragment` (`9e376607`, `48b33781`)
- fix(config): негодный ключ REALITY в std-base64 и негодный элемент списка портов не роняют весь конфиг
- fix(config): PSK WireGuard в альтернативном написании, комментарии и «+» в wg-quick `.conf`
- fix(config): форма обфускации AmneziaWG не теряет type узла (`fe5c0ecb`)
- fix(daemon): предел попыток перезапуска ядра у демона срабатывает (`dfed276c`)
- fix(linkmap): гонка на кэше таблиц значений — `sync.Once` (`02691a94`)
- fix(core): пин sing-box-lx 1.14.1-lx.8 — REALITY short_id, tuic.udp_relay_mode, MASQUE standard, gRPC service_name с `/`
- fix(grpc): абсолютный путь `serviceName` доезжает до сервера (#130)
- fix(vmess): `aes-128-ctr` убран, `aes-128-cfb` принимается
- fix(windows): иконка трея снова полноцветная (#83)

### Прочее (4)

- chore(contract): 43 бампа контракта с 1.0.4 до 1.1.46
- chore(ci): страж Win7 (`tools/win7guard`), codecov v5, ubuntu-24.04
- docs: `docs/TROUBLESHOOTING.md`, отчёт кампании `SPECS/133-F-N-REGISTRY_DRIVEN_LINK_MAPPER/REPORT_2_0_0.html`
- refactor(config): мёртвый легаси после linkmap снят (`uri_params.go`, `node_ref.go`, `detour_topo.go` и др.)

## v1.6.3

Подробные заметки: [docs/release_notes/1-6-3.md](docs/release_notes/1-6-3.md).

### Исправления (2)

- fix(config): эмиттер TLS собран по allowlist полей `OutboundTLSOptions` ядра — импорт sing-box JSON (массив outbound'ов, целый конфиг, подписка) больше не теряет `tls.certificate` у `naive` и `alpn`/пины `certificate_public_key_sha256`/`min_version`/`max_version`/`cipher_suites`/`client_*`/`fragment` у любых схем; встроенный эмиттер знал семь ключей и падал на `[]interface{}` из JSON там, где ждал `[]string`. Узлы, добавленные одним объектом JSON (EmitRaw), потерей не задевало; корпус `body/singbox/outbound_array_tls_fields`, `registry/tls.json` policy.emit_allowlist, TASKS_LXBOX §22 (LxBox #140, `189bdd4f`)
- fix(config): у `naive` эмитятся только `enabled`/`server_name`/`certificate`/`certificate_path` — ядро naive валит фатальной ошибкой весь конфиг на любом другом ключе TLS, так что один такой узел не давал ядру стартовать; `certificate_public_key_sha256` оно молча игнорирует (`7f2feafe`)

### Основное (1)

- feat(config): `key_share` в ссылках REALITY — параметр `key_share=hybrid|classical` у `vless://` и `anytls://` рядом с `pbk`/`sid` (имя ключа sing-box как есть, D-121, контракт 1.0.4). `classical` нужен REALITY-серверам Xray **старше** v26.9.8: они рвут соединение на гибридном ClientHello, и до сих пор такой сервер можно было подключить только правкой JSON-тела. Значение читается только у настоящего REALITY-узла (валидный `pbk`), мусор снимает ПОЛЕ а не узел (код `reality_key_share_invalid`, единый для ссылки и тела — прежде тело чистило ключ молча), а на ядре старше 1.14.1-lx.4 поле не эмитится вовсе: ключ ему неизвестен, и узел остался бы вообще без конфига

### Прочее (1)

- chore(core): пин ядра sing-box-lx 1.14.1-lx.4 — `tls.reality.key_share` (SPEC 089 ядра: `hybrid` / `classical`, пусто = как несёт отпечаток) и SPEC 088: `fragment`/`record_fragment` из шаблона (флаги `tls_fragment`/`tls_record_fragment`, `core/build/tls_transforms.go`) теперь доходят и до REALITY-узлов — прежние ядра их там молча игнорировали, так что у REALITY-узлов с включённым фрагментированием поведение на проводе меняется

## v1.6.2

Подробные заметки: [docs/release_notes/1-6-2.md](docs/release_notes/1-6-2.md).

### Исправления (3)

- fix(reality): `firefox` и `safari` больше не получают `reality_fp_not_chrome` — у ядра sing-box-lx ≥ 1.14.1-lx.3 их спеки (Firefox 148, Safari 26.3) несут key_share X25519MLKEM768 (решение владельца 16.09.2026); подсказка остаётся у ios/android/edge/360/qq; реестры warnings/tls, корпус vless, Protocols.md, TASKS_LXBOX §20.3 (`a50117f7`)
- fix(wireguard): `awg://<base64 .conf>#label` — вторая форма ссылки (панели с AmneziaWG 3.1) распознаётся и сводится к конвертеру .conf вместо потери узла как «missing private key»; кейсы `uri/wireguard/awg_conf_base64*`, note `protocols/wireguard.json`, TASKS_LXBOX §20 (`e6aad5a2`)
- fix(traffic): поллер Traffic Profiler в classic-режиме не бьётся в Clash API остановленного ядра и не пишет WARN `connection refused` раз в секунду — ждёт запущенное ядро, одно предупреждение на сбой и одно при восстановлении (`e6aad5a2`)

### Прочее (1)

- chore(core): пин ядра sing-box-lx 1.14.1-lx.3 — REALITY fp=firefox/safari с X25519MLKEM768 на Xray ≥ v26.9.8, хендшейк VLESS encryption не виснет (SPEC 086/087 ядра)

## v1.6.1

Подробные заметки: [docs/release_notes/1-6-1.md](docs/release_notes/1-6-1.md).

### Исправления (2)

- fix(reality): явный uTLS-отпечаток REALITY-узла больше не подменяется на chrome при сборке (D-119, заменяет D-104; подмена жила с v1.5.6) — отпечаток выбирает подписка (решение владельца), подмена отменяла её выбор, и узел с `fp=firefox` у провайдера не подключался; `chrome` пишется только вместо пустого и неявного `random`, uTLS у reality включается всегда, `reality_fp_not_chrome` стал мягкой подсказкой; контракт 1.0.3, кейс `uri/vless/reality_fp_firefox_kept`
- fix(build): `domain_resolver` DNS-сервера на выпавший сервер, когда единственная замена — сам этот сервер, берёт следующий пригодный сервер (не fakeip/hosts), а не снимается (SPEC 129, сверено с LxBox)

### Прочее (2)

- chore(dependabot): не предлагать `golang.org/x/sys` 0.48+ до перехода на Go 1.26 (PR #123)
- docs: парная версия LxBox — 2.24.0 вместо 2.23.3 (решение владельца), в заметках 1.6.0, контракте и спеках

## v1.6.0

Подробные заметки: [docs/release_notes/1-6-0.md](docs/release_notes/1-6-0.md).

### Возможности (6)

- feat(state)!: SPEC 127 волна 1 — схема состояния v8, одно пространство имён записей: у правила маршрута `num`/`name`/`refs[]`/`vars` на записи, а `body` = правило sing-box как есть (матчеры + `outbound`|`action`); у DNS-серверов и DNS-правил тело в `body`; корень `dns`/`warp` вместо `dns_options`/`warp_accounts`. Миграция v7→v8 по сырому документу при первой загрузке с копией `state.json.v7.bak`, `config.json` байт-в-байт прежний; `GET /state/full` и `PATCH /state/rules|dns` — в форме v8. Лаунчер 1.5.x файл v8 правильно не читает — откат только из `.v7.bak` (`bfd5fe15`)

- feat(backup)!: SPEC 127 волна 2 — формат бэкапа 1.0 (`lx_backup: 2`) = сериализация состояния v8: секции узлов, папки с настройками и составом, хопы цепочек в папку, идентификация подписки. Формат 1.0 — единственный формат экспорта: писатель 0.12 и выбор формата (константа дефолта, чекбокс в диалоге экспорта) сняты до релиза решением владельца (D-110), LxBox читает и пишет 1.0 с 2.24.0 (выходит после лаунчера; до его выхода файл 1.0 с десктопа в LxBox 2.23.2 не импортируется, обратное направление работает); импорт читает оба формата и сливает их одним кодом (BACKUP.md §9), запись чужого вида в секциях узла отбрасывается с `backup_section_record_dropped`. Debug API: `GET /backup/formats` (`writes: ["1.0"]`), `GET /backup/export` (`?format=0.12` → 400), `POST /backup/import` и зеркала `/remote/machines/{id}/backup/*` (`768ef591`)

- feat(contract)!: SPEC 127 волна 3 — контракт 1.0: `backup.schema.json` на `lx_backup: 2`, прежняя схема заморожена как `backup-0.12.schema.json`; BACKUP.md (§11 — что изменилось против 0.12), NODE_SECTIONS.md и ONE_NAMESPACE.md — норма; корпус `v10_*`; TASKS_LXBOX ## 16, D-109; `contract/VERSION` поднят до 1.0.0, когда LxBox 2.24.0 стал читать и писать 1.0 (D-110) (`05d4f35f`)

- feat(tailscale): жизненный цикл каталога состояния `bin/tailscale/<имя>` — удаляется вместе с узлом или папкой, переименовывается при смене финального тега (переименование, перенос, смена tag_policy), осиротевшие каталоги чистятся при сборке по всем хранимым узлам Tailscale, включая выключенные и снятые гейтом; явный `state_directory` не трогается, в бэкап каталог не едет (SPEC 122 §2.7) (`93ba81a0`)

- feat(tailscale): каноническая связка узла tailnet (NODE_SECTIONS.md §6) — одно правило маршрута с `domain_suffix [".ts.net"]` и `ip_cidr ["100.64.0.0/10","fd7a:115c:a1e0::/48"]` (при FakeIP один `ip_cidr` имена не матчил); голый узел получает связку по умолчанию на путях рождения узла, снимается она документом без `dns`/`route` на вкладке JSON; в многоузловом конфиге узел `tailscale` забирает записи по ссылке на свой тег; узел из подписки остаётся без связки с info `tailscale_from_subscription` (`ad6ccfa6`)

- feat(dns,backup)!: SPEC 129 — значения переменных шаблонного DNS-сервера живут в записи `dns.servers[kind=template].vars` (локальные имена объявления: `outbound`, `dns_ip`, `safe_profile`, `dom_resolver`), у лаунчера и LxBox одна форма; корневые `dns_<tag>_<var>` не пишутся, а читаются терпимо — загрузка, сборка, экспорт и импорт переносят их в запись (самый длинный тег), `config.json` байт-в-байт прежний. Нормы записи одни для DNS-сервера и `rules[kind=preset].vars`: только объявленные имена, значение подрезано, **равное умолчанию шаблона не пишется** (выбор умолчания в окне сервера и в строке пресета — сброс). Бэкап везёт все значения сервера, не только канал (`dns_ip = 8.8.4.4` больше не теряется); импорт накладывает `vars` совпавшей записи по именам, запись с тегом, которого шаблон не объявил, не ввозит (`backup_dns_entry_skipped`), сервер с неизвестным каналом выключает (`backup_unknown_outbound {server}`); переименование Направления переписывает и канал шаблонного DNS-сервера (D-118)

### Исправления (25)

- fix(template): шаблон при апгрейде заменяется скачанным, а не удаляется — качается в фоне до первого старта ядра и встаёт на место только разобранным (temp + rename), провал скачивания оставляет старый файл и повторяется на следующем запуске; первый запуск новой версии помечает конфиг устаревшим, и первый старт пересобирает его из нового шаблона; провал пересборки перед стартом (classic и daemon) — диалог с причиной, ядро не стартует, вместо молчаливого запуска прежнего `config.json` (`485f83fe`)

- fix(xray-json): `users[0].encryption` у VLESS переносится в outbound как есть — ключ VLESS Encryption (ML-KEM) терялся, и сервер с обязательным шифрованием рвал соединение (`bad http protocol version`); пусто или `none` — поля нет, как у share-ссылок (D-108, контракт 0.12.11) (#121, `2d64ca72`)

- fix(backup,rules): тело правила — ровно один объект, массив раскладывается на записи `name`, `name #2`… с общим `enabled` и местом исходной записи на оси, `config.json` тот же, что у правил, развёрнутых подряд; одна функция на оба входа — legacy `kind: json` 0.x (раньше пропускался целиком) и запись 1.0 с массивом в `body`, не-объект отбрасывается с `backup_unknown_field`; редактор правила отвечает на вставленный массив понятной ошибкой. Ссылка без `folder_id` на член папки, записанная финальным тегом, нормализуется при импорте 1.0 в `{folder_id, сырой тег}`, если такой финальный тег ровно у одного члена и корень это имя не занимает, — узел с таким `detour` и цепочка с таким хопом больше не выпадают из конфига fail-closed (D-111)

- fix(backup): импорт ставит правила на ось номерами файла, а не подряд с 1000 — системная голова `traffic-processing` (0), якоря шаблона и правила узлов (945) остаются ниже 1000, то есть перед `route.rules` шаблона, перехватчики 1110+ — перехватчиками; после сплошной нумерации включённый позже пресет вставал перед головой (sniff переставал быть первым), а новое правило — за перехватчики; неразмеченные правила файла встают в хвост оси, импорт своего экспорта ось не трогает (BACKUP.md §9 п. 7)

- fix(backup): одинаковые DNS-правила и DNS-серверы внутри файла ввозятся все — дубли ищутся только среди записей приёмника до импорта; из двух одинаковых правил `ntc.party` доезжало одно (BACKUP.md §9 п. 5)

- fix(state): плоские `user_agent` / `hwid` / `send_hwid` / `hash_device_model` подписки в файле v8 предрелизной сборки переносятся в `identity` при загрузке — первое сохранение молча их стирало (UA `Happ/3.3.6`); настройка, уже заданная в `identity`, главнее

- fix(build): собравшаяся корневая цепочка больше не помечается «источник не дал ни одного узла» — цепочка становится узлом на втором проходе, и пометка ставится только цепочке, которая не собралась, с причиной из её сборки; ложная пометка давала ⚠ в списке Sources, строку отчёта на «Итоге» и «1 failed» в сообщении обновления подписок

- fix(chains): каждая цепочка папки собирается своим outbound'ом — две цепочки в одной папке давали один outbound с тегом первой и позициями последней, вторая пропадала без предупреждения (NODE_LINK.md §9.3 п. 2)

- fix(backup): импорт ведёт ссылки за узлами туда, где их положило слияние — подписка, узнанная по URL, отдаёт свой id ссылкам файла на её узлы; `group.members[]` и `default` группы переписываются; ссылка на узел, который слияние уникализировало (`X` → `X-2`) или узнало по телу под другим тегом, идёт за ним, а не уходит на здешнего тёзку и не повисает (в том числе ссылка одним финальным тегом и строковая позиция цепочки 0.x); legacy 0.12: `detour_node_source_id` сервера даёт `{tag: тег узла}` у корневого узла и пару у члена папки вместо висячего `folder_id` (NODE_LINK.md §7.2, §7.4)

- fix(sources): операции с узлами и корневыми именами не оставляют висячих ссылок (решения владельца 15.09.2026) — переименование верхнего узла (server, chain) переписывает все ссылки на новое имя вместо сброса: detour, позиции цепочек, члены групп, цели правил, `route.final`, опции и литеральное умолчание Направлений, переменные пресетов, detour DNS (новое имя обязано быть свободным в корне); удаление узла — и верхнего, и в папке — гасит ссылки вместе с ним и называет задетое, одиночные цели по имени остаются, как при удалении Направления; переименование Направления переписывает и позиции цепочек в папках, и detour членов папок; переименование свёртки (`fold_tag`) переписывает ссылки тем же путём (NODE_LINK.md §6)

- fix(folders): заливка подписки в папку переуказывает detour копий на копии их релеев BYPASS в папке — копия больше не дозванивается через узел подписки и не выпадает из конфига, когда подписку выключают или удаляют; нет копии релея — detour остаётся на подписке (NODE_LINK.md §9.3 п. 8)

- fix(backup): экспорт 1.0 пишет Направления телом после слияния — шаблон или пресет, патчи пресетов и USER-патч (`build.ResolveDirections`, тот же вид, что `/state/outbounds/resolved`); ссылочные `proxy-out`/`vpn ②`/`ru VPN 🇷🇺` уезжали одним `tag` без `filter`, `include_direct`, `include` и `auto`, и на другой стороне Направление собирало другой состав узлов. Входы — UI, `GET /backup/export` и его зеркало `/remote/machines/{id}/backup/export` (без читаемого шаблона — `500`); `include_block` ставится по тегу блокировки шаблона, как галка формы

- fix(nodelink): провайдерская группа адресуется сырым тегом — позиция и detour на группу переживают смену `tag_policy`, позиция финальным тегом подсказывает группу; `group.default` — ссылка NodeLink, чтение терпит строку; dev-формы ссылок ранних сборок 1.6.0 поднимаются при загрузке, импорте и вставке записи (`core/state/nodelink_normalize.go`, D-115)

- fix(directions)!: Направление не хранит узлы — варианты только объявленные корневые имена: сырой JSON отклоняет узел и неизвестное имя, сборка уже сохранённого состояния предупреждает, резолв ссылок строку `addOutbounds` целью не считает (сохранённые ссылки поднимаются до пары), бэкап пишет в `include` только теги Направлений (`backup_local_only_dropped` на остальное), импорт отбрасывает чужое с `backup_direction_include_dropped`; `include_block` — тег блокировки приёмника

- fix(subscriptions): сырые теги в теле подписки — сперва все узлы, затем группы одним счётчиком (IDENTITY.md §1.3): группа-тёзка узла получает `X-2` независимо от позиции, узел, которому группа Xray-балансировщика отбирала имя, возвращает его без `-2`

- fix(ui): лаунчер больше не падает на Mac со спящим дисплеем — окно не центрируется, когда нет неспящего дисплея или GLFW не видит монитора; раньше Fyne 2.8.1 разыменовывал nil-монитор на первом `Show()`: при старте без `-tray` сразу, после старта с `-tray` — на первом открытом окне, и после пробуждения тоже (список мониторов GLFW снимается один раз при запуске); все вызовы идут через `fynewidget.CenterOnScreen`, на Windows и Linux без изменений

- fix(backup): один список известных целей импорта, по состоянию после слияния — цели правил, `route.final` и корневые имена подъёма ссылок; в нём системные теги шаблона приёмника (`direct-out`, тег блокировки, outbound'ы и endpoint'ы шаблона) и `-auto` Направлений. POST /backup/import в пустое состояние больше не выключает правила на `direct-out`/`block-out`/endpoint шаблона и не отбрасывает `route.final` на них (`backup_unknown_outbound`, `backup_final_dropped`); декодеры проверяют только пресет; загрузка в визард не переводит правило на системный тег шаблона на `direct-out` (`KnownRuleTargetTags` + `DeclaredRootNames`) (D-117)

- fix(backup,ui): восстановление на новой машине не выводит DNS из VPN — маршрут шаблонных DNS-серверов переносится (сначала переносимыми переменными `dns_<tag>_outbound`, в том же релизе — записью сервера, D-118); «Импорт…» в визарде без сохранённого состояния и правок сливает файл в пустое состояние, как POST /backup/import на свежей установке (`ImportBackupFile`): DNS-серверы, включённые в файле, больше не остаются выключенными умолчаниями шаблона (финальный DNS уходил на системный резолвер), Направления файла не вытесняются Направлениями шаблона (D-117)

- fix(build): переменная шаблона, которой нет в состоянии, берёт дефолт шаблона и в теле пресета — одно правило с секциями конфига (`template.VarValuesFor`, `PresetMergeContext.globalVarValues`); sniff и resolve больше не уезжают с `inbound: []`, а resolve — без `strategy`, когда inbounds собраны по дефолтам (любая машина с нетронутыми `tun`/`enable_proxy_in`, импорт в чистую установку); превью пресета, строки вкладки DNS и «Преобразовать в пользовательское» берут те же значения (`PresetGlobalVars`)

- fix(state): несортируемый пресет встаёт на номер шаблона при загрузке и сборке (`PinRequiredRuleNums` в `NormalizeRuleOrder`) — лечит оси, сдвинутые импортом 1.5.3–1.5.6: голова `traffic-processing` на 1000+ вставала за `route.rules` шаблона и за пресет, включённый после импорта (sniff переставал быть первым); номера и порядок остальных правил не трогаются, сортируемые пресеты, уехавшие тем импортом в 1000+, остаются — тот же номер ставит перетаскивание (D-117)

- fix(state): `Save` создаёт каталог состояния — POST /backup/import на чистой установке, где визард ни разу не сохранял, падал `500` «no such file or directory»

- fix(ui): остановка сигналом больше не пишет ложную панику в `logs/crash.log` — по SIGTERM/SIGINT драйвер Fyne сам гасит цикл событий и вызывает `glfw.Terminate()`, а `GracefulExit` из `main()` после штатной остановки звал `Application.Quit()`, и закрытие окон через уже терминированный GLFW падало с `NotInitialized`; теперь `Quit` просится только у живого цикла (`UIService.RunEventLoop`), а в логе остаётся «Application shutting down: event loop stopped by the driver»

- fix(macos): Cmd+Q, «Завершить» в Dock и выход из системы идут через `GracefulExit` — делегат NSApplication лаунчера (`platform.SetupDockReopenHandler`) заменял делегат GLFW целиком, без `applicationShouldTerminate:` AppKit отвечал `NSTerminateNow` и завершал процесс через `exit()` мимо остановки ядра (в classic-режиме ядро оставалось работать с маршрутами TUN); теперь делегат отвечает `NSTerminateLater`, `GracefulExit` идёт в горутине, а ответ AppKit уходит по её окончании, но не позже 5 с (`macQuitBudget`), `NSTerminateCancel` не возвращается никогда; остальные вызовы делегат пересылает делегату GLFW, и `applicationDidChangeScreenParameters` снова обновляет список мониторов

- fix(build): urltest с редким `interval` (`3h` из подписки, штатный `1h`) больше не роняет старт ядра — парного `idle_timeout` в конфиге не было, ядро подставляло 30m и отвергало пару `interval > idle_timeout`; правило 6 графового санитайзера (`core/build/outbound_graph_urltest.go`) поднимает `idle_timeout` до `interval`, сам `interval` не укорачивается (SPEC 128, #118)

- fix(build): DNS-сервер, чей канал указывает на тег, которого нет в конфиге (Направление переименовали или удалили), больше не резолвит напрямую мимо VPN — сервер не эмитится; DNS-правило на него отказывает (`action: reject`), `dns.final` на него снимается и последним правилом встаёт `{"action": "reject"}`, а `domain_resolver` серверов и узлов и `route.default_domain_resolver` заменяются резолвером по умолчанию шаблона (форма проверена `sing-box check` lx.39); секция `dns` собирается первой (SPEC 129 Н10, D-118)

### Прочее (7)

- docs(contract): контракт 1.0.1 — `$defs/nodeLink` (пара у члена контейнера, тег сырой), `autoGroup.default` anyOf nodeLink|string, `include` Направления — только теги Направлений, `detour` «Поддержка: обе», код `backup_direction_include_dropped`; NODE_LINK.md §8 — вариант А; корпус `v10_group_links`, `v10_dev_forms`, `v10_direction_include`; D-115

- docs(contract): D-117 — известные цели импорта одним списком после слияния с системными тегами шаблона приёмника (BACKUP.md §3), переменные маршрута шаблонных DNS-серверов переносимы (`registry/vars.json`, §9 п. 5), несортируемый пресет встаёт на номер шаблона приёмника (§9 п. 7); описание `rules[].num` в схеме — номера файла сохраняются (D-116); `contract/VERSION` не поднят

- docs(contract): поля стороны LxBox объявлены в схеме бэкапа (detour_policy, import_rules, on_update_action, ping_url/ping_timeout_ms папки, label цепочки, members_rule/pool_badge группы, update_interval_hours, verbatim, description/vars DNS-сервера) — лаунчер игнорирует молча, `tag_policy` корневого сервера отбрасывается; группа только по правилу не ввозится с `backup_group_degraded`; кейсы `v10_lxbox_fields`, `v10_group_degraded`

- chore(core): пин ядра sing-box-lx поднят до `1.14.0-lx.39` (`RequiredCoreVersion`): lx.37 — синк с апстримом v1.14.0 + 33 (sing-tun v0.9.3, wireguard-go v0.0.6), lx.38 — хотфикс ABBA-дедлока вложенных selector'ов (переключение внутреннего могло заморозить все новые соединения до рестарта ядра), lx.39 — UDP через SOCKS5-прокси с BND.ADDR `0.0.0.0`/`::`; пороги гейтов не меняются — tailscale с lx.31, AmneziaWG 3.x с lx.32 (`64d4ce2b`)

- ci(claude): бот по команде @claude вместо автотриажа issue (`a303c501`, `b0b56b99`)

- chore(constants): bump RequiredTemplateRef source-default (v1.5.6) (`a927f5cf`)

- docs(contract): контракт 1.0.2 — `dnsServer.vars` «Поддержка: обе» с нормами записи (TEMPLATE_LANG.md §6.4), слияние записи шаблонного сервера и Н9 (BACKUP.md §9 п. 5), `vars` пресетов по нормам (§9 п. 7); пять имён D-117 в `registry/vars.json` — `portable: false`, терпимая форма чтения; `backup_var_skipped {name, record, reason}`, `backup_unknown_outbound` + `server`; корпус `v10_dns_template_vars` с фикстурой объявлений `.template.json`, ожидания `v10_lxbox_fields` и `non_portable_var_skipped`, DNS-корпус на `state[].vars`; TASKS_LXBOX ## 18, D-118

## v1.5.6

Подробные заметки: [docs/release_notes/1-5-6.md](docs/release_notes/1-5-6.md).

### Возможности (4)

- feat(diagnostics): `logs/crash.log` — трасса фатальной паники дублируется в файл (`runtime/debug.SetCrashOutput`, Go ≥ 1.23; Win7-сборка — заглушка). Windows-бинарь собран без консоли, и паника на старте выглядела как «окно мелькнуло и пропало» без единой строки в логе

- feat(debugapi): `GET /debug/ui` — окна Fyne со стеком overlay-ев и фокусом, `POST /debug/ui/overlays/clear` — снять все overlay-и и разморозить окно без перезапуска; capability `ui`; 504 = заблокирован сам цикл Fyne (`fd88a15e`)

- feat(debugapi): `GET /debug/goroutines` — дамп стеков всех горутин как text/plain (тот же текст, что по SIGQUIT) без остановки процесса; для зависшего UI Fyne, когда stderr ушёл в /dev/null, а lldb-attach убивает процесс. Заголовок `X-Goroutines` — их число; goroutine 1 = цикл Fyne/GLFW (`8ebef696`)

- feat(sources): обфускация AmneziaWG правится у ГОТОВОГО узла — блок в Settings узла-сервера сразу после Detour: домен и браузер маскировки (id/ib) плюс junk-числа jc/jmin/jmax, правки пишутся в тело узла сразу. Виден только когда detour не выбран; назначенный detour снимает поля с узла, а узел, приехавший с обоими сразу, показывает предупреждение вместо блока (`36bb502`, `75908ef`, `890f4e1`, `30bea6b`)

### Исправления (9)

- fix(parser): узел с блоком `tls.reality` получает отпечаток uTLS из chrome-семейства — REALITY-сервер Xray ≥ v26.9.8 требует в ClientHello `key_share X25519MLKEM768`, который несут только chrome-спеки, а прочие отпечатки (и `random` в 4 случаях из 5) сервер молча уводит на камуфляжный сайт: ошибки нет, узел не везёт трафик. Подмена на `chrome` делается на сборке конфига, значение в узле остаётся исходным, на узел вешается код `reality_fp_not_chrome`; пустой отпечаток при reality пишется как `chrome` явно. Заодно битая пара `extra-headers` у naive перестала пропадать молча — код `naive_extra_headers_invalid` (SPEC 083 ядра, контракт 0.12.10, D-102/D-103)

- fix(configurator): «Save As» больше не оставляет без `config.json` — снимок по умолчанию становится текущим состоянием (state.json + пересборка), в диалоге чекбокс «Сделать текущим состоянием и пересобрать конфиг»; без state.json он заблокирован во включённом положении, а снятая галочка даёт прежнее «только снимок». Раньше на первом запуске Save As писал лишь `bin/wizard_states/<id>.json`, визард при следующем открытии был пуст, и каждый старт показывал «Configuration Not Found» (#122)

- fix(gl): гейт OpenGL больше не ставит Mesa3D сам — таймаут пробы (10 с) отделён от отказа драйвера и даёт диалог Retry/Ignore/Abort, установка только по явному «Yes»; перед загрузкой пинится `GALLIUM_DRIVER=llvmpipe` (если переменная пуста), а поставленные DLL проверяются пробой `-gl-probe-local` по полному пути с откатом при неуспехе. Состояние рендерера и факт «дошли до кадра» — в `bin/gl-state.json`, откат на железо — кнопкой в Диагностике и предложением гейта (DLL переименовываются в `.off`); `logs/native-stderr.log` собирает вывод нативного кода, которого у windowsgui-сборки не было вовсе. Смена рендерера теперь применяется самоперезапуском лаунчера (`OPENGL32.dll` стоит в таблице импорта exe и отображается до первой строки Go, поэтому внутри живого процесса рендерер не сменить), а проба железа на время дочернего процесса уводит локальный `opengl32.dll` в сторону — иначе она мерила Mesa и выдавала её за видеокарту (#125)

- fix(shutdown): сторожевой таймер выхода взводится до остановки ядра, а не после — «Quit» из трея выполняется на main-потоке Fyne, и зависшая остановка (macOS: privileged kill ждёт пароль) оставляла процесс без окна и иконки, куда сторож не дотягивался; 15 с на teardown + прежние 3 с на размотку Fyne, диалог ошибки на пути выхода не показывается

- fix(debugapi): `PATCH /state/rules` в режиме `append` (локально и для удалённой машины) не ставил `order_num` новым правилам — при следующей загрузке MarkRuleOrder раздавал им 1000, 1001…, дублируя номера уже существующих пользовательских правил; теперь каждое правило без номера получает `NextUserRuleNum`, а массив сохраняется отсортированным по оси

- fix(fynewidget): индикатор перетаскивания снимал из overlay-ев «верхний, кто бы он ни был» — при чужом overlay поверх обёртка индикатора оставалась навсегда, и окно переставало реагировать на клики при живом процессе (фризы 07–08.09.2026); теперь снимается по ссылке с возвратом лежавшего поверх, плюс очистка на любом DragEnd и Reset (`212b4eac`)

- fix(platform): macOS — нативный пикер файлов резолвит расширения в UTI внутри AppleScript (`UTType typeWithFilenameExtension:`); на macOS 26 голое `of type {"json"}` серило все файлы, и импорт бэкапа/«Add from file» были невозможны; без единого резолва — панель без фильтра (#120)

- fix(backup): импорт бэкапа — после `Import` пересобирается legacy-вид `CustomRules` (`state.RebuildLegacyRuleView`): `LoadState` берёт inline/srs-правила из него, и без пересборки правила из файла считались применёнными, но не доходили до вкладки Rules; `route.final` пишется в `vars["route_final"]` (канонический канал визарда) вместо нечитаемого `config_params["final"]`, экспорт читает его оттуда же — раньше лаунчер `route.final` не экспортировал вовсе (#111)

- fix(wireguard): вставленный .conf с маскировкой больше не теряет её молча — ip/id/ib переносятся из INI наравне с junk-числами; ссылка только с masquerade-сахаром теперь считается AWG-узлом, и её MTU ужимается до потолка AmneziaWG (без этого AWG отказывает молча: рукопожатие проходит, данные не идут) (`f367dd1`)

### Прочее (1)

- chore(core): пин ядра sing-box-lx поднят до `1.14.0-lx.36` (`RequiredCoreVersion`); пороги гейтов не меняются — tailscale с lx.31, AmneziaWG 3.x с lx.32 (`663a6933`)

## v1.5.5

Подробные заметки: [docs/release_notes/1-5-5.md](docs/release_notes/1-5-5.md).

### Возможности (3)

- feat(daemon): индикатор связи с демоном у строки Core Status — кружок как у машин на Remote (зелёный/жёлтый/красный, FATAL = красный), состояние по кадрам существующего стрима статуса; «служба не запущена» больше не показывает модальное окно (`d0c5933`, `3485bc0`)
- feat(xhttp): предупреждения гарда пары mode/uplink_data_placement — правка видна пользователю в строке источника, а не молча (`297ed81`)
- feat(wizard): поле «TUN route exclude addresses» в Settings — CIDR-префиксы (по одному на строку), которые auto_route оставляет вне туннеля на уровне маршрутов ОС (`tun.route_exclude_address`); default пустой, видно при включённых TUN и auto_route; контракт 0.12.8. Идея и первый вариант — PR #114 (alexandersafin) (`acf39e2`)

### Исправления (3)

- fix(rules): SRS-правило переживает переоткрытие Конфигуратора — legacy-вид несёт tag записи rule_set, кнопка «srs» на месте, «Итог» больше не падает на «rule-set: remote entry missing tag»; сборка визарда не дублирует правила текущего формата состояния (`230807d`)
- fix(xhttp): guard пары mode/uplink_data_placement — `header` вне режима packet-up заставлял ядро отвергнуть весь конфиг (`5ff6a7a`)
- fix(xhttp): host/path/mode только из плоских параметров ссылки — одноимённые ключи в extra игнорируются, как в Xray (контракт 0.12.7, D-097; кейс 4PDA #1755) (`dd16ca0`)

## v1.5.4

### Возможности (29)

- feat(release): архив Windows win64-full — exe, ядро lx.30, wintun.dll, шаблон и Mesa3D в mesa3d/ (подхватывается только без аппаратного OpenGL); маркер wizard_template.version бережёт вложенный шаблон от инвалидации на первом запуске
- feat(release): install-macos.sh 0.4 кладёт в бандл ядро и шаблон релиза
- feat(sources): перенос и копирование узла на верхний уровень — корень как полноправная цель «Move to…» / «Copy to…», одна механика выноса с пакетным извлечением папки
- feat(traffic): экспорт живого буфера вкладки Live в JSON — видимое по фильтру или весь буфер
- feat(ui): мастер-галка «все вкл / все выкл» в шапке открытой папки и подписки — тристейт, одна мутация на весь состав
- feat(contract): 0.12.6 — `ping_url`/`ping_timeout_ms` у directions[] — бюджет замера Направления живёт в LxBox, лаунчер игнорирует молча и провозит (D-096)
- feat(contract,backup): 0.12.4 — label Направлений и цепочек живёт в LxBox, лаунчер игнорирует (D-094); 0.12.5 — импорт бэкапа стал слиянием по идентичности для всех источников, норма §9 одна на обе стороны (D-095) (`2145a86`)
- feat(contract): 0.12.1 — запись отбраковки корпуса {ref, code, reason}, код dialer_proxy_unusable (D-088) (`c5b5a8f`)
- feat(contract): 0.12.0 — identity подписки и папки в бэкапе, снос label узлов и цепочек, реестр кодов, кейсы корпуса; fix(backup): цели правил пополняются группами свёрток (`54ee898`)
- feat(ui): принудительный замер строки в ПКМ-меню серверов; задержки участников в окне Info группы
- feat(sources): запись хранения источника («Copy JSON») принимается полем Add — папка/подписка/узел; внутри папки — узлами (`2c348b4`)
- feat(sources): служебные узлы подписки (релеи BYPASS) — метка Service, выделение через detour, дефолтный UA в списке (`8720626`)
- feat(sources): per-source идентификация подписки — UA-комбобокс из ассета и HWID с наследованием «как в системе» (`70e2b15`)
- feat(sources): per-source User-Agent для подписки — провайдеры ветвят выдачу по клиенту (`fdf1bc9`)
- feat(ui): SPEC 116 обкатка заход 3 — шапка drill-down с кнопками контейнера (Support/Edit/Fetch) и раскрывающимся объявлением провайдера (`b0317f2`)
- feat(ui): SPEC 116 обкатка заход 3 — Preview первой вкладкой, визитка источника над списком (имя, анонс с переносами, Support-ссылка) (`933453f`)
- feat(ui)!: SPEC 116 этап 3 — папки в UI, Unsupported-узлы, UI-фиксы обкатки v7 (`b9bd839`)
- feat(state)!: SPEC 118 W5–W8 — смерть легаси, UI из nodes[], бэкап v7↔0.11, голдены (`706d2b5`)
- feat(platform): уборка осиротевших правил sing-tun из брандмауэра Windows (`2216428`)
- feat(config)!: SPEC 118 W4 — сборка из материализованных nodes[], единый резолв, гард, пул (`e03b246`)
- feat(core)!: SPEC 118 W3 — fetch/merge, материализация на живом конвейере (`c5b4921`)
- feat(state)!: SPEC 118 W2 — миграция v6→v7, чистый парсер, эталоны golden (`e478a92`)
- feat(state)!: SPEC 118 W1 — типы v7, плоский корень, Load/Save, мост совместимости (`ec6f69a`)
- feat(parser): причины отбраковки и сообщение провайдера видны в UI (`9b5dbd7`)
- feat(remote): Host telemetry — карточки интерфейсов со всеми данными (вариант А) (`feedb39`)
- feat(wizard): LAN interfaces — добавление из списка машины кнопкой «+» (`0fd6373`)
- feat(wizard)!: вкладка «Итог» — сборка в памяти, отчёт предупреждений, гейт Save (SPEC 115) (`f4f1133`)
- feat(backup): полный отчёт импорта — пользователь видит всё, что не применилось (`c1dd99d`)
- feat(backup)!: бэкап = сериализация стейта — контракт 0.11.0, смерть extensions (SPEC 114) (`45890f7`)
- feat(remote): heartbeat активной машины + журнал обмена по клику на маркер (`2ca3ccd`)

### Исправления (47)

- fix(ui): «Add from file» в открытой папке (⋮ у поля Sources) — мультивыбор, как в окне папки; раньше стоял одиночный пикер вкладки
- fix(ui): Remote — результат устаревшей попытки Connect больше не гасит шапку и список уже подключённой машины; загрузка узлов включает органы управления той же рукой
- fix(configurator): Save при закрытии окна материализует конфиг по цели — удалённой машине пишется её config.json (Deploy больше не уносит прошлую сборку), локальный rebuild только для локальной цели; лог Save печатает реальный путь state.json
- perf(ui): drill-down контейнера окном строк — 100 сверху и снизу, распорки, сдвиг по прокрутке, заморозка на D&D, кэш эмиссии по ревизии (`27b3d67`)
- fix(review): ревью v1.5.3..HEAD — потери данных в миграции и бэкапе, эмиссия hysteria из JSON, релеи, хвосты UI (`829ad93`)
- fix(ui): строка типа в ПКМ-меню серверов больше не притворяется кнопкой — Disabled
- fix(ui): кнопка Add не бывает молчаливой — ошибка диалогом, итог и дубликаты коротким сообщением (`2c348b4`)
- fix(ui): нажатие на замер видно сразу — «…» держится 0.2 с и красится нейтрально (`b22ead7`)
- fix(ui): у одиночного узла и цепочки в списке источников нет счётчика узлов (`5b8ab05`)
- fix(detour): в списке целей только включённые узлы; действующий выбор остаётся видимым (`83ee291`)
- fix(detour): WireGuard-узел получает detour как любой другой — ядро его принимает (`5200b25`)
- fix(masque): разовая миграция ?network= → ?vhttp= в хранимых URI; vhttp=auto принимается (ядро >= lx.27) (`4ec0fc3`)
- fix(remote): медленная машина больше не оставляет на экране список предыдущего источника (`be3e906`)
- fix(subscription): снять ложное предупреждение о потере хопов — релеи материализуются (`1892ade`)
- fix(nodes): исходник wg-quick INI переживает правку тела и экспорт; имя узла берётся из комментария [Peer] и из собственного тега (`63847ff`)
- fix(ui): вкладка JSON показывает detour узла — как он уйдёт в конфиг (`0d7708e`)
- fix(subscription): BYPASS-конфигурации больше не схлопываются с прямыми — путь дозвона входит в подпись дедупа (`3287484`)
- fix(ui): блок идентификации подписки в две колонки (`7b066c6`)
- fix(ui): SPEC 116 обкатка заход 3 — кэш счётчиков узлов сбрасывается вместе с пулом (папка показывала «0 nodes») (`985de79`)
- fix(ui): SPEC 116 обкатка заход 3 — окно узла в папке показывает форму и имя УЗЛА, а не папки (`b77ca05`)
- fix(ui): SPEC 116 обкатка заход 3 — отступ под полосу прокрутки внутри скролла (Settings, JSON, строки Preview) (`54ab02e`)
- fix(ui): SPEC 116 обкатка заход 3 — Origin узла больше не съедается сторожем отката; снят двойной отступ полосы в Settings и JSON (`fb90c76`)
- fix(ui): SPEC 116 обкатка заход 3 — снят двойной отступ под полосу прокрутки в списке источников и в Preview (`860ec0a`)
- fix(ui): SPEC 116 обкатка заход 3 — тексты отчёта локализованы, #-комментарии тела не идут в ошибки, полоса прокрутки не лезет на иконки (`ae2e880`)
- fix(ui): у одиночного сервера в списке источников нет счётчика узлов (`b9058f1`)
- fix(ui): SPEC 116 обкатка заход 3 — «Fill from subscription…» доступен в drill-down папки, а не только в её окне (`b719246`)
- fix(ui): SPEC 116 обкатка заход 3 — прокрутка списка обнуляется при входе в контейнер и выходе из него (`a9019a3`)
- fix(ui): плейсхолдер поля источников — WG/AWG INI в перечне форматов (`db1496b`)
- fix(ui): SPEC 116 обкатка заход 3 — верх вкладки Sources разгружен: заголовок снят, форматы в плейсхолдере, подсказка в тултипе «?» (`dae6925`)
- fix(ui): SPEC 116 обкатка заход 3 — у неправимого узла иконка инфо вместо карандаша, пункт Rename убран из меню (живёт в окне узла) (`ac9c9f3`)
- fix(ui): SPEC 116 обкатка заход 3 — шапка подписки зовёт её profile-title, а не URL (`ace5fd1`)
- fix(ui): SPEC 116 обкатка заход 3 — шапка возврата растягивается (Border), сводка «⚠ N node error(s)» в шапке контейнера (`925f4f9`)
- fix(ui): SPEC 116 обкатка заход 3 — шапка возврата закреплена вместо «Sources», честный счёт живых членов авто-групп с ⚠ при нуле (`3a85d1c`)
- fix(ui): SPEC 116 обкатка заход 3 — левый клик по контейнеру снова проваливается, меню контейнера на правом (`b36ea83`)
- fix(ui): SPEC 116 обкатка заход 3 — шапка drill-down с видом контейнера, меню контейнера по клику, честный показ авто-групп подписки, пустые группы как Unsupported (`4f5d736`)
- fix(ui): SPEC 116 W13 заход 2 — баннеры как Unsupported, drill-down подписок, единое окно узла (`14db0df`)
- fix(ui): SPEC 116 W13 — обкатка: доставка fetch на диск, drill-down папки, Unsupported на JSON-телах (`5c15734`)
- fix(sources): единственный сломанный источник больше не выглядит здоровым (`b44f82e`)
- fix(remote): панель машин не дёргается — heartbeat перерисовывает строки только при смене состояния (`a1ab139`)
- fix(netiface): чужой туннель — легальный аплинк; режется только свой TUN (`8725d21`)
- fix(wizard): пикер интерфейса гаснет под галкой auto-detect (`5b823d0`)
- fix(wizard): Мастер сам скачивает шаблон при открытии, диалог с настоящей причиной (`aa947c0`)
- fix(rules): драг — зеркало LxBox §370: правило встаёт в любую позицию ниже головы (`9bd7de7`)
- fix(build)!: единая строгость detour — fail-closed на всех уровнях (SPEC 113-B) (`fe87355`)
- fix(ui): сеть и диск ушли с UI-потока; Local не видит группы роутера (SPEC 113-E) (`bd08474`)
- fix(rules): инвариант «файл = ось порядка» — правила всегда отсортированы по order_num (SPEC 113-C) (`77a8779`)
- fix(parser): ключ дедупа — подпись голой эмиссии; недостоверный разбор не трогает состояние (SPEC 113-A) (`9a4d229`)

### Рефакторинг (7)

- refactor(sources): релей провайдера существует всегда; ExposeRelays → RelaysInDirections — галка решает только выбор в Направлениях (`35896e6`)
- refactor(ui): SPEC 116 обкатка заход 3 — один шаблон строки узла на все списки; новая папка открывается сразу; кнопка копирования снята со строки контейнера (`e1ae358`)
- refactor(state)!: SPEC 116 обкатка заход 3 — Label стал legacy-входом (json:"-"), поле формы снято; у узлового источника нет вкладки Preview (`5f47590`)
- refactor(model)!: SPEC 117 W5+W6 — чистка legacy-транспорта, приёмка этапа 1 (`feca1da`)
- refactor(state)!: SPEC 117 W4 — обратный синк снесён, Save пишет только canonical (`5feaf65`)
- refactor(model)!: SPEC 117 W3 — write-пути и CRUD на canonical (`c814e37`)
- refactor(model)!: SPEC 117 W1+W2 — ревизия модели и перевод чтений на canonical (`f15706e`)

### Локализация (2)

- i18n(logs): диагностические логи переведены на английский; сводка релеев в свойствах подписки (`f293111`)
- i18n: диагностика сборки и миграции — английские ключи + переводы в ru.json (`2a64610`)

### Тесты (1)

- test(backup): обновить проверку под английский ключ «<file root>» (`5644a3a`)

### Документация (13)

- docs(spec-116): этап 3 — CODEMAP, спека папок под v7, вердикты §O; чистка стухших комментариев (`962fdba`)
- docs(spec-118): спека этапа 2 — схема v7 и материализация; вердикты O1-O3 (`39ab397`)
- docs(features): REVIEW-пометки сняты — оба решения подтверждены Сашей (`bfe5028`)
- docs(spec-117): спека этапа 1 — однонаправленность (SPEC/PLAN/TASKS) (`e2669f7`)
- docs(features): detour только у Server — поправка Саши (`c792d66`)
- docs(features): три живых документа логики — sources / directions / state (`bfe732b`)
- docs(spec-117): разведка кода против модели + капитанские решения (`2f402a2`)
- docs(spec-117): DRAFT — согласованная модель источников (сессия проектирования с Сашей) (`3e00fc1`)
- docs(spec-116): аудит спеки — закрыты пять дыр в поведении (`a45cc76`)
- docs(spec-116): молчаливая потеря источника — обязательный этап 0 (`d2d6ecf`)
- docs(spec): SPEC 115 — вкладка «Итог»: сборка в памяти, отчёт, гейт Save (`f65a968`)
- docs(parser): ParserConfig.ru — перевод «Цепочек хопов» + синхронизация с en/кодом (`dcacb33`)
- docs(spec): SPEC 113 (фиксы аудита A/B/C/E) + SPEC 114 (бэкап=стейт) + принципы бэкапа в контракте (`4cfd5db`)

### Прочее (7)

- chore: удалён временный live-тест подписки Liberty, попавший в коммит случайно (`053d60e`)
- chore(spec-117): этап 1 принят — статус C (`adc6ac5`)
- chore(testdata): анонимизация — добивающий проход по дублям снимка (`f9e7421`)
- chore(testdata): анонимизация голдена real-v088 — только мок-объекты (`7329208`)
- chore: локализация фиксов SPEC 113, дедуп ключа off, перевод раздела «Цепочки хопов» (`bd1beca`)
- chore(l10n,test): хвосты волны 1 — локали interface-пикера, дубль "off", gofmt, пины дедупа (`9c1ae57`)
- chore(constants): bump RequiredTemplateRef source-default (`182f1cc`)

