# corpus/backup — фикстуры LX Backup (контракт 1.0.1)

Кейсы переноса настроек между приложениями. Семантика — `contract/docs/BACKUP.md`,
принципы — `contract/docs/BACKUP_PRINCIPLES.md` (П1–П7, нормативны),
схема — `contract/schema/backup.schema.json`.

## Формат

```
<case>.pre.backup.json  ПРЕДСОСТОЯНИЕ (необязательно): импортируется в пустое
                        состояние ПЕРВЫМ, и уже в него сливается сам кейс
<case>.backup.json      входной файл бэкапа
<case>.expected.json    что обязано получиться после импорта:
                        {"rules": [{"name", "enabled", "refs"?,
                                    "outbound"?, "vars"?, "body"?}, …],
                         "vars": {...}, "warnings": ["код", …],
                         "warning_reasons": {"код": ["reason", …]}?,
                         "directions": [{…, "include"?: ["тег", …]}],
                         "chains": [{…, "hops"?: [ссылка, …]}],
                         "detours": {"тег узла": ссылка}?,
                         "groups": {"тег группы": {"members": [ссылка, …],
                                                   "default"?: ссылка}}?,
                         "subscriptions": {...}, "root_servers": [...],
                         "folders": {...}, "folder_ids": {...},
                         "dns": {...}, "sections": {...},
                         "replace_tags": {...},
                         "extensions_dropped": true|false}

ссылка = {"tag", "folder"? | "subscription"? | "folder_id"?}
```

Все ключи, кроме `rules` и `warnings`, **необязательны**: отсутствие ключа
значит «не проверяем», а не «ожидаем пусто».

**Ссылка в ожиданиях** (NodeLink, `docs/NODE_LINK.md`) — тег плюс адрес
контейнера, записанный так, чтобы он не зависел от машины-приёмника; адрес
задаётся не больше чем одним ключом:

- `folder` — ИМЯ папки, на которую обязан указывать `folder_id` после импорта
  (у совпавшей папки id локальный, у новой — из файла, поэтому в ожидании имя);
- `subscription` — URL подписки, на которую обязан указывать `folder_id`
  (ключ слияния подписки — URL; имя подписки может совпасть с именем папки);
- `folder_id` — ВИСЯЧАЯ ссылка: `folder_id` обязан остаться ровно таким, как
  в файле, а контейнера с этим id после импорта быть не должно (импорт такую
  ссылку не снимает и адрес ей не выдумывает, `BACKUP.md` §6);
- ни одного ключа — ссылка обязана быть корневой (без `folder_id`).

### Ключи, которые различают ОДИНАКОВО ВЫГЛЯДЯЩИЕ реализации

Эти ключи заведены не для полноты, а потому что без них две разошедшиеся
стороны проходят кейс одинаково зелёными:

- `warning_reasons` — причина у кода, где она нормирована перечнем
  (`backup_section_record_dropped`: `kind` | `rule_set` | `not_allowed` |
  `unknown_key`; последнюю ставит только сторона со строгим разбором тела).
  Список кодов причины не различает, и сторона, отбросившая запись «не по
  той причине», показала бы пользователю неверное объяснение потери.
- `folder_ids` — имя папки → её ULID после импорта. Ступень слияния «сперва
  по `id`, затем по имени» (BACKUP.md §9 п. 3) иначе ненаблюдаема: состав,
  собранный любой из них, выглядит одинаково. Совпавшая по `id` папка
  держит ЛОКАЛЬНЫЙ id — по нему и видно, какая ступень сработала.
- `chains[].hops` — позиции как ССЫЛКИ (форма выше). Канон `chain`
  схлопывает хопы до плоских тегов, и перепись `folder_id` по карте id
  (BACKUP.md §6) в нём не видна вовсе.
- `detours` — личный `detour` УЗЛОВ (корневых серверов и членов папок):
  тег носителя → ссылка. Карта ИСЧЕРПЫВАЮЩАЯ — detour у узла, которого в
  ожиданиях нет, ошибка: ссылка, выдуманная импортом, опаснее потерянной.
  Общий `detour` самого контейнера сюда не входит. Состав папок и канон
  цепочек detour не показывают, а перепись `detour.folder_id` — та же
  механика, что у хопов.
- `groups` (контракт 1.0.1) — провайдерские группы (`kind: auto`, в корне и
  в папках): тег группы → `members` (ссылки по порядку, число строго) и
  `default` (ссылка; отсутствие ключа — «не проверяем»). Карта
  ИСЧЕРПЫВАЮЩАЯ, как `detours`: группа, которой в ожиданиях нет, — ошибка.
  Состав папок называет группу только тегом, и перепись
  `group.members[].folder_id` / `group.default` по карте id, как и подъём
  dev-форм (`docs/NODE_LINK.md` §7.3), без этого ключа не видны.
- `directions[].include` (контракт 1.0.1) — опции-теги Направления после
  импорта В ПОРЯДКЕ записи, без служебных `direct`/`block` (их сверяют
  `include_direct` / `include_block`). Отсутствие ключа — «не проверяем»,
  пустой список — «опций нет». Норма «Направление не хранит узлы»
  (`docs/NODE_LINK.md` §8) иначе ненаблюдаема: сторона, сохранившая узел или
  неизвестное имя в опциях молча, проходила бы кейс зелёной.
- `rules[].vars` — переменные ЭТОГО правила (`kind: preset`), не глобальные
  `vars` состояния: это разные сущности, и верхнеуровневый ключ их не
  покрывает.
- `rules[].body` — тело правила (форма sing-box) deep-equal. Нужно там, где
  тело собирает сам импорт: норма «одно правило — одно тело» (D-111) кладёт в
  каждую запись свой элемент массива КАК ЕСТЬ, а вид цели (`outbound`) не
  отличает `"action": "reject"` от `"outbound": "reject"` и не видит матчеров
  вовсе.
- `dns.servers[].body` и скаляры `dns.strategy` / `dns.final` /
  `dns.default_domain_resolver` — тело едет «байт в байт» только у одного
  кодека; у второй стороны оно проходит через её собственный декодер.
- `sections` ключуются тегом ЛЮБОГО носителя — и корневого узла, и члена
  папки: у члена папки свой путь слияния.

### Конвенция предсостояния (контракт 0.12.5, D-095)

Импорт — **слияние** по идентичности, а не замена, и проверить его импортом в
пустоту невозможно: там слияние и замена дают один и тот же итог, а стороны,
разошедшиеся в ключе слияния, остаются неразличимы. Поэтому кейс, который
проверяет слияние, кладёт рядом `<case>.pre.backup.json`.

Порядок раннера строгий: пустое состояние → импорт `pre` → импорт самого
кейса → сверка с `expected`. Предупреждения предсостояния в сверку **не
идут** — оно декорация сцены, а не предмет кейса; сверяются только
предупреждения основного импорта. Файла `pre` нет → импорт идёт в пустое
состояние, как во всех прежних кейсах. `<case>.pre.backup.json` **не является
самостоятельным кейсом**: раннер обязан отсеять его из списка кейсов, иначе
он погонит его отдельно и будет искать несуществующий `<case>.pre.expected.json`.

### Ожидания слияния

`subscriptions` — **исчерпывающая** карта `url → {label, prefix, postfix,
enabled?, nodes?, pending_disabled?}`. Ключ тот же, по которому идёт слияние
(`url` как есть). Список исчерпывающий не для красоты: подписка, которой в
ожиданиях нет, — это либо не оставленная локальная, либо задвоенная, и обе
ошибки видны только сверкой всего набора. `nodes` — сырые теги узлов, которые
обязаны пережить слияние (состав локальной подписки в файл не едет и
потеряться не вправе); `pending_disabled` — отметки выключения, ждущие
первого fetch, сверяются отсортированными (это объединение двух множеств,
порядок в нём смысла не несёт). `enabled` необязателен: отсутствие ключа
значит «не проверяем».

`root_servers` — теги корневых одиночных узлов **в порядке состояния**
(совпавшие держат локальную позицию, новые встают в конец). Дедуп по телу
проверяется именно отсутствием второй записи в этом списке.

Оба поля необязательны: отсутствие ключа значит «не проверяем».

`chains[]` в ожиданиях — исчерпывающий список `{tag, label?, enabled?, chain}`:
`chain` сверяется deep-equal канона (включая `null`-значения `rewrite` —
RFC 7396), число записей — строго (пропущенная по занятому тегу запись не
должна материализоваться второй). `label` — поле **LxBox** (D-094): у него оно
проверяется как значение в re-export, у лаунчера ключа в ожидании нет вовсе —
он подпись цепочки молча игнорирует и не пишет.
`enabled` в ожиданиях **необязателен**: отсутствие ключа значит «не
проверяем», а не «ожидаем false» — иначе умолчание схемы (true) стало бы
требованием выключенности во всех остальных кейсах.

`rules[].refs` в ожиданиях — все URL наборов srs-правила по порядку
(черновик D-100): проверяет, что импортёр берёт `refs`, а без него — `ref`.
Необязателен: отсутствие ключа значит «не проверяем».

`extensions_dropped` — файл несёт упразднённый механизм `extensions`
(схема 0.10.x). Импортёр обязан его отбросить, назвать одним warning'ом на
файл и не воскресить при обратном экспорте.

Раннеры: Go — `core/backup/corpus_test.go`, Dart — `test/contract/backup_corpus_test.dart`.
Ключи `groups` и `directions[].include` добавлены в контракте 1.0.1
необязательными: старые кейсы их не несут и проверяются как раньше; раннер
LxBox поддерживает их по этому описанию.

## Что фиксируют кейсы

Не «happy path», а именно те решения, которые легко нарушить:

- **битая символическая ссылка** (`broken_outbound_ref`) — правило с
  несуществующим `outbound` импортируется ВЫКЛЮЧЕННЫМ, а не теряется и не
  включается (включённое правило с мёртвой целью роняет конфиг ядра целиком);
- **`route.final` в никуда** (`final_target_missing`) — не применяется: иначе
  весь трафик уходит в несуществующий outbound;
- **непереносимая переменная** (`non_portable_var_skipped`) — не применяется,
  факт назван (на другой машине то же имя значит другое: путь, интерфейс,
  платформенный флаг);
- **чужой `kind` правила** (`unknown_rule_kind_skipped`) — пропускается с
  warning, остальные правила приезжают: одно чужое правило не роняет весь файл;
- **сырое правило `kind: json` 0.x** (`legacy_012_json_rule_array`) — это тот
  же inline: тело-объект даёт одну запись, тело-массив раскладывается по норме
  «одно правило — одно тело» (D-111, `BACKUP.md` §2) на `Raw array`,
  `Raw array #2` с общим `enabled` и телом каждого элемента как есть, а
  элемент-строка отбрасывается с `backup_unknown_field`. Запись `Keep` стоит в
  файле ВЫШЕ массива, а номером — ниже его частей: сторона, раздавшая частям
  номера N+1, N+2…, получит `Keep` посреди частей, а не после них;
- **ось порядка** (`order_renumbered_preserving_sequence`) — порядок оси
  тот же, что у файла (номера файла сохраняются, `BACKUP.md` §9 п. 7; кейс
  сверяет порядок, а не номера);
- **цепочка со всеми полями канона** (`chains_roundtrip`) — приезжает
  источником, правило на её тег — рабочим (тег пополняет известные цели),
  `null` внутри `rewrite` переживает перенос. Подпись `label` — поле LxBox
  (D-094): он её возвращает (side-specific ожидание), лаунчер молча
  игнорирует, и предупреждения нет ни у одной стороны;
- **бюджет замера Направления** (`directions_ping_roundtrip`) — `ping_url` и
  `ping_timeout_ms` у Направления — поля LxBox (D-096): он их возвращает
  (side-specific ожидание), лаунчер молча игнорирует и провозит, правило на
  тег Направления приезжает рабочим, предупреждения нет ни у одной стороны;
- **занятый тег цепочки** (`chain_tag_duplicate`) — вторая запись с тем же
  тегом не применяется и ВСЕГДА предъявляется `backup_chain_exists`:
  молчание склеило бы случайных тёзок незаметно;
- **умолчание `enabled` у цепочки** (`chain_disabled_enabled_default`) — в
  одном файле выключенная запись (`enabled: false`) и запись без ключа:
  первая обязана приехать выключенной, вторая — включённой. Обе стороны
  читают отсутствие ключа как `true`; кейс существует затем, чтобы сторона,
  однажды прочитавшая его как `false`, упала здесь, а не у пользователя;
- **упразднённый `extensions`** (`extensions_dropped`) — карман провоза
  отброшен с warning'ом и не воскресает в обратном экспорте (П3). Файл этого
  кейса написан по схеме 0.10.x НАМЕРЕННО: это вход теста «старый файл
  читается общим правилом», а не legacy-путь кода — отдельного разбора
  старого формата не существует (П4);
- **старый файл лаунчера** (`legacy_0_10_launcher_extensions`) — то же с
  наполненным `extensions.launcher` (id, skip, detour_node_hash): общие поля
  записи применяются, карман отбрасывается ОДНИМ warning'ом, потеря его
  содержимого — задокументированная цена разрыва (`BACKUP.md` §10);
- **неизвестные ключи** (`unknown_keys_warned_import_continues`) — ключи
  чужой стороны на всех уровнях (корень, запись подписки, поле правила) дают
  warning, а импорт продолжается: понятное применяется, непонятое названо
  (П3/П6). `directions[].label` в этом же файле warning'а НЕ даёт: с
  контракта 0.12.4 это объявленное поле LxBox, а не неизвестный ключ (D-094);
- **чужой ТИП знакомого ключа** (`boolean_skip_type_mismatch`) —
  `subscriptions[].skip` пришёл boolean'ом (форма LxBox 0.10.x) при списке
  фильтров отсева в 0.11.0. Отбрасывается ОДНО поле с
  `backup_field_type_mismatch`, всё остальное — соседняя подписка с корректным
  `skip`, Направление, правило — приезжает (`BACKUP.md` §10). Строгий разбор
  ронял бы здесь весь файл, то есть терял бы всё прочее молча, вопреки П6.
  Это единственная коллизия типов между 0.10.x и 0.11.0;
- **счёт позиционного деривативa** (`replace_tag_index`, D-081) — тег группы
  свёрнутой подписки контракт не несёт, он вычисляется: префикс тегов, а при
  пустом префиксе — `<номер записи в subscriptions[]>:` плюс `select`. В файле
  сервер стоит ПЕРВЫМ источником, подписка — единственная в своей секции,
  значит имя группы `1:select`, а не `2:select`. Правило и `route.final` того
  же файла метят в него и обязаны приехать РАБОЧИМИ: цель приезжает этим же
  файлом, и принимающая сторона, чей список известных целей снят до импорта,
  обязана пополнить его группами приехавших свёрток. Считала сторона номер по
  позиции среди всех источников — здесь она получит `2:select`, выключенное
  правило и отброшенный final.

Ожидание `replace_tags` (`{URL подписки: имя группы}`) — необязательное поле
конверта: отсутствие ключа значит «не проверяем». Оно единственное место, где
имя группы видно снаружи, — в самом контракте такого поля нет.

**Часть входных файлов НЕ валидна против `backup.schema.json` — намеренно.**
Схема нормативна для 0.11.0 и обязана отвергать `skip: true`; терпимость к
такому полю живёт в импортёре, а не в схеме. Кейсы
`boolean_skip_type_mismatch`, `extensions_dropped` и
`legacy_0_10_launcher_extensions` — входы «файл прошлой схемы читается общим
правилом», и валидатор схемы к ним неприменим. `legacy_012_json_rule_array`
не проходит `backup-0.12.schema.json` так же намеренно: схема объявляет
`match` объектом, а массив на этом месте — ровно тот вход, который норма
D-111 обязана разложить; замороженная схема 0.12 не правится. Все остальные `*.backup.json`
обязаны проходить схему.

Данные синтетические: хосты `example-N.com`, UUID из единиц. Секретов и
реальных URL провайдеров в фикстурах нет.

Ключи disabled-отметок в корпусе — 64 hex-символа: это **legacy-форма**
(identity-хеш до SPEC 112). Она оставлена намеренно — фикстуры проверяют, что
такой ключ переживает перенос НЕТРОНУТЫМ и доезжает до приёмника, где его
мигрирует первый разбор источника (`docs/IDENTITY.md` §5.1). Новая
идентичность — тег узла (`docs/IDENTITY.md` §1), и в бэкапе она едет обычной
строкой ключа; отдельного кейса под неё нет, потому что для формата обмена
ключ непрозрачен: он копируется как есть.

**Требует зеркальной правки LxBox** — обе стороны обязаны и сохранять
legacy-ключи при переносе, и мигрировать их по одному правилу, и одинаково
отбрасывать `extensions` (статус зеркала — `docs/BACKUP_PRINCIPLES.md`).

## Папки (контракт 0.12.0)

Кейс `folders_roundtrip` — сборка папки на импорте. Папка **не имеет секции в
файле**: собственных данных, кроме имени, она не несёт, а вторая секция
потребовала бы держать два места в согласии. Вместо этого запись `servers[]`
носит пометку `folder` — имя папки, и обе стороны собирают контейнер ПО ЭТОМУ
ИМЕНИ: записи с одинаковым `folder` становятся одной папкой, запись без
пометки остаётся в корне списка.

Нормативны две вещи, и обе проверяются кейсом:

- **порядок членов** — порядок записей в файле. Перетасованный состав это
  расхождение: пользователь раскладывал узлы сам;
- **одно имя = одна папка** — второй встреченный `folder: "Proton"` дополняет
  уже собранную, а не заводит тёзку.

Ожидание `folders` (`{имя папки: [теги членов по порядку]}`) — необязательное
поле конверта: отсутствие ключа значит «не проверяем». Сравнение идёт БЕЗ
сортировки, потому что порядок здесь и есть предмет договорённости.

Что в файл 0.12 не ехало и почему: настройки САМОЙ папки (политика тегов,
свёртка, общий detour) и члены, которых `servers[]` выразить не может
(цепочки, неразобранные записи). Это не забывчивость формата, а его граница —
потерю объявлял экспорт 0.12 кодом `backup_local_only_dropped`, одним
предупреждением на папку с перечнем. Односторонний ключ в общей схеме был бы
возвращённым тайным грузом, ради сноса которого убран `extensions` (П1/П3). В
формате 1.0 у папки своя запись со всеми этими полями, а 0.12 с синхронных
релизов не пишет ни одна сторона (D-110); кейс остаётся кейсом legacy-чтения.

**Требует зеркальной правки LxBox**: папка = контейнер одиночных серверов у
обеих сторон, и собирается она по имени одинаково.

Side-specific ожидание `chains_roundtrip.expected.lxbox.json` (класс различия
**B** по `docs/IDENTITY.md` §4a — платформенное поле записи, решение D-094):
подпись цепочки `label` объявлена в схеме как поле **LxBox**, он её пишет и
читает, поэтому в его ожидании `chains[0].label` присутствует. Лаунчер зовёт
цепочку тегом (SPEC 112): приехавшую подпись он игнорирует МОЛЧА (`warnings`
пусты у обеих сторон — предупреждения об объявленном чужом поле не бывает,
`BACKUP.md` §1) и на экспорте не пишет, поэтому в базовом ожидании ключа
`label` нет. Различие ровно в одном ключе; всё прочее у ожиданий совпадает.

Side-specific ожидание `directions_ping_roundtrip.expected.lxbox.json` (класс
различия **B** по `docs/IDENTITY.md` §4a, решение D-096): `ping_url` и
`ping_timeout_ms` Направления — объявленные поля LxBox (бюджет замера узлов в
приложении, не `auto.url`/`auto.idle_timeout` ядра), он их пишет и читает, и в
его ожидании оба ключа у `directions[0]` присутствуют. Лаунчер такой настройки
не имеет: приехавшие значения игнорирует МОЛЧА (`warnings` пусты у обеих
сторон) и на экспорте не пишет, поэтому в базовом ожидании ключей нет. Различие
ровно в двух ключах; всё прочее у ожиданий совпадает.

Side-specific ожидание `replace_tag_index.expected.lxbox.json` (класс различия
A по `docs/IDENTITY.md` §4a, решение D-081): у LxBox свёртка (`fold`) не
применяется и группа `1:select` не создаётся, поэтому правило `Work` и
`route.final`, метящие в неё, приезжают выключенными с предупреждениями
`backup_unknown_outbound` и `backup_final_dropped`; поля `replace_tags` в
ожидании нет. Базовое ожидание описывает сторону лаунчера, где группа
создаётся и правило включено.

Side-specific ожидание `v10_sources_union.expected.lxbox.json` (формат 1.0,
различие моделей сторон, согласовано с LxBox 14.09.2026): у LxBox цепочки
живут только в корне `sources[]`, поэтому член папки `kind: chain`
(`de-via-nl`) не применяется и называется `backup_source_kind_unsupported`, а
в `folders.Europe` его нет; `postfix` политики тегов подписки у LxBox нет
(ключа в ожидании нет); свёртка не применяется (ключа `replace_tags` нет).
Ступени слияния папок, `folder_ids` и перепись `hops[].folder_id` совпадают с
базовым ожиданием лаунчера.

## Слияние источников (контракт 0.12.5)

Кейс `merge_sources_by_url` — единственный на сегодня кейс с предсостоянием, и
он фиксирует все четыре ключа слияния разом (D-095, `docs/BACKUP.md` §9).

Предсостояние: подписки **A** и **B** (у B локальные настройки — префикс
`b:`, включена, отметка `local-mark`), корневой сервер **S1**, папка
**Proton** с членом **P1**.

Предсостояние несёт ещё и сервер **S3**, заданный `config_json` (для проверки
канонической формы тела).

Файл кейса: подписка **B** с ДРУГИМИ настройками (`label` «B new», префикс
`bb:`, `disabled: {x}`), подписка **C**, сервер с тем же телом, что у S1, но
под чужим тегом `S1-renamed-elsewhere`, ещё один S1 под тегом
`S1-other-fragment` и **другим `#фрагментом`** в URI, новый сервер **S2**,
папка **Proton** с членами `P1-elsewhere` (тело P1) и **P2**, и сервер
`S3-reordered` — то же тело, что у S3, но с **переставленными ключами
`config_json`** и собственными `tag`/`detour` внутри JSON.

Что обязано получиться:

- подписки **A, B, C**: A осталась нетронутой (её в файле нет), B держит
  локальную позицию и `id`, но настройки взяла из файла, а отметки
  выключения ОБЪЕДИНИЛИСЬ (`local-mark` + `x`), C дописана в конец;
- корневые серверы **S1, S3, S2** (в этом порядке — совпавшие держат
  локальную позицию, новый встал в конец). Три дубля узнаны **по телу** и
  пропущены **без warning** — это не потеря, а «у тебя уже есть»: дубль S1
  под чужим тегом, дубль S1 с другим `#фрагментом` (фрагмент — имя узла, а
  не тело) и `S3-reordered` (порядок ключей `config_json` не значим, а
  верхнеуровневые `tag`/`detour` из сравнения сняты — форма identity-хэша,
  D-007);
- папка **Proton** = `[P1, P2]`: P1 узнан по телу внутри папки, P2 дописан
  в конец;
- **предупреждений нет вовсе**: ни одна из этих операций потерей не является.

Сломать кейс легко ровно теми способами, ради которых он написан: сливать
серверы по тегу (появятся `S1` и `S1-renamed-elsewhere`), сравнивать `uri`
вместе с фрагментом (появится `S1-other-fragment`), не снимать `tag`/`detour`
у `config_json` (появится `S3-reordered`), заменять `disabled` вместо
объединения (пропадёт `local-mark`), терять локальное (исчезнет A) или
задваивать совпавшее (две записи на `sub-b`). Каждая из трёх ветвей дедупа
проверена отключением: без неё кейс краснеет.

## Ссылки на узлы — NodeLink (контракт 1.0.x, D-112)

Кейс `v10_node_links` (+ `.pre.backup.json`) — форма `{folder_id?, tag}` у
всех ссылок «через кого» и её перепись на импорте. Норма —
`docs/NODE_LINK.md`.

Предсостояние: папка **Work** с ЛОКАЛЬНЫМ id и членом `local-at`.

Файл кейса: подписка **Provider NL** (префикс `nl:`), папка **Work** с
ДРУГИМ id (совпадает по имени) и префиксом `[W] `, члены `de-1` и `de-2`;
корневые серверы `relay-root`, `tokyo`, `orphan`, `via-direction`; цепочки
`mixed-route` и `dir-first`; Направление `proxy-eu`.

Что обязано получиться:

- `detour` члена папки (`de-2`) и корневого узла (`tokyo`) на `de-1` — парой,
  `folder_id` переписан на локальный id папки **Work** (ожидание называет
  папку именем);
- позиции `mixed-route` вперемешку: корневой узел — `{tag}`; член папки с
  префиксом — СЫРОЙ тег `de-1`, а не `[W] de-1`; член подписки — `US-1` с
  `folder_id` подписки (узлов подписки в файле нет — до первого обновления
  такая ссылка не разрешается, и это не потеря);
- корневые имена без `folder_id`: Направление позицией 0 (`dir-first`) и
  целью `detour` (`via-direction`);
- ссылка на контейнер, которого в файле нет (`orphan` →
  `01FLDGHOST0000000000000000`), ввозится КАК ЕСТЬ и предупреждения не даёт:
  недостижимую цель разбирает сборка (у лаунчера — `the referenced source
  is gone`, узел выпадает fail-closed);
- **предупреждений нет вовсе.**

Сломать кейс легко теми способами, ради которых он написан: писать финальный
тег вместо сырого, не переписывать `detour.folder_id` по карте id (переписывая
только хопы), снимать или «чинить» ссылку на неизвестный контейнер на
импорте, приписывать корневому имени адрес папки.

Кейсом пока НЕ закреплено: подписка, совпавшая по URL, с другим id — ссылка
на её член; `group.members[]` в папке, совпавшей по имени; ссылка на члена,
которого слияние уникализировало или узнало по телу под другим тегом. У
лаунчера это исправлено в 1.6.0 и закреплено Go-тестом
(`core/backup/merge_test.go` `TestImportLinksFollowMergeAddresses`,
`docs/NODE_LINK.md` §9.3 п. 5); кейс корпуса обязывает и раннер LxBox и
добавляется по согласованию сторон.

## Группы, `default` и Направления без узлов (контракт 1.0.1, D-115)

Норма — `docs/NODE_LINK.md` §2.1, §7.3, §8. Все три кейса формата 1.0.

**`v10_group_links`** (+ `.pre.backup.json`). Предсостояние: папка **Work** с
ЛОКАЛЬНЫМ id и членом `local-at`. Файл: папка **Work** с ДРУГИМ id (совпадает
по имени) и префиксом `[W] `, члены `de-1`, `de-2` и провайдерская группа
`Best` (`kind: auto`, selector) — `members[]` и `default` объектом с
`folder_id` ФАЙЛА; корневой `relay-root`; цепочка `via-best` с позицией на
группу `{folder_id файла, "Best"}` — СЫРОЙ тег группы, а не `[W] Best`.
Обязано получиться: члены, умолчание и позиция указывают на локальную
**Work** (`groups`, `chains[].hops`); предупреждений нет.

**`v10_dev_forms`**. Файл сборки лаунчера до контракта 1.0.1: в папке **Dev**
(префикс `d:`) группа `G` с членами `{tag}` без `folder_id` и `default`
строкой `"y"`; позиция цепочки `via-g` записана ФИНАЛЬНЫМ тегом группы
`{folder_id, "d:G"}`. Обязано получиться: члены — пары на **Dev**, умолчание —
пара `y`, позиция — сырой тег `G`; предупреждений нет.

**`v10_direction_include`** (+ `.expected.lxbox.json`). Направление
`streaming` несёт в `include` тег Направления файла `proxy-eu`, тег свёртки
папки файла `fold-pick`, тег корневого узла `relay-root` и строку без цели
`ghost`. У лаунчера (базовое ожидание): узел и неизвестное имя в опции не
попадают и называются ОДНИМ `backup_direction_include_dropped`; тег
Направления и тег свёртки (объявленное корневое имя приёмника) остаются.
Side-specific ожидание LxBox (ответ на вопрос 5, `TASKS_LXBOX.md` §17.8):
неизвестную строку `include` он хранит, на импорте не предупреждает (сборка
её не эмитит и предупреждает сама), поэтому `include` у него полный и
предупреждений нет. Экспорт лаунчера такой `include` не пишет вовсе — в
`include` едут только теги Направлений (`BACKUP.md` §2); кейс — вход
файла от сборки до 1.0.1 или чужой руки.

Сломать кейсы легко ровно теми способами, ради которых они написаны:
адресовать группу финальным тегом, оставить `default` строкой или без
переписи `folder_id`, не поднять член без `folder_id`, сохранить узел в
опциях Направления молча.

## Поля стороны LxBox и деградация групп (контракт 1.0.1)

Норма — `docs/BACKUP.md` §2 «Поля стороны LxBox», §10 (код
`backup_group_degraded`); перечень — `TASKS_LXBOX.md` §16.7.

**`v10_lxbox_fields`** — все объявленные поля стороны LxBox в одном файле:
`detour_policy` / `import_rules` / `import_rules_enabled` / `on_update_action`
у подписки, `detour_policy` и `tag_policy {prefix}` у корневого сервера,
`detour_policy` / `ping_url` / `ping_timeout_ms` у папки, `label` у цепочки,
`members_rule` и `pool_badge` у группы с явным составом, `update_interval_hours`
у srs-правила, `verbatim` у inline-правила, `description` у DNS-серверов и
`vars` у шаблонного. У лаунчера импорт проходит **без предупреждений**, а
состояние такое же, как после импорта того же файла без этих полей (Go-тест
`core/backup/lxbox_fields_test.go`); `tag_policy` у корневого сервера в
состояние не попадает и финальный тег не меняет (`root_servers`). У LxBox
поля применяются; его раннер сверяет их по своей модели.

**`v10_group_degraded`** (+ `.expected.lxbox.json`) — один код на две разные
деградации сторон. В папке **Rules** группа `by-rule` задана только правилом
отбора (`members_rule`, без `members[]`), группа `pick` — selector с
`default`; цепочка `via-rule` стоит позицией на `by-rule`. Лаунчер (базовое
ожидание): `by-rule` не ввозится — `backup_group_degraded` с reason
`members_rule unsupported` (`warning_reasons`), `pick` приезжает как есть с
умолчанием, позиция цепочки остаётся ссылкой на невввезённую группу и
разбирается сборкой fail-closed. LxBox (side-specific ожидание, класс A по
`docs/IDENTITY.md` §4a): `by-rule` ввозится группой по правилу, а `pick`
становится urltest без умолчания — тот же код со своей причиной.
