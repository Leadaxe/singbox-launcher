# Тот же баннер ЕДИНСТВЕННОЙ записью тела: при expired/depleted 3x-ui
# возвращает его вместо всего состава (internal/sub/service.go:484-486 —
# `return []string{dummyLink}`). Прежде подписка выглядела РАБОЧЕЙ и
# ОДНОУЗЛОВОЙ, и человек видел сервер, которого нет, вместо причины.
# Ожидание: ни одного узла, запись в dropped[] с info-кодом — тело разобрано
# (nodes[] пуст ПРИ непустом dropped[], а не «тело не распознано»).
socks://127.0.0.1:1080#Subscription%20expired
