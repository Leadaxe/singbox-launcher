# Xray-элемент trojan БЕЗ `streamSettings` вовсе. Секция обязана опознать его и дать
# узел на голом TCP без TLS: `streamSettings` — контейнер, а не обязательное поле.
# Кейс заведён ревизией зеркала §3 п.1: у LxBox `json.type_of` при ОТСУТСТВУЮЩЕМ пути
# проходит (у нас — нет), и разница видна ровно на элементе без контейнера; чтобы её
# не пришлось искать на живых телах, граница закреплена кейсом.
[{"remarks": "tj-bare", "outbounds": [{"tag": "proxy", "protocol": "trojan", "settings": {"servers": [{"address": "tj.example", "port": 443, "password": "testpass123"}]}}]}]
