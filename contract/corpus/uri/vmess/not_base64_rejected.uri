# не-base64 полезная нагрузка -> отказ без исключения
# источник: LxBox app/test/parser/round_trip_test.dart:212-216
# Код — form_unrecognized (CANON §4.1): секция схему опознала, но ни одна форма
# не прочитала пейлоад. `not-base64` лежит в алфавите base64url и «декодируется»
# в байты, которые не текст, — оболочка не раскрылась (контракт 1.1.73, задача
# LxBox 560; прежде ожидание держало field_missing).
vmess://not-base64
