# LX Shared Contract

Единый контракт данных между десктопным лаунчером (Go) и LxBox (Flutter/Dart).
Программа и решения: `SPECS/103-F-O-LX_SHARED_CONTRACT/` (SPEC, PLAN, DECISIONS).

**Контракт — это данные и документы, ни строки исполняемого кода.** Раннеры живут
в проектах и читают этот каталог.

## Состав

| Путь | Что это |
|------|---------|
| `VERSION` | semver контракта: major — изменение правил канонизации, minor — новые конструкции/разделы, patch — уточнения |
| `docs/CANON.md` | правила канонизации канонического узла (нормативно) |
| `docs/IDENTITY.md` | алгоритм identity-хеша и ключа (нормативно) |
| `docs/TEMPLATE_LANG.md` | язык шаблонов v1 (нормативно) |
| `docs/BACKUP.md` | семантика LX Backup v1 (нормативно) |
| `registry/protocols/<scheme>.json` | реестр протоколов: по файлу на схему |
| `registry/transports.json`, `registry/tls.json` | общие суб-схемы транспортов и TLS |
| `registry/allowlists.json` | канонические allowlist'ы (uTLS fp, ss-методы, …) |
| `registry/warnings.json` | коды warning'ов РАЗБОРА источников (локализация — на стороне приложений) |
| `registry/backup_warnings.json` | коды warning'ов БЭКАПА: экспорт/импорт состояния, с указанием стороны эмиссии (`export`/`import`/`both`). Отдельный словарь — граница другая, и общий файл смешал бы два несвязанных набора |
| `registry/limits.json` | лимиты (URI, нод, тело, цепочки) |
| `registry/vars.json`, `registry/presets.json` | переносимые имена переменных и preset id |
| `registry/containers.json` | контейнеры тел (`vpn://`, wgconf INI, base64) |
| `schema/*.schema.json` | JSON Schema узла, Направления, свёртки и цепочки источника, бэкапа, реестра |
| `diagrams/*.mmd` | нормативные mermaid-блок-схемы пайплайна |
| `docs/generated/**` | **генерируется из реестра**, руками не править (см. абзац ниже) |
| `tools/gendocs/` | генератор этих страниц (`go generate ./contract/...`) |
| `corpus/` | golden-фикстуры (фаза 1+) |
| `embed.go`, `doc.go` | **единственный код в каталоге** (см. абзац ниже) |

`embed.go` (`package contract`) — исключение из правила «контракт это только
данные», и оно намеренно узкое: файл не содержит ни одного решения, ни одной
проверки и ни одной таблицы, он лишь объявляет `//go:embed registry/*.json`,
чтобы реестр уезжал внутрь бинаря лаунчера и санитайзер с эмиттером
(`core/config/registry`, `core/config/nodeflow`, SPEC 131) читали те же самые
байты, что лежат в репозитории, — без копии реестра в `assets/` и без чтения с
диска в рантайме. Правила по-прежнему живут только в JSON. LxBox этот файл
игнорирует: Dart-сторона берёт те же файлы своими средствами.

`docs/generated/` — **человекочитаемая документация, собранная из реестра**:
оглавление, страница на каждую схему (параметры ссылки, поля тела, что делаем с
мусором) и словарь кодов с обратным индексом. Эти файлы **руками не правятся**:
их переписывает `go generate ./contract/...` (генератор — `tools/gendocs`,
директива — `doc.go`), а CI-джоба `Contract` на каждом PR перегенерирует их и
падает на `git diff --exit-code contract/docs/generated`. Правка нужного текста
начинается с `desc_en`/`desc_ru` в реестре, а не со страницы. Нормативен
по-прежнему реестр, а не эти страницы: они — его чтение вслух.

## Структура файлов реестра

Раздел описывает УСТРОЙСТВО файлов реестра: какие в них секции, что в секции
лежит и кто её исполняет. Строгая форма — `schema/registry.schema.json` и
`schema/registry_body.schema.json`; содержимое, прочитанное вслух, —
`docs/generated/`. Здесь — карта, по которой читают и то и другое.

Общее для всех файлов: корневой `v` — версия формы файла; `desc_en`/`desc_ru`
— текст для людей (уезжает в `docs/generated/`), `impl` и `note` — заметки
разработчикам (в документацию НЕ уезжают, это правило закреплено в схеме),
`refs {go, dart}` — где место реализовано в коде приложений. `refs` есть не
везде: у `multiplex.json`, `dialer.json` и словарей его нет.

Ссылки на код лаунчера в `refs.go`, `impl`, `go` (у кодов `warnings.json`),
`note` и описаниях пишутся путём от корня репозитория и, при нужде, именем:
`core/config/nodeflow/sanitize.go:Sanitize` (функция, тип, переменная,
константа, `Тип.Метод`, `Тип.Поле`). Номеров строк нет — они протухают с
первой правкой файла; голого имени файла без каталога тоже нет. Файл апстрима
пишется путём от корня своего репозитория (`common/tls/utls_client.go`), а
если путь начинается с каталога лаунчера — с префиксом репозитория
(`3x-ui:internal/sub/service.go`). Общий движок (`core/config/linkmap`,
`core/config/nodeflow`, `core/config/registry`) в `refs.go` протокола не
перечисляется: правило, которое исполняет движок, — это данные файла, и в
`refs.go` остаётся только код, специфичный для схемы. Линтер
`TestRegistryCodeRefsResolve` (`core/config/registry_refs_test.go`)
проверяет, что каждый путь существует, а имя объявлено в этом файле. Dart-
ссылки не проверяются.

### Кто что исполняет

Узел идёт по конвейеру (CANON §8): вход → **маппер** → сырое тело →
**санитайзер** → тело, годное ядру → модель приложения → **эмиттер** тела.

| Секция | Отвечает на вопрос | Кто читает сегодня | Статус |
|---|---|---|---|
| `uri` | как прочитать ссылку: какой кусок ссылки в какое поле тела | `tools/gendocs` (страницы «Link parameters») | **описание**: справочник полей для людей и для сверки со второй стороной. Исполняются секции `mappers.uri` (ниже), а не эта |
| `mappers.<вид>` (`uri`, `xray`, `singbox`, `conf`) | какой кусок источника в какое поле тела, какая форма ведёт текст, как собирается ссылка обратно | `core/config/registry` → `core/config/linkmap` | **исполняется** (SPEC 133): разбор ссылки, элемента Xray и текста wg-quick ведёт один общий движок по этим таблицам; рукописных парсеров и эмиттеров по протоколам не осталось. Устройство — `contract/docs/MAPPER_ENGINE.md`, грамматика атрибутов — `schema/registry_mapper.schema.json` |
| `mapper` | структурные замены, которые таблицей «параметр → поле» не выразить | `tools/gendocs` (раздел «Replacements») | **описание**: перечень для людей и для сверки глазами. Правила, ставшие исполняемыми, живут записями секций `mappers.<вид>` |
| `body` | какие поля бывают в теле, что делать с негодным значением и в каком порядке писать ключи | `core/config/registry` → `core/config/nodeflow` | **исполняется**: один общий движок, схемных `if scheme == …` в коде нет. Второго парсера секции `body` в репозитории тоже нет — `gendocs` берёт её через тот же загрузчик |
| `emit` | возможна ли у схемы share-ссылка, как называются её параметры на выходе и в каком порядке идут | `core/config/registry` → `core/config/linkmap` (`emit.go`) | **исполняется** (SPEC 133): обратный ход идёт по ТЕМ ЖЕ записям `mappers.uri`, а `emit`-атрибуты объявляют то, что из таблицы не выводится — вид ссылки для ЧУЖИХ клиентов: `param_order`, `emit.names`, `emit_when`, `json_always`. Нормы обратного хода — `contract/docs/MAPPER_ENGINE.md` §9 |
| `warnings.json` | что сказать человеку по коду | `core/config/registry` (`WarningText`, `WarningAdvice`), `tools/gendocs` | **исполняется**: приложения своих таблиц текстов не держат, ru/en берутся из реестра |

Правило на будущее: новое поведение добавляется атрибутом в реестре и
поддержкой атрибута в движке, а не веткой `if scheme == …` в коде.

### `registry/protocols/<scheme>.json` — файл протокола

На примере `vless.json`:

```
vless.json
├─ v, scheme:"vless", kind, singbox_type:"vless"        — паспорт
│     kind: "outbound" (14 схем) | "endpoint" (wireguard, tailscale) | "group"
├─ aliases: []        — другие написания схемы (socks: socks5/socks4/socks4a; wireguard: wg, awg)
├─ sources: ["uri","singbox","xray"]   — какими входами узел приходит;
│     эти же имена — значения условия `except_sources` в правилах тела
├─ extension: "desktop" | null          — маркер расхождения проектов (README §«Правила
│     изменения» п.3): схема реализована только на десктопе; НЕ «поля вне тела ядра»
│
├─ uri                — КАК ЧИТАТЬ ССЫЛКУ (описание, см. таблицу выше)
│  ├─ userinfo  { maps_to, meaning, desc_*, impl }                то, что до «@»
│  ├─ query                                                       свои параметры схемы
│  │  └─ <param> { type, values, allowlist, default, aliases, maps_to, ext, desc_*, impl }
│  │       `allowlist` — имя набора в allowlists.json; `ext` — параметр вне ядра
│  │       общие параметры (sni, alpn, fp, pbk, sid, path, host, …) здесь НЕ
│  │       повторяются — они в tls.json и transports.json
│  └─ fragment: "label"                                          то, что после «#», — имя узла
│
├─ mapper: [ … ]      — СТРУКТУРНЫЕ ЗАМЕНЫ (описание)
│  └─ { id, from, to, kind, desc_*, impl, code?, applies_to? }
│       kind ∈ { drop, spelling, structure, default, split }
│       пример: flow=xtls-rprx-vision-udp443 → flow=xtls-rprx-vision + packet_encoding=xudp
│
├─ body               — КАК ПРОВЕРЯТЬ ТЕЛО (исполняется)
│  ├─ core: "1.14.1-lx.4"     ядро, по структурам которого описаны поля
│  ├─ order: [ … ]            порядок ключей при записи; он же порядок полевых кодов (CANON §6)
│  ├─ fields
│  │  └─ <поле> { атрибуты — см. ниже }
│  │       { type:"ref", ref:"tls" | "transports" | "multiplex" | "dialer.common" | … }
│  │       "__dialer": { type:"ref", ref:"dialer", inline:true }  — поля dialer.json вливаются в корень тела
│  └─ relations: [ … ]        правила о НАБОРЕ полей, а не о поле (см. ниже)
│
├─ emit               — про share-ссылку (описание)
│  ├─ share_uri: true|false   возможна ли ссылка у схемы вообще
│  ├─ param_order: [ … ]      нормативный порядок параметров
│  └─ note
│
└─ refs { go:[…], dart:[…] }
```

Не у каждого файла есть все секции: `chain.json` — только `body` (ссылкой не
приходит, `sources` нет), `group.json` — без `body` (это не узел ядра, и
`emit.share_uri` у него `false`).

### Атрибуты поля `body.fields.<поле>`

Полный перечень — `schema/registry_body.schema.json`, определение `field`.
Ниже — то, что реально исполняет санитайзер (`core/config/nodeflow`), и
отдельной строкой — то, что сегодня остаётся заметкой.

| Группа | Атрибуты | Смысл |
|---|---|---|
| Тип | `type` (`string`/`int`/`uint16`/`bool`/`enum`/`duration`/`object`/`array`/`string_array`/`listable_string`/`awg_range`/`ref`), `values`, `format` (`base64`, `base64_32`, `cidr`, `port`, `uuid`, `host`, `hex`, `ipv4`, `url_path`), `pattern` (RE2 ∩ Dart), `min`/`max`, `len`, `len_parity`, `items` | какое значение годное |
| До проверки | `normalize` (`trim`, `trim_lower`, `hex_only`, `range_order`), `normalize_code`, `absent_values` | как привести значение и какие значения равны отсутствию ключа |
| Объект не задан | `absent_when {ключ: значение, …}` — у поля-объекта либо у секции суб-схемы (`tls`), откуда переезжает в каждое поле с `ref` | совпали ВСЕ перечисленные ключи → объект снимается ЦЕЛИКОМ и ТИХО, кода нет: `tls:{enabled:false}` для ядра значит «TLS не задан». Судится ДО правил полей объекта и ДО связей соседей — снятый объект «не задан» для любой проверки наличия (норма порядка — `docs/CANON.md` §6.1) |
| Исход | `on_invalid { action: drop \| coerce \| drop_node \| unwrap, value?, key?, else_code?, code }`, `default`, `required` | что делать с негодным: снять поле, заменить, отбраковать узел, развернуть объект-обёртку до его члена `key` (1.1.57) — и каким кодом это назвать |
| Связи | `conflicts [{with, code}]`, `requires [{path, equals?, when?, unless_set?, set?, code}]`, `allowed_for` / `forbidden_for` + `forbidden_codes {схема: код}` | поле в зависимости от соседей и от схемы. `requires` без соседа снимает поле; с `set` (1.1.61) — поле остаётся, а соседа дописывает значение `set` с кодом (судится по готовому телу: поле, не пережившее своих правил, ничего не дописывает) |
| Условные | `default_when`, `max_when`, `min_when` — с `when.any_set` (судит НАЛИЧИЕ ключей); `except_sources` и `note_code` живут ВНУТРИ `max_when`, не у поля | правило для рода узла, а не для поля; `except_sources` — единственное место, где ВХОД влияет на результат |
| Без изменения | `advisory [{ values \| except, when, code }]` | значение остаётся, узел получает код |
| Условная замена | `coerce_when { values, value, when, code }` (1.1.61) | ГОДНОЕ значение из `values` при условии `when` заменяется на `value` с кодом; условие судится по готовому телу, после обхода |
| Каталог strip цепочки | `on_hop_required { action: unstrip, code }` (1.1.61) — у ключа `chain.json` strip.fields | ключ, который снял бы с тела звена на позиции ≥ 1 требуемый им путь (санитайзер вернул бы его `requires … set`), снимается с патча цепочки с кодом; цепочка собирается (`config.ChainUnstripRequired`, `nodeflow.StripBlocked`) |
| Служебное | `secret` (маскирует значение в warning'е), `tristate` (пустое значение пишется явно), `managed` (ключ пишет сборка, снимается молча) | |
| Роль | `role` (`credential` \| `private_key`, 1.1.59) — только у поля верхнего уровня тела протокола, каждая роль не больше одного поля на схему | по роли общий код находит поле без имени схемы: `credential` — учётные данные узла (читаются одинаково на всех входах), `private_key` — приватный ключ, уезжающий в share-ссылку (выдача только после подтверждения) |
| Гейты сборки | `min_core`, `platform` | исполняет `nodeflow/gate.go` при сборке config.json, а не при разборе; ⚠ на узле не ставится |
| Узловой гейт ядра | `on_core_unsupported { action: drop_node, code }` (1.1.60) — у тела протокола, у поля и в `range_form`; `range_form { min_core?, build_tag?, level?, on_core_unsupported? }` — только у `awg_range` | требование рядом (`build_tag`/`min_core`) не выполнено текущим ядром (теги сборки + версия) → УЗЕЛ снимается на сборке с кодом, конфиг собирается без него (`nodeflow.NodeCoreRefusal`). Без атрибута `build_tag`/`min_core` тела описательные, а поля — под полевым гейтом выше |
| Уровень | `levels` (у тела протокола, по возрастанию), `level`, `level_mark` (у поля), `range_form.level` (1.1.60) | подпись узла: старший уровень заданных полей и форм-диапазонов + суффиксы (`registry.Level`) |
| Только документация | `all_or_nothing`, `deprecated`, `drop_always`, `skip`, `lx_only`, `build_tag` (кроме узлового гейта и снятия расширения формой), `impl_decision`, `decision_pending`, `aliases` | объявлены в схеме (часть — и в Go-структуре `registry.Field`), но ни одной строкой кода не исполняются |
| Текст | `desc_en`, `desc_ru` (в документацию), `impl` (нет) | |

`body.relations[]` — правило о НАБОРЕ путей, которое парой «поле ↔ поле» не
выражается: `{ kind, paths, defaults, action, code, desc_*, impl }`. Сегодня
такой вид один — `ranges_disjoint` (непересечение диапазонов `h1..h4` у
AmneziaWG); он исполняется санитайзером. Это НЕ атрибут поля.

### Общие файлы

```
tls.json
├─ tls
│  ├─ params   { security, sni, alpn, insecure, fp, ech }   — общие параметры ССЫЛКИ (как uri.query)
│  ├─ reality  { pbk, sid, key_share }
│  └─ policy   { quic_strip, disabled_block, plaintext_ports, utls_junk,
│                singbox_import, emit_allowlist }
│        НЕ настройки: это записанные прозой РЕШЕНИЯ про TLS с указанием, где
│        каждое исполнено. Часть из них давно стала правилами в body.fields
│        (quic_strip = forbidden_for + forbidden_codes), и policy остаётся
│        объяснением «почему», а не источником поведения
├─ mapper: [ { id, applies_to:[схемы], from, to, kind, … } ]  — замены, общие для нескольких схем
├─ body { core, order, fields }                                — поля блока tls тела
└─ refs

transports.json
├─ transports { ws, grpc, http, httpupgrade, xhttp }            — сторона ССЫЛКИ
│  └─ <транспорт> { singbox_type, params:{…как uri.query…}, maps_to, impl }
├─ mapper: [ … ]
├─ body { core, discriminator:"type", values:[…], variants }    — сторона ТЕЛА; здесь ещё
│  │                                                              и quic, у которого параметров
│  │                                                              ссылки нет, — всего 6 вариантов
│  └─ variants.<тип> { order, fields }                          — своя таблица полей на каждый тип
└─ refs

multiplex.json   body { core, order, fields }
dialer.json      common { core, order, fields } — поля, на которые схемы ссылаются поимённо
                   (ref:"dialer.common", ref:"dialer.common.network");
                 body { core, order, fields, skipped } — вливается в корень тела через
                   "__dialer" с inline:true
                 skipped — поля ядра, которые тело не несёт НИ ОДНИМ входом
                   (skipped ≠ «поля нет у ядра»)
```

### Словари

```
warnings.json          75 кодов
├─ text_params_implicit: ["path","value"]      подстановки, доступные каждому тексту
└─ warnings.<code>
   { severity: error|warning|info, params:[…],
     title_en/ru, text_en/ru, cause_en/ru, fix_en/ru,     что показать человеку
     desc, go, dart }                                      заметки разработчикам
     params нормативен: санитайзер выбрасывает подстановки, которых код не объявил

backup_warnings.json   warnings.<code> { severity, params, side: export|import|both, desc }
allowlists.json        allowlists.<имя> { values, note }   — на них ссылается uri.query.<param>.allowlist
limits.json            limits.<имя> { value, current:{go,dart}, note }
containers.json        containers.<имя> { desc, refs, + свои ключи на контейнер:
                         detect, decode, expand, subscription_body, variants, guards,
                         recursion, selection, limits, label } — vpn://, wgconf INI, base64
vars.json              vars.<имя> { type, portable, in, note }
presets.json           presets.<id> { in, label, note }
```

### Как читать правило целиком

Негодное значение `encryption` у vless: `vless.json` → `body.fields.encryption`
(`normalize: trim`, `absent_values: ["none"]`, `pattern`,
`on_invalid.action = drop_node`, `on_invalid.code = vless_encryption_invalid`)
→ `warnings.json` → `warnings.vless_encryption_invalid` (уровень и тексты на
двух языках). В коде приложений этого правила нет — есть движок, который умеет
`pattern` и `drop_node`.

## Правила изменения

1. **Контракт раньше кода.** Новый протокол/параметр/приём начинается с PR сюда
   (реестр + фикстуры), реализации — вторым шагом.
2. Golden-файлы `corpus/**/*.expected*` нормативны: перегенерация раннером
   с `-update` — это осознанный PR с ревью диффа, не рутина.
3. Каждое расхождение проектов либо чинится, либо называется явно —
   бесхозных различий не бывает. Для протоколов и кейсов корпуса это маркер
   `"extension": "desktop"|"mobile"` (`registry/protocols/*.json`,
   `meta.extension` — им раннер пропускает чужие кейсы). Для ПОЛЕЙ схемы
   бэкапа маркера нет: поле объявляется в схеме, а кто его применяет,
   говорит колонка «Поддержка» в `docs/BACKUP.md` §2 (D-094).
4. Решения протоколируются в `SPECS/103-F-O-LX_SHARED_CONTRACT/DECISIONS.md`.
5. **Арбитраж неоднозначного парсинга** (D-019): если из кода/тестов обоих
   проектов непонятно, как разбирать параметр подписки — порядок сверки:
   поведение ядра sing-box-lx → как параметр парсит **Happ** → конвенции
   Xray/v2rayN. Вердикт — в note реестра + DECISIONS.md.

## Потребители

- Лаунчер: конформанс-тесты `core/config/subscription/contract_test.go` (фаза 1).
- LxBox: копия каталога через `app/tool/sync_contract.sh` + пин `contract.lock`;
  тесты `app/test/contract/` (фаза 1).

## Версии

| Версия | Что появилось |
|--------|---------------|
| 0.1.0 | Каркас: канонический узел, реестры протоколов/лимитов/warning'ов/переменных, корпус URI (282 фикстуры), язык шаблонов |
| 0.1.1 | `#enable`, рекурсия условий, помеченные ключевые слова (`#and`/`#or`/`#value`/`#else`/`#on_change`/`#set`), раздел `corpus/template/deps/` |
| 0.2.0 | Корпус тел подписок (`corpus/body/`) и бэкапов (`corpus/backup/`); коды деградации на узле; правило `scheme` в CANON §1; закрыты расхождения gecko, выбора vpn://-контейнера и несжатого профиля |
| 0.3.0 | Направления (SPEC 104): `schema/direction.schema.json`, корпус `corpus/direction/`, перенос целей правил в бэкапе (`directions[]`, схема v1.1) |
| 0.4.0 | Свёртка подписки в группу (SPEC 108): `schema/source_fold.schema.json` (`auto` через `$ref` на форму Направления), кейсы `fold_*` в корпусе Направлений, поле `fold` в расширении подписки бэкапа |
| 0.5.0 | Полный конфигуратор DNS (SPEC 109): корпус `corpus/dns/` (материализация DNS-сервера), вложенная форма записей `{vars, server}` разворачивается при загрузке (`NormalizeDNSOptions`); закрыты разрывы N8 (форма записей) и N9 (имя типа `dns_server`) |
| 0.6.0 | Цепочки хопов (SPEC 110): `schema/source_chain.schema.json`, кейсы `chain_*` в корпусе Направлений, коды `chain_*` в реестре warning'ов |
| 0.7.0 | Перенос цепочек в бэкапе (SPEC 110, схема v1.2): корневая секция `chains[]` вместо блоба `extensions.launcher`, идентичность и merge по `tag` (warning `backup_chain_exists`), нормативный порядок записей; кейсы `chains_roundtrip` и `chain_tag_duplicate` в `corpus/backup/` |
| 0.7.1 | Умолчание `enabled` у цепочки закрыто корпусом: поле в ожиданиях `chains[]` (необязательное) и кейс `chain_disabled_enabled_default` — отсутствие ключа читается как `true` обеими сторонами |
| 0.7.2 | Рубеж загрузки в корпусе шаблонов (D-077): необязательное поле `load` в `<case>.expected.json` — `accept` (дефолт) / `reject` (валидатор обязан отвергнуть; рантайм-ожидания продолжают проверяться толерантным прогоном) / `either` (не нормирован — класс undeclared-имён). Классифицированы все 94 кейса: 7 reject, 8 either |
| 0.9.0 | **У Направления снесено поле `label`**: имя ровно одно — `tag`. Два имени у одной сущности означали, что в списке пользователь видит одно, а в выпадашке целей правил — другое, и связать их не может. `direction.schema.json` держит `additionalProperties:false`, поэтому запись с `label` больше НЕ проходит валидацию; при чтении старого состояния ключ игнорируется (значение отбрасывается, именем остаётся `tag`), при записи не эмитится. Переименование Направления = смена `tag` вместе со всеми ссылками на него. **Требуется встречное изменение в LxBox**: перестать писать `label` у каналов и показывать `tag`. Правило НЕ распространяется на `label` узлов, пресетов и источников (подписка/сервер/цепочка) — там ссылочного тега в паре нет. Корпус `corpus/direction/` перечищен (7 кейсов). **Уточнено в 0.12.4**: у LxBox `label` Направления живёт — он его пишет и читает, а лаунчер игнорирует молча (D-094); схема снова знает поле, но целью правил остаётся `tag`, и встречная правка «перестать писать `label`» для LxBox отменена. **Уточнено в 0.12.0**: у `servers[]`/`chains[]` ссылочный тег в паре как раз появился (`node_tag`/`tag`), и `label` там снесён по этому же образцу; у `subscriptions[]` правило по-прежнему не действует |
| 0.8.0 | Снос легаси-приёма MASQUE (D-078): `?network=`/`?server_name=` больше не читаются (игнор как неизвестный параметр; `vhttp`/`sni` — единственные имена), плоские `network`/`sni`/`skip_cert_verify` sing-box-импорта стрипаются без переноса значений; фикстуры `legacy_*` переделаны на «легаси не читается» (`legacy_params_ignored`), канон-кейс переименован в `canonical_dual_stack_sni` |
| 0.10.0 | **Идентичность узла = его тег** (SPEC 112): identity-хеш (sha256 от эмитированного JSON) упразднён. Ключ — сырой провайдерский тег, уникализированный в рамках источника и снятый ДО tag_prefix/tag_mask; содержимое узла (server, port, ключи, SNI, транспорт, mtu) в идентичность не входит. Причина: провайдер вправе поменять сервер под тем же именем — это тот же узел, а хеш зависел ещё и от формы хранения (`uri` и `config_json` одного узла хешировались по-разному), из-за чего detour-ссылка на узел молча протухала и зависимый источник fail-closed выпадал из конфига. Ссылка detour переехала с `detour_node_hash` на ОБЪЕКТ `detour_node_source_id` + `detour_node_tag` (id источника-цели + identity-тег узла внутри него) — **амендмент 0.10.0 (SPEC 112-A), номер версии не поднимается: 0.10.0 ещё не выпущен**. Финальный конфиговый тег в состоянии больше не хранится: он вычисляется на каждой сборке, потому что хранимый протухал от смены `tag_prefix` источника-цели. Сборка строго двухпроходная (сначала все финальные теги, потом резолв ссылок по единой карте), резолв строгий — обе части ссылки обязаны сойтись, иначе fail-closed с сообщением, называющим обе стороны по именам; смену идентичности узла (переименование его тега) UI отрабатывает сбросом ссылок и предупреждением. Контент-дедуп упразднён; вместо него дедуп записей на PARSE-слое по ключу подключения `scheme\|server\|port\|credential` — **амендмент 0.10.0 (SPEC 112-B), версия не поднимается по той же причине**: ключ живёт один разбор источника, в состояние не пишется, на отметки и ссылки не влияет, выживает первая запись, пустой ключ не схлопывает. Ключ жёстче прежнего хеша (один сервер + один креденшл с разными SNI = одна запись), и by-design различие с LxBox `nodeIdentityKey` из-за этого СХЛОПНУЛОСЬ — per-app override'ы корпуса, поставленные ради него, подлежат снятию при ревью (`docs/IDENTITY.md` §1.2, §4). Legacy-ключи (64 lowercase hex) мигрируют при первом разборе источника, для detour — в полную ссылку, а при неопознанном хеше с fallback на `detour_node_label` как tag-only ссылку (`docs/IDENTITY.md` §5.1–5.2). **Требуется зеркальная правка LxBox** (процесс как chains/0.7.1): до неё перенос disabled-отметок между приложениями сломан |
| 0.11.0 | **Бэкап = сериализация состояния** (SPEC 114): механизм `extensions` и обязанность провозить чужой блоб УПРАЗДНЕНЫ. Все поля из `extensions.launcher` (`id`, `skip`, `outbounds`, `node_tag`, `fold`, `exclude_from_global`, `expose_group_tags_to_global`, `detour_tag`, `detour_node_source_id`/`detour_node_tag`/`detour_node_label`) растворены в схеме сущностей НЕОБЯЗАТЕЛЬНЫМИ полями; `detour_node_hash` в схему не входит. Нормативные принципы вынесены в `docs/BACKUP_PRINCIPLES.md` (П1–П7) и побеждают при конфликте с `docs/BACKUP.md`. `additionalProperties` открыт на любом уровне — схема не валит чужой файл с лишним полем, отбрасывать непонятое с warning обязан импортёр (П3). Legacy-путей чтения нет (П4): старый файл разбирается общим правилом, `extensions` любой глубины даёт ОДИН warning на файл с перечнем затронутых записей. Экспорт — чистая функция состояния: два экспорта одного состояния байт-идентичны, а импортированное состояние неотличимо от настроенного руками (П1) — теневые поля `ForeignExtensions`/`_backup_fields` снесены. Режим импорта один — replace; merge удалён как недостижимый из UI (`docs/BACKUP.md` §9). **Уточнено в 0.12.5**: режима «заменить всё» больше нет — источники СЛИВАЮТСЯ по идентичности (D-095), замещаются только правила, DNS, переменные и `route.final`. **Требуется зеркальная правка LxBox** |
| 0.11.1 | MASQUE `vhttp` принимает `auto` (h3 с откатом на h2; ядро >= lx.27, дефолт ядра с lx.28) наравне с `h3`/`h2` — кейс `vhttp_auto` в `corpus/uri/masque/`; код `masque_vhttp_invalid` срабатывает только вне этой тройки. Parse-дефолт без `vhttp` по-прежнему явный `h3`. Хранимые URI старого диалога WARP с `?network=` лаунчер разово переписывает в `?vhttp=` при загрузке состояния (правка хранения, D-078 не затронут). **Зеркальная правка LxBox**: masque_parser принимать `auto` |
| 0.12.0 | **Снос `label` у `servers[]` и `chains[]`** (по образцу 0.9.0): у канона v7 имя узла одно — тег (SPEC 112), а вторая подпись расходилась с ним и молча терялась. В схеме поля больше НЕТ; чтение оставлено legacy-входом для файлов 0.11 и раньше — у сервера без `node_tag` подпись ещё может стать тегом, разошедшаяся отбрасывается с `backup_label_dropped`. У `subscriptions[]` `label` ОСТАЁТСЯ: это имя источника, ссылочного тега в паре с ним нет. **Объект `subscriptions[].identity`** — чем подписка представляется провайдеру (`user_agent`, `send_hwid`, `hwid`, `device_os`, `ver_os`, `device_model`, `hash_device_model`); все ключи необязательны, отсутствие ключа значит «как в системе», а не «выключено» (потому у булевых — три состояния). Лаунчер применяет четыре ключа, mobile-only тройку и незнакомое отбрасывает ОДНИМ `backup_source_identity_dropped` на подписку с перечнем. **Папка = контейнер одиночных серверов у обеих сторон**: отдельной секции нет, запись `servers[]` носит поле `folder` — имя, и обе стороны собирают контейнер по нему; порядок членов и «одно имя = одна папка» нормативны. Настройки самой папки и её члены не-server в файл не едут. **Новый код `backup_local_only_dropped`** — «настройки своей стороны, которым в схеме дома нет», один на оба направления и обе стороны: у лаунчера это `relays_in_directions`, настройки папки и её члены не-server, у LxBox — `import_rules`, свои настройки папки, политика detour источника; identity-код остался за ключами `identity`. Словарь всех кодов бэкапа вынесен в `registry/backup_warnings.json`. **`hysteria` (v1) = `extension: desktop`** — у LxBox парсера нет, и по правилу 3 различие помечено, а не бесхозно; раннер стороны пропускает каталоги и кейсы чужого extension. **Канон кейса `dialer_proxy`** — вложенный `chain[]` внутри узла-владельца (CANON §2.1-2.2), порядок хопов нормативен; материализация в отдельные служебные узлы с detour это модель состояния лаунчера, вне контракта разбора (D-085). Правило позиционного дериватива тега замены — D-081. Новые кейсы корпуса: `replace_tag_index`, `folders_roundtrip`, `hysteria2_bandwidth` (обе стороны), `hysteria_bandwidth` (v1, `meta.extension: desktop` в expected), `dialer_proxy` и `dialer_proxy_missing`. Ожидания корпуса бэкапа пополнились необязательными полями `replace_tags` и `folders`. **Зеркальные правки LxBox**: `identity`, снос `label` у `servers[]`/`chains[]`, сборка папки по `folder`, `backup_local_only_dropped`, sync корпуса |
| 0.12.1 | **Запись отбраковки корпуса — `{ref, code, reason}`** (D-088): нормативны `ref` (что отвергнуто) и `code` (машинная причина из `registry/warnings.json`), а `reason` — человеческий текст СТОРОНЫ и сравнением **не покрывается**: у лаунчера это формат ошибки Go, у LxBox свой, и побайтовая нормативность заставила бы вторую сторону копировать чужие строки. `code` необязателен — ожидание без него проверяется только по `ref` (старые URI-golden кодов не знают, коды им заведёт отдельный проход); объявленный `code` сравнивается строго. Новый код реестра **`dialer_proxy_unusable`** (severity `error`, params `target`/`cause`): цель `streamSettings.sockopt.dialerProxy` непригодна — узел-владелец отбраковывается целиком, иначе он ушёл бы напрямую (тихая деанонимизация); `cause ∈ not_found | service_outbound | group | not_convertible | cycle | depth`, лаунчер сегодня даёт `not_found`. Перегенерирован один кейс — `corpus/body/xray/dialer_proxy_missing`. **Зеркальная правка LxBox**: маппинг причин в код, раннер сравнивает `ref`+`code` и игнорирует `reason` |
| 0.12.2 | **`warp[].sni` и `warp[].idle_timeout` объявлены в схеме с пометкой `extension: mobile`** (D-091) — это конец последнего кармана `extensions.lxbox`: у LxBox оба поля лежат на MASQUE-аккаунте, у лаунчера их на аккаунте нет (SNI берётся из пула на сборке конфига, `idle_timeout` ставит UI), поэтому «обе стороны» тут невозможны, а тайный карман недопустим (П1/П3) — остаётся объявленное чужое поле. Новое общее правило `docs/BACKUP.md` §1: **поле, объявленное в схеме с пометкой `extension` чужой стороны, принимающая сторона игнорирует МОЛЧА** (D-092) — это не «непонятое» (П3), а чужое объявленное, и warning давал бы шум на каждом импорте; провоз насквозь допустим, но не обязателен (лаунчер `warp[]` везёт сырым JSON). В `registry/backup_warnings.json` два side-specific кода, которые эмитирует **LxBox**: `backup_dns_entry_skipped` (DNS-запись файла не применена) и `backup_warp_skipped` (WARP-аккаунт пропущен). **Зеркальная правка LxBox**: писать `sni`/`idle_timeout` плоско полями записи `warp[]`, снести `extensions.lxbox` |
| 0.12.3 | **Ещё три поля WARP-аккаунта объявлены в схеме с пометкой `extension: mobile`** (D-093, по правилу D-091): `warp[].keep_alive` (MASQUE, период keep-alive, строка длительности как `idle_timeout`), `warp[].endpoint` (wg-аккаунт, `host:port`, LxBox пишет только не дефолтный) и `warp[].awg` (wg-аккаунт, объект параметров AmneziaWG, состав открытый). У LxBox все три лежат НА аккаунте, у лаунчера на аккаунте их нет: `endpoint` и параметры AWG он берёт из пула `warp_endpoints.json` на сборке конфига, keep-alive ставит UI. Приехавшие значения лаунчер не применяет — молча, по правилу D-092 «чужое объявленное игнорируется без предупреждения», — но провозит сырым JSON вместе со всем разделом `warp[]`. Карман `extensions.lxbox` закрыт целиком. **Зеркальная правка LxBox**: писать `keep_alive`/`endpoint`/`awg` плоско полями записи `warp[]`, карман не возвращать |
| 0.12.4 | **Поле `label` возвращено у `directions[]` и `chains[]` — объявленным полем LxBox** (D-094): LxBox подпись Направления и цепочки пишет и читает, лаунчер её игнорирует МОЛЧА (имя у него — тег, SPEC 112) и на экспорте не пишет. `direction.schema.json` открыл необязательное `label`: там `additionalProperties:false`, и запись LxBox обязана проходить валидацию, а не отвергаться; `backup.schema.json` вернул `label` у `chains[]` и у своего `$defs.direction`. Предупреждений об этих полях больше нет ни у одной стороны: `backup_label_dropped` остался ровно за `servers[]` (legacy-вход, подпись, разошедшаяся с тегом), а `directions[].label` перестал быть неизвестным ключом и `backup_unknown_field` не даёт. Решение перекрывает D-082 в части `chains[]` и встречную правку 0.9.0 для LxBox; `servers[]` не тронут. **Терминология**: концепция «`extension: mobile/desktop`» для ПОЛЕЙ схемы бэкапа снята как мёртвая — правило `docs/BACKUP.md` §1 переписано как «поле объявлено в схеме; каждая сторона применяет то, что умеет, остальное молча игнорирует и по возможности провозит сырым JSON», а кто что применяет, говорит колонка **«Поддержка»** (лаунчер / LxBox / обе). Пометки `extension: mobile` у пяти полей `warp[]` заменены на «Поддержка: LxBox» — смысл тот же. Маркер `extension` в `registry/protocols/*.json` и `meta.extension` в корпусе не тронуты: это механизм пропуска кейсов раннером, другая граница. Side-specific golden `corpus/backup/chains_roundtrip.expected.lxbox.json` (класс различия B, `docs/IDENTITY.md` §4a). **Зеркальная правка LxBox**: §405 — вернуть `label` в модель, UI и бэкап Направлений и цепочек |
| 0.12.5 | **Импорт бэкапа — СЛИЯНИЕ по идентичности, а не замена** (D-095, решение владельца: «хочу, чтобы лаунчер работал как LxBox, у LxBox более здоровая логика»). Норма §9 `docs/BACKUP.md` переписана единым списком из 8 пунктов, одним на обе стороны: (1) подписки — по `url` **байт в байт** без нормализации, совпавшая держит локальные `id`, состав и историю, а настройки берёт из файла (`label` только непустой, `identity` слепком целиком — отсутствие объекта = сброс на «как в системе», отметки `disabled` ОБЪЕДИНЯЮТСЯ); (2) одиночные серверы — дедуп по ТЕЛУ, тег не участвует, тело берётся из ИСХОДНИКА записи, а не из материализованного outbound'а (иначе живущий материализованный узел не совпадёт с приехавшим `uri` и сервер удвоится на каждом импорте): `uri` после TrimSpace и **без фрагмента `#имя`**, `config_json` — в канонической форме identity-хэша (D-007: сняты верхнеуровневые `tag`/`detour`, ключи отсортированы рекурсивно, без пробелов, сырой UTF-8 без экранирования); совпало → пропуск БЕЗ warning, иначе тег уникализируется суффиксом `-2`; (3) серверы с `folder` — имя папки КАК ЕСТЬ (без trim, с учётом регистра), члены по телу внутри папки; (4) цепочки и Направления — по тегу, совпадение точное с учётом регистра; (5) **DNS — слияние «своё сильнее»**, а не замена (серверы по `kind`+`tag`, dns-правила по `kind`+`ref`+телу; `dns.final`/`strategy` замещаются), `vars` — только переносимые, `route.final` — только при известной цели; (6) `warp[]` — добавление, дубли по ключу аккаунта (`device_id`, иначе приватный ключ) молча; (7) `rules[]` — **единственная** секция полной замены; (8) порядок: совпавшие держат локальную позицию, новые в конец в порядке файла, `id` совпавшей локальный, новой — из файла, при коллизии свежий ULID. Решение перекрывает D-089 и часть 0.11.0 про replace. Конвенция корпуса: `<case>.pre.backup.json` импортируется в ПУСТОЕ состояние первым, затем в него сливается `<case>.backup.json` — без предсостояния слияние и замена неразличимы. Ожидания корпуса пополнились необязательными `subscriptions` (`{url → {label, prefix, postfix, enabled, nodes, pending_disabled}}`) и `root_servers` (теги корневых узлов по порядку). Кейс `corpus/backup/merge_sources_by_url`. П1 и П5 `BACKUP_PRINCIPLES.md` уточнены (тождество `import(export(x))` читается из чистого состояния; адресация и ключ слияния — не одно и то же). **Зеркальная правка LxBox**: дедуп одиночных серверов по телу с канонизацией `config_json`, точное сравнение тегов с учётом регистра, `warp[]` добавлением по ключу аккаунта |
| 0.12.6 | **`ping_url` и `ping_timeout_ms` у `directions[]` — объявленные поля LxBox** (D-096, по правилу D-094): у LxBox бюджет замера узлов Направления (URL пробы и таймаут одного замера, мс) настраивается на Направление, и в LX-формат он не ехал — молчаливая потеря (П6). Поля объявлены в `direction.schema.json` (там `additionalProperties:false`) и в `backup.schema.json` `$defs.direction`, «Поддержка: LxBox»; лаунчер игнорирует молча и провозит, на экспорте не пишет. Это НЕ `auto.url`/`auto.idle_timeout` — параметры urltest-двойника ядра, — а бюджет замера в приложении. Кейс `corpus/backup/directions_ping_roundtrip` с side-specific ожиданием `.expected.lxbox.json` |
| 0.12.7 | **XHTTP: `host`/`path`/`mode` берутся ТОЛЬКО из плоских параметров ссылки** (D-097): одноимённые ключи внутри `extra=` игнорируются целиком — и пустые, и непустые. Это исключение из правила «при конфликте `extra` выигрывает» (SPEC 002 §1.5), и оно взято у самого Xray: `infra/conf/transport_method.go`, `SplitHTTPConfig.Build()` после разбора `extra` безусловно затирает базовую тройку внешними значениями (`extra.Host = c.Host; extra.Path = c.Path; extra.Mode = c.Mode`), то есть плоские побеждают даже будучи пустыми. Для прочих ключей `extra` остаётся базой, и пустое значение из `extra` плоское не перекрывает (кейс `xhttp_v2_extra_wins_over_flat` про `xPaddingBytes` в силе). Основание — живой кейс 4PDA #1755: ссылка несла плоские `mode=packet-up&path=/hls/v2/track/…/&host=media.…` и `extra` с пустыми `host`/`path`/`mode`; по старому слиянию `path` съезжал на `/` (сервер отвечал `unexpected upload status 404`), а пустой `mode` давал `auto`, из-за чего ядро роняло ВЕСЬ конфиг на `uplink_data_placement can be header only in packet-up mode`. Новые кейсы корпуса: `corpus/uri/vless/xhttp_extra_empty_mode_keeps_flat` (пустая тройка в `extra`) и `corpus/uri/vless/xhttp_extra_host_path_mode_ignored` (непустая тройка в `extra`). Существующие golden не менялись. **LxBox: §410 уже сделан**, встречной правки не требуется |
| 0.12.8 | Новая desktop-переменная `tun_address_exclude` (`text_list`, непереносима): `tun.route_exclude_address` — CIDR-префиксы, которые `auto_route` оставляет вне туннеля на уровне маршрутов ОС. Default пустой; пустой список эмитится как `[]`. Идея — PR #114. **XHTTP §416/§217: пара `mode`/`uplink_data_placement` нормируется на эмиссии** (обе стороны, с warning): `header` без режима → дописать `mode: packet-up`; `header` при явном другом режиме → снять placement, режим не трогать; `packet-up` — без изменений. Ядро иначе отвергает весь конфиг. Корпус: `xhttp_uplink_header_placement_adds_packet_up` (новый), `xhttp_uplink_header_placement_reset` (ожидание приведено к сбросу, 5ff6a7a) |
| 0.12.9 | **`chains[].chain.interrupt_exist_connections`** (SPEC 124 лаунчера, SPEC 075 ядра): необязательный bool в `source_chain.schema.json`, рвать ли внешние соединения цепочки при переключении позиции тумблером в рантайме; отсутствие = false, пишется только `true`, ключ конфига ядра одноимённый. Поддержка: обе — LxBox эмитит `chain` outbound и бэкап цепочек. Кейс корпуса `corpus/direction/chain_interrupt_connections`; `chain_strip_and_timeout` не тронут. **Зеркальная правка LxBox**: поле в модели цепочки, форма, эмиссия, бэкап (TASKS_LXBOX §11) |
| 0.12.10 | **REALITY принимает только chrome-подобный ClientHello** (D-104, SPEC 083 ядра): там, где эмитится блок `tls.reality`, отпечаток В КОНФИГЕ обязан быть из chrome-семейства (`chrome`, `chrome_psk`, `chrome_psk_shuffle`, `chrome_padding_psk_shuffle`, `chrome_pq`, `chrome_pq_psk`) — только их utls-спеки несут `key_share X25519MLKEM768`, которого требует REALITY-сервер Xray ≥ v26.9.8; без него сервер МОЛЧА уводит соединение на камуфляжный сайт, и узел мёртв без единой ошибки. Пустой fp при reality пишется как `chrome` ЯВНО; отпечаток вне семейства подменяется на `chrome` и даёт на узле новый код `reality_fp_not_chrome`. Подмена — НА СБОРКЕ КОНФИГА у обеих сторон (Go `core/build/tls_transforms.go` `HealRealityFingerprints`, Dart `post_steps/heal_unknown_utls_fingerprints.dart`), значение в `entry` остаётся исходным: оно нормативно (CANON §2). Дефолт `random` под код не попадает, но подменяется наравне. Note у `fp` в `registry/tls.json` переписан: прежнего расхождения «Dart подставляет chrome, Go — random» больше нет. Кейсы: `uri/vless/reality_fp_firefox_forced_chrome` (новый), `grpc_reality_no_flow`, `reality_tcp_no_flow`, `allowinsecure_lowercase_zero`. **LxBox: правило уже реализовано** — встречная правка только привязать `RealityFingerprintWarning` к коду реестра (TASKS_LXBOX §14). **Диалект ссылки naive зафиксирован явным перечнем** (D-105): набор параметров прежний (`padding`, `extra-headers`, DuckSoft 2020), новый код `naive_extra_headers_invalid` (info) на отброшенной паре `extra-headers` — раньше отброс уходил только в лог; молчаливый игнор неизвестных query-ключей и дефолтный `quic_congestion_control: "bbr"` у `naive+quic` записаны в норму как есть. Кейс `uri/naive/extra_headers_bad_name_dropped`. **LxBox: встречная задача** — `NaiveExtraHeadersInvalidWarning` (TASKS_LXBOX §14) |
| 0.12.11 | **Xray-JSON: `users[0].encryption` у VLESS переносится в `entry.encryption`** (D-108, issue лаунчера #121): правило то же, что у URI-пути — после `trim` пусто или `none` без учёта регистра → поля нет, иначе значение как есть, без обрезки и нормализации (ключ ML-KEM `mlkem768x25519plus.native.0rtt.<key>` ~1600 символов base64url с `-`/`_`). Баг был только у лаунчера, LxBox поле переносил и раньше. Кейсы `corpus/body/xray/vless_encryption_mlkem` и `vless_encryption_none_dropped`. **От LxBox**: sync `contract/` и прогон двух кейсов; код менять не должно понадобиться |
| **1.0.0** | **Бэкап стал сериализацией состояния — формат 1.0, `lx_backup: 2`** (D-109, D-110; SPEC 127 лаунчера): маппера между «формой файла» и «формой состояния» больше нет, записи файла — те же записи, что лежат в состоянии, с теми же именами полей. Что изменилось: четыре секции `subscriptions[]`/`servers[]`/`chains[]` + поле `folder` свёрнуты в ОДНУ `sources[]` с дискриминатором `kind` (папка получила собственную запись с `id`, `name`, `tag_policy`, `fold`, `detour` и составом в `nodes[]` — туда едут узлы всех видов, включая цепочки и неразобранные записи, которых 0.12 выразить не мог); `node_tag`→`tag`, `uri`/`config_json`→`origin{kind,raw}`+`body`, `label` подписки→`name`, `tag{prefix,postfix,mask}`→`tag_policy{prefix,postfix}`; у правила `match`+`outbound`→`body` (матчеры и цель одним куском в форме sing-box), `ref` у srs→`refs[]`, вид `json` снят; у DNS `name`→`tag`, `value`→`body`; плоская четвёрка `detour_*`→объект `detour{folder_id?, tag}`, позиции цепочки — `hops[]` той же формы вместо строк; **новый ключ `fold_tag`** — явное имя группы свёртки (позиционный дериватив D-081 давал папке и первой свёрнутой подписке ОДИН тег и молча подменял явное имя); появился дом у `sections` узла, `relays_in_directions` и настроек папки, поэтому `backup_local_only_dropped` писатель 1.0 не эмитит вовсе. Тонкий слой (исключения, зафиксированы обеими сторонами 14.09.2026): `directions[]`, `fold`, `disabled{}`, `identity{}`, `vars`, `route{final}`, `warp[]` — формы не менялись. Схемы: `schema/backup.schema.json` → 1.0 (`sources.items` — union по `kind` через `if/then`, чтобы ошибка валидации называла вид; `additionalProperties` открыт везде, кроме прежних мест и `sections`), прежняя копия сохранена как **`schema/backup-0.12.schema.json`** и заморожена для legacy-чтения. Слияние §9 по смыслу не менялось; уточнены три вещи: папка 1.0 матчится сперва по `id`, затем по имени (и только среди папок, существовавших ДО импорта — у пользователя бывают тёзки), preset-серверы DNS сливаются по `kind`+`ref` (ключ `kind`+`tag` схлопывал их все в один), перенумерация оси идёт ОДНИМ проходом по корневым и узловым правилам вместе. `NODE_SECTIONS.md` — норма (форма записей §1 = корневая, отбраковка B3 с `reason`, `@self` по умолчанию, висячий `endpoint`, жизненный цикл каталога Tailscale), `ONE_NAMESPACE.md` — норма. **Запись только 1.0** (D-110): с синхронных релизов (лаунчер 1.6.0, LxBox 2.24.0) обе стороны пишут только 1.0 и читают 0.x как legacy всегда; окно «два писателя, дефолт 0.12» отменено решением владельца, выбора формата на экспорте нет ни у одной стороны. Прогнать файл через Debug API лаунчера: `GET /backup/formats` (`writes: ["1.0"]`), `GET /backup/export`, `POST /backup/import` (файл 0.x или 1.0). **`contract/VERSION` поднят до 1.0.0**: LxBox 2.24.0 читает и пишет 1.0 — правило обеих сторон выполнено. Уточнены нормы по вопросам LxBox: форма `ref` preset-сервера DNS — `"<preset_id>:<tag>"` (§2), нумерация оси при импорте сплошная с 1000 одним проходом (§9 п. 7), `backup_source_kind_unsupported` — обе границы, у `backup_section_record_dropped` причина `unknown_key` (строгий разбор тела; лаунчер её не эмитит); side-specific golden `corpus/backup/v10_sources_union.expected.lxbox.json`. **Зеркальная правка LxBox**: `## 16` — сделано в 2.24.0 (чтение 1.0 + legacy 0.x одним слиянием, запись только 1.0, корпус 1.0 своим раннером) |
| 1.0.1 | **Норма ссылок NodeLink (D-112)**: ссылка на узел во всех проектах — `{folder_id, tag}`; у члена папки или подписки `folder_id` обязателен (`id` контейнера, у узла подписки — `id` самой подписки), `tag` сырой; корневая цель — `{tag}`; финальный тег живёт только внутри `body` sing-box. Норма `docs/NODE_LINK.md`: форма, резолв (без глобального поиска по тегу), не-узловые цели, операции, перепись id на импорте, известный разрыв Направлений, сверка сторон. Корпус: кейс `corpus/backup/v10_node_links`, ключи ожиданий `detours{}` и адрес ссылки `folder` / `subscription` / `folder_id`. Задача LxBox — `TASKS_LXBOX.md` `## 17`. **Группы, `default` и Направления без узлов (D-115, решения владельца 15.09.2026, форма согласована с LxBox)**: провайдерская группа адресуется `{id контейнера, сырой тег группы}` (у группы подписки — `id` подписки, сырой тег уникализируется общим счётчиком с узлами до `tag_policy`, сперва узлы, затем группы — `IDENTITY.md` §1.3), свёртка — `{tag: fold_tag}`; `group.default` — NodeLink (`$defs/autoGroup.default`: anyOf `nodeLink` \| string, писатель пишет объект), dev-формы лаунчера до 1.0.1 читатель поднимает (`NODE_LINK.md` §7.3); Направление на узлы не ссылается (вариант А, нового поля нет) — `directions[].include` только теги Направлений, прочие опции экспорт называет `backup_local_only_dropped`, чужую строку импорт лаунчера отбрасывает новым кодом `backup_direction_include_dropped` (`registry/backup_warnings.json`); `detour` у server/folder/subscription — «Поддержка: обе». Корпус: `v10_group_links`, `v10_dev_forms`, `v10_direction_include` (+ `.expected.lxbox.json`), новые необязательные ключи раннера `groups{}` и `directions[].include`. **Поля стороны LxBox объявлены** (`TASKS_LXBOX.md` §16.7, `BACKUP.md` §2): `detour_policy`, `import_rules`, `import_rules_enabled`, `on_update_action` у подписки, `detour_policy` и `tag_policy {prefix}` у сервера, `detour_policy`/`ping_url`/`ping_timeout_ms` у папки, `label` у цепочки, `members_rule`/`pool_badge` у группы, `update_interval_hours` у srs, `verbatim` у inline, `description`/`vars` у DNS-сервера — лаунчер игнорирует молча, отсутствие в файле значение приёмника не сбрасывает; код `backup_group_degraded` (`side: both`); кейсы `v10_lxbox_fields`, `v10_group_degraded` (+ `.expected.lxbox.json`). **Ось правил при импорте — номера файла сохраняются (D-116, уточняет D-110 и строку 1.0.0 выше)**: размеченное правило держит `num` из файла, неразмеченные корневые встают в хвост не ниже 1000, файл без размеченных корневых остаётся неразмеченным (`BACKUP.md` §9 п. 7, `NODE_SECTIONS.md` §5); DNS-дубли ищутся только среди записей приёмника до импорта (§9 п. 5). Порядок сырых тегов подписки — сперва узлы, затем группы (`IDENTITY.md` §1.3). **`contract/VERSION` поднят до 1.0.1**: LxBox подтвердил форму и делает свою часть до синхронного релиза (`TASKS_LXBOX.md` §17.8) **Импорт в пустое воспроизводит файл (D-117, VERSION не поднят)**: известные цели импорта — один список после слияния, с системными тегами шаблона приёмника (`BACKUP.md` §3); переменные маршрута шаблонных DNS-серверов (`dns_<tag>_outbound`, `dns_safe_dns_dot_dom_resolver`) переносимы в `registry/vars.json` (§9 п. 5); несортируемый пресет встаёт на номер шаблона приёмника (§9 п. 7). |
| 1.0.2 | **Значения переменных живут в записи (D-118, SPEC 129)**: `dns.servers[kind=template].vars` — поле ОБЕИХ сторон, значения по локальным именам объявления сервера; нормы записи Н1–Н4 (пустой объект не пишется, законны только объявленные имена, значения подрезаны, умолчание не пишется) — одни для `dns.servers[kind=template].vars` и `rules[kind=preset].vars` (`docs/TEMPLATE_LANG.md` §6.4). Слияние записи шаблонного сервера — наложение `vars` по именам, необъявленный тег — `backup_dns_entry_skipped`, неизвестный канал — сервер выключен с `backup_unknown_outbound {server}` (`docs/BACKUP.md` §9 п. 5). Пять имён D-117 (`dns_<tag>_outbound`, `dns_safe_dns_dot_dom_resolver`) — `portable: false`, терпимая форма чтения: читатель переносит в запись. Параметры кодов: `backup_var_skipped {name, record, reason}` (`not_portable \| undeclared \| superseded \| no_record`), `backup_unknown_outbound` + `server`. Корпус: `corpus/backup/v10_dns_template_vars` с фикстурой объявлений `.template.json` (нормализация только по ней — умолчания шаблонов у сторон разные), ожидания `v10_lxbox_fields` и `non_portable_var_skipped`, DNS-корпус на `state[].vars`. Маркер `lx_backup: 2` не меняется |
| 1.0.3 | **REALITY: явный отпечаток не подменяется (D-119, заменяет D-104)**: uTLS у reality включается всегда, `chrome` пишется только вместо пустого и неявного `random`; `reality_fp_not_chrome` — мягкое предупреждение. Реестры `tls.json`, `warnings.json`; кейс `uri/vless/reality_fp_firefox_forced_chrome` переименован в `reality_fp_firefox_kept` |
| 1.0.4 | **`tls.reality.key_share` — обмен ключами REALITY (D-121, SPEC 089 ядра)**: ядро `sing-box-lx` ≥ `1.14.1-lx.4` завело у REALITY-блока закрытый enum `hybrid`\|`classical` (`option/tls.go` `OutboundRealityOptions.KeyShare`); `hybrid` требует `X25519MLKEM768`, `classical` срезает его из `key_share` и `supported_groups` — это нужно REALITY-серверам Xray **старше** v26.9.8, которые на гибридном ClientHello рвут соединение, пусто = как несёт uTLS-отпечаток. **Share-URI: параметр `key_share`** (вариант Б к вопросу `TASKS_LXBOX.md` §23, решение владельца 17.09.2026) — имя ключа sing-box как есть, прецедент именования §453/§269; чужого диалекта у поля нет, поэтому имя вводят обе стороны согласованно. Схемы `vless://` и `anytls://` (в `trojan://` REALITY не парсится ни одним проектом), читается **только при валидном `pbk`**, разбор `trim`+lower-case. **Мусор деградирует ПОЛЕ, а не узел**: ключ снимается, узел живёт с REALITY и обменом по умолчанию отпечатка, код `reality_key_share_invalid` (`registry/warnings.json`, `info`) — единый для URI и JSON-тела; прежде JSON-путь чистил ключ молча, теперь `sanitizeSingboxReality` возвращает код наружу. Оставить значение как есть нельзя: ядро отвергает его отказом **всего конфига**, а не узла. **Гейт версии ядра — полевой**: на ядре старше `1.14.1-lx.4` ключ ему неизвестен (а неизвестный ключ тоже роняет весь конфиг), поэтому эмиттер поле снимает, узел остаётся REALITY и warning-кода не получает. Xray-JSON аналога не имеет. Реестр `registry/tls.json` → `reality.key_share` (`policy: drop`, «Поддержка: обе»); кейсы `uri/vless/reality_key_share_hybrid`, `reality_key_share_classical`, `reality_key_share_bad_dropped`, `reality_key_share_without_pbk_ignored`, `uri/anytls/reality_key_share_hybrid`, `body/singbox/outbound_array_tls_fields`. Попутно исправлена протухшая ссылка `reality_short_id_invalid` → `singbox_sanitize.go:209` (кода там не ставится). **Встречная задача LxBox** — тот же параметр в парсерах и эмиттере ссылок, привязка класса к коду (`TASKS_LXBOX.md` §23.1) |
| 1.1.0 | **Единый конвейер узла — схема тела протокола в реестре, `warnings[]` с путём (SPEC 131, D-122)**. Аддитивно. **`body`-секции** во всех `registry/protocols/<scheme>.json` (+ новый `chain.json`), `tls.json`, `transports.json`, новых `multiplex.json`/`dialer.json`: 384 поля из структур ядра `1.14.1-lx.4` (`option/*.go`, lx-поля от базы форка), атрибуты по `schema/registry_body.schema.json` — `type`/`values`/`format`/`required`/`secret`/`tristate`/`all_or_nothing`/`on_invalid{action,value?,code}`/`normalize`/`advisory`/`aliases`/`allowed_for`/`forbidden_for`/`conflicts`/`requires`/`min_core`/`platform`/`lx_only`/`desc_en`/`desc_ru`/`impl`; `on_invalid` у 103 полей, naive описан теми же атрибутами (23 TLS-поля `forbidden_for`, код `tls_field_unsupported_naive`), схемных исключений в конвейере нет. **`maps_to`** у 196 параметров `uri.*` — мост ссылка → путь тела; `note` разделён на `desc_en`/`desc_ru` (люди) и `impl` (разработчики). **CANON §6**: `warnings[]` = `{code, path?, value?, params?}`, порядок полевых кодов = `body.order`, строки-коды читаются; **CANON §8** — конвейер маппер → санитайзер → эмиттер и инвариант «значение с вердиктом B в `entry` не остаётся»; гейты ядра (`min_core`/`platform`) — на сборке. `schema/node.schema.json`, `schema/backup.schema.json`: запись warnings (в бэкапе — не авторитетна). **`warnings.json`**: у всех кодов `title_en/title_ru/text_en/text_ru` (подстановки `{path}`/`{value}`+`params`, приложения таблиц не держат); новые `unknown_key`, `type_invalid`, `alias_shadowed`, `field_conflict`, `field_requires`, `tls_field_unsupported_naive`, `partial_object_defaulted`, `ss_method_legacy`. **Целевые правила при расхождениях Go/Dart** — по вердиктам ядра (`TASKS_LXBOX.md` §24.2, 18 пунктов): ECH разделён (URI-`ech=` снимается, `tls.ech{}` из JSON проходит — пересмотр D-006, ECH в ядре компилируется всегда); ss — 18 методов ядра, legacy stream-шифры → `ss_method_legacy`; vmess `security` — набор ядра (`aes-128-ctr` не существует и ронял конфиг, `aes-128-cfb` терялся); xhttp `mode`/`seq_placement`/`x_padding_placement`/`x_padding_method` — enum ядра, case-sensitive; порт при `-udp443` не переписывается; WG-ключи проверяются; `allowlists.json` `ss_methods`/`vmess_security` приведены к ядру. Линтер реестра — `core/config/registry_body_test.go`. Кейсы: `uri/vmess/vmess_security_{cfb,ctr}`, `uri/vless/xhttp_mode_invalid`; ожидания с `path` придут с W2 лаунчера. **Встречные задачи LxBox** — `TASKS_LXBOX.md` §24 |
| 1.1.1 | **xmux: связи судятся по значению, `all_or_nothing` — только документация (SPEC 131 DRIFT §7.20)**. Уточнение правил, найденное прогоном настоящего state: у 13 живых `vless`+`xhttp` узлов санитайзер портил рабочую секцию `transport.xmux`. (1) **`conflicts`/`requires`/`forbidden_when` судят ЗНАЧЕНИЕ, а не наличие ключа**: «не задано» = нет ключа, `null`, `""`, `false`, `0`, пустой объект/массив **и число-строка из одних нулей** (`"0"`, `"0-0"` — диапазоны XmuxRange приходят строками). Ядро считает `max_concurrency` ↔ `max_connections` конфликтом только когда оба > 0 (`transport/v2rayxhttp/xmux.go`); проверка по наличию ключа снимала `max_concurrency:"16-32"` и возвращала дефолт `"1-1"`. Правило общее — так же судятся `certificate` ↔ `pins`, `reality` ↔ `ech`. (2) **`all_or_nothing` не влечёт действия санитайзера**: ядро при частичной секции оставляет незаданные поля нулями (= без лимита), дефолты соседей дописывать нельзя; атрибут остаётся описанием поведения ядра (`schema/registry_body.schema.json`, `impl` у `transports.json` `xmux`). Код **`partial_object_defaulted` снят** из `registry/warnings.json`. Кейс: `body/singbox/vless_xhttp_xmux_zero_neighbours` (тело проходит без изменений и без warnings). Встречная задача LxBox — `TASKS_LXBOX.md` §24.9 |
| 1.1.2 | **`reality_fp_not_chrome` понижен до `severity: info`**. Код не сообщает об изменении узла: отпечаток уходит в конфиг ровно таким, каким его дала подписка (D-119, подмены нет ни у одной стороны). Он предупреждает о ВОЗМОЖНОМ отказе сервера, а у живого узла это сведения, а не проблема — на прежнем `warning` узел с `fp=qq` выглядел сломанным, будучи рабочим. Прочие severity не тронуты. Встречная задача LxBox — `TASKS_LXBOX.md` §24.11 (там же — как лаунчер развёл три уровня по строке узла: `error`/`warning` в подстроке со своим глифом, `info` значком у имени, все коды в тултипе, отдельная группа «К сведению» в карточке) |
| 1.1.3 | **gRPC `service_name`: нормализация снята целиком** (ядро `sing-box-lx` 1.14.1-lx.8, SPEC 093 ядра). Правило `normalize: grpc_service_name` у `registry/transports.json` → `grpc.service_name` удалено, значение передаётся ядру **как есть в любой форме**; ведущий «/» ядро разбирает как **custom path Xray** и кладёт на провод готовым путём (`/a/b/Tun` → `/a/b/Tun`, `/a/Stream` → `/a/Stream`), экранируя **каждый сегмент отдельно** и отбрасывая хвост `|…`; без ведущего «/» поведение прежнее — имя сервиса, ядро дописывает `/Tun` и экранирует имя одним сегментом (`a/b` → `/a%2Fb/Tun`). Тем самым отпал и прежний перевод `/service/Tun` → `service` (issue #130, лаунчер 09787fbb), и ограничение ядра, из-за которого он чинил только односегментное имя: после lx.8 перевод стал бы вредным. Гейт `min_core` **не вводится** (решение владельца) — обе стороны пинят lx.8, а старому ядру такое значение и раньше не помогало. Значение убрано из enum `normalize` в `schema/registry_body.schema.json` и из линтера; `desc_en`/`desc_ru`/`impl` поля переписаны. Корпус: `uri/trojan/grpc_service_name_leading_slash`, `body/xray/grpc_service_name_leading_slash`, `uri/vless/grpc_service_name_leading_slash` перенормированы на «как есть», новые `uri/vless/grpc_service_name_custom_path_one_segment` (`/x/Tun`), `…_custom_path_two_segments` (`/a/b/Tun`), `…_custom_path_other_stream` (`/a/Stream`), `…_plain_multi_segment` (`a/b`) — тело без изменений, warnings пусто. Встречная задача LxBox — `TASKS_LXBOX.md` §24.12 (их план §468 совпадает) |
| 1.1.4 | **QUIC: uTLS и REALITY — правило реестра, а не проза** (SPEC 131). У `registry/tls.json` `body.fields.utls` и `body.fields.reality` в `forbidden_for` добавлены четыре QUIC-схемы (`hysteria`, `hysteria2`, `tuic`, `masque`) рядом с прежним `naive`. Ядро строит TLS QUIC-протоколов через `aTLS.Config.STDConfig()` (`sing-quic` `qtls.Dial`/`DialEarly`/`CreateTransport`; у masque — `protocol/masque/outbound.go:406`), а у `UTLSClientConfig` и `RealityClientConfig` этот метод возвращает ошибку — узел с таким блоком не поднимается вовсе. **`hysteria` v1 названа наравне с остальными**: она ходит тем же `qtls.Dial` (`sing-quic hysteria/client.go:264`), прежняя проза `policy.quic_strip` её не упоминала, хотя частная ветка `quicOutboundTypes` включала. **Исход — снят весь блок, узел живёт, код уровня `info`**: новый `tls_not_applicable_quic` (`registry/warnings.json`), а не общий `tls_field_unsupported_naive` (`warning`) — у naive снятое поле это потерянная настройка, а на QUIC оно не применилось бы в принципе. Один код на блок; `key_share` и `short_id` своих кодов не дают — снят не они, а весь REALITY. **Минимальное расширение схемы**: атрибут `forbidden_codes` (`schema/registry_body.schema.json`) — словарь «схема → код», переопределяющий общий `code` для отдельной схемы из `forbidden_for`; схема без записи берёт общий. Знают о нём санитайзер (`core/config/nodeflow/sanitize.go` `forbiddenCode`), линтер реестра и генератор документации. `policy.quic_strip` переписан из прозы в описание правила. **Снята частная ветка** `quicOutboundTypes` в `core/config/subscription/singbox_sanitize.go` (срез был молчаливым — только `debuglog`) и молчаливый пропуск `fp` в URI-парсерах `hysteria`/`hysteria2`/`tuic`: маппер переводит `fp`/`pbk`/`sid`/`key_share` в тело как есть (общий `applyTLSCamouflageFromQuery`), снимает их реестр — **и теперь с кодом на узле**. Тела узлов не изменились. Документация: у QUIC-схем `fp` больше не обещается рабочим — в Link parameters и Body fields строка «Not applicable to `<scheme>`: removed → код», правила значения запрещённого блока не печатаются. Корпус — парные кейсы `uri/hysteria2/reality_fp_stripped_quic_pair` ↔ `body/singbox/hysteria2_quic_tls_pair` и `uri/tuic/fp_stripped_quic_pair` ↔ `body/singbox/tuic_quic_tls_pair` (тело и коды совпадают у обеих половин); перенормированы `uri/hysteria2/fp_present_no_utls_quic` и `utls_fingerprint_pinsha256` — тело прежнее, добавился код. Встречная задача LxBox — `TASKS_LXBOX.md` §24.13 (`TlsSpec.toSingboxForQuic` → по реестру, узел получает код) |
| 1.1.5 | **Потолок MTU у AmneziaWG — правило реестра, а не код парсера; исключение по входу** (SPEC 131, находка №5 `LEGACY_AUDIT`). Правило «у AWG-узла `mtu` выше 1280 заменяется на 1280» жило кодом в парсере ССЫЛКИ (`awgMaxMTU` в Go, `awgClampMtu` в Dart) и потому **не применялось к телу из sing-box-импорта** — один и тот же узел ссылкой и объектом получал разный MTU; срез при этом был молчаливым. Переведено в правило значения у `wireguard.body.fields.mtu`: **`default_when`** даёт AWG-узлу 1280, когда `mtu` не задан (кода нет — это дефолт, а не замена), **`max_when`** держит потолок 1280. **Два новых атрибута схемы**: `max_when` (условный потолок — обычный `max` снял бы `mtu` у каждого обычного WireGuard-узла) и условие `when.any_set` у него и у `default_when` (условие называет не поле, а РОД узла: набор из 27 awg-полей, любого довольно). Условие смотрит на **наличие ключа**, а не на непустоту значения — в отличие от `conflicts`/`requires`: `jc: 0` — законное «мусор выключен» у настоящего AWG-узла. **Парность входов нарушена НАМЕРЕННО** (решение владельца 18.09.2026, DRIFT §7.23): вход `singbox` внесён в `except_sources` — тело в форме ядра значение СОХРАНЯЕТ и получает `awg_mtu_high` (`info`), все прочие входы (ссылка, `.conf`, `awg://base64`, форма AWG) получают замену и `awg_mtu_clamped` (`warning`). Основание по владению: тело sing-box человек или подписка написали сами в собственной форме ядра. Это ЕДИНСТВЕННОЕ место контракта, где вход узла влияет на результат, и вход обязан определяться стабильно (`Origin.Kind` → `sources`), иначе исключение теряется при перезапуске. **Снято**: `awgMaxMTU` и ветка клампа в `node_parser_wireguard.go`, mapper-правило `awg_mtu_clamped` (оно стало правилом ЗНАЧЕНИЯ); комментарий `awg3.go`, противоречивший коду, исправлен. **Схема догнала живой реестр**: при `additionalProperties: false` она не знала `default_when`, `normalize_code`, `advisory.except`/`advisory.when`, `relation.equals` и формат `base64_32`. Тела узлов не изменились нигде; у восьми AWG-кейсов корпуса с исходным `mtu > 1280` добавился код. Корпус — намеренно РАЗНЫЕ парные кейсы `body/singbox/endpoints_awg_mtu_high` ↔ `uri/wireguard/awg_mtu_1420_clamped`. Встречная задача LxBox — `TASKS_LXBOX.md` §24.14 (`awgClampMtu` → по реестру, два класса предупреждений, исключение по входу) |
| 1.1.6 | **Три запроса LxBox к реестру: свой код у конфликта, `tls_insecure` как `advisory`, dial-поля keepalive** (SPEC 131). **(1) `vision_with_transport`** — конфликт `vless.flow` ↔ `transport` звался общим `field_conflict`, текст которого обещает «ядро отвергает такую пару и не запустит весь конфиг», тогда как ядро пару ПРИНИМАЕТ (вердикт D): снимается бессмыслица, узел живёт. Конфликт назван СВОИМ кодом (`severity: info`), рукописного места в Go не было и нет. Расширения формы `conflicts` НЕ потребовалось: снимается сторона-ДЕКЛАРАНТ, а связи читают исходное тело (`pathPresent` → `srcRoot`), поэтому `transport` виден, хотя по `body.order` идёт позже `flow`; «младшее по order» описывало не то, какое поле снимается. Проверены все 22 `conflicts` реестра — ни одному не нужна противоположная сторона. Параметры кода приведены к тому, что реально подставляет слой связей (`path`, `with` вместо недостижимого `transport`). **(2) `tls_insecure`** — переведён в `advisory` у `registry/tls.json` `body.fields.insecure` (условие «значение `true`»), как `reality_fp_not_chrome` у `utls.fingerprint`; значение СОХРАНЯЕТСЯ, код `info`. Параметры объявлены (`path`, `value`) — прежде список был пуст, и `{path}`/`{value}` в тексте остались бы неподставленными. Схема: `advisory.values`/`except` принимают **boolean** — у bool домен закрыт типом, а не списком `values`, и правило «код ровно на true» иначе не записать; линтер требует вхождения в `values` только у полей, которые значения перечисляют сами. **(3) dial-поля keepalive** — `disable_tcp_keep_alive`, `tcp_keep_alive`, `tcp_keep_alive_interval` есть в `DialerOptions` ядра (`option/outbound.go`, CORE_SCHEMA §1.2), но числились строками `skipped`, и санитайзер снимал их как `unknown_key` — ⚠ на рабочем узле за поля, которые ядро принимает. Описаны в `dialer.json` `body.fields`. Общее правило: **`skipped` ≠ «поля нет у ядра»** — skip годится только тому, что тело не несёт НИ ОДНИМ входом; поле, приезжающее входом `singbox`, обязано быть в `fields`, даже если лаунчер его не пишет сам. Тела узлов не изменились нигде; коды добавились у 11 кейсов корпуса с `insecure=true` и сменились у двух vision-кейсов. Тем же решением владельца из `skipped` подняты и `network_strategy`, `network_type`, `fallback_network_type`, `fallback_delay` — **без `values`**: словари `C.StringToNetworkStrategy`/`C.StringToInterfaceType` из `option/*.go` не видны, но отсутствие словаря не повод снимать настройку, которую ядро принимает; значение судит ядро, мусор оно отвергнет на старте. В `skipped` осталось 7 строк — платформенные, низкоуровневые и устаревшее `domain_strategy`. Новый кейс `body/singbox/dialer_tcp_keep_alive` (узлы `keepalive-full`, `keepalive-off`, `net-strategy`) |
| 1.1.7 | **`vmess.security`: у коэрсинга свой код, рукописное сведение снято** (SPEC 131, запрос агента LxBox). Поле держало `on_invalid: coerce → auto` с ОБЩИМ кодом `type_invalid`, а корпус (`uri/vmess/vmess_security_ctr`) от подмены ждал МОЛЧАНИЯ — и получал его, потому что до конвейера значение сводили руками: `normalizeVMessSecurity` (вход `uri`) и её копия `normalizeVMessSecurityValue` (вход `xray`) подменяли негодный шифр ещё в маппере, до санитайзера доезжало уже годное `auto`, правило реестра не срабатывало. Результат зависел от входа: молча на `uri` и `xray`, с кодом на `singbox`. Новый код **`vmess_security_unknown`** (`severity: warning`, `params: path`+`value`, title/text/cause/fix en+ru): `type_invalid` говорит «поле снято: неверный тип», а здесь поле НЕ снимается, а ПОДМЕНЯЕТСЯ, и тип значения ни при чём — узел уезжает на том шифре, который выберет сервер, а не на том, что просила подписка. Правило исполняет ТОЛЬКО реестр; `normalizeVMessSecurityValue` удалена целиком (вторая копия allowlist'а), `normalizeVMessSecurity` ужата до двух случаев, суждением о значении не являющихся: перевод диалекта `chacha20-ietf-poly1305` → `chacha20-poly1305` и подстановка `auto` на пусто/`null`/`undefined` («не задано» — подписка ничего не просила). Ключ `security` при этом обязан появиться: у ядра поле без `omitempty`, а `default` реестра тело не наполняет (CANON §2.4 материализует только `default_when`), и опущенный ключ у `required`-поля уронил бы узел кодом `field_missing`. Одинаковый результат на всех трёх входах. Ожидания корпуса: изменился РОВНО ОДИН файл — `vmess_security_ctr.expected.json`, и в нём только `warnings[]`, `entry` прежний; `json_scy_null_to_auto` и `json_security_key_null` не шелохнулись (проверка, что «не задано» кода не даёт). Заодно пройден весь реестр: записей `on_invalid.action = coerce` три, у двух свой код был изначально (`masque.vhttp` → `masque_vhttp_invalid`, `tls.utls.fingerprint` → `utls_fp_unknown`), `vmess.security` был последним с общим. Правило на будущее: **`coerce` без своего кода — почти всегда ошибка**, текст `type_invalid` описывает снятие, а не подмену. Встречные задачи LxBox — `TASKS_LXBOX.md` §24.16 |
| 1.1.9 | **`vless.encryption`: правило ФОРМЫ вместо грамматики ядра; норма страховки «ядро отвергло узел»** (решения владельца 18.09.2026, SPEC 131/132). Полную грамматику постквантового слоя (снята сессией ядра, лежит справкой в `SPECS/131-F-O-UNIFIED_NODE_PIPELINE/VLESS_ENCRYPTION_GRAMMAR.md`) в клиентах **не дублируем**: копия обязана сверяться на каждом бампе пина, а отставшая начинает отбраковывать ГОДНЫЕ узлы. Вместо неё два эшелона — дешёвое правило формы в реестре и общая страховка. **Нормативный порядок, одинаковый на всех входах:** URL-декодирование на входе ссылки → обрезка краёв всей строки (`normalize: trim`, тихо, в тело едет обрезанное) → пусто или ТОЧНОЕ `none` = слоя нет, поля и кода нет → иначе `pattern` `^mlkem768x25519plus(\.[^.]+){3,}$`, не совпало = `drop_node` `vless_encryption_invalid` (в код уезжает СЫРОЕ значение, до обрезки). Вид, rtt, padding и ключи не судятся; пробелы ВНУТРИ сегментов законны — ядро тримит сегменты с четвёртого. Имя метода зашито в выражение: при бампе пина сверять с `protocol/vless/lx_encryption.go`. Отказ именно `drop_node`, а не снятие поля: узел без `encryption` к серверу, который слой требует, не подключится, а молча снять шифрование = тихо понизить защиту. **Сравнение с `none` стало ТОЧНЫМ** (было `EqualFold`): ядро сличает литерал с учётом регистра, `None` для него настоящее значение, роняющее весь конфиг, — прежнее поведение прятало негодное значение от собственных проверок; запись `mapper` про «`none` любого регистра» снята, переводить нечего. **Два новых общих атрибута реестра:** `pattern` (диалект = общее подмножество Go RE2 и ECMAScript/Dart, без lookaround/backreference/inline-флагов, якоря `^…$` в самом выражении; невалидное выражение санитайзер пропускает, поэтому компиляцию и якоря ловит линтер) и `absent_values` (литералы «этого нет» — после `normalize`, до ограничений, поле не пишется, кода нет, сравнение точное и только строковое; без него правило хоронило бы узел за выключенную настройку). **Страховка (SPEC 132, общая норма с LxBox 478):** новый параграф `CANON.md` §9 «Core-rejected node» — формат строки ошибки ядра `initialize <outbound|endpoint>[<i>] <type>[<tag>]: <text>` (lx.7+, форма без тега у старых ядер), правило разбора тега (кандидаты по `]: ` справа налево, побеждает первый сопоставившийся с тегами собранного конфига), сопоставление только по финальному тегу, модель хранения (обычный `enabled=false` + необязательная причина, авторитетная и идущая в бэкап), три нормативных запрета. Код `core_rejected` (error, `params: [reason]`) объявлен заранее, до реализации; поле причины — `disabled_reason` в обоих `$defs` `backup.schema.json` (у LxBox то же поле зовётся `reason`: у нас `Reason` занят текстом парсера у `kind=unsupported`). Реализации страховки в этом заходе НЕТ. Попутно снята протухшая запись: у URI-параметра `encryption` стояли `ext: "mobile"` и «Go параметр НЕ парсит» — обе неверны, Go читает параметр в `node_parser_core.go:770-775`. Кейсы корпуса (18 новых, ни одно существующее ожидание не изменилось): отказы — три сегмента, пустой сегмент, другой метод, метод в другом регистре, `garbage`, `None` (ссылка и тело); годные — ключ 32 (43 симв.), ключ 1184 (1579 симв.), padding-сегменты, пробел внутри 4-го сегмента, пробелы по краям, percent-кодированное значение; «нет слоя» — `none`, пусто, ` none `; плюс два узла в одном теле (негодный в `dropped`, здоровый жив). Встречные задачи LxBox — `TASKS_LXBOX.md` §24.19 |
| 1.1.10 | **Причина «ядро не приняло узел» — запись `core_rejected` в `warnings[]`, отдельного поля НЕТ** (решение владельца 19.09.2026, SPEC 132 лаунчера / фича 478 LxBox; реализация страховки в лаунчере — этот же заход). Поле `disabled_reason`, объявленное в 1.1.9 в обоих `$defs` `backup.schema.json`, **снято**: `warnings[]` уже персистятся и едут в бэкап, и второе поле с тем же смыслом рядом с ними означало бы два ответа на один вопрос. Причина живёт записью `{"code": "core_rejected", "params": {"reason": "<дословный текст ядра>"}}`; «выключило приложение» = у выключенного узла есть эта запись, её отсутствие при `enabled=false` = «выключил человек». Дубль по коду замещается свежим, запись идёт первой в списке (вердикт уровня узла, не деградация поля). **Запись авторитетна**, в отличие от прочих кодов списка: те производные и замещаются целиком любым пересчётом санитайзера по телу, а вердикт ядра пересчётом не воспроизводится и стираться им не вправе — реализации нужно ОДНО место-правило замещения производных кодов, иначе каждая точка пересчёта сотрёт причину молча (в лаунчере их шесть). **Вердикт привязан к ТЕЛУ узла** (новая норма §9.4): снимают его ровно два события — (1) тело изменилось (refetch принёс другое тело под тем же тегом, правка узла в форме или JSON): запись стирается И узел включается обратно, следующая сборка проверит его заново; оживает только узел, выключенный СТРАХОВКОЙ, выключенный человеком сменой тела не включается; сравнение тел семантическое, а не побайтовое, иначе любая нормализация снимала бы вердикт; (2) человек включил узел обратно. **Смена версии ядра вердикты НЕ снимает** — ручного механизма достаточно, обход всех сохранённых состояний не окупается. Описание `warnings[]` в обоих `$defs` `backup.schema.json` получило оговорку про это исключение из «читатель не доверяет и пересчитывает». Встречные задачи LxBox — `TASKS_LXBOX.md` §24.19 |
| 1.1.8 | **`socks.version` достижим ссылкой: схема несёт версию; `security_none_no_tls` снят с `anytls`** (SPEC 131, запрос агента LxBox). Гипотеза «поле `version` эмиттер пишет, парсер тела не читает» подтвердилась НАПОЛОВИНУ: вход `singbox` читал поле всегда и правильно (`"4"`/`"4a"` доезжают до тела, `"6"` снимается `type_invalid` — проверено корпусом до правок), мёртвым был путь ССЫЛКИ — схем `socks4://`/`socks4a://` не существовало вовсе (`unsupported scheme`, узел терялся целиком), а URI-парсер писал `version: "5"` константой, и эмиттер всегда отдавал `socks5://`. Своего query-параметра под версию у socks-ссылки нет ни в одном диалекте, поэтому её несёт **схема** — как суффикс `proxy-https://` несёт TLS: новое правило маппера **`socks_scheme_is_version`** (`kind: structure`), схемы `socks4`/`socks4a` в `aliases` схемы `socks`, таблица `socks/socks5 → "5"`, `socks4 → "4"`, `socks4a → "4a"`. Парсер и эмиттер читают ОДНУ таблицу (`node_parser_socks.go`), узел с версией 4/4a переживает round-trip своей же ссылки. `version: "5"` остаётся в теле ЯВНО вопреки общему правилу «дефолт ядра не материализуем» (CANON §2.4): значение стоит в ожиданиях корпуса и в телах всех живых socks-узлов обеих сторон, и снятие переписало бы их все ради нуля разницы для ядра — ни одно существующее ожидание не изменилось. Пароль при `socks4` (которого у версии 4 нет — userinfo это userid) переносится как есть: маппер значения не судит. Своего кода не заводили — негодную версию снимает enum поля общим `type_invalid`, узел живёт как socks5. **`anytls` убран из `applies_to` правила `security_none_no_tls`**: у anytls TLS обязателен (ядро отвергает такой outbound `C.ErrTLSRequired`, `protocol/anytls/outbound.go:45` — фатал на весь конфиг), парсер строит TLS-блок безусловно, а `security=none` лишь игнорируется при сохранении `sni`/`fp`/`alpn`/`insecure` (кейс `uri/anytls/security_none_params_kept`). Правило стояло на нём документацией о поведении, которого нет; исполнись оно — узел ронял бы конфиг. Кода и здесь не заводили: снимать нечего, предупреждение было бы шумом. Кейсы корпуса (8): `uri/socks/socks4_userid`, `socks4a_no_user`, `socks4_with_password`; `body/singbox/socks_version_{4,4a,5_explicit,absent,invalid}`. Встречные задачи LxBox — `TASKS_LXBOX.md` §24.17; ответ про общий тест «каждое поле `body.fields` читается парсером тела» — §24.18 (на JSON-входе дефект структурно невозможен: тело идёт картой через реестр, списка читаемых ключей нет; возможен на входе ссылки, и дешёвого линтера `maps_to` у нас нет) |
| 1.1.11 | **Ключи WireGuard и правила AmneziaWG судит ТОЛЬКО реестр** (SPEC 131, решение владельца «все проверки — в реестре, в коде только движок»). Рукописные валидаторы парсера ссылки снимали негодные значения МОЛЧА, и то же значение на входе sing-box-JSON уезжало в ядро мимо проверки. Переведены: `wg_key_invalid` (ключи `private_key`/`peers[].public_key`/`peers[].pre_shared_key` — `format base64_32`, нормализация написания base64 остаётся переводом диалекта), `range_order` (перестановка границ диапазона у AWG 2.0 `h1`–`h4` — `swap`, у AWG 3.x — `strict`), `min_when.absent_is_zero` (паддинг `s1`–`s4` короче 12), `relations.ranges_disjoint` (пересечение `h1`–`h4`). Встречная задача LxBox — `TASKS_LXBOX.md` §24.21 |
| 1.1.12 | **«`tls:{enabled:false}` = TLS не задан» стало правилом РЕЕСТРА; новый атрибут `absent_when`** (SPEC 131, решение владельца «все проверки — в реестре, в коде только движок»). Правило жило ДВУМЯ рукописными копиями (`singbox_sanitize.go` на входе sing-box-JSON и гард `EnforceRealityFingerprint` на сборке; третья, в старом per-scheme эмиттере, снята контрактом 1.1.11), и потому тело, приехавшее МИМО обоих — ручной JSON вкладки, чужой бэкап, — доезжало до `config.json` с выключенным блоком; у ядер 1.14.0-lx.5..lx.18 это SIGSEGV на первом dial (SPEC 045): конструктор TLS отдаёт `(nil, nil)`, а dialer строится по НАЛИЧИЮ блока. **Новый атрибут `absent_when {ключ: значение, …}`** у поля-объекта: совпали ВСЕ перечисленные ключи → объект снимается ЦЕЛИКОМ и ТИХО, кода нет — это запись «настройки нет», а не деградация. `absent_values` для этого не годится: он про значение САМОГО поля и только строковый. Тем же атрибутом, а НЕ особым случаем, описаны вложенные `utls`/`reality`/`ech` со своим `enabled: false`. У секции-суб-схемы (`tls`) атрибут объявлен ОДИН раз и переезжает в каждое поле с `ref` при разрешении — иначе он был бы копией в полутора десятках протоколов. **Норма порядка (новый `docs/CANON.md` §6.1, нормативна для обеих сторон):** `absent_when` судится ДО правил полей самого объекта и ДО связей соседей (`conflicts`/`requires`/`forbidden_for`, условия `when.any_set`); снятый объект «не задан» для любой проверки наличия, и вместе с ним исчезает всё, что внутри (`tls.reality.enabled` при снятом `tls`). Без этой нормы `tls:{enabled:false, reality:{…}}` давал бы коды на поля блока, которого в теле не будет, а сосед терял бы своё значение из-за конфликта с несуществующим блоком. Сравнение — по печатной форме скаляра (как у `values`/`advisory`), поэтому `false` и `"false"` совпадают: тело приезжает и разбором JSON, и от маппера, где булев флаг бывает строкой. **Снято:** ветка `enabled:false` из `sanitizeSingboxTLS` (проверка «tls не объект» осталась — ядро отвергает такое на РАЗБОРЕ, раньше любых правил значений, и атрибутом это не выражается) и мёртвый гард `reality["enabled"]` из `EnforceRealityFingerprint`. **Тела корпуса не сдвинулись нигде**, ни одно существующее ожидание не изменилось; новый кейс `body/singbox/tls_disabled_block` (6 узлов: выключенный tls с мусором внутри → блока и кодов нет; выключатель СТРОКОЙ; `reality:{enabled:false}` внутри живого tls → снят только reality; `utls:{enabled:false}`; выключенный `ech` НЕ конфликтует с живым REALITY; контроль — тот же негодный pbk в ЖИВОМ tls код получает). Встречная задача LxBox — `TASKS_LXBOX.md` §24.22 |
| 1.1.13 | **Исполняемые секции-мапперы: `blocks` у `tls`/`transports`, `mappers` у trojan** (SPEC 133, решение владельца «управляем превращением ссылок/JSON/прочих источников в узлы через реестр ПОЛНОСТЬЮ»). Добавлено, ничего не заменено: описательные `tls.params`/`transports.<type>.params` остаются словарём для документации, рядом появился **исполняемый** слой. `blocks.<диалект>` — переиспользуемые записи общих блоков в ДВУХ диалектах (`uri` и `xray`): имена ключей у Xray свои (`serverName`/`publicKey`/`shortId`, `wsSettings`/`grpcSettings`), и один блок с `aliases` их не покрывает, потому что различается ещё и форма значения (`host` у ws — `headers.Host`, у http — список, у httpupgrade — скаляр). `mappers.<kind>` у протокола — таблица на вид источника (`uri`/`xray`/`singbox`), подключающая блоки через `include: ["tls#uri", "transports#uri"]`. Каждая секция несёт свой `detect` (реестр сам показывает, как отличить ссылку от Xray-JSON от sing-box-JSON — рукописного сниффера формата в коде не остаётся), `body_source` (каким входом тело приходит в санитайзер: на это опирается `except_sources`) и `unknown_key` (неизвестный ключ получает код `uri_param_unknown`/`json_field_unknown` вместо молчания). Грамматика заморожена — `SPECS/133-…/PRIMITIVES.md` §0, машиночитаемая форма `schema/registry_mapper.schema.json`. **Тела корпуса не сдвинулись:** все три раннера (`TestContractCorpusURI`, `…Body`, `…EmitRoundTrip`) зелёные без единого изменённого ожидания — секции пока только объявлены, разбор по-прежнему ведёт старый парсер. Встречная задача LxBox — `TASKS_LXBOX.md` §24.23 |
| 1.1.14 | **Схема секций-мапперов приведена к живому реестру; первая пачка секций от LxBox** (SPEC 133, приёмка черновиков 19.09.2026). **Схема (`schema/registry_mapper.schema.json`) отвергала то, что уже написано в `trojan.json`/`vless.json` и заморожено в `PRIMITIVES.md` §0** — находка LxBox: `decode_extra.preserve_plus` переименован в **`plus_literal`** (имя FROZEN, §0.4; реестр и движок звали его так уже, отстала схема); `emit.param_order` принимает строку **`"alphabetical"`** рядом с массивом имён — это ПРАВИЛО, а не перечень (`q.Encode()` во всех эмиттерах даёт алфавит, и список имён был бы дублем правила); `impl` разрешён на `forms[]`, `userinfo`, `label`, `emit`, `unknown_key` и на секции целиком — прозаическая сноска «чем код отличается от таблицы» нужна везде, а не только у записи `params`; `userinfo.required` (ссылка без userinfo — не узел этой схемы, судит ОБОЛОЧКУ, а не запись). **Новый примитив `format`** (`PRIMITIVES.md` §0.4a, ДОБАВЛЕНИЕ — `plus_literal` остаётся FROZEN): формат значения задаёт умолчания декода, `base64*` → `plus_literal`, **`pem`** → раздельное правило, потому что `+` в PEM несёт ДВА смысла — пробел в заголовочных строках `-----BEGIN …-----` и литерал в base64-теле, и булев флаг негоден в обоих положениях (D133-15; у нас заплаты нет вовсе — `node_parser_ssh.go:32` берёт ключ из `Query()`, и тело ключа портится молча). **Линтер `TestContractMapperSectionsMatchSchema`**: все секции `mappers` и все диалектные `blocks` проверяются на ЗАКРЫТОСТЬ множества имён — рубеж нужен на данных, потому что загрузчик на неизвестный ключ молчит (`encoding/json` его выбрасывает), ровно от этого молчания и затеяна кампания. **Влито семь секций `uri` (shadowsocks, vmess, socks, http, naive, anytls, ssh) и две `xray` (vmess, shadowsocks)** — объявлены, как у trojan; разбор по-прежнему ведёт старый парсер. Поправлено приёмкой там, где арбитр наш код/корпус: у `vmess` и `xray/vmess` снят `materialize_default` у порта (отсутствующий порт роняет узел, фолбэка 443 нет); у `xray/shadowsocks` снят `empty: significant` у пароля (пустой method/password отбраковывает узел — рубеж против плейсхолдеров просроченной подписки); `scheme_source` у ss и socks оставлен `as_written` (переход на `singbox_type` меняет ТЕГ, то есть identity, — это волна D133-6 целиком, а у socks поверх стоит per-app override корпуса); код `ech` приведён к существующему `ech_ignored`. **Общие блоки — добавлением:** `tls.blocks.uri_with_host` (цепочка `sni→peer→host` для http; отдельным вариантом, а не третьим источником общей записи, потому что у vmess и anytls `host` адресует ТРАНСПОРТ) и `dialer.blocks` с `tcp_keep_alive*` (поля ядро принимает и `body` их описывает с 1.1.6, не хватало исполняемых записей; ни один парсер их не читает — дельта односторонняя). **Дельты D133-15…19, причуды Q133-32…35.** Не влиты `xray/socks` (описывает узел, которого у нас нет: socks с Xray-входа становится ЗВЕНОМ цепочки, `xray_outbound_convert.go:408-430`) и `xray/hysteria2` (`detect` подменяет нашу `hysteria`-ветку и теряет путь `protocol:"hysteria2"`; выпали obfs/up_mbps/down_mbps/alpn). **Тела корпуса не сдвинулись:** раннеры зелёные без единого изменённого ожидания. Ответ и встречные вопросы LxBox — `TASKS_LXBOX.md` §24.20 |
| 1.1.15 | **Движок ИСПОЛНЯЕТ секции-мапперы; схема trojan переключена на него** (SPEC 133). Секции перестали быть объявлениями: `core/config/linkmap` разворачивает `include`, подставляет `$base`, раскладывает записи на проход A (селекторы) и проход B и исполняет FROZEN-примитивы таблицы; разбор ссылок trojan ведёт он, а не рукописный парсер. Устройство — новый общий документ `docs/MAPPER_ENGINE.md`. **Четыре правки секций, найденные сверкой с identity-фикстурами** (журнал — `SPECS/133-…/QUIRKS.md` Q133-32…36): (1) `transports.blocks.uri.httpupgrade.host` — источник ОДИН (`query.host`), фолбэк на `sni` был написан по ws-образцу и в коде его нет; (2) `transports.blocks.uri.ws` — добавлены записи `ed`/`eh`, ВТОРАЯ форма early data (плоская пара в query), которую раньше знал только код; хвост пути `?ed=N` её побеждает; (3) `tls.blocks.uri.security` — ветка `none` объявлена как `{"tls": null}` вместо `{}`: `null` СНИМАЕТ путь (G2), а пустой набор означал бы «не писать», и `sni` со своим `default_from` успевал написать `tls.server_name` в блок, которого быть не должно; (4) записи `sni`/`alpn`/`fp`/`insecure`/`disable_sni` блока `tls#uri` встали под `when: {tls.enabled: true}`, а `pbk` стал `selector: true` — он объявляет СТРУКТУРУ (блок reality), а не значение. **Нормы, обязательные обеим сторонам:** порядок объявления записей нормативен и восстанавливается из ИСХОДНОГО текста JSON (Go-карта его теряет, обход хеш-таблицы дал бы разные тела между запусками); `priority` — меньше значит раньше, и первый писавший держит путь, пока пришедший позже не объявил `merge`. **Тела корпуса не сдвинулись:** все три раннера зелёные без единого изменённого ожидания; новый раннер `TestEngineVsFixtures` сверяет движок с фикстурами, разрешённые дельты — в `core/config/linkmap/testdata/deltas_allowed.json`, парном к `DELTAS.md`. **Два ДОБАВЛЕНИЯ к FROZEN-грамматике** (переименований нет): `overlay` — наложенное пространство источников (`mappers.<kind>.overlays[]`): вложенный слой чужого диалекта (JSON в query-параметре `extra` у xhttp) адресуется тем же `source` под своим именем. Слой ОТДЕЛЬНЫЙ, а не слитый с query, и это принципиально: кто из двух побеждает, решает САМА запись порядком источников, потому что у xhttp решение РАЗНОЕ — у обычных полей побеждает `extra`, у базовой тройки `mode`/`path`/`host` плоский слой, причём даже пустой (D-097, кейс 4PDA #1755: со слиянием `path` съезжал на `/` и сервер отвечал 404). И оператор `not_in` у `when` — симметричный `in` по записи, но НЕ по смыслу: по несуществующему адресу `not_in` истинен, а `in` ложен, и правило вида «запрещено при таких-то значениях» выражается только им. **vless переключён и зелёный** — 103 кейса корпуса без единой разрешённой дельты; вместе с trojan 134 кейса на движке. В секции по ходу доехало то, чего в реестре не было вовсе: весь плоский набор XHTTP v2 (26 записей), срезание `?`-хвоста у `xhttp.path`, структурное правило `uplink_data_placement` и слой `extra`. Заведена запись `tls.blocks.uri.ech` (`maps_to: null` + `on_present`): `ech=` не переводится никуда ни у одной схемы, но и молчать о нём нельзя. Встречная задача LxBox — `TASKS_LXBOX.md` §24.25 |
| 1.1.16 | **Движок ведёт разбор на ПРОДАКШЕН-пути; восемь схем переключены** (SPEC 133). До сих пор движок жил параллельно и проверялся только сверкой с фикстурами — теперь `ParseNode` сначала спрашивает его, и ссылку ведёт секция реестра, а не рукописный парсер. Развилку решает НЕ код, а временный служебный атрибут секции `live: true` плюс её собственный `detect`: списка схем в коде нет и не появится, а когда `live` встанет у всех секций, атрибут исчезнет вместе со старым входом. Переключены `trojan`, `vless`, `anytls`, `socks`, `ssh`, `http`, `naive`, `shadowsocks` — весь корпус `uri` зелёный на РЕАЛЬНОМ пути (`TestContractCorpusURI`), а не только на сверке тел. **Четыре ДОБАВЛЕНИЯ к FROZEN-грамматике** (переименований нет, `PRIMITIVES` §0.10): (1) `extract` по элементам `list` — группы `$key`/`$value` строят объект тела, и годность пары решает САМА регулярка записи, а не код: алфавит имени заголовка и запрет CR/LF/NUL переехали в объявление (так `extra_headers` у naive и `headers` у http перестали быть функцией `parseNaiveExtraHeaders`); (2) `$default_port` — служебный ключ `scheme_sets`: дефолт порта бывает свойством НАПИСАНИЯ, а не схемы (`proxy-http` → 80, `proxy-https` → 443 при одной схеме `http`), в тело он не пишется, а питает `materialize_default` записи порта; (3) `forms[].decode` объектом `{decoder, scope}` с областью применения `all`/`userinfo`/`authority` — base64 у `ss` накрывает РАЗНЫЕ куски в разных формах (SIP002 кодирует userinfo, legacy — весь authority, а метка `#…` в обеих формах остаётся снаружи), и декодер «на весь текст» ломал обе; (4) `userinfo.decode` и `forms[].space` стали ИСПОЛНЯЕМЫМИ — были объявлены, но движок звал лексер ссылки всегда. **Пять новых атрибутов записи**, каждый снимает молчание: `on_when_false` (значение во входе было, а структурное правило не дало ему доехать — так `uplink_data_placement=header` при несовместимом режиме снимается с кодом `xhttp_param_reset`, раньше исчезал тихо), `on_implies_written` (`implies` и вправду дописал недостающее — `xhttp_mode_forced_packet_up`), `value_map_case: sensitive` (там, где ядро сравнивает литерал точно: `encryption=None` у vless обязано быть ОТВЕРГНУТО, а регистронезависимое попадание проглатывало его как «слоя нет» и молча понижало защиту), `userinfo.required` (ссылка без userinfo не узел — объявляется у `userinfo`, потому что поля приходят позициями `into`, а записи под ними у части схем нет вовсе), `on_invalid: keep` заработал у `type` (значение везётся КАК ПРИШЛО, годность судит санитайзер: `min_idle_session=abc` обязан доехать строкой и получить `anytls_min_idle_invalid`, а не исчезнуть на маппере). **Записи со значением `null` теперь ОБЪЯВЛЕНЫ, а не пропущены**: «параметр знаем, читать нечего» (`spx`, `echfq` у REALITY) — иначе `uri_param_unknown` ругался на то, что реестр перечислил своей рукой; источник `overlay` (`extra` у xhttp) объявленным считается по той же причине. **Правки секций, найденные прогоном продакшен-пути:** `http` подключает `tls#uri` И `tls#uri_with_host` (второй перекрывает цепочку `sni` позицией, `alpn`/`insecure`/`utls` остаются — один только вариант блока терял их), своя запись `security` гейтится по написанию схемы; селектор транспорта получил НАБОР знаемых написаний — на незнакомом значении транспорта быть не должно вовсе, иначе оно уезжало в тело и санитайзер вешал узлу `type_invalid` (корпус `vless/h2_tls`); у `naive.server` снят `required` — отбраковку пустого адреса делает САНИТАЙЗЕР (`field_missing`), а не маппер, и причина входит в контракт. Дельта **D133-20**: код `uri_param_unknown` (решение владельца, SPEC §14.1) начал ЗВУЧАТЬ — ставить его раньше было некому. Планы реестра строятся ОДИН раз на процесс (`linkmap.Planes`): подписка на 500–2000 узлов иначе перечитывала бы JSON на каждую ссылку. Журнал странностей — `SPECS/133-…/QUIRKS.md` Q133-41…46. Встречная задача LxBox — `TASKS_LXBOX.md` §24.26 |
| 1.1.17 | **vmess на движке — ссылочная область реестра переключена ЦЕЛИКОМ** (SPEC 133). Девятая и последняя написанная секция `uri` стала `live`; рукописный разбор восьми схем предыдущей волны и vmess удалён (−1500 строк), вместе с ним ушли `node_parser_{http,naive,anytls,ssh,ss,socks,vmess}.go`. Остаток кампании теперь только в схемах, у которых секции НЕТ (hysteria, hysteria2, masque, tuic, wireguard) — страж `notLiveYet` пуст. **Что понадобилось для формы-КОНТЕЙНЕРА.** vmess — первая схема, у которой пейлоад не ссылка, а объект JSON v2rayN, и это вскрыло пять мест, где движок исполнял не то, что объявлял реестр: (1) **форма опознаётся по РАСПАКОВАННОМУ телу**, а не по исходному тексту — у обеих форм vmess оболочка одна (`base64` на authority), и что под ней лежит, видно только после декодирования; прежде `detect` смотрел на текст со схемой впереди и не срабатывал никогда, а `default`-форма забирала всё; (2) **`forms[].space: "json"` стал исполняемым** — `UnwrapURI` звал лексер ссылки всегда, и объявление пространства было объявлением без исполнителя; (3) **плоский слой контейнера**: скалярные ключи верхнего уровня видны и как `query.<имя>` — без этого общие блоки `transports#uri`/`tls#uri`/`dialer#uri` в контейнере не читают НИЧЕГО, и пришлось бы либо переписать их на «источник по формам» в каждой записи, либо завести копии (так же устроен и LxBox); (4) **`Source.ForForm`** — при записи источника картой по формам исполнение брало склейку ВСЕХ форм, отсортированную по имени: метка узла формы v2rayN приезжала из фрагмента ссылки, хотя секция объявляет ей `json.ps` и прямо пишет, что фрагмент не читает; (5) **дефект лексера**: `splitAuthorityPart` резал текст по `?` и `/` — символам, законным в алфавите base64 (RFC 4648 §4), — и блоб legacy-формы рвался пополам, теряя query, транспорт и TLS МОЛЧА. **Исполнены три объявленных, но спавших примитива:** `omit_default` (значение, равное дефолту ядра, в тело не пишется: `aid=0` у vmess ядро понимает как отсутствие ключа, а записанный ноль входит в identity), `materialize_default` через `value_map[""]` (у записи без `sets` дефолт объявляет именно он — корпус видел `field_missing` на восьми кейсах, где панель ключ `scy` не пишет вовсе), `default_from` по ПУТИ ТЕЛА (SNI по умолчанию равен адресу сервера, а адрес у разных форм приезжает из разных источников — имя источника назвало бы одну форму). Предикат `type_of` получил путь **`$root`** — сам документ, а не ключ в нём. **Новый общий блок `tls#uri_security`**: запись `security` вынесена из `tls#uri`, потому что истину TLS через `query.security` несут НЕ все подключающие — у vmess это `json.tls` (v2rayN, истина ровно строка `"tls"`) и `query.tls` (legacy), D133-16. Пока запись жила в общем блоке, vmess получал от неё TLS на КАЖДОМ узле без `security`, которого в его ссылке нет никогда. Та же ловушка, что у REALITY (Q133-47), и лечится тем же — разделением блока. **Нормализация `fp`**: `normalize: trim_lower` у всех четырёх записей с `$ref: tls.fp_dialect` — таблица `{prefix,strip}` лоуэркейсит ключ для ПОИСКА, но на промахе отдаёт значение неизменным, и канонический `fp=QQ` уезжал в конфиг как есть. Страж остатка `uri_param_unknown` научился считать объявленными источники `json.<имя>` и источник метки; `v` и `serverName` у vmess объявлены записями `null` («параметр знаем, читать нечего»). Журнал странностей — `SPECS/133-…/QUIRKS.md` Q133-49…52. Встречная задача LxBox — `TASKS_LXBOX.md` §24.27 |
| 1.1.18 | **hysteria2 на движке; секция принята от LxBox** (SPEC 133). Первая секция, написанная НЕ нами: взята из их `contract_draft/uri/hysteria2.json` целиком, с двумя точечными правками (ниже). Корпус `uri/hysteria2` — 23 кейса — зелёный и на сверке тел, и на РЕАЛЬНОМ пути с первого прогона после правок. Рукописный `node_parser_hysteria2.go` удалён; `hysteria2_ports.go` остаётся — его таблицы нужны эмиттеру и hysteria v1. **Четыре примитива, которых движок не умел:** (1) `scheme_sets` с ключом `"*"` — присваивания, общие для ВСЕХ написаний схемы: у hysteria2 TLS включён всегда (транспорт QUIC), параметра `security` в ссылке нет вовсе, и выразить это записью нечем — источника у неё не будет; (2) `normalize: port_range_spec` — диапазон ссылки (`20000-30000`, через дефис) в форму ядра (`20000:30000`, через двоеточие; на дефисе ядро валит весь конфиг «bad port range»), одиночный порт становится парой `N:N`; (3) **нормализация СПИСКА идёт по элементам**, а не по строке целиком — `port_range_spec` над «41000,42000-43000» видел одну строку с дефисом и оставлял первый элемент голым; (4) `prepend_group` у группы `extract` — значение другой группы приписывается спереди: multi-port authority `host:443,20000-30000` регулярка режет на `first=443` (он же `server_port`) и `spec=,20000-30000`, а список портов узла — ОБЕ части вместе. **Предикат формы проверяется по двум текстам** — сначала по распакованному телу, потом по нераспакованному куску под областью декодера. Два вида предикатов описывают разные вещи, и оба законны: `json`/`ini` говорят о ПЕЙЛОАДЕ (их можно проверить только после `decode` — формы vmess), а `regex`/`text` описывают ОБОЛОЧКУ (`^[A-Za-z0-9+/=_-]+$` — это алфавит кодировки, и на распакованном тексте он ложен по построению). Второй текст — именно кусок под `scope`, а не вся ссылка: приклеенное спереди `hysteria2://` ломает алфавит двоеточием и слэшем. **`ParsedNode.Query` берётся из РАСПАКОВАННОГО пространства** (`Result.Query`), а не повторным разбором исходного текста: у обёрнутых форм — base64-пейлоад hysteria2, контейнер vmess — снаружи нет ни одного параметра, и справка для skip-фильтров выходила пустой там, где ссылка их несёт. **Два расхождения с секцией LxBox**, оба в сторону явного объявления (подробности и обоснование — `TASKS_LXBOX` §24.30): у формы `wrapped` объявлена область `scope: "authority"` (декодер «на весь текст» получает строку вместе с `hysteria2://` и отказывает), у группы `$multiport.spec` объявлен `list` (после `prepend_group` значение — список диапазонов, а цель `server_ports` массив и у ядра, и у записи `mport`, которая сливается сюда через `merge: append`) |
| 1.1.19 | **tuic на движке; секция принята от LxBox** (SPEC 133). Вторая их секция, взята целиком с ОДНОЙ правкой: у `password` снят `required` — принять их строгость значило бы выбрасывать живые узлы с пустым паролем (наш `node_parser_tuic.go` писал поле только непустым и узла не ронял), а решения владельца на это нет; их собственная пометка говорит то же. Корпус `uri/tuic` (14 кейсов) сошёлся на сверке тел с первого прогона. **Находка реального пути, найденная тремя красными кейсами:** звено цепочки `label.source` считалось ОТВЕТИВШИМ до нормализации. Цепочка tuic — `["fragment", "path"]`, и ссылка `tuic://…@host/?reduce_rtt=1` несёт путь «/» при отсутствии фрагмента: узел получал имя «/». Тот же дефект глушил следующие звенья пробельной подсказкой. Теперь звено нормализуется ДО принятия, и пустое после нормализации (включая голый «/») уступает следующему. Сверка ТЕЛ этого не видела — метка в тело не входит. Рукописный `node_parser_tuic.go` удалён. Вместе с ним ушла **воронка «userinfo-пароль в node.Query»**: tuic был последней схемой, которой она была нужна (у hysteria v1 учётные данные живут в `auth=`). Именно из-за неё у ssh «работало» ненаписанное `?password=` — QUIRKS Q133-49. Остаток рукописного пути — hysteria v1, masque, wireguard |
| 1.1.20 | **masque на движке; секция принята от LxBox** (SPEC 133). Третья их секция, взята целиком без правок. Корпус `uri/masque` (8 кейсов) зелёный на сверке тел и на реальном пути. Рукописный `node_parser_masque.go` удалён. **Дефект лексера, найденный этой схемой:** путь ссылки резался по ПЕРВОМУ `/` после `://`, а в userinfo `/` законен — у masque там base64(SEC1 DER) приватного ключа, где слэш это 63-е значение алфавита. Половина ключа уезжала в путь, остаток становился ИМЕНЕМ ХОСТА, и разваливались все восемь кейсов. Теперь `/` ищется после ПОСЛЕДНЕГО `@`: `@` законен и внутри userinfo, а после authority его уже быть не может (query и fragment отрезаны раньше). Рукописный путь чинил то же самое percent-кодированием ДО разбора (`percentEncodeWGUserinfoSlashes`) — движку эта заплата не нужна, он режет сам. **Три примитива, объявленных, но не исполнявшихся:** (1) `split_into` — ОДИН список входа раскладывается по нескольким путям тела по условию на элемент (`when.item.matches`/`not_matches`, `take: "first"`): локальные адреса туннеля приезжают одним `address=`, а ядро держит их двумя полями по семейству, `ip` и `ipv6`; парой записей это не выразить — обе читали бы один источник и обе писали бы весь список; (2) `default_when: {absent: true, value: …}` с `materialize_default` — дефолт-КОНВЕНЦИЯ обеих сторон (`profile: cloudflare`, `vhttp: h3`, `mtu: 1280`), которая входит в тела живых узлов и в их identity, в отличие от дефолта ядра (CANON §2.4 его не материализует); (3) `normalize: cidr_prefix` — голый адрес получает префикс «весь хост» (`/32` или `/128` по наличию двоеточия). `required` научился работать у записи БЕЗ своего `maps_to`: у `split_into` он значит «хоть один из путей заполнен» — ссылка masque без единого годного адреса не узел, и корпус ждёт `parse_error`. Дельта **D133-21**: снятые написания masque (`?network=`, `?server_name=`, убраны контрактом 0.8.0 / D-078) теперь ЗВУЧАТ кодом `uri_param_unknown` — запись `$legacy_flat` секции. Тело не меняется; исчезает молчание |
| 1.1.21 | **hysteria v1 на движке; рукописный разбор ссылок УДАЛЁН ЦЕЛИКОМ** (SPEC 133). Секция написана нами (у LxBox ссылочной формы v1 нет) по черновику `SCHEMES.md` §7 и рукописному `node_parser_hysteria.go`; корпус `uri/hysteria` (8 кейсов) сошёлся на сверке тел С ПЕРВОГО ПРОГОНА и зелёный на реальном пути. **Главное в этой записи — не схема, а то, что вместе с ней ушло.** hysteria v1 была последней схемой универсального пути, и с ней удалены: `switch` по написанию ссылки в `ParseNode`, весь разбор `net/url` под ним, **`buildOutbound` целиком** (та самая функция, ради замены которой затеяна кампания) и её бывшие помощники — `applyTLSQueryExtras`, `applyTLSCamouflageFromQuery`, `tlsServerNameFromQuery`, `noteECHIgnored`. В `ParseNode` осталось три ветки: `vpn://` (профиль Amnezia), движок реестра и `wireguard`/`awg` — единственная схема, ещё не переведённая, и у неё свой парсер, строящий ENDPOINT, а не outbound. Отсюда же ушла последняя Go-константа кода деградации, принадлежавшая ссылочному входу: `ech_ignored` ставит запись `ech` общего блока `tls#uri` своим `on_present`. **Три записи, которыми v1 отличается от hysteria2** (обе схемы — разные протоколы, а не «старая и новая версия одного»): учётные данные в query (`auth=` и три его написания), userinfo — лишь ЗАПАСНОЙ слот; `obfs` — ПЛОСКАЯ строка-секрет, а не объект `{type,password}`, причём голое `obfs=xplus` это объявление ТИПА и снимается `value_map` в null (иначе ядро шифровало бы по слову «xplus», и узел молча переставал бы соединяться); `upmbps`/`downmbps` берут ведущие цифры регуляркой — панели пишут «100 mbps» и «50m», — а дефолт (100) подставляет САНИТАЙЗЕР, потому что узел, пришедший JSON-телом, иначе валил бы весь конфиг «missing upload speed» |
| 1.1.22 | **Род узла и три примитива тела — заготовка под wireguard** (SPEC 133). Правило значения научилось судить не только ТЕЛО, но и то, какой протокол просил ВХОД. Условие `when` получило ключ **`source_kind`**: род объявляет секция маппера атрибутом `kind_when` по написанию схемы и форме, в тело род не пишется, а до санитайзера доезжает контекстом рядом с `body_source` (`SanitizeFromKind`). Без него потолок MTU у AmneziaWG невыразим по построению: негодные awg-значения снимает правило поля, и к проверке потолка тело становится неотличимо от обычного WireGuard — а узел всё ещё AmneziaWG, и при завышенном MTU туннель поднимается, но данные по нему не идут. Оба ключа условия работают через ИЛИ: прежний `any_set` по телу остаётся НАВСЕГДА, потому что у входа `singbox` рода от входа нет вовсе. Норма — `contract/docs/MAPPER_ENGINE.md`, «Контекст санитайзера: `body_source` + `kind`». Связи тела получили вид **`cooccurrence`** с условием `when` и служебным оператором `$range_width`: сочетание `random_trailers` с широким диапазоном magic-заголовков (`hi-lo > 65535`) — свойство ПАРЫ настроек, ни одна из которых не битая, поэтому ни `on_invalid`, ни `conflicts` его не выражают; узел живёт, человеку сообщается цена сочетания. Прежде правило жило рукописным `awg3RandomTrailersWithWideHeaders` и потому не работало на входе `singbox`. Нормализаторы **`cidr_prefix`** и **`base64_std`** переехали из маппера в `body.fields`: обе операции правят ЗНАЧЕНИЕ, а не написание ссылки, и обязаны действовать на всех входах — до сих пор они жили в парсере ССЫЛКИ (`normalizeWGPrefixes`, `normalizeWGKey`) и вход `singbox` обходили. Дельта **D133-22** (тела узлов с неканоническим ключом или адресом без префикса меняются; идентичность узла — тег, и она не двигается). Два новых кода: `wgconf_dns_ignored` (info — `Interface.DNS` в теле endpoint'а не применяется) и `wgconf_extra_peer_dropped` (warning — несколько `[Peer]`, взят первый; сегодня Go делает это молча) |
| 1.1.23 | **wireguard/awg на движке — последняя схема; рукописных парсеров ссылок не осталось** (SPEC 133). Секция `mappers.uri` ведёт ОБЕ формы ссылки одной таблицей записей: `url` (`key@host:port`) и `conf_b64` (целый wg-quick под base64, который раздают панели AmneziaWG 3.x) — у них общий набор `maps_to` и разные пространства, и какое читать, решает `source` картой по формам. Парная `mappers.conf` описывает тот же набор поверх `space: "ini"` для вставленного `.conf` (`body_source: "wgconf"`). Уровень-МАССИВ в пути (`peers[].address`) — новый примитив движка: тело wireguard держит пира массивом, и без него путь оседал буквальным ключом `peers[]`. Атрибуты `label.fallback.server_path`/`port_path` (были в схеме, не было в модели) говорят, ГДЕ в теле лежат адрес и порт, когда узел — endpoint, а не outbound: по ним живут дефолтный тег, дедуп, skip-фильтры и UI. **Пять дефектов движка, вскрытых этой схемой:** `required` у записи со `list` всегда считал поле пустым (составное значение печаталось в «»); дефолт с `materialize_default` ПЕРЕБИВАЛ уже прочитанное значение, и порядок объявления двух записей решал, доедет ли порт из `Endpoint`; дефолт записи со `list` не резался по разделителю и уезжал одной строкой; `plus_literal` выводился только из `type`, хотя норма D133-7 говорит про `format`, — из-за чего `+` внутри ключа защиты заголовка AWG3 становился пробелом и узел падал с `awg3_header_key_invalid`; звено метки умело читать только ВХОД, а не разобранное тело. **Два дефекта за пределами движка:** эмиттер share-URI кодировал userinfo ДВАЖДЫ (`url.User(url.PathEscape(...))` → `%252F` вместо `%2F`), и чужой клиент читал ключ как мусор — своя же симметричная ошибка в старом разборе это скрывала; границы `min`/`max` у типа `int_array` не проверялись вовсе (`[]int` проваливался в ветку «мерить нечего»), и `reserved=1,2,999` уезжал в ядро — теперь границы относятся к ЭЛЕМЕНТУ, у `peers[].reserved` объявлено `0..255`. **Два кода, объявленных и никогда не ставившихся, заработали:** `wgconf_dns_ignored` (DNS из `.conf` и из `vpn://`-профиля) и `awg3_random_trailers_wide_headers` (связь `cooccurrence`, теперь на всех входах, а не только на ссылке). Алиас `preshared_key` (с подчёркиванием) читался только Dart — PSK такой ссылки терялся МОЛЧА; секция объявляет оба написания. Удалено: `node_parser_wireguard.go` целиком, `parseWGConfBase64Link`, разбирающая половина `awg3.go`, ветка `wireguard` в `ParseNode` и константа `WarnAWG3RandomTrailersWideHeaders`. Расхождение с LxBox — цепочка метки: источник `hint` принят в грамматику как НЕОБЯЗАТЕЛЬНЫЙ (не передан — звено пропускается), а хост `Endpoint` у нас звено per-app (**D133-24**) |
| 1.1.24 | **Атрибут `live` снят — движок единственный путь разбора ссылок** (SPEC 133). Служебный флаг кампании прожил ровно столько, сколько в конвейере было два пути: он отвечал, какой из них ведёт эту схему, и снимал нужду перечислять имена схем в коде. Секции есть у всех четырнадцати схем, рукописных парсеров не осталось — атрибут удалён из схемы, из модели и из всех файлов протоколов, `SelectLiveURI` стала `SelectURI`, страж остатка снят. Текст, который не опознала ни одна секция, — «схема не поддержана», а не повод искать второй путь. Заодно закрыта дыра в сверке: список сверяемых схем перестал быть поимённым файлом и выводится из реестра, а каталог корпуса берётся по ИМЕНИ ФАЙЛА протокола, а не по схеме — у shadowsocks схема зовётся `ss`, каталога `ss` нет, и все 51 её кейса сверялись НУЛЁМ молча; схема с секцией и без кейсов теперь краснеет |
| 1.1.25 | **`single_into` обязателен у всех секций с `userinfo`** (SPEC 133, договорённость `TASKS_LXBOX` §24.29 п. 3). Умолчания «первое имя `into`» больше нет: атрибут объявлен у всех четырнадцати секций, включая те, где он совпадает с `into[0]`, и линтер `TestContractUserInfoDeclaresSingleInto` требует явного написания. Причина не в аккуратности — позиция и смысл расходятся там, где ошибиться дороже всего: у naive одиночный userinfo это ПАРОЛЬ, хотя `into[0]` зовётся `username`; у ssh и socks наоборот имя, потому что пароля в такой ссылке нет по протоколу; у shadowsocks `into[0]` вообще ШИФР. Молчащий атрибут читается как «здесь думать не надо» — и каждый раз это неправда. Поведение НЕ менялось: значения совпадают с сегодняшними. Линтер заодно требует, чтобы цель была одной из объявленных в `into`: имя вне `into` не перенаправляет само пространство userinfo, и запись, читающая `userinfo.user` напрямую, получила бы тот же текст вторым путём (Q133-43) |
| 1.1.26 | **`dialer#uri` у vless/trojan; `duration_bare_seconds` переезжает в тело** (SPEC 133, D133-26). Две половины одной пачки. (1) Блок `dialer#uri` (`tcp_keep_alive`, `tcp_keep_alive_interval`, `disable_tcp_keep_alive`, контракт 1.1.6/D133-19) подключён у **vless и trojan** — прежде он стоял у семи схем из девяти, и у самой частой пары параметры ссылки уезжали в никуда. Чтение ранее молча игнорируемого: тела узлов без этих параметров не меняются. Единообразно с LxBox. (2) Нормализатор `duration_bare_seconds` (голое число секунд → `"30s"`) снят из МАППЕРА и объявлен в `body.fields.normalize` у пяти полей: `dialer` `tcp_keep_alive`/`tcp_keep_alive_interval`, `tuic.heartbeat`, `anytls` `idle_session_check_interval`/`idle_session_timeout`. Это правило **значения**, а не построения тела, и в маппере действовало на одном входе из трёх: голые секунды приезжают со ссылки, из `streamSettings.sockopt` (ЧИСЛОМ) и из тела в форме ядра. Перенос — следом за `cidr_prefix`/`base64_std` (D133-22) и по той же причине. Число из JSON нормализуется ТОЛЬКО у объявившего поля: иначе единица измерения угадывалась бы за источник. Негодное после нормализации снимается общим правилом `duration` с кодом `type_invalid` (у LxBox сегодня тишина, дельту «молча → с кодом» они принимают). Корпус: `uri/vless/dialer_keepalive`, `uri/trojan/dialer_keepalive_disabled`, `uri/anytls/idle_session_bare_seconds` — у keep-alive покрытия не было вовсе. Встречная задача LxBox — `TASKS_LXBOX.md` §24.33 |
| 1.1.27 | **Вход Xray-JSON исполняется движком: секции `mappers.xray` доведены, общие блоки чинены** (SPEC 133). Разбор элемента Xray-конфига у лаунчера ведёт теперь движок, а не рукописный конвертер. (1) **`transports#xray`: у ВСЕХ путей не хватало `streamSettings.`** — было `json.wsSettings.path`, `json.grpcSettings.serviceName`, `json.xhttpSettings.mode`, то есть путь от корня элемента, тогда как контейнеры лежат внутри `streamSettings`. Полный путь стоял у одного `$selector.network`, поэтому транспорт ОПОЗНАВАЛСЯ, а поля его терялись молча. (2) **`xhttp` у `transports#xray` был на четыре записи вместо 29**: `$settings` читала в никуда (`flatten` без `maps_to`), ~25 полей XHTTP и вложенный `xmux` не объявлены вовсе. Таблица приведена к `blocks.uri.xhttp` запись в запись; диалект Xray выражен `source` — контейнер под двумя именами (`xhttpSettings` ∥ `splithttpSettings`), `xmux` настоящим вложенным объектом внутри `extra` и рядом с ним, правило слоёв прежнее (`extra` раньше плоского, кроме тройки `mode`/`path`/`host` — D-097). (3) **`tls#xray.alpn` → `maps_to: null`** (решение владельца 21.09.2026): с общего блока alpn не пишется, читается он только у hysteria и объявлен там секцией схемы; `null` держит чтение объявленным, чтобы ключ не уехал в `json_field_unknown`. (4) **`dialer#xray`: знак sockopt данными** через `when {lt,gt}` — `tcpKeepAliveIdle` > 0 пишет длительность, < 0 поднимает `disable_tcp_keep_alive` через `implies`, 0 не даёт записи. Два написания ключа = две пары записей: `when` сравнивает ОДИН источник, и перечисленные ключами одного условия написания соединились бы логическим И. (5) **`forms[].detect` по `type_of`** вместо ветки `default` у всех четырёх схем — `streamSettings: "none"` означает битую запись, а не «транспорта нет», и `default` собирал из неё рабочий plain-TCP узел, которого провайдер не присылал. Уточнение против черновика LxBox: голое `type_of` отнимает узел у элемента, где `streamSettings` ОТСУТСТВУЕТ (законный plain-TCP), поэтому написано «объект ИЛИ ключа нет» через `any`/`key_absent`; у `shadowsocks` условие только на `settings` (транспорта у plain-SS не бывает, кейс `ss_basic`). (6) **`unknown_key.ignore`** у всех четырёх — список LxBox дословно (`protocol`, `tag`, `remarks` + контейнеры `settings`, `streamSettings`, `mux`, `sendThrough`, `proxySettings`); объявление вперёд: на JSON-входе `unknown_key` пока не исполняется (у движка есть обход имён query, JSON-аналога нет), кода `json_field_unknown` с этого входа не ставится ни одного. (7) **`include dialer#xray`** подключён у vless и trojan. Движок получил общий вход элемента документа (`SelectElementSection`/`ParseElement`/`UnwrapElement`, вид источника — ПАРАМЕТР, имени диалекта в коде нет) и раннер сверки `TestEngineVsXrayCorpus`: тела движка против ожиданий, снятых СТАРЫМ конвертером — тот самый страж, отсутствие которого стоило LxBox ~25 полей XHTTP. Дельты в `testdata/deltas_allowed.json`: keep-alive из sockopt (D133-19, не читался вовсе), `alter_id: 0` при `materialize_default` (D133-25), `vless_h2` — Q133-16 разрешён в сторону чинки (склейка массива `host` через `fmt.Sprint` давала мусор `["[a.example b.example]"]`). Встречная задача LxBox — `TASKS_LXBOX.md` §25 |
| 1.1.28 | **Hysteria у входа Xray и снятие рукописного конвертера: разбор элемента Xray ведёт движок в БОЮ** (SPEC 133). (1) **Блокер «один `protocol` → две схемы» снят решением владельца 21.09.2026 — двумя секциями `mappers.xray` со ВЗАИМОИСКЛЮЧАЮЩИМИ `detect`**, каждая в файле своей схемы; новых примитивов не заведено, движок выбора не тронут. Предикат версии повторяет прежний конвертер дословно: сперва `streamSettings.hysteriaSettings.version`, и ЛИШЬ при его отсутствии `settings.version`; нет нигде = v1. Секция `hysteria2` несёт ДВЕ формы — `servers` (апстримное `protocol:"hysteria2"`, адрес в `settings.servers[0]`) и `flat` (написание форков, адрес ПЛОСКО в `settings`); записи, которых у апстримной формы не было (обфускация, полоса, alpn, фолбэк `server_name`), объявлены через `when {$form: flat}` либо источником только для этой формы. Расхождение с черновиком LxBox сохранено и объявлено: у них `version: 1` роняется кодом `protocol_unsupported`, у лаунчера Hysteria v1 из Xray ЖИВОЙ (`auth_str`, `up_mbps`, `down_mbps`), и принять их ветку значило бы потерять рабочие узлы. (2) **`value_of` / `value_in` расширены на ЛЮБОЙ скаляр JSON** (было — только строка): строка сравнивается со строкой fold-case, число с числом, булево с булевым, и **типы не приводятся**. Приведение было бы сменой поведения, а не удобством: `version: "2"` строкой прежний конвертер за двойку не считал (`xrayJSONInt` строк не читает) и вёл элемент в v1. Добавление — прежние записи работают как работали. (3) **Боевой загрузчик Xray переведён на движок** (`xray_element_engine.go`): `xrayNodeFromOutbound` зовёт `parseXrayElementByEngine`, списка схем в коде не осталось. Санитайзер во входе НЕ зовётся — он стоит стадией ниже, в единственной точке рождения тела, и коды оттуда приезжают с путём и значением; ранний вызов давал бы каждую деградацию дважды (поймано корпусом `xray/vless_fp_junk`). (4) **Рукописный конвертер СНЯТ**: `xray_hysteria.go` удалён целиком, из `xray_outbound_convert.go` и `xray_protocols.go` ушли все шесть конвертеров протоколов с выемками адреса, TLS и транспорта, а с ними `normalizeVMessSecurity` и `xrayFlattenScalars`. Оставлено то, что решает УРОВЕНЬ ДОКУМЕНТА (какой элемент становится узлом, какой хопом цепочки, какой группой-балансером): `xray_json_array.go`, `xray_balancer.go`, скаляры карты, ссылка из `sockopt` и сборка хопа из socks-элемента — это вход следующей волны. (5) **Текст отказа `required` берётся из `desc_en` записи**, когда она его объявила. Причина едет ЧЕЛОВЕКУ, и «обязательное поле \"uuid\" пусто» отвечает не на его вопрос: у пустого id в Xray-подписке смысл определённый — сервер отдал заглушку, подписка протухла, — и прежний конвертер это говорил. Новый атрибут не заводился: `desc_en` уже есть у каждой записи и означает ровно «что это поле значит человеку». (6) **Гард пары `mode`/`uplink_data_placement` перенесён в `blocks.xray.xhttp` ДАННЫМИ**: правило ядра («header работает только в packet-up») от диалекта не зависит, а жило данными на входе ссылки и рукописным `noteXHTTPPlacementGuard` на входе Xray — две копии одного решения. Корпус `xray` обновлён по объявленным дельтам (D133-19 keep-alive из sockopt, D133-25 `alter_id: 0`, Q133-16 массив `host`) плюс `malformed_stream`: элемент со `streamSettings: "none"` узлом больше не становится (следствие правки `forms[].detect` контракта 1.1.27 п. 5). Новая дельта — **D133-28**: версия hysteria вне {1, 2} не опознаётся ни одной секцией, узла нет (как и прежде), но код отказа теперь общий, а не собственный. Заодно `_impl` → `impl` в `unknown_key` пяти секций — линтер грамматики ловил закрытое множество имён. Встречная задача LxBox — `TASKS_LXBOX.md` §26 |
| 1.1.29 | **Служебное имя в присваивании не едет в тело — правило общее для всех приёмов** (SPEC 133, находка LxBox 21.09.2026, Q133-57). Отсечение имён `$…` (служебное значение для записей таблицы) и `_…` (прозаическая сноска автора реестра) стояло ровно в одном месте — у `scheme_sets`, — и стояло там потому, что однажды выстрелило: `$default_port` уехал в тело литеральным ключом, и узел потерял `server_port` (Q133-44). Остальные приёмы присваивания (`sets` селектора, `implies`, `defaults` секции) шли той же дорогой и ждали первого такого ключа. Сведено в одну проверку `isReservedAssignKey` у ВСЕХ приёмов: вопрос про ИМЯ, а не про то, каким приёмом имя попало в карту. У `scheme_sets` своя ветка остаётся — там `$`-ключ не отбрасывается, а запоминается для записей таблицы. Живых `_…` в реестре нет, `$`-ключ ровно один: дефект был потенциальным и стал бы живым на первой такой записи LxBox. **Две другие находки той же заявки проверены фактом и НЕ подтвердились.** (1) WG-ключ с неканонической последней группой узел не роняет: `format base64_32` и `normalize base64_std` судят мягким декодером (`base64.StdEncoding` в Go не проверяет хвостовые биты), `…NDV=` и `…NDU=` дают одни и те же 32 байта, узел живёт, а нормализация приводит запись к канонической — ровно как делает ядро; строгого режима нет нигде. Кейс корпуса `uri/wireguard/key_noncanonical_trailing_bits` фиксирует свойство против будущей замены на `Strict()`. (2) `unknown_key` у `mappers.conf`: настройка тут ни при чём — `unknown_key` в движке НИЧЕГО не делает с телом ни при `keep`, ни при `drop`, он только ставит код на необъявленный параметр ссылки, а тело строят исключительно объявленные записи. Незнакомый ключ `.conf` до ядра не доходит по построению; настоящая цена другая — он отбрасывается МОЛЧА (Q133-58), и чинится это вместе с переводом `.conf` на секцию, а не настройкой атрибута. **Заодно вскрыто и записано:** секция `mappers.conf` написана полностью и не исполняется НИКЕМ (Q133-59) — боевой путь ведёт конвертер `.conf` → ссылка, раннера сверки у вида `conf` нет. Та же конфигурация, в которой у `transports#xray` пережили несколько ревизий неверные пути у всех записей блока. Поручение «объявить `ini_dialect` данными» развёрнуто по порядку: сперва исполнитель, потом диалект. Встречная задача LxBox — `TASKS_LXBOX.md` §27 |
| 1.1.30 | **`ini_dialect` данными; вход `.conf` исполняет секция, а не конвертер** (SPEC 133, поручение владельца). Порядок поручения развёрнут по факту: сперва ИСПОЛНИТЕЛЬ, потом диалект — секция `mappers.conf` была написана полностью и не исполнялась никем (Q133-59), а данные без исполнителя не проверяются ничем. (1) **Раннер `TestEngineVsConfCorpus`** — третий в семье после сверок ссылок и Xray; корпус `wgconf` вырос с одного кейса до восьми (AWG-набор, сахар маскировки `ip/id/ib`, Endpoint IPv6 в скобках, имя узла комментарием, вторая `[Peer]`, незнакомые ключи, отсутствие `AllowedIPs`), ожидания сняты СТАРЫМ боевым путём. (2) **Дефект, найденный первым же прогоном** (Q133-60): движок снимал у ini «метку-фрагмент» по первому `#`, хотя у ini-документа это законный синтаксис тела — файл с «# US-FREE#137» под `[Peer]` терял всё ниже комментария и отвергался как «`peers[].public_key` пусто». Признак обёртки теперь — наличие `://` в тексте: декодер со `scope` пересобирает пейлоад обратно в обёртку, поэтому `vmess://<base64>#Имя` и `awg://<base64>#Имя` её снимают, а голый `.conf` — нет. Оба перекоса проверены фактом (исключение `ini` целиком роняет `awg_conf_base64`, условие «нет декодеров» — восемь кейсов vmess). (3) **`ini_dialect` объявлен данными**: `key_case`, `value_case`, `line_comment_prefixes` с алиасом `comment_prefixes`, `inline_comments`, `repeated_key`, `sections.<S>.repeat` + `on_extra` → код. Исполняет его разборщик ini; пустая секция = сегодняшний диалект wg-quick дословно. Этим закрыто расхождение, из-за которого на одном месте жили ТРИ поведения: движок сливал вторую `[Peer]` с первой, прежний конвертер брал ключи только первой и отбрасывал остальные МОЛЧА, норма требует «первая + `wgconf_extra_peer_dropped`». Теперь код ставится с параметром `count` (общее число секций). Встречная задача LxBox — `TASKS_LXBOX.md` §28 |
| 1.1.31 | **Вход `.conf` ведёт СЕКЦИЯ: рукописный конвертер снят** (SPEC 133). Боевой путь wg-quick переведён с `wgConfToURI` (конвертер `.conf` → ссылка с разбором получившегося URI) на `mappers.conf`; сняты `wgConfToURI`, `wgPeerNameComment`, `wgEndpointHost` (~150 строк, где каждый новый набор — AWG3, сахар маскировки — приходилось дописывать вторым списком поверх объявленного в реестре). Переключены все входы: `vpn://`-профиль (одиночный и мульти-контейнерный), вставленный текст, миграция, разбор тела подписки. Промежуточная ссылка в разборе тела больше не строится — блок несёт СВОЙ узел (`ConvertedWGBlock.Node`), потому что обратный перевод терял то, чего в ссылке нет по построению: код `wgconf_dns_ignored` (DNS в тело не едет) и метку из комментария `[Peer]`. **Три дефекта, вскрытых переключением.** (1) `required` НЕ проверялся у записи с `extract` (Q133-64): проверка спрашивала путь из `maps_to`, которого у такой записи нет, и `required: true` не означал ничего — блок с `[Peer]` без `Endpoint` становился «узлом» с пиром без адреса и порта. Ветка `extract` дописана по образцу `split_into`; запись такого вида в реестре ровно одна. (2) `+` → пробел применялось и к ini-документу (Q133-62): форм-семантика существует там, где значения приезжают query-строкой, а в `.conf` `+` литерален всегда — ключ Proton `0GCSi+xv9…` становился `0GCSi xv9…` и узел отбраковывался как «не 32-байтный ключ». Признак теперь у ПРОСТРАНСТВА (`forms[].space`), а не у формата поля. (3) `hint` был объявлен в цепочке метки и не исполнялся (Q133-63): источника с таким именем движок не знал, звено молча пропускалось — на переводе `vpn://` узлы «AWG Node» мгновенно переименовались в `203.0.113.7`. Исполнено: `Space.Hint`, `ParseURIHint`. **Корпус** `wgconf` вырос с 1 кейса до 12 (четыре тела взяты у LxBox из `pipeline_identity_before.json`); ожидания сняты старым путём ДО переключения, две объявленные дельты обновлены в день переключения. `SelectKind` — общая форма `SelectURI` с видом источника параметром. Встречная задача LxBox — `TASKS_LXBOX.md` §29 |
| 1.1.32 | **Незнакомый ключ `.conf` перестал теряться молча** (SPEC 133, Q133-58 закрыт). Код `wgconf_param_unknown` (severity `info`: до тела ключ не доходит по построению, узел исправен), запись `unknown_key` у `mappers.conf` несёт игнор-список ключей wg-quick, которые НЕ описывают узел (`PostUp`/`PostDown`/`PreUp`/`PreDown`/`Table`/`SaveConfig`/`FwMark`/`PresharedKeyFile`/`PrivateKeyFile`) — ругаться на них значило бы ругаться на исправный конфиг провайдера. **Вскрылось попутно:** запись `unknown_key` у секции СТОЯЛА и раньше, но не срабатывала никогда — проверка необъявленного спрашивала только имена query, а у документа предмет другой: ключи секций ini. Добавлены `Space.INIKeys` и `Plan.DeclaredINI`; объявленность считается по ИСТОЧНИКУ (`ini.Peer.PersistentKeepalive`), а не по имени записи (`keepalive`), иначе объявленным не выглядел бы ни один ключ. Первыми кодом отмечены реальные случаи: `BogusKey` и написание `preshared_key` в snake_case из фикстур LxBox. **Записан долг** (Q133-65): линтер кодов смотрит только на коды схемы ТЕЛА и не смотрит на коды секций-мапперов — из-за чего `uri_param_unknown` (16 мест) и `json_field_unknown` (8 мест) используются, доезжают до узла и НЕ объявлены в `warnings.json` вовсе, то есть UI не может их нарисовать. Тексты этих двух кодов — решение владельца, заведено отдельной задачей |
| 1.1.33 | **Порт `ss` по умолчанию 8388; тексты двух AWG-кодов называют путь, значение и последствие** (SPEC 133, решения владельца). (1) `mappers.uri.defaults.server_port` у shadowsocks: `443` → `8388` (D133-29). 443 пришло не из решения, а из общего умолчания старого кода (`node_parser_core.go:145` ставил его всем схемам, ss-ветка не переопределяла) — наш дефект. Не ловился, потому что ни одна из девяти фикстур порт не опускает: дефолт не исполнялся нигде. Заведён кейс `uri/shadowsocks/no_port_default_8388`; проверено фактом — возврат на 443 роняет корпус, то есть у решения теперь есть сторож. Живых узлов правка не двигает: меняется только узел, чья ссылка порт не несёт, а такой прежде молча получал 443 и не соединялся. (2) `awg_header_invalid` и `awg3_field_invalid`: было «field {field} removed», что человеку не говорит ничего. Стало — ПУТЬ и ЗНАЧЕНИЕ (`{path}`/`{value}`) плюс прямое последствие: ядро откатится на обычный заголовок/поведение WireGuard, и если сервер ждёт AmneziaWG, рукопожатие может не сойтись. `path` добавлен в `params` (иначе `declaredParams` отфильтровал бы его), проверено рендером на обоих языках. Плейсхолдеров в `cause`/`fix` намеренно нет: подстановка работает только в `title`/`text` (`Registry.WarningText`), в совет она не заходит |
| 1.1.34 | **Необъявленный ключ называет СЕБЯ: код на каждое имя, и все коды мапперов объявлены** (SPEC 133, Q133-65 закрыт; нормы согласованы с LxBox). (1) **`uri_param_unknown` ставится ПО ОДНОМУ на каждый необъявленный параметр**, имя уезжает в `path`, порядок — появления в источнике. Прежде код был один на узел: дедуп деградаций идёт по паре (код, `path`), а `path` у кода движка был пуст, и второй незнакомый параметр исчезал молча — ровно та потеря без слов, против которой код и заведён. Ссылка `naive` с двумя такими параметрами теперь даёт два кода. (2) **`json_field_unknown` получил ИСПОЛНЕНИЕ**: `unknown_key` у секций `xray`/`singbox` был объявлен реестром с контракта 1.1.27, но ключи JSON-элемента не перечислял никто, и кода с этого входа не ставилось ни одного. Добавлен обход ВЕРХНЕГО уровня элемента (вложенный путь параметром не является — то же правило, что при сборке `Declared`). Исполнение сразу обнажило перекос реестра: xray-секция `shadowsocks` была единственной без списка `ignore`, и элемент получал код на собственные `protocol`/`tag`/`settings`; список приведён к списку остальных секций. (3) **Три кода объявлены в `warnings.json`** (`severity: info` у первых двух — до тела ключ не доходит, узел исправен): `uri_param_unknown`, `json_field_unknown` и найденный новым линтером `xray_extra_entries_dropped` (примитив `on_len_gt` у `$extra_vnext`/`$extra_servers`/`$extra_users`; ОБЪЯВЛЕН, НО ПОКА НЕ ИСПОЛНЯЕТСЯ — текст заведён к моменту включения, а не после него). (4) **Линтер кодов расширен на секции-мапперы** (`TestRegistryMapperCodesDeclared`): прежде он судил только схему ТЕЛА и коды `mappers.*` не собирал никогда, из-за чего два живых кода доезжали до узла без заголовка, текста и подсказки. Обход рекурсивный по сырому JSON — типизировать секцию значило бы завести второй её читатель рядом с движком; страж ловит поле `code` в ЛЮБОМ месте секции, включая те, которых сегодня ещё нет. (5) **`wgconf_dns_ignored` и родня несут `path`** — ИМЯ ЗАПИСИ (`dns`), потому что у записи с `maps_to: null` пути в теле нет по построению; без него две таких потери на одном узле схлопывались бы в один код. (6) **Раннеры корпуса краснеют при нуле найденных кейсов** — молчаливый `Skip` означал бы «контракт сверен», когда не сверено ничего. Встречная задача LxBox — `TASKS_LXBOX.md` §31 |
| 1.1.35 | **Три «правила только у Go» оказались уже выраженными данными — проза устарела, не реестр** (SPEC 133, разбор красных LxBox `contract_24_2_rules`). Заявка называла три правила, описанных в `impl` прозой со словами «читает ТОЛЬКО Go»; проверка фактом показала, что **два из трёх давно живут данными и исполняются общим движком**, а проза описывала мир, которого нет с контракта 1.1.28 (рукописный конвертер Xray снят целиком). (1) `hellorandom*` → `random` — это `tls.fp_dialect.prefix`, обычная именованная таблица `value_map`; длинный префикс матчится раньше короткого, поэтому `hellorandomized` не проигрывает `hellorandom`. Проверено прогоном: `HelloRandom` → `random`, `hellorandomized` → `randomized`, `HelloChrome_120` → `chrome`. (2) Xray-алиас `splithttp` — это `$selector.network.value_map` (`splithttp` → `xhttp`) плюс второе написание контейнера в `source` каждой записи xhttp (`splithttpSettings` рядом с `xhttpSettings`). Проверено прогоном: элемент с `network: "splithttp"` даёт `transport.type: "xhttp"` с полями, а не plain tcp. Обе записи `impl` переписаны: сказано, что правило выражено данными, и названо, ЧТО стороне проверить у себя. Цена старой прозы не нулевая — LxBox читал её как «у нас этого не будет» и держал своё расхождение живым. (3) Третье правило — `tuic.password` — данными **не выражается и выражено быть не должно**: это не диалект, а РАЗНАЯ СТРОГОСТЬ (`required: true` у LxBox против «пустой пароль не пишет ключа, но узел живёт» у нас). Принять их строгость = выбросить живые узлы у тех, у кого пароль пуст; решения владельца на это нет. Оставлено расхождением, записано открытым вопросом. (4) **`ech_ignored` называет ЗНАЧЕНИЕ**: в `text_en`/`text_ru` было только имя параметра (`{query_name}`), а человеку важно, ЧТО именно сняли — стало `{query_name}={value}`. Значение уже приезжало в параметрах кода (`on_present` подставляет его сам) и просто не выводилось; `params` не трогали — `value` неявный (`text_params_implicit`). Проверено рендером на обоих языках |
| 1.1.36 | **Обратный ход на движке: ссылка собирается ТОЙ ЖЕ таблицей, что её читает** (SPEC 133). `core/config/linkmap/emit.go` исполняет секции `mappers.uri` в обратную сторону: `maps_to⁻¹`, `value_map⁻¹`, `sets` сопоставлением, `compose` (обращение `extract`), `into⁻¹` у userinfo, `form_from`. Имён схем в движке нет (страж `TestNoSchemeNamesInEngine`). **Судят два раннера, и оба нужны:** `TestEngineEmitVsSnapshot` — ссылка движка = снимок РУКОПИСНОГО эмита байт в байт по 314 телам корпуса, кроме поимённого списка отличий (судится В ОБЕ СТОРОНЫ: объявленный кейс обязан отличаться); `TestEngineEmitRoundTrip` — круг `parse(emit(body)) == body`. Снимок сторожит ВИД ссылки (чужие клиенты), круг — её СОДЕРЖАНИЕ; зелёный снимок при красном круге означал бы, что рукописный эмиттер терял поле и движок потерю повторил. **Норма вида — де-факто формат схемы, а не симметрия таблицы** (решение владельца): Copy link существует для обмена с ЧУЖИМИ клиентами, отсюда `security` явно у vless/trojan/anytls и имя флага insecure свойством СХЕМЫ (`allowInsecure` / `allow_insecure` / `insecure`). **Починены пять потерь рукописного эмита** (D133-E1…E9): keep-alive не писался вовсе; имена xhttp уезжали snake_case там, где клиенты Xray ждут camelCase; `zero_rtt_handshake=1` у tuic — имя ТЕЛА, а не ссылки; пробел писался `+` вместо `%20`; узел hysteria2 без пароля ссылкой не выражался вообще. **Три находки нормы.** (1) Канон имени на выходе — ПЕРВЫЙ ИСТОЧНИК записи, а не первое в `aliases`: `aliases` перечисляет альтернативные написания ЧТЕНИЯ, и взяв первое из них, ссылка получала имя чужого диалекта (`extra-headers` вместо `headers`). §0.6 уточнён. (2) Ветка `sets` под ПУСТЫМ ключом обратным ходом не выбирается никогда — пустой ключ есть отсутствие параметра, а не написание; без правила `security=` выигрывал у `security=tls` на каждом TLS-узле. (3) `decode_extra.passes` действует и на выходе: путь, объявивший два прохода чтения, обязан быть закодирован трижды (печать query — первый уровень), иначе `/%2F` возвращается с круга как `//`. **Примитивы добавлением, без переименований:** `emit.names`, `emit.json_map`, `emit.json_always` (КАРТА «ключ → заполнитель», а не список имён — заполнитель у ключей разный), `emit.userinfo{form,padding,empty_separator,keep_empty_tail}`, `emit.omit_port`, `userinfo.emit`, `round_trip`+`round_trip_why`, `emit_as` (+`pairs`), `emit_pair_sep`, `emit_normalize`, `emit_value_map` (явное обращение неинъективной таблицы), `compose` объектом. Встречная задача LxBox — `TASKS_LXBOX.md` §33 |
| 1.1.37 | **Пустой пароль tuic — узел с предупреждением; `type=splithttp` в ссылке перестал терять транспорт** (SPEC 133, два решения владельца). (1) **Q133-67 закрыт в нашу сторону**: ссылка `tuic://<uuid>:@host` (и без двоеточия) — УЗЕЛ, но несёт `password_empty` (severity `warning`, `path: password`). Строгость LxBox (`required: true`) не принята — она выбрасывает живые узлы у всех, у кого пароль пуст; молчать тоже нельзя, сервер такую аутентификацию почти наверняка отклонит, и узел выглядел бы «сломанным без причины». Выражено НОВЫМ общим примитивом **`on_empty`** (DRAFT): код за пустое либо отсутствующее значение источника — отличается от `required` тем, что узел остаётся, от `on_invalid` — тем, что значения нет вовсе и судить нечего. Имя кода тоже общее, не под схему: поле «пароль» есть у половины схем. Кейсы корпуса `uri/tuic/password_empty_after_colon` и `password_absent_no_colon` — «пусто = отсутствует» (§0.6) ведёт к коду обе формы. (2) **Алиас `splithttp` в ССЫЛКЕ**: он был выражен данными только в диалекте Xray (`blocks.xray.$selector.network.value_map`), а у `blocks.uri.$selector.type` набор `when.in` закрыт и `splithttp` в нём не было — ссылка Xray с `type=splithttp` уезжала ГОЛЫМ TCP, без кода. **Не регрессия перехода на движок**: рукописный парсер алиаса тоже не знал, его `switch` по типу имел только ветку `xhttp` (ветка снята с контрактом 1.1.28) — давний дефект обеих реализаций. Исправлено ТЕМ ЖЕ примитивом, что в диалекте xray; кейс `uri/vless/xhttp_type_splithttp_alias` даёт `transport.type: "xhttp"` с полями. На ВЫХОДЕ пишется канон `xhttp`, не алиас: обращение `value_map` неинъективно (в xhttp ведут два ключа), и это объявлено `emit_as: "raw"`. Встречная задача LxBox — `TASKS_LXBOX.md` §34 |
| 1.1.38 | **Вычитка текстов кодов предупреждений en/ru** (SPEC 134 TASK 3). Заголовки `uri_param_unknown`, `json_field_unknown`, `wgconf_param_unknown` и `naive_extra_headers_invalid` называют конкретное имя — в списке узлов строка самодостаточна. Четыре расхождения `params`↔плейсхолдеры закрыты: `value` снят у `wg_key_invalid` (маскируется, в текст не входит), `field` — у `awg_header_invalid`/`awg3_field_invalid`, `method` — у `ss_method_legacy` (текст на неявных `path`/`value`). У `xhttp_param_reset` убран неиспользуемый `reason`, текст сведён с фактическими `field`/`value`. Группы B/C: уточнены формулировки вокруг технических `{reason}`/`{cause}`, заголовки `detour_with_listen_port`, `masque_vhttp_invalid`, `ws_early_data_converted`; в `chain_strip_utls_on_reality` пояснён ClientHello. Служебные `go` у записей с удалёнными `node_parser_*` перенаправлены на `contract/registry/**` + `core/config/linkmap` / `nodeflow`. Проверка плейсхолдеров — скрипт из TASK 3 §3.4 (0 расхождений); `go generate ./contract/...` |
| 1.1.40 | **Формат ЭЛЕМЕНТА у списков и `base64_rawurl` — два запроса LxBox, оба выражены реестром** (SPEC 133; номера 1.1.36–1.1.39 заняты параллельной волной эмита). Оба случая роняли не узел, а ВЕСЬ конфиг ядра. (1) **`item_pattern` + `on_item_invalid`** — новая пара атрибутов поля тела: негодный элемент списка снимается с кодом, годные соседи доезжают до ядра. Отдельным именем, а не через `pattern`, потому что тот судит значение ЦЕЛИКОМ и провал одного элемента снимал бы список со всеми остальными; линтер требует `item_pattern` только у `listable_string`/`string_array` (у скалярного поля он не сработал бы ни разу, а читался бы как рабочее правило). `on_item_invalid` — имя из грамматики МАППЕРА (`extract` по элементам списка): операция одна, словарей два. Путь кода — ЭЛЕМЕНТ (`server_ports[0]`), иначе дедуп по паре (код, путь) схлопнул бы два разных негодных элемента в одно сообщение. Выставлено у `hysteria2.server_ports` (`^[0-9]{1,5}:[0-9]{1,5}$`: адрес из `mport=198.51.100.24:443,20000-30000` пролезал в список портов и давал «bad port range») и у `tls.alpn` (идентификатор ALPN — токен; элемент с запятой внутри означает неразрезанный список). У `alpn` рядом заведён `normalize: "trim"` — без него `alpn=h2, h3` терял бы живое значение из-за ведущего пробела. Правило живёт в ТЕЛЕ, а не в маппере (§6.0): оно судит содержимое значения и обязано действовать на всех входах, включая sing-box-JSON, где тот же список человек пишет руками. Дельта `D133-33`. (2) **`normalize: "base64_rawurl"`** у `tls.reality.public_key` — зеркало `base64_std`: ядро декодирует ЭТОТ ключ только `RawURLEncoding`, и написание со «+», «/», «=» проходит `format base64_32` (32 байта после декода там правда), а ядро отвечает «decode public_key: illegal base64 data» отказом всего конфига. Какой алфавит нужен полю, решает ядро, а не форма значения, поэтому имя стоит у поля и из `format` не выводится. Канонизация меняет тело живых узлов — тот же класс, что D133-22, дельта `D133-34`. Три новых кода в `warnings.json`: `hysteria2_server_ports_item_invalid`, `tls_alpn_item_invalid`. Кейсы корпуса: `uri/hysteria2/mport_host_port_item_dropped`, `uri/vless/reality_pbk_base64_std` (второй — тот же ключ, что у `reality_tcp_no_flow`, в std-написании, и ожидания тела у двух кейсов совпадают). Встречная задача LxBox — `TASKS_LXBOX.md` §36 |
| 1.1.42 | **`on_len_gt` исполняется; порядок warnings[] уточнён; `wgconf_dns_ignored` несёт value** (SPEC 133). (1) Примитив `on_len_gt{n,action:"note",code}` у `$extra_vnext`/`$extra_servers`/`$extra_users`: движок читает массив через `LookupRaw`, при `len>n` ставит `xray_extra_entries_dropped` с `path`=имя записи и `value`=число элементов (Q133-66 закрыт); кейс `body/xray/vless_extra_vnext`. (2) CANON §6: коды маппера с `maps_to:null` идут впереди кодов санитайзера; внутри группы — порядок объявления правил. (3) `on_present` подставляет `value` в Params — раннер конверта переносит `params.value`/`params.count` в поле `value` ожидания; корпус wgconf/amnezia дополнен. Встречная задача LxBox — `TASKS_LXBOX.md` §38 |
| 1.1.41 | **Виды источника объявлены таблицей: `registry/source_kinds.json`** (SPEC 133, уровень ДОКУМЕНТА по черновику LxBox §480). Чем оказался текст подписки ЦЕЛИКОМ, судится тем же языком предикатов `detect`, что и уровень элемента: отдельного «документного» языка норма не допускает, иначе сниффер формата вернулся бы в код. Термин владельца — «вид источника», ключ записи `source_kind` (`kind` уже занят видом источника у секции-маппера). У каждого вида: `detect` (чей это текст), `unwrap` + `redetect` (снятие оболочки и повторный суд), `elements` (нарезка: `$self` / `lines` / `texts` / `[]` / `outbounds[]+endpoints[]` / `[].outbounds[]`) и `mapper` — каким маппером читать ЭЛЕМЕНТ. Двенадцать веток, порядок задаёт `priority`, а не позиция строки; ровно одна ветка `default`. Новые примитивы `detect`: `first_section_fold` (первая НЕ-комментарная секция ini — `.conf`, у которого первой идёт `[Peer]`, конфигом wg-quick не является), `prefix_trim` (первый непробельный символ), `min_len`, `requires_after_unwrap` (правдоподобие распакованного: строка ссылок без спецсимволов тоже проходит алфавит base64), `max_unwrap_depth`. Три ветки Xray заведены впервые (`xray_config`, `xray_config_array`, `xray_outbound_array`, `xray_outbound`): признак диалекта — ключ `protocol` у ЭЛЕМЕНТА, без него полный Xray-конфиг уезжал в ветку sing-box и давал ноль узлов. Дельты `D133-35` (предел глубины распаковки), `D133-36` (диалект массива судится по всему массиву, а не по первому элементу), `D133-37` (голые outbound'ы Xray стали опознаваться), `D133-38` (`.conf` судится первой секцией). Временный признак «`://` в тексте» снят с `linkmap/parse.go`: оболочку снимает тот, у кого декодер объявлен с областью (`scope`) — признак структурный, из формы, а не из входа. Раннер `TestSourceKinds` — 18 кейсов «текст → вид → число элементов» плюс линтер формы таблицы; `Q133-68` (отвергнутая обёртка оставляла себе имя победителя) закрыт. Рукописный `body_classify.go` с боевого пути ещё НЕ снят — следующая волна; `xray_json_array.go` не переводится по границе `SCHEMES.md` §13.5 (`Q133-69`). Встречная задача LxBox — `TASKS_LXBOX.md` §37 |
| 1.1.71 | **SPEC 143 закрыта на desktop; ссылки на папку SPEC 143 после закрытия** (`SPECS/143-F-C-TEMPLATE_CANON_WALKER`). `TEMPLATE_LANG.md`: разрыв C4 описан закрытым (строгий режим Go и legacy-обходчик удалены, preset-путь и шаблонные DNS-серверы на каноне), у `int` невалидное значение называет код `template_int_invalid`. Нормативное поведение, коды, реестр и корпус не менялись. Итог для LxBox — `TASKS_LXBOX.md` §67 |
| 1.1.70 | **Новый код `template_fragment_dropped {owner, kind, reason}`: фрагмент пресета или шаблонного DNS-сервера, оставшийся после подстановки без обязательного поля** (SPEC 143 волна 3). Тела пресетов и шаблонных DNS-серверов на desktop собираются каноническим обходчиком; обходчику объявлены все переменные шаблона, так что пустая глобаль даёт Dropped ключа (§5.1), а не литерал `"@name"` и не выпадение фрагмента. После каскада гейты валидности: правило маршрута без `outbound`/`action`, DNS-правило без `server`/`action`, набор правил без источника (`url`/`path`/`rules` по `type`), DNS-сервер адресного типа без `server`, правило без условий — выпадают с этим кодом; он идёт в «Итог» сборки видом `template_degraded`. Фрагмент, целиком снятый `#if`/`#enable` автора, кода не даёт. Корпус и формат остальных кодов не менялись. Встречная задача LxBox — `TASKS_LXBOX.md` §66 (у них задача 555 п.6) |
| 1.1.69 | **Коды `template_*` на desktop доходят до отчёта сборки** (SPEC 143 волна 2). Главный конфиг и `on_change.set` собираются каноническим обходчиком; `template_var_undeclared`, `template_unknown_directive`, `template_int_clamped`, `template_int_invalid` с параметрами по реестру идут в «Итог» сборки записью нового вида `template_degraded` (первой в списке), Save не блокируют. В `registry/warnings.json` у четырёх кодов поле `go` переписано на реальные точки постановки и канал в отчёт. Формат кодов, параметров, тексты и корпус не менялись. Встречная задача LxBox — `TASKS_LXBOX.md` §65 (у них задача 555 п.5) |
| 1.1.68 | **Язык шаблонов: сплайс на один уровень, без коллапса, `options` ортогональны `type` + `options_open`, `@runtime.*` в значениях** (SPEC 143 волна 0, решения владельца 25.09.2026, D-123…D-127). (1) `TEMPLATE_LANG.md` §4.4: ветка условного элемента массива, если она массив, вливается в родительский массив на один уровень; литерал и ссылка на `text_list` не различаются; вложение — двойными скобками `[[…]]`. (2) §3: одноэлементный `["@name"]` не сворачивается в скаляр, форму значения задаёт только `type`. (3) §2.1/§2.2: `options` (строковая и объектная формы равноправны) допустимы при любом типе, кроме `bool`, и тип не меняют (N5 закрыт); новый флаг `options_open: true` — своё значение с приведением по `type`; `enum` = `text` + закрытые `options`; `text_list` + `options` = множественный выбор; каст в число только по `type` (C5 закрыт полностью). (4) §7.2: `@runtime.platform/arch/target` в позиции значения в любой секции; на mobile имена объявлены со значением `null` → Dropped, неизвестное поле → `template_var_undeclared {name}` + плейсхолдер. (5) §9: C5, N5 закрыты; N13–N15 — ход Dart. `registry/vars.json`: у `urltest_tolerance`, `tun_mtu`, `proxy_in_listen_port` сняты пометки C5. Корпус шаблонов +8: `subst/single_element_array_keeps_array`, `subst/text_list_in_value_position`, `array_element/literal_array_branch_splices`, `array_element/double_brackets_nest`, `array_element/text_list_branch_splices`, `types/int_with_options_stays_int`, `types/options_open_custom_value`, `unresolved/preset_body_declares_all_template_vars`; прежние ожидания не менялись. Встречная задача LxBox — `TASKS_LXBOX.md` §64 (у них задача 555) |
| 1.1.67 | **`core_rejected` при переносе бэкапом снимается; код `group_member_dropped` у выпавшего члена Auto-группы на сборке** (SPEC 142 волна 12, решение владельца 25.09.2026). (1) Запись `{code: "core_rejected"}` — кэш вердикта конкретного ядра на конкретной машине. Экспорт пишет её как есть (вместе с `enabled: false`); импорт запись СНИМАЕТ, `enabled` берёт из файла как есть — узел импорт НЕ включает, вердикт заново вынесет ядро приёмника, когда человек включит узел. Авторитетность записи действует внутри одного состояния: пересчёт санитайзера по телу её по-прежнему не стирает, снимают её только смена тела и включение узла человеком. Исключение «записи как есть» — только узел-группа `kind=auto` (1.1.66), к `core_rejected` не относится; узлы подписки едут картой `disabled{}` без причины, формат не расширяется. Текст отставал с 1.1.10 («читатель стирать не вправе»), код импорта лаунчера уже снимал запись: поправлены описание `warnings` в обоих `$defs` `backup.schema.json`, `BACKUP.md` §2 (строка `warnings[]`), `CANON.md` §9.4. (2) Новый код `group_member_dropped` (warning, params `tag`, `member`) — член Auto-группы не разрешился в узел на СБОРКЕ (ссылки нет, цель выключена или отброшена сборкой), группа живёт без него; запись отчёта сборки, одна на члена. Прежде запись шла текстом без кода. `group_member_missing` (потеря при разборе, на узле-группе с `count`) не подходит: другой этап и другой адресат. Корпус не менялся. Встречная задача LxBox — `TASKS_LXBOX.md` §63 |
| 1.1.66 | **`warnings[]` у узла-группы; `group_member_missing` на самой группе** (SPEC 142 волна 11a, решение владельца 25.09). (1) Бэкап: `warnings` у `kind=auto` разрешены (`backup.schema.json` `$defs/node` и `$defs/sourceServer`: «server и auto»). У группы тела нет, пересчитать коды нечем — читатель кладёт её записи в состояние как есть (исключение из «производные, при переносе не авторитетны» CANON §6 — только для auto); у `server` правило прежнее. BACKUP.md §2 — строка `warnings[]`, CANON §6 — абзац про узел-группу. (2) `group_member_missing` (warning, params `count`) ставится на узел-группу, одна запись на группу; потеря на нескольких шагах разбора (импорт sing-box / резолв балансировщика Xray, затем дорезолв состава тела) складывает число. Прежде код был уровня тела подписки (`FetchWarning.Code`) — снят оттуда, текст с именами потерянных остался в сводке источника без кода. Поле `go` кода обновлено. Корпус +1: `body/singbox/group_member_missing` (selector с необъявленным членом и служебным `direct` → группа с одним членом и кодом); прежние ожидания не менялись. Встречная задача LxBox — `TASKS_LXBOX.md` §62 |
| 1.1.65 | **Коды warnings.json приведены к месту постановки; `listen_port` WireGuard уступает detour сборки; ssh без user — root явно** (SPEC 142 волна 10b). (1) Поле `go` у 13 кодов, которые волна 10a довела до пользователя (`detour_cycle_broken`, `detour_target_missing`, `detour_to_group`, `detour_chain_too_deep` — на узле импорта sing-box; `group_empty` — `dropped[].code`/код тела/отчёт сборки; `max_nodes_exceeded` — `FetchWarning.Code`; `source_detour_missing` — отчёт, одна запись на пару источник→цель; `chain_*` ×6 — `ChainDegradation.Code`/`EmissionWarning.Code`), указывает на фактическое место. (2) `ssh.json` `body.fields.user.default_when {absent, value: root, code: ssh_user_default}` — тело sing-box без `user` получает root явно (ядро и так берёт root, protocol/ssh/outbound.go); тексты кода переписаны под тело, а не ссылку. Identity (тег) не затронута. (3) `body_dialect_unrecognized` снят: промах, который он называл, предотвращает классификация, события нет. (4) Зарегистрирован `direction_filter_matched_nothing` (warning, params `direction`, `filter`, `count`) — корпус `direction/empty_direction_blocks` ставил код, которого в реестре не было. (5) `wireguard.json` `listen_port.conflicts {with: detour}` — код `field_conflict` → `detour_with_listen_port`; detour — managed-поле, его пишет сборка после санитайзера, поэтому связь проверяется по готовому телу сразу после проставления detour (Go — `registry.Registry.YieldsTo`, общий вопрос «какие поля уступают дописанному соседу»): уступает `listen_port`, detour не трогается (fail-closed). Тексты кода переписаны (params `tag`, `target`). Ядро: sing-box-lx protocol/wireguard/endpoint.go NewEndpoint. (6) Новые коды сборки `source_detour_self`, `source_detour_cycle` (error, params `tag`) — detour, назначенный пользователем, указывает на сам узел / замыкается кольцом: узел выпадает fail-closed; прежде — только текст. Корпус +1: `body/singbox/ssh_user_default`; прежние ожидания не менялись. Встречная задача LxBox — `TASKS_LXBOX.md` §61 |
| 1.1.64 | **TLS у masque по решению владельца; `encryption` у Xray-входа vless судится с учётом регистра** (SPEC 142 волна 9, хвосты кампании). Masque (`tls.json` body): `alpn`, `ech`, `kernel_tx`, `kernel_rx` получили `forbidden_for: masque` (у `alpn`/`kernel_*` — `forbidden_codes.masque`), у `reality` код запрета для masque сменён `tls_not_applicable_quic` → `masque_tls_field_ignored` (новый, info, params `path`) — снимаются на ЛЮБОМ `vhttp`; `fragment`, `record_fragment` (и устаревшие плоские синонимы masque) — связь `conflicts {with: vhttp, when: {vhttp: h3}}`, код `masque_tls_fragment_h3` (новый, info, params `path`, `with`): снимаются только при `vhttp: h3`, на `h2`/`auto`/пустом (у ядра = auto) остаются; `server_name`, `disable_sni`, `insecure` не тронуты, `utls`/`certificate`/`min_version`/`cipher_suites` — вне решения. Факты ядра — sing-box-lx `protocol/masque/legacy_options_lx.go` warnUnsupportedTLSOptions, `protocol/masque/outbound.go` resolveVHTTP. Сборка спрашивает то же правило по телу (`registry.Registry.FieldAllowedOn`) и глобальную фрагментацию masque на h3 не дописывает. vless `mappers.xray.params.encryption`: `normalize: trim` + `value_map_case: sensitive` — `None` у Xray-входа больше не снимается молча как «слоя нет», а, как на ссылке и в теле sing-box, отбраковывает узел `vless_encryption_invalid` (ядро сличает литерал `none` точно, `protocol/vless/outbound.go`). gendocs: у связей печатается условие `when`. Корпус +2: `body/singbox/masque_tls_owner_rules`, `body/xray/vless_encryption_none_wrong_case_rejected`; прежние ожидания не менялись. Встречная задача LxBox — `TASKS_LXBOX.md` §60 |
| 1.1.63 | **C5–C10 и пробел движка — данными реестра на всех входах** (SPEC 142 волна 7). Новые примитивы. Тело (`registry_body`): связь набора `relations.kind: ordered` (значения полей по неубыванию; action `drop` — снять ВСЕ участвующие поля, только у `ordered`), атрибут поля `item_forbidden {values, code}` (запрещённое ЗНАЧЕНИЕ элемента списка снимается элементом, после normalize), режим normalize `cidr_masked` (голый адрес → префикс хоста, биты хоста за префиксом обнуляются), атрибут тела протокола `exit_capable_when` (condition: когда узел годится выходом в Направление; без атрибута — всегда). Маппер (`registry_mapper`): источники `context.<путь>` (значение от вызывающего — распаковщика контейнера) и `ref.<as>.<путь>` (сосед по документу), атрибут записи `deref {key, as}` (значение — ссылка на соседа, слой кладётся до `when`), атрибут записи `substitute {sep, join, tokens}` (плейсхолдеры значения из других источников), оператор `when` `type_of`. Норма санитайзера (CANON §6.2): поле, снятое правилом значения или связи, для последующих связей и условий отсутствует, как снятое запретом схемы; `requires` к обязательному полю своего объекта, снятому своим правилом, снимает зависимое без второго кода; `requires … set` к снятому соседу его материализует. Данные: wireguard `relations` ordered jmin/jmax (`fields_order_invalid`, новый, warning); tailscale `advertise_routes` normalize `cidr_masked` + `item_forbidden` 0.0.0.0/0, ::/0 (`tailscale_default_route_advertised`, новый, warning), тело `exit_capable_when {any_set: [exit_node]}`; dialer.json блок xray `fragment_via_dialer` (deref dialerProxy → freedom с fragment → tls.fragment); wireguard `conf` `mtu_container` (`context.container.mtu`) и `dns.substitute` ($PRIMARY_DNS/$SECONDARY_DNS из `context.profile`); признак контейнера Amnezia — detect вида `amnezia_link` по имени распаковщика (литерала `vpn://` в Go нет). Корпус +4: `body/singbox/endpoints_awg_junk_size_order`, `endpoints_awg_masquerade_removed_cascade`, `tailscale_advertise_routes`, `body/xray/dialer_chain_hop_freedom_fragment`; прежние ожидания не менялись. Встречная задача LxBox — `TASKS_LXBOX.md` §59 |
| 1.1.62 | **Ссылки реестра на код приведены к правде и проверяются линтером** (SPEC 142 волна 8, находки D1–D4). Данные реестра (правила, значения, коды, тексты кодов) не менялись — только заметки разработчикам: `refs.go`, `impl` (в т. ч. `mapper[].impl`), `go` у кодов `warnings.json`, `note` и указатели в описаниях. 141 запись `refs.go` указывала на удалённые рукописные парсеры/эмиттеры (`node_parser_<схема>.go`, `shareuri_*.go`, `singbox_sanitize.go`, `outbound_tls_emit.go`) или на строки за концом файла; осталось 53, все в форме `путь.go[:Имя]`. У протоколов, которые целиком исполняет движок, `refs.go` снят (правило — данные файла); остался код, специфичный для схемы. ~335 строк прозы и `go` переведены на актуальное место исполнения (движок linkmap/nodeflow или атрибут реестра), проза, утверждавшая неправду о поведении Go, поправлена по факту. Формат ссылки закреплён (абзац выше «Кто что исполняет»): путь от корня репозитория, имя через `:`, без номеров строк и голых имён файлов; апстрим — путём своего репо, при совпадении каталога с префиксом (`3x-ui:`). Линтер `TestRegistryCodeRefsResolve` (`core/config/registry_refs_test.go`, джоба Contract, триггер — любой `.go`) проверяет все строки реестра, кроме `dart`. Корпус и identity не менялись. |
| 1.1.61 | **REALITY ↔ uTLS — одно правило реестра на всех входах** (SPEC 142 волна 6, находки C4 и B4). Новые примитивы (схема `registry_body`): `requires[].set` — требуемого соседа нет, поле НЕ снимается, сосед материализуется значением `set` с кодом связи; `coerce_when {values, value, when, code}` у поля — условная замена ГОДНОГО значения; оба судятся по ГОТОВОМУ телу после обхода (поле/сосед, снятые своими правилами, ничего не вызывают), коды встают на место по `body.order`. `on_hop_required {action: unstrip, code}` — у ключа каталога `strip` цепочки. (1) `tls.reality.enabled` requires `tls.utls.enabled` с `set: true`, код `reality_utls_enabled` (info): REALITY без uTLS больше не снимается (было `field_requires`), uTLS дописывается `{enabled: true}` — ядро без него REALITY не поднимает (reality_client.go:61). (2) `tls.utls.fingerprint.coerce_when`: `random` при `tls.reality.enabled` → `chrome`, код `reality_fp_random_pinned` (info) — ядро разворачивает random один раз при старте в chrome/firefox/edge/safari/ios, у edge/ios нет X25519MLKEM768. Неявный random D-009 от явного в теле неотличим — код у обоих; узел с негодным pbk (REALITY снят) остаётся с random. (3) `chain.json` strip `tls.utls`: `on_hop_required {unstrip, chain_strip_utls_on_reality}` — ключ снимается с патча цепочки, если тело звена на позиции ≥ 1 его требует (санитайзер вернул бы путь связью с `set`); цепочка собирается, код severity warning (было error и исключение цепочки). Сборочная копия (Go `HealRealityFingerprints`/`EnforceRealityFingerprint`, Dart `heal_unknown_utls_fingerprints`) и рукописный конфликт цепочки (Go `ChainRealityConflict`) сняты; тела вне санитайзера (замороженное тело состояния, ручной config_json) получают только правила-починки. Корпус: 17 uri/vless-кейсов REALITY без fp (random → chrome + код; у `flow_vision_xhttp_suppressed` — и override `.expected.lxbox.json`) и `alpn_multiply_encoded` (явный fp=random), новые `body/singbox/reality_without_utls`, `reality_fp_random`. Встречная задача LxBox — `TASKS_LXBOX.md` §57 |
| 1.1.60 | **Узловой гейт ядра и уровень протокола — данными реестра** (SPEC 142 волна 5, находка C3; хвосты B5/B8). Новые атрибуты (схема `registry_body`): `on_core_unsupported {action: drop_node, code}` — у тела протокола, у поля и в `range_form`; `range_form {min_core, build_tag, level, on_core_unsupported}` — требования формы-диапазона `N-M` у типа `awg_range`; `levels` (тело протокола, по возрастанию), `level`, `level_mark` (поле). (1) **Узловой гейт.** Узел снимается на сборке, когда требование с `on_core_unsupported` не выполнено ядром (его теги сборки из `sing-box version` + версия): у тела протокола — `build_tag`/`min_core` секции; у заданного поля — его `build_tag`/`min_core`; у значения-диапазона — `range_form`. Данные: naive (`with_naive_outbound`, код `naive_unavailable`; purego-сборка без libcronet = тега нет), tailscale (`with_tailscale`, `tailscale_core_unsupported`), поля AmneziaWG 3.x (`header_protection_key`, `content_padding_addition`, `rekey_after_time`, `rekey_timeout`, `reject_after_time`, `keepalive_timeout`, `max_handshake_attempts`, `random_trailers`, `disable_cookies` — `with_awg` + `1.14.0-lx.32`, `awg3_core_unsupported`) и диапазонный `peers[].persistent_keepalive_interval` (`range_form`, те же требование и код). Прежде — три пробы по имени протокола и рукописный список полей `awg3.go` (`HasAWG3Fields`). Остальные схемы с `build_tag`/`min_core` тела (wireguard, quic-протоколы, masque, chain) гейтом не охвачены: атрибут не объявлен. (2) **Уровень.** `wireguard` несёт `levels: [awg, awg1.5, awg2, awg3, awg3.1]`; `level` у jc/jmin/jmax/s1/s2/h1–h4 (`awg`), i1–i5 (`awg1.5`), s3/s4 (`awg2`), полей 3.x (`awg3`), random_trailers/disable_cookies (`awg3.1`); `range_form.level: awg2` у h1–h4, `awg3` у keepalive; ip/id/ib — `awg1.5` + `level_mark: "+"`. Подпись = старший уровень + суффиксы — прежде рукописная функция UI. (3) Снятие AmneziaWG формой: поля с `build_tag: with_awg` удаляются, диапазон с `range_form.build_tag: with_awg` схлопывается в нижнюю границу (прежде — рукописная `clearRangedKeepalive`). Коды — существующие. Корпус не менялся (гейт живёт на сборке). Встречная задача LxBox — `TASKS_LXBOX.md` §56 |
| 1.1.59 | **Роль поля `role`: учётные данные узла и приватный ключ share-ссылки — данными реестра** (SPEC 142 волна 4, находки C2 и C6). Новый атрибут поля тела `role` (схема `registry_body`, `field.role`, enum `credential` \| `private_key`): ставится только у поля верхнего уровня тела протокола, каждая роль — не больше одного поля на схему (линтер реестра). (1) **`credential`** — учётные данные узла, поле учётной записи в слоте userinfo ссылки: `uuid` у vless/vmess/tuic, `password` у trojan/hysteria2/anytls/ss, `auth_str` у hysteria, `user` у ssh, `username` у naive/socks/http; у wireguard/masque/tailscale роли нет. Шифр ss/vmess учётными данными не считается. Значение читается из готового тела одинаково на всех входах — прежде его выбирали три рукописные копии по имени схемы (выемка входа sing-box/Xray, обратный путь тела состояния, «первый компонент userinfo» у ссылки) с разным итогом: ссылка ssh/naive/socks/http давала имя пользователя, JSON — пусто; ссылка ss — шифр, JSON — пароль; ссылка wireguard/masque — приватный ключ, JSON — пусто. (2) **`private_key`** — приватный ключ, который уезжает в share-ссылку (выдача ссылки только после подтверждения): `private_key` у ssh, wireguard, masque. Прежде — таблица по типу в коде (`share_uri_secret.go`). Состав тот же. Кодов нет, корпус и identity не меняются. Встречная задача LxBox — `TASKS_LXBOX.md` §55 |
| 1.1.58 | **Каталог `strip` цепочки, связи AmneziaWG и Tailscale — данными реестра; формы и сборка читают реестр** (SPEC 142 волна 3, находки B2–B10). Новых примитивов нет — только данные. (1) **`chain.json` `body.strip`** получил `order`+`fields`: закрытый каталог ядра (`protocol/chain/transform.go` `stripCatalog`) — `tls.fragment`, `multiplex.padding`, `xhttp.padding` (bool, `default: true`) и `tls.utls` (`default: false`); `default` = снимается ли ключ при включённом `strip_evasion` (санитайзер его не материализует). Неизвестный ключ `strip` теперь снимается общим `unknown_key` — раньше доезжал до ядра, а оно отвергает весь конфиг. Имена полей каталога содержат точку (ключи ядра), путь `strip.tls.utls` — не вложенность. (2) **wireguard `body.ip`**: `requires: [{path: id, when: {ip: quic}, code: field_requires}]` — `ip=quic` без домена ядро отвергает («id is required for ip=quic»); `dns`/`sip`/`stun` id не требуют (impl `id` поправлен: прежде говорил «обязателен при quic\|dns\|sip»). (3) **wireguard `body.id`**: `pattern` строгого LDH ядра (`validateMasqueDomain`: метки `[A-Za-z0-9_-]` до 63 байт, дефис не с краю, допустима завершающая точка) — прежний `format: host` пропускал `@`, `;`, не-ASCII, на которых ядро роняет весь конфиг. (4) **tailscale `body.exit_node_allow_lan_access`**: `requires: [{path: exit_node, code: field_requires}]` — ядро применяет его только при заданном `exit_node`. Коды — существующие (`field_requires`, `awg3_field_invalid`, `unknown_key`). Go: сборка (`core/build/tls_transforms.go`) решает, к каким схемам применять глобальную фрагментацию TLS, по `tls.fragment` `allowed_for/forbidden_for` реестра, а не своим списком (masque теперь получает фрагментацию как все: ядро исполняет её на h2-ноге и лишь предупреждает на `vhttp: h3`); формы AWG, WireGuard, Tailscale и WARP берут enum/дефолты/границы из реестра и проверяют ввод пробным санитайзером. Кейсов корпуса не добавлено, существующие ожидания не менялись. Встречная задача LxBox — `TASKS_LXBOX.md` §54 |
| 1.1.57 | **Новый примитив `on_invalid: unwrap`; рукописный санитайзер импорта sing-box снят целиком** (SPEC 142 волна 2, находки B1 и C1). Последние два правила `SanitizeSingboxOutboundMap` (`core/config/subscription/singbox_sanitize.go`, файл удалён) переехали в реестр и работают на ВСЕХ входах тела — импорт sing-box, ручной JSON, бэкап/state через `materializeBody`, — а не только на импорте, и с кодом, а не молча. (1) **`on_invalid.action = unwrap`** (схема `registry_body`, `onInvalid`): значение пришло ОБЁРТКОЙ соседнего диалекта — объектом там, где поле ждёт скаляр. Объект, чей член `key` приводится к типу поля, непуст (строка из одних пробелов — пуста) и проходит ограничения поля, заменяется этим членом с кодом `code`; объект без годного члена — поле снято с кодом `else_code` (по умолчанию `type_invalid`; параметры кода — скалярные члены объекта, кроме самого `key`); не объект — поле снято с `type_invalid`. Имён схем в движке нет. Первый потребитель — `hysteria.body.obfs`: `{action: unwrap, key: password, code: obfs_object_flattened, else_code: obfs_password_missing}` — объект `{type, password}` от hysteria2 в узле v1 становится строкой-паролем. Новый код **`obfs_object_flattened`** (info, `params: path`). (2) **masque: плоские `network`/`sni`/`skip_cert_verify`** убраны из `body.fields`/`order` и перечислены в `body.skipped` с причиной; снимает их общий `unknown_key` (значения не переносятся — 0.8.0, D-078). Раньше поля были объявлены `deprecated`, атрибут движок не исполняет, и на ручном JSON и бэкапе ключи доезжали до тела (плоский `sni` рядом с `tls.server_name` — fail-fast ядра на весь конфиг). Описательный `uri.query.sni/insecure.maps_to` masque поправлен на `tls.server_name`/`tls.insecure`. Кейсы корпуса (3): `body/singbox/hysteria_obfs_object_flattened`, `hysteria_obfs_object_no_password` (оба `extension: desktop`), `masque_legacy_flat_keys`. Существующие ожидания не менялись. Встречная задача LxBox — `TASKS_LXBOX.md` §53 |
| 1.1.56 | **xhttp `uplink_data_placement` ↔ `mode` только для header/cookie на всех входах; ss `plugin_opts` ↔ `plugin` в теле** (`TASKS_LXBOX.md` §52, LxBox §546/§547). Ядро отвергает вне packet-up только `header`/`cookie` (transport/v2rayxhttp/meta.go:115-117), `body`/`auto` законны в любом режиме — прежняя запись маппера дописывала packet-up и им и снимала их при stream-one с ложным `xhttp_param_reset`. (1) Тело, `transports.json` xhttp: `mode.default_when {absent, value: packet-up, code: xhttp_mode_forced_packet_up, when: {transport.uplink_data_placement: {in: [header, cookie]}}}`; `uplink_data_placement.requires [{path: transport.mode, equals: packet-up, when: {transport.uplink_data_placement: {in: [header, cookie]}}, code: xhttp_param_reset}]`. (2) Мапперы uri/xray: запись `uplinkDataPlacement` получает селектор `$value: {in: [header, cookie]}`, новая `uplinkDataPlacementOther` (`$value: {not_in: […]}`, `round_trip_only: parse`) пишет остальное как есть. (3) `shadowsocks.json` body: `plugin_opts.requires [{path: plugin, code: field_requires}]`. Новые примитивы: `condition` тела несёт предикаты по значению путей в грамматике `when` маппера (скаляр / `in` / `not_in`, И между собой и с any_set/source_kind); `relation.when` (`requires`/`conflicts` действуют по условию); `$value` в `when` маппера — селектор записи, промах = молчаливый пропуск без `on_when_false`. Нормы санитайзера (общие, без имён схем): связи и условия видят поля объекта, обход которого ещё идёт (материализованный `default_when` внутри `transport` виден соседу); пустая строка у обычного поля и литерал-выключатель (`absent_values`) для `default_when` равны отсутствию ключа, материализация снимает пометку; пустая строка не выполняет `any_set` и предикаты, число 0 — значение. Identity не меняется. Кейсы корпуса (9): `body/singbox/vless_xhttp_uplink_{header_no_mode_adds_packet_up, header_stream_one_reset, header_packet_up_kept, body_stream_one_kept, auto_no_mode_kept}`, `body/singbox/shadowsocks_plugin_opts_{without_plugin_dropped, with_plugin_kept}`, `uri/vless/xhttp_uplink_body_stream_one_kept`, `body/xray/vless_xhttp_uplink_body_stream_one_kept`. |
| 1.1.55 | **VLESS: Vision с VLESS Encryption не снимается из-за транспорта** (`TASKS_LXBOX.md` §51, LxBox §544). Новое общее поле связи `relation.unless_set` (schema/registry_body): связь не действует, если задан любой из путей; «задан» судится тем же предикатом, что сосед связи (непустое, не снятое схемой, не литерал-выключатель `absent_values`). Литерал-выключатель теперь невидим связям и из исходного тела (предварительный проход). `vless.flow.conflicts[transport]` несёт `unless_set: [encryption]`: с encryption flow остаётся, кода `vision_with_transport` нет; без encryption и при точном `none` — как раньше. Identity не меняется. Узлу нужно ядро с фиксом Vision+encryption (sing-box-lx#29). Кейсы корпуса (4): `uri/vless/flow_vision_xhttp_encryption_kept`, `uri/vless/flow_vision_xhttp_encryption_none_suppressed`, `body/xray/vless_vision_xhttp_encryption`, `body/singbox/vless_vision_transport_encryption_kept`. |
| 1.1.54 | **hysteria2: ссылки 3x-ui с gecko читаются без потерь** (`TASKS_LXBOX.md` §50). `minPacketSize`/`maxPacketSize` — алиасы `obfs-min-packet-size`/`obfs-max-packet-size` (написание v2rayN, 3x-ui пишет пару при `obfs=gecko`); канон эмита, тело и identity не меняются, `requires … equals: gecko` срабатывает на обоих написаниях. `security` заведён ТОЛЬКО у hysteria2: `tls`/пусто — молча, иное — узел жив, `uri_param_unknown` (в 1.1.53 п.(7) «для всех схем» не заведён осознанно). Раньше размеры из панели терялись молча, ядро брало дефолт 512. Кейсы корпуса (3): `uri/hysteria2/3xui_gecko_camelcase`, `security_non_tls_warn`, `salamander_camelcase_sizes_dropped`. |
| 1.1.53 | **Ревизия зеркала LxBox: снятие 27 оверлеев, вид share-ссылок у трёх схем, 13 секций `mappers.singbox`, кандидаты из 72 публичных подписок** (SPEC 133; основание — `REVIEW_LXBOX_MIRROR_2026-09-24.md`, решения владельца «всё по рекомендации»). Зеркало совпадало байт в байт, а расхождения жили РЯДОМ: 27 файлов `contract_draft/**` плюс семантика двух примитивов Dart-движка, которых не видели ни синк, ни lock-проверка. Дельты — D133-E10, D133-E11, D133-E12; D133-E1 и D133-24 переписаны, D133-C8 обзавёлся кейсом. (1) **ВИД ССЫЛКИ у трёх схем.** `security=reality` у vless теперь ПИШЕТСЯ (D133-E10, прав LxBox): исключение `omit_default: security:reality` отменяло у reality-ветки ровно то, ради чего заведена вся D133-E1 — у Xray-клиентов отсутствие `security` означает `none`, и узел без параметра прочтётся БЕЗ TLS; `pbk` объявляет РАЗНОВИДНОСТЬ TLS, а не его наличие. `ss` пишет userinfo base64url БЕЗ `=`-паддинга (D133-E11, прав LxBox — эталон SIP002; чтение обеих форм у обеих сторон остаётся, percent-декод идёт ДО base64). Контейнер v2rayN получил `param_order` ПЕРЕЧНЕМ вместо алфавита и `aid` СТРОКОЙ `"0"` вместо числа (D133-E12, взято из оверлея LxBox): контейнер читается частью клиентов ПОЗИЦИОННО и по ТИПУ, алфавит переставлял ключи живых ссылок (`host`/`path` уезжали вперёд `add`); `ps`/`scy` остаются в `json_always`, `json_map` сохраняется. 47 кейсов снимка рукописного эмита объявлены отличиями в `emit_deltas_allowed.json` — снимок заморожен и не перегенерируется. (2) **ЗАКРЫТА ДЫРА ЧТЕНИЯ sockopt.** Отрицательный `tcpKeepAliveInterval` у Xray означает «keep-alive выключить», но знак интервала гейтился только `gt: 0`: узел, выключивший keep-alive ОДНИМ интервалом, приезжал без `disable_tcp_keep_alive` — намерение автора терялось МОЛЧА. Новая запись гейтится ВТОРЫМ условием по уже построенному телу, иначе пара `idle: 30 + interval: -5` получала бы взаимоисключающие поля сразу (кейсы `sockopt_keepalive_negative_interval_only` и `..._negative_interval`). (3) **13 секций `mappers.singbox`** забраны механически по норме MAPPER_ENGINE §8a (были только trojan и vless). **Исправлен дефект черновика**: у форм `endpoint` (wireguard, tailscale) предикат назывался `has_key`, которого в грамматике НЕТ — движок читает только `required_keys` (`linkmap/detect.go:276`), то есть форма не срабатывала НИКОГДА и каждый элемент-endpoint падал в форму `outbound`. (4) **Булев без `emit_as` — СЛОВОМ** (PRIMITIVES §0.12a, FROZEN): Go писал словом, Dart цифрой, и расхождение компенсировалось оверлеем `emit_as: raw` у трёх полей xhttp. Гард — `TestEmitOverlays/bool_default_spelled`, судит именно УМОЛЧАНИЕ. (5) **`disable_sni` ⇒ `server_name` отсутствует** (находка задачи 532 LxBox): гейт стоял только у записи схемы tuic, а блочная `tls#uri.sni` писала имя из присутствующего `query.sni` и записи схемы не изымала (§7.1: запись под `when` переопределением не считается) — узел уезжал с ОБОИМИ полями. Семантически прав LxBox. Аналогов у hysteria2/anytls нет: `disable_sni` объявляют только tuic и masque, а masque блок `tls#uri` не подключает вовсе. (6) **anytls `sni` получил `on_invalid: default_from`** — правило `sni_heuristic_falls_back_to_server` перечисляло anytls в `applies_to`, а секция его не несла (реестр противоречил сам себе). ⚠ Попутно найдено: действие `default_from` движком НЕ ИСПОЛНЯЕТСЯ вовсе — ни у anytls, ни у hysteria2, который несёт правило с 1.1.16 (`applyMissing` судит только пустое/отсутствующее значение); кейс закрепляет сегодняшнее поведение, остаток — TASKS §«Что НЕ сделано» п.6. (7) **Прогон 72 публичных подписок** (§525, 73 938 узлов, 9300 `uri_param_unknown`): ПРОЧИТАНЫ `servername`/`serverName` (170 узлов — третье звено цепочки `sni`, после `sni` и `peer`) и `packet-encoding` (101, везде `xudp` — второе звено записи `packetEncoding`); ТИХО ИГНОРИРУЮТСЯ без кода `authority` (498 — gRPC-параметр Xray, у sing-box поля НЕТ вовсе, лосс по дизайну) и кэш-бастеры `_t`/`__rand`/`seq`/`nonce` (678). Осознанно НЕ заведены: `security` (читают все схемы, кроме «ss»-узлов, а 303 из 368 ss-строк с query — это `amp;`-мусор, искалеченный vless), `skip-cert-verify`/`vcn` (в query корпуса НОЛЬ вхождений — это Clash-YAML-имена), `amp;<имя>` (4706 — дефект чужого списка, код здесь единственный сигнал), мусор-метки (`descriptions`, `fm`, `cs`/`pcs` — непонятны, а не безвредны), `ssr` (остаётся `scheme_unsupported`). (8) **Прочее**: D133-24 переведена из PER-APP в ПРИНЯТО (оверлея `conf/` у LxBox нет с `538dceb5`); D133-C8 обзавёлся кейсом `reality_key_share_not_carried` БЕЗ смены поведения (общая дыра обеих сторон, решение за владельцем); у masque `idle_timeout` исправлен ТЕКСТ описания — было «пусто = дефолт ядра (5m)», а простой MASQUE по умолчанию ВЫКЛЮЧЕН (сверено с ядром `origin/lx` 7de943642, верно для lx.12 и lx.13; глобальный дефолт появится в lx.13 как `lx.masque.idle_timeout`, ключ узла сильнее), семантика поля не менялась. Кейсы корпуса (10): `uri/vless/xhttp_bool_default_spelled`, `servername_alias_for_sni`, `packet_encoding_dashed_alias`, `ignored_params_silent`, `uri/anytls/sni_label_falls_back_to_server`, `uri/tuic/disable_sni_with_sni`, `uri/http/proxy_https_security_none_drops_tls`, `body/xray/sockopt_keepalive_negative_interval_only`, `hysteria_version_3_unrecognized`, `trojan_without_stream_settings`, `reality_key_share_not_carried`. Встречная задача LxBox — `TASKS_LXBOX.md` §49 |
| 1.1.52 | **Баннеры-обманки панелей, `socks://base64(user:pass)`, overlay `extra` у vmess, полоса `up`/`down` у hysteria2, обфускация TCP-заголовком → отбраковка и неизвестный ключ ВНУТРИ контейнеров** (SPEC 133; дефекты двух аудитов 24.09.2026 — генераторы четырёх панелей и outbound-параметры Xray-core; номер 1.1.51 занят параллельной волной §47). Класс дефектов этой волны один: узел либо уезжал «рабочим» и не соединялся, либо терял свойство МОЛЧА. (1) **Баннер провайдера, притворившийся ССЫЛКОЙ** (D133-55). Признаком баннера было ОТСУТСТВИЕ `://` (`isProviderBannerLine`), а обе крупные панели пишут его синтаксически валидной ссылкой — и он становился полноценным узлом-пустышкой: Remnawave отдаёт `vless://…@0.0.0.0:1#⚠ Subscription expired` с нулевым uuid (`createFallbackHosts`), 3x-ui — `socks://127.0.0.1:1080#<remark>` (`internal/sub/service.go:483`), причём при expired/depleted ЕДИНСТВЕННОЙ записью тела. Пользователь с истёкшей подпиской получал «рабочую» подписку из одного сервера `0.0.0.0:1` вместо сообщения о том, что подписка истекла. Норма: запись с целью из списка «заведомо не сервер» — баннер, а не узел; список объявлен ДАННЫМИ (`source_kinds.json`, `uri_lines.banner_targets`: `0.0.0.0`, `127.0.0.1`, `::`, `::1`, `localhost`), чтобы эвристика не размазывалась по разборщику. Судится ТОЛЬКО адрес: баннером бывает и `vless`, и `socks`, а порт признаком не является — у 3x-ui он законный `1080`. Проверка ПОСЛЕ разбора: до чтения цели такая ссылка от годной ничем не отличается. Код `provider_banner_link` (severity **info**: узла тут не было никогда, но выпадение обязано быть названным), ремарка после `#` едет в причине СООБЩЕНИЕМ ПРОВАЙДЕРА (`message_from: fragment`) — она и есть единственное содержимое баннера. (2) **`socks://base64(user:pass)` — пароль перестал теряться** (D133-56). v2rayN пишет socks-ссылку так ВСЕГДА (`SocksFmt.ToUri`), а реестр резал строку по `:`, которого в ней нет: `username` становился всей base64-строкой, пароль исчезал МОЛЧА. Конвейер `["percent","base64?"]` — как у `shadowsocks`, — плюс новый закрытый атрибут `decode_requires_separator`, работающий В ОБЕ СТОРОНЫ: разделитель есть во входе → форма открытая и конвейер не применяется; разделителя нет → декодер пробуется, но результат принимается лишь когда разделитель в нём ПОЯВИЛСЯ. Обе половины обязательны — без второй try-decode рушит открытое ОДИНОЧНОЕ имя: `socks4://useridonly@host` есть законный userid socks4 (пароля у версии 4 нет по протоколу), но `useridonly` проходит RawStdEncoding и уезжает семью байтами мусора (проверено прогоном: ломался кейс `socks/socks4_userid`). Ровно этой парой условий читает и сам v2rayN. Пароль с `:` ВНУТРИ доезжает целиком (`limit: 2` режет по первому двоеточию уже раскодированной строки). (3) **vmess: overlay `extra` ОБЪЕКТОМ внутри контейнера** (D133-57). Слой был объявлен только у vless, и у vmess xhttp-параметры терялись молча, хотя ТОТ ЖЕ вход у vless разбирался полностью. Причина двойная: секция слоя не объявляла, и значение до слоя не доехало бы — Marzban кладёт `extra` ВЛОЖЕННЫМ ОБЪЕКТОМ (`payload["extra"] = extra`, `v2ray.py:249`), а сборка слоя читала источник через `jsonScalar`, который на объекте молчит НАМЕРЕННО (§4). Теперь `source: ["json.extra","query.extra"]` — две формы одной схемы, спора между ними нет (у формы v2rayN нет query, у legacy нет json); декодеры применяются только к ТЕКСТОВОЙ форме. Плоское написание 3x-ui отдельного объявления не требует: панель разливает те же ключи в корень vmess-JSON (`applyXhttpExtraObj`), а ключи корня видны общим плоским слоем. Обратный ход объявлен ПРОБЕЛОМ, а не асимметрией: форма-контейнер собирается закрытой картой `emit.json_map`, ключа `extra` в ней нет, и пробел зафиксирован поимённо предикатом `xhttpExtraNotEmittedByContainer` (пара к `dialerKeepAliveNotEmitted`) — `extra` в эмите есть НОВЫЙ примитив формы контейнера, то есть смена отдаваемой людям ссылки, а она по правилу 5 делается волной эмита. (4) **Полоса hysteria2 `up`/`down`** (асимметрия с hysteria v1, D-8 аудита панелей): у v1 короткие написания читались с самого перевода на движок, у v2 не читались вовсе — `up=100&down=500` давало ДВА `uri_param_unknown` и полосу теряло молча. Добавлены алиасы и `normalize: bandwidth_mbps` (тот же примитив, что у xray-ветки файла с 1.1.48): суффикс единицы (`100mbps`, `300 Mbps`) снимается, иначе `type: int` на такой строке даёт отсутствие значения. Канон `upmbps` стоит первым и побеждает при двух написаниях рядом. (5) **Обфускация TCP-заголовком → ОТБРАКОВКА УЗЛА** (D133-58, РЕШЕНИЕ ВЛАДЕЛЬЦА 24.09.2026, делегировано; **меняет тела живых узлов** — тех, что и так не работали). Xray маскирует plain-TCP под HTTP: в первом пакете едет поддельный HTTP-запрос, транспорт остаётся TCP. Ссылочная форма (`type=tcp|raw` + `headerType=http`) маппилась в `transport.type: "http"` — НЕВЕРНЫЙ маппинг, а не приблизительный: у sing-box `http` есть транспорт HTTP/2, на проводе другой протокол, и сервер, ждущий камуфляж, обрывал h2-рукопожатие; JSON-форма (`tcpSettings.header.type`/`rawSettings.header.type`) терялась МОЛЧА — узел собирался чистым TCP. Обе формы теперь дают `drop_node` с кодом `transport_header_unsupported` (severity **error**, значение в `value`): собрать узел без обфускации можно, но он заведомо мёртв, и предлагать его человеку значит прятать отказ. `none` и пусто = отсутствие камуфляжа, кода не дают. Два написания в `source` (`tcpSettings` до переименования Xray, `rawSettings` после, апстрим #3994); гейт xray-записи по ИСТОЧНИКУ (`json.streamSettings.network`), потому что у tcp/raw транспорта в теле нет вовсе. Текст причины у кода СВОЙ: «транспорт http ядром не поддержан» было бы неправдой — транспорт `http` у ядра есть, не поддержана именно подделка заголовка. Переписаны ожидания, несшие `transport.type:"http"` от `headerType=http`; у `uri/vless/userinfo_raw_space` предмет ДРУГОЙ (пробел в userinfo), поэтому ссылка переведена на `ws` — иначе кейс проверял бы отбраковку вместо того, ради чего написан (тот же перевод у Go-теста `TestParseNodeAcceptsSpaceInUserinfo`); `uri/vless/http_transport_host_list` НЕ тронут — там `type=http` прямым текстом, настоящий H2. (6) **Неизвестный ключ ВНУТРИ объявленных контейнеров** (D133-59; тела узлов НЕ меняются, добавляются только info-коды). `json_field_unknown` судил лишь верхний уровень элемента, а контейнеры стоят в его `ignore` — и всё, что лежало внутри них и не названо ни одним `source`, терялось АБСОЛЮТНО МОЛЧА (аудит Xray-outbound, §0): человек не узнавал, что часть его конфига не прочитана. Теперь такой ключ даёт ТОТ ЖЕ info-код с ПОЛНЫМ путём (`streamSettings.xhttpSettings.foo`), без отбраковки — непрочитанный лист узел не ломает. Роль контейнеров стала ДВОЙНОЙ: наверху молчат, внутрь идёт обход; молчание внутри объявляется путями `source` плюс тремя правилами — объявленный путь (включая `maps_to: null`), путь-РОДИТЕЛЬ (запись, читающая объект целиком, читает и его листья, имена которых принадлежат подписке) и новый закрытый атрибут `nested_quiet` (поддеревья, молчащие целиком при частичном объявлении: сегодня одно на все девять xray-секций — `streamSettings.sockopt`, у которого полей десятки и все серверные). Листом считается скаляр либо пустой объект/массив; индекс массива входит в путь числом, как его адресует `source`; глубина ограничена 12 сегментами. Объявленность считается НА ИСПОЛНЕНИИ, а не в плане: якорь `$base` известен только там. Норма вскрыла в корпусе 11 ожиданий (диф — только добавленные коды): `wsSettings.ed`/`eh` (xray-блок `ws` их не читает вовсе — реестр обещал чтение прозой, а `source` нет: `ws_ed_flat_only` терял `ed=1500` молча), `realitySettings.keyShare` (реестр сам пишет «Xray-JSON аналога нет»), нечитаемые члены `xhttpSettings.extra.*`, `streamSettings.network` у секций `http`/`hysteria2`, которые `transports#xray` не включают. Плюс: сноска `impl` у ссылочной записи `spx` (в 1.1.50 её получила ТОЛЬКО xray-запись `spiderX`, и эта половина нормы осталась голым `null` — следующий читатель не мог отличить решение от пропуска). Кейсы корпуса (9): `body/uri_list/provider_banner_link_dropped`, `provider_banner_link_only_entry`, `uri/socks/socks5_base64_userinfo`, `socks5_base64_userinfo_colon_password`, `uri/vmess/xhttp_extra_object_xmux`, `uri/hysteria2/up_down_short_aliases_suffix`, `body/xray/raw_header_http_dropped`, `raw_header_none_kept`, `nested_unknown_key_noted`. Схема расширена тремя закрытыми атрибутами (`banner_targets`, `decode_requires_separator`, `nested_quiet`). Встречная задача LxBox — `TASKS_LXBOX.md` §48 |
| 1.1.51 | **Ответ LxBox от 24.09.2026 на Q133-69…75, §39, §42, §45 — учёт и проза; тела и вид ссылок не меняются, снят один мёртвый код** (SPEC 133; номер 1.1.50 занят параллельной волной §46). (1) **`ws.ed`/`ws.eh` — проза выхода и D-008**: строка зеркала 1.1.46 «Dart возвращает пару `ed+eh` в query» снята ещё в 1.1.49; в 1.1.51 переписан остаток у `transports.ws.params.ed` («Dart добавляет дефолт, сейчас эмитит только max_early_data» противоречил сам себе): D-008 — про ВХОД, хвост пути подразумевает имя заголовка у обеих сторон, плоская `ed=N` без `eh` — только у лаунчера (`implies` у `blocks.uri.ws.ed`), per-app кейс `uri/trojan/ws_ed_flat_no_eh`; у `eh` — выход по ответу LxBox (пару в query в штатном случае не пишет ни одна сторона). `docs/IDENTITY.md` §3 уточнён. (2) **Учёт Q133-70…75**: 70, 71, 72, 73, 75 закрыты задачей LxBox 495; **Q133-74 (`keep_empty_tail` у socks4) ею НЕ закрыт** — решение владельца 24.09.2026: обе стороны исполняют флаг с 1.1.50 (у лаунчера исполняется с 1.1.36); именной круг `TestEmitOverlays/socks4_keep_empty_tail`, `PRIMITIVES.md` §0.12b, `QUIRKS.md` Q133-74. (3) Ответы: `clash_yaml` — вида источника в `source_kinds.json` нет (снят в §24.27), Clash YAML уходит в `uri_lines`; **код `clash_yaml_unsupported` снят из `warnings.json` как мёртвый** (не ставила ни одна сторона, `go`/`dart: null`; `diagrams/body_classify.mmd` без ветки Clash), детект Clash YAML — отдельной фичей по спросу; `unwrap_failed` — не код `warnings.json`, а внутреннее поле `SourceResult.UnwrapFailed`, имени в норме нет; §45.1 — LxBox снимает ini-предикат `=` с кода при синке 1.1.49; Q133-69 — граница «сборка документа вне движка» (`xray_json_array.go`/`xray_balancer.go`, `SCHEMES.md` §13.5) подтверждена обеими сторонами, остаток классификатора (`body_classify.go` / `_classifyLegacy`) снимается отдельной дельтой. (4) **Предел длины ссылки** — дополнение к D133-52 (1.1.50): значение `max_uri_length` в `limits.json` уже 65536 (разбор подписки читает его из реестра с SPEC 131 W2c, `current.go`/`impl` поправлены в 1.1.50); константа конфигуратора `MaxURILength = 8192` (`ui/configurator/utils/constants.go`) снята — форма прямой ссылки (`ValidateURI`) и разбор подписки проверяют длину одной функцией `subscription.CheckURILength` по реестру, отказ — `uri_too_long{length, limit}`; страж `TestValidateURILengthFromRegistry`. Ждёт владельца: сведение плоской `ed=N` (`implies`); Q133-73 (`_isUntranslatedCanon` у LxBox) и Q133-74 решены в 1.1.50 (§46). Встречная задача LxBox — `TASKS_LXBOX.md` §47 |
| 1.1.50 | **Диалект Xray: три потерянных протокола, mux, транспорт без пары у ядра и `alpn` с общего блока** (SPEC 133; аудит outbound-параметров Xray-core, панелей и `maps_to` против ядра — три отчёта 23–24.09.2026). Дефекты этой волны объединяет не поле, а КЛАСС: узел либо терялся целиком, либо уезжал «рабочим» и не соединялся. (1) **`mappers.xray` у `wireguard`, `socks` и `http`** — секций не было вовсе, и элемент не опознавала ни одна из них: узел пропадал с диагнозом «протокол не поддержан», хотя ВСЕ целевые поля у ядра есть (сверено по json-тегам `option/wireguard.go`, `option/simple.go`). У wireguard род узла — `endpoint`, как у формы `.conf`, а `endpoint` пира раскладывается тем же регексом и фолбэком, что там (второго определения «что такое endpoint» секция не заводит); `reserved` читается списком трёх чисел, а base64-написание того же поля объявлено кодом `xray_reserved_base64_unsupported` — примитива «base64 → список байтов» у движка нет, и заводить его ради одного написания одного поля значило бы расширять грамматику под частный случай. `domainStrategy` → `xray_domain_strategy_ignored`: эквивалент у ядра ЕСТЬ (`domain_resolver.strategy`), но реестр объявляет `domain_resolver` КОРОТКОЙ формой — строкой с тегом DNS-сервера, — и стратегия без сервера ядром отбрасывается; записать её значило бы либо сменить форму поля сразу у всех схем, либо выдумать тег сервера, которого элемент не называл. `noKernelTun`/`remoteDNS` — объявленное чтение без записи (эквивалента нет вовсе). У socks/http пара `user`/`pass` переводится в `username`/`password`, у http добавлен `headers` картой. (2) **`mux` → `multiplex`**: файл `multiplex.json` нёс только `body`, источников не было ни одного — сервер с обязательным mux одиночные соединения не принимает. Блок `blocks.xray` подключается четырьмя схемами (`vless`, `vmess`, `trojan`, `shadowsocks`). СЕМАНТИКА `concurrency` сверена с Xray: это максимум подпотоков в ОДНОМ соединении, то есть `max_streams` ядра, а не `max_connections`; отрицательное значение и ноль означают у Xray «mux выключен» и потому СНИМАЮТ блок целиком через `sets: null`, а не пишут число — иначе элемент, явно отключивший mux, получил бы `enabled: true` с неограниченным числом потоков, ровно обратное запрошенному. `xudpConcurrency` и `xudpProxyUDP443` — чтение без записи: отдельного предела для XUDP у ядра нет, а `reject`/`allow`/`skip` полем mux не выражается (`skip` значит «мимо mux, но через узел»), и маппить наугад значило бы менять путь трафика QUIC молча. (3) **`on_invalid` у селектора транспорта ИСПОЛНЯЕТСЯ** (Q133-17, M-01 закрыты). Объявление `{action: drop, code: transport_unsupported}` стояло у `$selector.network` с перевода на движок, но промах `value_map` означал «вези как пришло»: `network: "kcp"` уезжал в `transport.type` дословно, санитайзер снимал такой транспорт правилом enum МОЛЧА, и узел выходил РАБОЧИМ plain-TCP — сервер, который ждёт mKCP, такое соединение не примет, а человек не получал ни кода, ни причины. Это худший вид расхождения: молчаливая подмена узла. У `quic` тот же промах кончался иначе — тип доезжал до тела и ронял бы ВЕСЬ конфиг на unknown field. Чтобы объявление стало исполнимым, у записи появился закрытый атрибут `allow` — имена, ПРОХОДЯЩИЕ таблицу как есть (`ws`, `grpc`, `httpupgrade`, `xhttp`): без него «промахом» оказывалось и законное значение, и `drop` снимал бы годный узел. Код едет машинным полем отказа (`linkmap.RejectError`), а не подстрокой текста. (4) **`tlsSettings.alpn` переносится с ОБЩЕГО блока** — РЕШЕНИЕ ВЛАДЕЛЬЦА 24.09.2026, отменяет решение 21.09.2026 (D133-C9). Прежде запись стояла `maps_to: null`, то есть у `vless`/`vmess`/`trojan` из Xray-конфига ALPN терялся, и узел с h2-only или h3-only сервером НЕ СОЕДИНЯЛСЯ: ALPN есть часть рукопожатия, и сервер, которому нечего выбрать из предложенного, обрывает соединение — потеря была объявлена, но от этого не переставала быть отказом. (5) **Массив и карта в источнике доезжают до записи** — почин, без которого пункты 1 и 4 невыразимы. `jsonScalar` на массиве молчит намеренно (§4: массив НЕ приводится к строке — склейка через `fmt.Sprint` давала `host: ["[a.com b.com]"]`, Q133-16), но запись, объявившая `list`, скаляра и не ждёт: `httpSettings.host: ["a.com","b.com"]` — типовая живая форма — не доезжала ВООБЩЕ, и `coerce_scalar` у той же записи покрывал лишь обратный случай. То же для карты при `type: object` (`headers` у Xray — объект, и у ядра `badoption.HTTPHeader` тоже). Исправлены ожидания трёх кейсов, снятых со старого конвертера с этой самой потерей: `vless_h2` (два хоста), `vless_alpn_tls` и `hysteria2_bandwidth_suffix_finalmask_obfs` (`alpn`). (6) **Нестроковый элемент списка снимается ЭЛЕМЕНТОМ** — решение владельца 24.09.2026, согласовано с LxBox §510 M1; норма 1.1.49 пересмотрена. Прежде один `443` числом в `tls.alpn: [443, "h2"]` ронял поле целиком кодом `type_invalid`, и человек оставался без ALPN вовсе, хотя `h2` рядом был годен: цена ошибки провайдера ложилась на него целиком. Теперь путь кода — ЭЛЕМЕНТ (`alpn[0]`), код берётся из `on_item_invalid` поля, и это ровно та же норма, что у `item_pattern` с 1.1.40 — два правила об одном («ядру такой элемент не годится») обязаны вести себя одинаково. Список, оставшийся ПУСТЫМ, = отсутствие значения (поле не пишется), а не `type_invalid`: причину уже назвал код на элементе. Число к строке НЕ приводится — `443` не идентификатор ALPN, и подстановка уехала бы в ClientHello отдельным протоколом, которого не существует. (7) **Плоские написания `sessionIDPlacement`/`sessionIDKey`** (запрос LxBox): в 1.1.47 имя из json-тега Xray добавлено только в слой `extra`, а форки пишут тот же ключ и плоско — тогда он не читался ни одним источником. (8) **Предел длины ссылки — канон 65536 у обеих сторон** (решение владельца 24.09.2026): расхождение `limits.json` закрыто, Go читает значение из реестра. Прежние 8192 отбивали ДЛИННУЮ, НО ПОЛНОСТЬЮ ВАЛИДНУЮ ссылку кодом `uri_too_long` в самом начале конвейера, до всякого санитайза (ловушка Л18): ссылка vless с mlkem768x25519plus-ключом штатно занимает ~2000 символов, а base64-обёртки Amnezia и профили с сертификатами — больше. Предел не снят, а поднят. (9) **`keep_empty_tail` (Q133-74) закрепляется контрактом**: `socks4://userid:@host` — у socks4 пароля нет по протоколу, но разделитель клиенты пишут всегда, и его отсутствие часть из них читает как «имени нет». Флаг реестр объявлял и прежде, эмит лаунчера его исполняет (снимок `socks/socks4_userid`); норма — «обе стороны исполняют с 1.1.50». **`pinnedPeerCertificateChainSha256` НЕ маппится** и объявлен `null` с нотой: ядро хеширует SPKI ЛИСТОВОГО сертификата (`VerifyPublicKeySHA256`, `common/tls/std_client.go:278`), Xray — сырой DER цепочки, то есть прообраз РАЗНЫЙ, и перенос значения сломал бы рукопожатие у каждого узла, который пин задал. Кейсы корпуса (13): `body/xray/wireguard_settings_full`, `wireguard_domain_strategy_ignored`, `socks_settings_users`, `http_settings_headers`, `shadowsocks_uot`, `mux_concurrency`, `mux_concurrency_disabled`, `transport_kcp_dropped`, `transport_quic_dropped`, `tls_alpn_mapped`, `xhttp_flat_session_id_aliases`, `body/singbox/tls_alpn_item_nonstring`, `uri/vless/long_uri_within_canon_limit`. Фикстуры `unsupported_protocol` (корпус и Go-тест) переведены с `wireguard` на `trojan-go`: wireguard перестал быть неподдержанным, и держать его в кейсе про неизвестный протокол значило бы проверять обратное заявленному. Встречная задача LxBox — `TASKS_LXBOX.md` §46 |
| 1.1.49 | **Хвосты после релиза 2.0.0 (решения владельца 19.09.2026, `TASKS_LXBOX.md` §42)** (SPEC 133; номера 1.1.47 и 1.1.48 заняты параллельными волнами — алиасы XHTTP `sessionIDPlacement`/`sessionIDKey` и обёртки источника). (1) **Предикат «комментарий с `=` не имя» — данными.** Атрибут метки `label.comment{take, require_no}` — ровно та форма, что зафиксирована в `PRIMITIVES.md` §0.8: `take: "first"` — первый подходящий комментарий секции до первого поля, `require_no: "="` — строка с `=` это отключённая настройка («# Bouncing = 0»), а не имя. Прежде предикат жил в лексере движка частным случаем (`strings.Contains(name, "=")`), то есть разделитель wg-quick был зашит в общий код; теперь своего умолчания у движка нет — без `comment` годится первый непустой комментарий как есть. Выставлен у обеих секций wireguard, читающих `ini.$comment.Peer` (`conf` и форма `conf_b64` секции `uri`). Тела и метки живых узлов не меняются. Кейс корпуса `body/wgconf/ini_comment_setting_not_label` (без атрибута метка была бы «Bouncing = 0» — проверено). (2) **Род группы = тип тела sing-box** (§32.6 закрыт): `registry/protocols/group.json` → `genus{values, by_source}`. Отдельного имени рода («balancer_group») не заводится — род называется значением `entry.type`. `values` (`selector`, `urltest`) — допустимые типы тела узла-группы; по ним сторона, пишущая в конверт тип, приводит его к схеме `group` (`singbox_type: "selector|urltest"` обратного хода не даёт — у нас загрузчик его сознательно пропускает). `by_source`: sing-box несёт тип сам (`$as_is`), Xray-балансировщик рода не объявляет → `urltest`, синтетический autogroup LxBox → `urltest`. Конверт не меняется: `scheme` группы — `group` (CANON §1), CANON §5 дополнен. Страж `TestContractGroupGenus` сверяет род каждого узла-группы корпуса тел с таблицей по виду источника (каталог корпуса). (3) **Сверка оверлеев эмита (§33)**: `ws.eh` (`round_trip: false`), ss padding (`emit.userinfo.padding`), vmess `json_map`/`json_always`, `omit_port` у naive (объявлен, не выставлен нигде), `emit.refuse_when`, `round_trip_only` — все шесть исполняются движком эмита по данным, имён в коде нет. Расхождения были только в ДОКУМЕНТАХ и прозе: `refuse_when` и `round_trip_only` не описаны в `PRIMITIVES.md` (только в схеме) — дописаны в §0.12 добавлением; `json_always` там значился списком — уточнён картой «ключ → заполнитель» (норма с 1.1.36); у `transports.ws.params.eh` висела пометка `ext: mobile` (сгенерированная документация называла параметр «только LxBox») и проза «Go `ed`/`eh` не читает» — обе устарели с переводом ссылки на движок, пометка снята, проза переписана под норму: `eh` и плоский `ed` читают обе стороны, в ссылку не пишется ни тот, ни другой. Раннер `TestEmitOverlays` — круг `emit → parse → sanitize` на каждое правило с проверкой следа правила в ссылке; таблица сверки — `PRIMITIVES.md` §0.12b. (4) **`dropped[]`: `index` и `code` (целевая форма §32.5)** — CANON §4.1, `schema/node.schema.json`. `index` — ДОПОЛНИТЕЛЬНЫЙ адрес рядом с `ref` (смысл `ref` не менялся, 53 файла не ломаются по смыслу): позиция отвергнутого элемента в нарезке `elements` вида источника (`source_kinds.json`), с нуля; у одиночной ссылки всегда 0. `code` обязателен у КАЖДОЙ отбраковки, включая отказ РАЗБОРА, у которого кода прежде не было вовсе: движок отказывает типизированной ошибкой (`linkmap.RejectError`) с общими кодами `field_missing` (обязательное поле/запись/userinfo; у userinfo `field` — первое `into` секции) и новым **`form_unrecognized`** (ни одна форма не прочитала текст: оболочка не раскрылась, пейлоад не JSON/ini, у строки нет схемы; строке `xxx://` с неизвестной схемой — `scheme_unsupported` параллельной волны 1.1.48, граница — CANON §4.1); слой подписки ставит `protocol_unsupported` (тип записи неизвестен) и `uri_too_long`. Боевая запись отбраковки (`RejectedBodyRecord.Code`) получает те же коды; материализация читает текст, поэтому UI не меняется. Схема: `required: [ref, index, code]`, `reason` — необязательный человеческий текст (D-088). Ожидания корпуса без `index`/`code` сравниваются снисходительно, но раннер лаунчера сам отказывается выдать отбраковку без них; все 56 ожиданий с `dropped[]` перегенерированы — диф только `index`/`code` (и ненормативный `reason`). `params` из формы LxBox в эту волну не вошли (§42 назвал `index` и `code`). (5) **`server_ports` у hysteria/hysteria2 — пара uint16 без ведущих нулей** (ревью LxBox: мусор ронял весь конфиг). `item_pattern` hysteria2 был `^[0-9]{1,5}:[0-9]{1,5}$` и пропускал `99999:99999`, `65536:65536`, `00443:00444` — ядро отвечало «bad port range» отказом ВСЕГО конфига; теперь `^(U16):(U16)$`, где U16 = `6553[0-5]|655[0-2][0-9]|65[0-4][0-9]{2}|6[0-4][0-9]{3}|[1-5][0-9]{4}|[1-9][0-9]{0,3}|0`. У hysteria v1 формат элемента не судился вовсе — выставлен тот же паттерн с новым кодом `hysteria_server_ports_item_invalid`. Паттерн продублирован в двух файлах НАМЕРЕННО: `ref` на общее определение (`dialer.common`) переносит поверх только `required`/`code`/`forbidden_for`/`allowed_for`/`forbidden_codes`, и `on_item_invalid` со своим кодом и `conflicts` с `realm` на ссылке терялись бы. Тексты `hysteria2_server_ports_item_invalid` уточнены (диапазон 0–65535, причина — порт вне диапазона/ведущие нули). Нестроковые элементы (`tls.alpn: [443, "h2"]`, `server_ports: [443]` во входе sing-box) в ядро не проходили и прежде: `coerce` строкового списка отвергает элемент-не-строку, и поле снимается с `type_invalid` (ошибка ТИПА поля, а не формата элемента) — движок не менялся, поведение закреплено кейсом. Кейсы `uri/hysteria2/mport_out_of_range_item_dropped`, `uri/hysteria/mport_out_of_range_item_dropped` (`extension: desktop`), `body/singbox/list_non_string_items`. (6) **Решения владельца 24.09.2026** (`TASKS_LXBOX.md` §45.6): граница кодов непрочитанного — `scheme_unsupported` строке `xxx://` с неизвестной схемой, `form_unrecognized` непрочитанному тексту (CANON §4.1; кейс `uri/naive/bare_scheme_rejected` переведён на `scheme_unsupported`); голый IPv6 в `Endpoint` `.conf` остаётся как есть до реального конфига (§29); нестроковый элемент строкового списка — снимать только его, внедрение после ответа LxBox по §45.5; `scheme` группы в конверте остаётся `group`, `genus` — правило приведения. Встречная задача LxBox — `TASKS_LXBOX.md` §45 |
| 1.1.48 | **Обёртки источника: `vpn://` с голым `.conf`, схема `amneziawg://`, тихий игнор служебных `incy://`/`happ://`, одиночный конфиг Xray, полоса hysteria2 с суффиксом и обфускация из `finalmask`** (живые входы четырёх пользователей, у каждого ноль узлов при исправной подписке). Шесть норм, все — уровня ОБЁРТКИ, а не полей: (1) у ветки `amnezia_link` объявлены `payload_forms` — под `vpn://` лежит либо пакет-профиль Amnezia, либо голый `.conf` wg-quick/AWG (форма `bare_conf`, предикат тот же, что у вида `wireguard_conf`); прежде такой payload уходил в ветку qCompress, первые четыре байта INI читались как объявленная длина, и диагноз «declared uncompressed size … out of range» уводил чинить не то. Паддинг `=` необязателен — панель его срезает. (2) `amneziawg` добавлен в `scheme_in` и `aliases` секции wireguard: панели пишут полное имя схемы, и ссылка отбраковывалась как «unsupported scheme», хотя все её поля секция уже читала — те же узлы приезжали рядом через `vpn://`; `dns` из query в тело не едет и даёт `wgconf_dns_ignored`, как у формы `.conf`. (3) у ветки `uri_lines` объявлены `service_schemes` — `incy://routing/…` и `happ://routing/…` (те же команды приходят и заголовком `Routing:`): это не узлы и не «неподдержанный протокол», а команда соседнему клиенту; отбраковываются с info-кодом `service_record_ignored`, а не ошибкой — у формально рабочей подписки пользователь читал пять отказов при восьми живых узлах. Обязателен хвост `routing/`: схема, объявившая иное, тихого игнора не заслуживает. (4) вид `xray_config` (prio 35) реестр объявлял с самого появления, а рукописный классификатор диалект не спрашивал и отдавал одиночный конфиг Xray ветке sing-box — каждая запись отбраковывалась «missing type», узлов ноль; код `body_dialect_unrecognized` называет этот промах. (5) `normalize: bandwidth_mbps` у `up_mbps`/`down_mbps`: форки пишут полосу строкой с суффиксом (`"100mbps"`, `"300 Mbps"`), и `type: int` на ней давал отсутствие значения — полоса терялась молча; `kbps`/`bps` округляются вниз, `gbps` умножается на 1000, неизвестный суффикс значения не даёт. (6) обфускация hysteria2 из `finalmask.udp[0]` — записи `obfs_type_finalmask` и `obfs_password_finalmask`: секрет лежит вложенно в `settings.password`, весь блок `finalmask` был объявлен чтением-без-записи, и salamander вместе с паролём терялся целиком (узел не поднимался). Годность типа и секретность пароля судят поля ТЕЛА, второй проверки в маппере нет. Плюс машинные коды отбраковкам, которые их не имели: `scheme_unsupported` у схемы, которую не ведёт ни одна секция (прежде причина ехала только текстом Go, и вторая сторона не могла её сверить). Мелочь: ссылка `impl` у `base64_wrapped` вела на `subscription_decode.go`, файл давно `decoder.go`. Схема `registry_mapper.schema.json` расширена двумя закрытыми атрибутами (`payload_forms`, `service_schemes`). Кейсы корпуса: `body/vpn/bare_conf_awg3`, `uri/wireguard/amneziawg_scheme_full_name`, `body/uri_list/service_scheme_routing_ignored`, `body/xray/single_config_vless_reality_mlkem`, `body/xray/hysteria2_bandwidth_suffix_finalmask_obfs`. Встречная задача LxBox — `TASKS_LXBOX.md` §44 |
| 1.1.47 | **XHTTP `extra`: proto-имена Xray `sessionIDPlacement`/`sessionIDKey` — алиасы канона** (issue #131). Живые `vless+xhttp` ссылки несут в `extra` (URL-encoded JSON) написание Xray-прото, а записи `sessionPlacement`/`sessionKey` читали только написание sing-box — идентификатор сессии уходил в дефолт ядра `path`, хотя сервер ждал cookie с кастомным ключом. `seqPlacement`/`seqKey` доезжали и до правки: у них имена прото и sing-box совпадают. Алиасы `sessionIDPlacement` → `transport.session_placement` и `sessionIDKey` → `transport.session_key` добавлены в `source` всех трёх входов (URI `extra`, URI `query`, Xray-JSON `extra` у `xhttpSettings` и `splithttpSettings`) и в `aliases` описательной секции. **Канон стоит первым** — при двух написаниях рядом побеждает написание sing-box. Ядро документирует ту же пару: `option/v2ray_xhttp.go` — прото `sessionIDPlacement`, JSON `session_placement`. `sessionIDLength`/`sessionIDTable` НЕ заведены умышленно: `"0"` без таблицы означает «не задано», а протащить одно поле без пары — отказ ядра «must be set together». Пустые `host`/`path`/`mode` из `extra` не тронуты (D-097). Кейсы корпуса: `uri/vless/xhttp_extra_session_id_aliases`, `uri/vless/xhttp_extra_session_canon_beats_id_alias`, `body/xray/xhttp_extra_session_id_aliases`, `body/xray/xhttp_extra_session_canon_beats_id_alias`. Тела прежних узлов не изменились: алиас читается только там, где канона нет |
| 1.1.46 | **Xray dialerProxy → freedom с fragment: код `xray_fragment_mapped` снят** (решение владельца). Норма 1.1.45 без изменений: цель `streamSettings.sockopt.dialerProxy` с протоколом `freedom` и `settings.fragment` — не хоп, при `tls.enabled` ставится `tls.fragment=true` молча. Код info снят: Xray режет ClientHello вслепую по length и ждёт фиксированный interval; sing-box парсит ClientHello, режет каждую метку SNI (public suffix не трогается), включает TCP_NODELAY, ждёт ACK или `fragment_fallback_delay` (500 мс по умолчанию); `record_fragment` — тот же разрез на уровне TLS-записей — механика ядра строго лучше, предупреждать не о чем. Кейс `body/xray/dialer_proxy_freedom_fragment` (ожидания без warnings). Встречная задача LxBox — `TASKS_LXBOX.md` §41 |
| 1.1.45 | **Xray dialerProxy → freedom с fragment — не хоп** (дефект v1.3.0). Цель `streamSettings.sockopt.dialerProxy` с протоколом `freedom` и `settings.fragment` больше не трактуется звеном цепочки: узел-владелец остаётся прямым, фрагментация переносится в `tls.fragment=true` при включённом TLS; код `xray_fragment_mapped` (info) несёт исходные packets/length/interval в `value`, length/interval в sing-box не переносятся. Freedom без fragment — dialerProxy молча игнорируется. Прочие служебные цели (blackhole, dns, loopback) — отбраковка как раньше. Кейс `body/xray/dialer_proxy_freedom_fragment`. Встречная задача LxBox — `TASKS_LXBOX.md` §41 |
| 1.1.44 | **Зеркальные стражи LxBox pre-2.0.0: CIDR, multi-peer emit, дробные порты** (SPEC 134). Код не менялся — три вероятных дефекта партнёра на наших нормах **не воспроизводятся**. (1) `format cidr` (`net.ParseCIDR`) отвергает `1.2.3.4/64`, `::::/128`, префикс вне семейства, отрицательный и с пробелом; кейс `uri/wireguard/address_cidr_invalid` — узел с `type_invalid`. (2) `emit.refuse_when` у wireguard: два пира → отказ с текстом «ОДИН удалённый сервер», не ссылка по первому (`[]interface{}` и `[]map[string]interface{}`). (3) Дробный `port`/`mtu`/`reserved` не усекается: `asInt`/`coerce` отбраковывает, `443.0` допустим; кейсы `uri/vmess/fractional_port`, `body/xray/vmess_fractional_port`. Встречная задача LxBox — `TASKS_LXBOX.md` §40 |
| 1.1.43 | **`merge: append` исполняется; двойная base64-обёртка раскрывается; BOM снимается на входе** (SPEC 134, внешнее ревью). (1) `merge` у занятого пути ИСПОЛНЯЕТСЯ движком, а не только разрешает запись: `append`/`prepend` конкатенируют списки (скаляр и путь другой формы — как `overwrite`). До правки обе формы вели себя как `overwrite`, и у hysteria/hysteria2 запись `mport` вытесняла диапазоны селектора `$multiport` из authority — `host:443,20000-30000` вместе с `?mport=` молча терял половину port-hopping'а. Кейс `uri/hysteria2/mport_and_authority_range`, круг `parse(emit(body))` сходится. Дельта `D133-41`. (2) `requires_after_unwrap` больше не гасит цикл `redetect` на ПРОМЕЖУТОЧНОМ слое: слой двойной обёртки — чистый алфавит base64, правдоподобие на нём проваливается, хотя документа ещё не достигли; отличает «обёртка не кончилась» от «обёртки не было» повторный детект (выиграл ТОТ ЖЕ вид). Предел `max_unwrap_depth` (D133-35) впервые достижим — на третьей обёртке отказ тихий, без паники и рекурсии. Дельта `D133-42`. (3) `NewContent` срезает `U+FEFF`: BOM сдвигал первый значащий символ, и JSON/`.conf` уезжали в построчную ветку одним блобом. Дельта `D133-43`. (4) Детерминизм и латентное: порядок замен `label.value_map` (длинный ключ раньше короткого), `on_len_gt` продолжает цепочку альтернативных источников, мёртвый `mode` у `decodeOnce` снят — разницу «путь/query» несёт `plus_literal`. Дельта `D133-44`. Встречная задача LxBox — `TASKS_LXBOX.md` §39 |
