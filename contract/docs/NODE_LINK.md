# Ссылка на узел: NodeLink — норма для всех проектов (контракт 1.0.x)

Статус: **норма** (D-112, решение владельца 14.09.2026). Действует у лаунчера
(singbox-launcher), у LxBox, в контракте (схема и корпус бэкапа) и в API обеих
сторон. Документ `contract/VERSION` не поднимает: версию с LxBox согласует
координатор.

Формулировка владельца: «ссылки на ноды в формате {folder_id, tag} должны
стать форматом по всем проектам нашим».

Смежные нормы: `IDENTITY.md` §1–§2 (идентичность узла = тег), `BACKUP.md`
§4/§6/§9 (перенос), `NODE_SECTIONS.md` (`@self`), `ONE_NAMESPACE.md` §1.
Адреса кода лаунчера (`файл:строка`) даны по ветке `develop` на момент влития
документа — это справка для сверки, а не норма: сдвинулся код — правится
адрес. Поведение сборки, описанное в §5, проверено прогоном конвейера
лаунчера на синтетических источниках, а не только чтением.

## 1. Зачем

Финальный тег узла в `config.json` — производная, а не имя. У члена папки или
подписки он складывается из политики контейнера, сырого тега и глобальной
уникализации (§3) и потому меняется от правок, к самому узлу не относящихся:
префикс подписки, перенос узла в другую папку, одноимённый сосед в соседнем
источнике. Ссылка, хранящая финальный тег, — кэш, который протухает молча:
тот же класс багов, из-за которого SPEC 112 снёс контент-хэш (`IDENTITY.md`
§2.1).

NodeLink хранит то, что от этих правок не зависит, — **id контейнера и сырой
тег узла в нём**. Отсюда:

- **переименование папки или подписки** (`name`) ссылок не касается: адрес —
  `id`, а не имя;
- **смена `tag_policy`** (префикс, суффикс) меняет только финальные теги, ни
  одна ссылка не переписывается — главное преимущество модели (лаунчер:
  `core/config/canonical_emit.go:28-29`, тест
  `core/config/canonical_emit_test.go:151-170`);
- **обновление подписки** сливает узлы по сырому тегу и переносит `enabled` и
  `detour` совпавших, поэтому ссылки на них живут
  (`core/state/subscription_merge.go:8-10`, `43-69`, `104-125`);
- **перенос на другую машину**: id контейнеров переписываются по карте (§7.2),
  сырые теги у обеих сторон одинаковы (`IDENTITY.md` §1).

Финальный тег вычисляется каждой сборкой и не лежит ни в хранении, ни в
бэкапе, ни в API (`BACKUP.md` §2, П5).

## 2. Форма

```json
{ "folder_id": "<ULID папки или подписки>", "tag": "<сырой тег узла внутри неё>" }
```

| Поле | Тип | Обязательность | Смысл |
|---|---|---|---|
| `tag` | string, непустой | всегда | член контейнера — СЫРОЙ тег узла; корневая цель — тег корневого узла или корневое имя (§5.2) |
| `folder_id` | string | у члена контейнера ОБЯЗАТЕЛЕН; у корневой цели отсутствует | `id` записи `sources[]` вида `folder` или `subscription` — единственная идентификация контейнера |

Правила записи — у обеих сторон одинаковые:

1. Член контейнера адресуется ТОЛЬКО парой. Ссылку `{tag}` на члена папки или
   подписки писатель не создаёт; читатель её терпит (§7.3).
2. Корневая ссылка — объект без ключа `folder_id`, пустой строкой он не
   пишется (лаунчер: `omitempty`, `core/state/sources_v7.go:39-40`). Читатель
   трактует отсутствие ключа и пустую строку одинаково.
3. При заданном `folder_id` в `tag` лежит сырой тег, никогда не финальный:
   `"US-1"`, а не `"nl:US-1"`.
4. Других ключей у ссылки нет. Подпись для UI (`detour_node_label` формата
   0.12) снята: имя узла и есть его тег (SPEC 112).
5. `id` КОРНЕВОГО узла (`sources[].id` у `server` и `chain`) адресом не
   является: корневой узел адресуется `{tag}`, у члена папки своего `id` нет
   вовсе (`BACKUP.md` §2).

Лаунчер: `state.NodeLink` — `core/state/sources_v7.go:37-43`; сборочное
зеркало — `core/config/configtypes/types.go:275-282`. Схема —
`schema/backup.schema.json` `$defs/nodeLink`; её описание («`folder_id` —
лаунчер; LxBox адресует хоп тегом») и пометка «Поддержка: launcher» у
`detour` в `BACKUP.md` §2 написаны до D-112 и правятся вместе с версией
контракта.

### 2.1 Примеры

Корневой узел:

```json
"detour": { "tag": "relay-root" }
```

Член папки: папка «Work» с `tag_policy.prefix = "[W] "`, в конфиге узел
зовётся `[W] de-1`:

```json
"detour": { "folder_id": "01FLDWORK00000000000000000", "tag": "de-1" }
```

Член подписки: подписка с `tag_policy.prefix = "nl:"`, в конфиге узел зовётся
`nl:US-1`. Позиции цепочки вперемешку:

```json
"hops": [
  { "tag": "relay-root" },
  { "folder_id": "01FLDWORK00000000000000000", "tag": "de-1" },
  { "folder_id": "01SUBNL0000000000000000000", "tag": "US-1" }
]
```

Члены провайдерской группы той же подписки:

```json
"group": {
  "group_type": "selector",
  "default": "US-1",
  "members": [
    { "folder_id": "01SUBNL0000000000000000000", "tag": "US-1" },
    { "folder_id": "01SUBNL0000000000000000000", "tag": "US-2" }
  ]
}
```

### 2.2 Узел подписки: что лежит в `folder_id` и `tag`

**`folder_id` — `id` самой подписки**, записи `sources[]` вида
`subscription`. Подписка — такой же контейнер, как папка, и другого адреса у
её узлов нет.

- Лаунчер: проекция подписки отдаёт `ProxySource.ID = s.ID` и
  `CanonicalSource.FolderID = s.ID` (`core/state/adapter_source.go:33-50`,
  `100-110`); словарь целей сборки кладёт узлы ЛЮБОГО контейнера в
  `byFolder[FolderID]` (`core/config/nodelink_resolve.go:74-92`). Ссылки
  внутри подписки fetch материализует её же id: члены провайдерской группы —
  `core/config/migrate_materialize.go:138-142`, detour владельца на релей
  BYPASS — `core/config/relay_materialize.go:118-133`; `subID` там —
  `src.ID` (`core/config_service_subscriptions.go:164`).
- Это **не** id папки, в которую подписку «заливают». Заливка создаёт в папке
  КОПИИ узлов с собственным адресом `{id папки, тег копии}`
  (`core/state/subscription_merge.go:166-276`): ссылка на копию и ссылка на
  оригинал в подписке — разные ссылки.
- Это **не** свёртка. `fold` / `fold_tag` — группа с корневым именем, её
  адресуют корневой ссылкой `{tag: "<fold_tag>"}` (§5.2).

**`tag` — сырой тег узла в подписке**: имя от провайдера (фрагмент ссылки,
`tag` / `ps` / `remarks` записи тела), уникализированное В ПРЕДЕЛАХ подписки
(`X`, `X-2`, `X-3`) до всякой `tag_policy` (`IDENTITY.md` §1;
`core/state/sources_v7.go:134-137`). Служебный релей BYPASS получает тег
`<тег владельца> · relay` (`core/config/relay_materialize.go:48`).

Обновление подписки и ссылка на её узел (`core/state/subscription_merge.go:71-164`):

| У провайдера | Узел в `nodes[]` | Ссылка на узел |
|---|---|---|
| узел остался под тем же именем | тело освежено, `enabled` и `detour` перенесены (`43-69`, `118-123`) | жива |
| узел исчез | удалён (`127-136`) | висячая: detour и позиция — fail-closed, член группы — prune (§5) |
| узел переименован | прежний удалён, новый добавлен включённым | висячая: за переименованием у провайдера ссылка НЕ следует (`IDENTITY.md` §1.2) |
| ответ обрезан капом | исчезнувшие не удаляются (`128-134`) | жива |
| ответ недостоверный | `nodes[]` не трогаются (`97-99`) | жива |
| подписка выключена | узлы не собираются | не разрешается на этой сборке (`it has no node`), оживает с включением |

Узлы подписки в бэкап не едут (`BACKUP.md` §2): до первого обновления на
приёмнике ссылка на член подписки не разрешается, и это не потеря.

## 3. Сырой тег против финального

**Сырой тег** (`sources[].tag`, `nodes[].tag`) — имя узла и его идентичность
в контейнере (SPEC 112). На нём живут merge-ключ обновления, `enabled`,
отметки `disabled{}` и ссылки NodeLink.

**Финальный тег** — имя outbound'а в `config.json`. Формула сборки лаунчера
(`core/config/canonical_emit.go:391-443`):

```
член контейнера:  final = uniq( norm( expand(prefix) + tag + expand(postfix) ) )
корневой узел:    final = uniq( norm( tag ) )
```

- `expand` — переменные `{$tag} {$scheme} {$protocol} {$server} {$port}
  {$label} {$comment} {$num}` (`canonical_emit.go:426-443`); `{$tag}` — сырой
  тег, `{$num}` — порядковый номер узла в контейнере, выключенные считаются
  (`canonical_emit.go:83-106`);
- `norm` — ремонт UTF-8, `❯ » ›` → ` > `, цветные геометрические эмодзи →
  `*`, схлопывание пробелов, обрезка краёв
  (`internal/textnorm/proxy_display.go:12-32`);
- `uniq` — ГЛОБАЛЬНАЯ уникализация на всю сборку (`X`, `X-2`) в порядке
  `sources[]`: выключенный узел включённого контейнера слот потребляет
  (`canonical_emit.go:66-75`), неразобранная запись — нет
  (`core/state/adapter_source.go:117-127`), выключенный источник целиком — нет
  (`core/config/outbound_generator.go:1269-1273`).

Уникальность:

- сырой тег уникален В КОНТЕЙНЕРЕ; правка, дающая дубль, отвергается
  (лаунчер: `ui/configurator/tabs/source_edit_window.go:2353-2357`,
  `ui/configurator/tabs/preview_node_ops.go:433-438`);
- тег корневого узла уникален в КОРНЕВОМ пространстве — среди корневых узлов,
  цепочек, Направлений и их `-auto`, тегов свёрток и их `-auto`, системных
  тегов шаблона (гард — `SPECS/features/directions.md` §8);
- корневая ссылка адресует ФИНАЛЬНЫЙ тег корневого узла
  (`core/config/nodelink_resolve.go:94-102`). Он равен хранимому, пока тег
  нормализован и не столкнулся на `uniq` с финальным тегом члена контейнера,
  стоящего в `sources[]` выше: такое столкновение даёт корневому узлу суффикс
  `-2`, и корневая ссылка на него перестаёт разрешаться. Держать тег корня
  нормализованным и уникальным — обязанность писателя.

## 4. Где NodeLink обязателен и где запрещён

### 4.1 Обязателен: всякая ссылка «через кого»

| Место | Хранение (лаунчер, state v8) | Бэкап 1.0 | Сборка |
|---|---|---|---|
| личный detour узла `server` (в корне и в контейнере) | `Node.Detour` (`sources_v7.go:152-155`) | `sources[].detour`, `nodes[].detour` | fail-closed |
| общий detour контейнера (папка, подписка) | `detour` записи контейнера | `detour` у `folder` / `subscription` | fail-closed; действует на `server` без личного, `chain` и `auto` пропускает (`canonical_emit.go:132-148`) |
| позиции цепочки, ближний хоп первым | `Node.Hops` (`sources_v7.go:156-157`) | `hops[]` | fail-closed, цепочка целиком |
| члены провайдерской группы `auto` | `AutoGroup.Members` (`sources_v7.go:108`) | `group.members[]` | prune |

У `chain` и `auto` detour не существует типом (`sources_v7.go:536-555`).

**API.** Ручка, адресующая узел хранения (прочитать, изменить, удалить по
адресу), принимает и отдаёт NodeLink. Лаунчер сегодня отдаёт ссылки как лежат
в `GET /state/full` (форма state v8, `core/debugapi/state_endpoints.go:133-138`)
и в `GET /backup/export`; ручек, адресующих узел хранения для правки, у него
нет. Рантайм-ручки говорят финальным тегом — это не хранение (§4.3).

**UI.** Окно, открытое на узле, держит его адрес NodeLink, а не индекс в
списке: индекс протухает от перетаскивания и удаления соседей (лаунчер:
`ui/configurator/business/node_address.go:1-8`, `70-102`).

### 4.2 Запрещён: внутри `body`

`body` — объект sing-box как есть (`ONE_NAMESPACE.md`, `BACKUP.md` §2). Внутри
него живут только ФИНАЛЬНЫЕ теги, и вписывает их сборка:

- `tag` и `detour` из тела outbound'а сняты при материализации
  (`core/config/migrate_materialize.go:274-290`); «через кого» — поле записи,
  сборка подставляет его резолвом (`core/config/nodelink_resolve.go:359-363`);
- цель правила маршрута (`rules[].body.outbound`) и `route.final`;
- `detour` DNS-сервера (`dns.servers[].body.detour`) и `server` DNS-правила;
- `@self` / `@{self}` в секциях узла — финальный тег ЭТОГО узла, подставляет
  сборка (`NODE_SECTIONS.md`); сам узел для подстановки находится по паре
  `{id контейнера, сырой тег}` (`core/config/canonical_emit.go:212-218`).

Объект NodeLink внутри `body` не пишется никогда: ядро его не понимает, а
сборка тело не разбирает.

### 4.3 Не ссылки на узел

| Что | Чем адресует | Почему не NodeLink |
|---|---|---|
| отбор Направления (`filter`, `invert`) и его умолчание (`default`) | регулярка по финальному тегу | отбор множества, а не ссылка |
| `directions[].include` (addOutbounds) | корневые имена строками | известный разрыв — §8 |
| `default` провайдерской группы | сырой тег члена строкой | расхождение формы — §9.3 |
| `disabled{}` подписки | сырой тег | отметка на узле, а не ссылка на него (`IDENTITY.md` §5) |
| `origin.sub_url` | URL подписки | связь копии с подпиской, переживает перенос машин (`SPECS/features/sources.md` §Происхождение) |
| выбор в селекторе (cache.db, Clash API), проба цепочки `/chains/{tag}/probe` | финальный тег | рантайм ядра, в хранении не лежит (`SPECS/features/directions.md` §10) |

## 5. Резолв

Резолв один на все виды ссылок и выполняется ПОСЛЕ того, как финальные теги
получили все узлы всех источников — цель вправе стоять в `sources[]` ниже
ссылающегося (двухпроходность, `IDENTITY.md` §2.1). Лаунчер: словарь целей —
`core/config/outbound_generator.go:1449`, позиции цепочек — `:1455`, detour и
члены групп — `:1462`; финальный рубеж — санитайзер графа
`core/build/outbound_graph_sanitize.go:19-42`.

### 5.1 Таблица случаев

| № | Ссылка | Условие | Исход | Текст лаунчера (`core/config/emission_warning.go:32-47`) |
|---|---|---|---|---|
| 1 | `{folder_id: F, tag: T}` | в контейнере F собран включённый узел с сырым тегом T | цель — финальный тег этого узла | — |
| 2 | `{folder_id: F, tag: T}` | контейнера F в сборке нет: удалён, не приехал, F — id корневого узла | не разрешилась | `the referenced source is gone` |
| 3 | `{folder_id: F, tag: T}` | F есть, но узла T в сборке нет: отсутствует, выключен, выключен весь контейнер, запись `unsupported`, тело не собралось | не разрешилась — ОДИН исход на все причины | `it has no node "T"` |
| 4 | `{tag: T}` | T — финальный тег корневого узла | цель — этот узел | — |
| 5 | `{tag: T}` | T — корневое имя: Направление и его `-auto`, тег из `addOutbounds` Направления, тег свёртки и её `-auto`, системный тег шаблона (`direct-out`) — `nodelink_resolve.go:118-150` | цель — это имя; узла за ним нет | — |
| 6 | `{tag: T}` | T — сырой ИЛИ финальный тег члена контейнера (и не корневое имя из № 5) | не разрешилась: корень контейнеры не видит, глобального поиска нет | `target "T" is not among nodes, Directions and folder replacements` |
| 7 | detour или позиция члена контейнера без `folder_id` | цель — сосед по тому же контейнеру | трактуется как корень → № 6 | как № 6 |
| 8 | член группы `auto` ВНУТРИ контейнера без `folder_id` | — | трактуется как свой контейнер → № 1 / № 3; к корню не откатывается (`core/config/canonical_emit.go:250-253`, `448-464`; комментарий `445-447` обещает откат — прав код) | как № 3 |
| 9 | любая | `tag` пуст | не разрешилась | `the reference is empty` |

Выключенная и отсутствующая цель — один исход
(`core/config/nodelink_resolve.go:193-197`): выключенный узел в конфиг не едет,
и ссылка на него не может «пройти мимо».

Строгость зависит от вида ребра (`nodelink_resolve.go:17-28`):

| Ребро | Цель не разрешилась | Цель выпала сама | Ссылка на себя | Кольцо |
|---|---|---|---|---|
| detour узла, личный или общий | носитель выпадает с ⚠ (`nodelink_resolve.go:349-352`) | носитель выпадает — каскад (`:353-355`) | носитель выпадает (`:356-358`) | выпадают все участники (`:285-293`, `370-438`) |
| позиция цепочки | цепочка выпадает ЦЕЛИКОМ (`canonical_emit.go:288-310`, `core/config/chain_nodes.go:213-230`) | цепочка выпадает на санитайзере графа | ядро отвергает, форма не предлагает (`ui/configurator/tabs/source_chain_hops.go:97-100`) | исключено порядком (§5.2) |
| член группы | член выпадает с ⚠, группа живёт (`nodelink_resolve.go:442-463`) | член выпадает | — | — |

- Группа, потерявшая всех членов, не эмитится, с ⚠ (`nodelink_resolve.go:306-311`):
  пустой urltest роняет старт ядра.
- `default` вне состава снимается с ⚠ (`nodelink_resolve.go:469-488`).
- Недостижимая цель detour или позиции никогда не превращается в прямое
  соединение: анонимность не деградирует молча (`SPECS/features/directions.md`
  §6; SPEC 077 упразднён).
- Каскад: выпавший носитель роняет тех, кто ходил через него. Лаунчер
  повторяет проход до неподвижной точки (`nodelink_resolve.go:264-283`) —
  итог тот же, что у топологического порядка «цель раньше ссылающегося», —
  и снимает кольца после неё.

### 5.2 Не-узловые цели: группа, свёртка, цепочка, Направление

Корневые имена адресуются корневой ссылкой `{tag: "<имя>"}` без `folder_id`
(`nodelink_resolve.go:49-52`, `112-150`); узлы — парой. «Да» в таблице —
законно по модели; последний столбец — что делает сборка лаунчера сегодня.

Группа `auto` в корне `sources[]` в состоянии лаунчера законна, но в бэкап не
едет (`BACKUP.md` §2); у провайдерской группы в контейнере члены — узлы того же
контейнера.

| Цель | Форма | detour | позиция | член `auto` | Сборка лаунчера |
|---|---|---|---|---|---|
| узел `server` в корне | `{tag}` | да | да | да, у группы в корне | разрешается (№ 4) |
| узел `server` в контейнере | `{folder_id, tag}` | да | да | да | разрешается (№ 1) |
| член СВЁРНУТОЙ папки | `{folder_id, tag}` | да | да | да | разрешается: узлы свёрнутой папки в конфиге есть (`SPECS/features/directions.md` §5). Форма лаунчера такую позицию не предлагает (`source_chain_hops.go:249-272`) |
| провайдерская группа `auto` в контейнере | `{folder_id, tag: сырой тег группы}` | да | да | нет: группа в группе запрещена (`SPECS/features/sources.md` §Auto) | ⚠ разрешается только ФИНАЛЬНЫМ тегом группы: у узла-группы `IdentityTag` не ставится (`canonical_emit.go:242-246`), словарь берёт финальный (`nodelink_resolve.go:176-181`), форма пишет финальный (`source_chain_hops.go:216-220`). Сырой тег при непустом `tag_policy` даёт № 3. Вид члена сборка не проверяет |
| свёртка (`fold_tag`, `<fold_tag>-auto`) | `{tag}` | да; форма detour тегов свёрток не предлагает (`ui/configurator/business/outbound.go:109-114`) | да (`source_chain_hops.go:178-190`) | да, у группы в корне | разрешается (№ 5, `nodelink_resolve.go:134-138`) |
| Направление и его `-auto` | `{tag}` | да (`ui/configurator/business/detour.go:123-133`) | да (`source_chain_hops.go:134-147`) | да, у группы в корне | разрешается (№ 5, `nodelink_resolve.go:123-133`) |
| системный тег `direct-out` | `{tag}` | нет смысла: форма исключает (`detour.go:19-32`) | да, позиция 0 — «первый хоп без прокси» (`source_chain_hops.go:149-152`) | — | разрешается (№ 5, `core/config/chain_validate.go:136`) |
| цепочка в корне | `{tag}` | не предлагается формой (`detour.go:110-188`) | только позиция 0 и только цепочка, стоящая в `sources[]` ВЫШЕ | — | ⚠ не разрешается никогда: словарь целей строится до того, как цепочки становятся узлами (`outbound_generator.go:1449-1457`), и тегов цепочек в нём нет (`nodelink_resolve.go:61-150`). Вложенная цепочка выпадает целиком (№ 6), хотя форма её предлагает (`source_chain_hops.go:154-176`) |
| цепочка в папке | `{folder_id, tag}` | не предлагается формой | как у корня | — | ⚠ не разрешается (№ 3). Две цепочки в одной папке эмитятся ОДНИМ outbound'ом: тег первой, позиции последней, вторая пропадает без предупреждения (`canonical_emit.go:282-316`, `chain_nodes.go:43-54`) |

**Цепочка внутри цепочки** (SPEC 110 T5, `BACKUP.md` §4 «порядок записей
нормативен»): разрешена только на позиции 0 и только ссылкой на цепочку,
объявленную выше по `sources[]`. Ссылка вперёд и цепочка на позиции ≥ 1
роняют ссылающуюся цепочку целиком (`chain_nodes.go:92-104`, `236-240`;
`core/build/outbound_graph_sanitize_test.go:147-161`). У лаунчера правило
реализовано для позиций-строк (`core/config/chain_emit_test.go:216-257`), но
канонический путь до него не доходит — см. строку «цепочка в корне».

Что переписывается при переименовании таких целей — §6.

## 6. Операции и ссылки

Правила для обеих сторон:

1. Операция, меняющая АДРЕС узла (переименование, перенос между
   контейнерами), переписывает ссылки на новый адрес во всех видах ссылок:
   detour узлов и контейнеров, позиции цепочек, члены и `default` групп
   (`SPECS/features/directions.md` §9).
2. Операция, меняющая только ФИНАЛЬНЫЙ тег (`tag_policy`, имя контейнера),
   ссылки не трогает.
3. Ссылка никогда не переуказывается на ДРУГОЙ узел — тёзку, соседа, узел под
   похожим именем. Цели нет — ссылка висит до сборки или гасится с
   называнием задетых (`IDENTITY.md` §2.1).
4. Выбор в селекторах ядра (cache.db) адресован финальными тегами и протухает
   при любой смене финального тега; переписать его сторона не может и обязана
   предупредить (`SPECS/features/directions.md` §10).

Что делает лаунчер сегодня:

| Операция | Адрес цели | Лаунчер | Соответствие |
|---|---|---|---|
| переименование узла контейнера | `tag` | перепись всех видов ссылок `{F, old}` → `{F, new}` и `default` групп, задетые источники называются (`ui/configurator/business/node_move.go:446-464`, `565-652`; вызовы — `preview_node_ops.go:461`, `source_edit_window.go:2381`) | ✅ правило 1 |
| переименование корневого узла (`server`, `chain`) | `tag` | СБРОС: снимаются только `detour` корневых записей и контейнеров на прежнее имя (`ui/configurator/business/detour_refs.go:36-106`; вызов — `source_edit_window.go:2394-2399`, `2462-2496`). Позиции цепочек, detour членов папок и члены групп остаются на прежнем имени и выпадают на сборке; у цепочки форма только предупреждает (`ui/configurator/tabs/source_chain_tab.go:621-631`) | ⚠ `IDENTITY.md` §2.1 требует сброса, `SPECS/features/directions.md` §9 — переписи; вопрос — TASKS_LXBOX `## 17` |
| перенос узла: в папку, из папки, в корень | `folder_id` и `tag` (уникализация в цели) | перепись всех видов ссылок (`node_move.go:88-153`, `234-302`, `343-393`); ссылки корня, которые переписать нельзя (цели правил, `route.final`, detour DNS, addOutbounds Направлений), называются (`node_move.go:155-222`) | ✅ правило 1 |
| смена `tag_policy` | — | ссылки не трогаются; предупреждение о протухшем выборе (`ui/configurator/tabs/source_tag_shift_warning.go:15-21`) | ✅ правило 2 |
| переименование папки или подписки | — | ссылки не трогаются | ✅ правило 2 |
| удаление узла из контейнера | цели нет | `detour` снимается — носитель пойдёт напрямую; позиция удаляется из цепочки — маршрут короче; член и `default` снимаются; задетые называются (`node_move.go:466-552`; вызов — `preview_node_ops.go:491-528`) | ⚠ §9.3 п. 7: сборка на ту же висячую ссылку ответила бы fail-closed |
| удаление корневого узла, папки, подписки | цели нет | ссылки не трогаются (`ui/configurator/tabs/source_tab.go:1330-1339`, `1401-1414`); на сборке — fail-closed / prune с ⚠ | ✅ правило 3 |
| обновление подписки | узел исчез или переименован провайдером | §2.2 | ✅ |
| копия узла в папку или в корень | у копии НОВЫЙ адрес: `{папка, уникальный тег}` или `{уникальный тег}` | ссылки на оригинал за копией не идут; ссылки самой копии (её detour, позиции, члены) копируются как есть и ведут туда же, куда у оригинала (`node_move.go:57-86`, `395-421`, `789-819`) | ✅ |
| заливка подписки в папку | копии в папке | члены групп-копий переуказываются на копии в папке, `default` без копии снимается (`core/state/subscription_merge.go:278-346`); detour копий на релеи BYPASS остаётся на узлах ПОДПИСКИ (`ui/configurator/business/folder_fill_subscription.go:102-133`) | ⚠ §9.3 п. 8 |
| переименование Направления | корневое имя | перепись: цели правил, `route.final`, addOutbounds, переменные пресетов, detour DNS, позиции и detour КОРНЕВЫХ записей (`ui/configurator/business/direction_rename.go:100-240`); позиции цепочек в папках и detour членов папок не переписываются (`:197-234`) | ⚠ правило 1 выполнено частично |
| переименование свёртки (`fold_tag`) | корневое имя | ссылки не переписываются, только предупреждение о выборе (`source_tag_shift_warning.go:15-21`) | ⚠ правило 1 не выполнено |

## 7. Импорт и экспорт

### 7.1 Экспорт

Ссылки пишутся ровно так, как лежат в хранении: файл — сериализация
состояния (`BACKUP.md` §1). `folder_id` — id контейнера на машине-экспортёре,
финальных тегов в файле нет. Лаунчер: `Source10.Detour / Hops / Group` —
`core/backup/backup10.go:92-94`.

### 7.2 Перепись id контейнеров

Карта «id контейнера в файле → id здесь» строится слиянием `sources[]`
(`BACKUP.md` §9): совпавший контейнер отдаёт ЛОКАЛЬНЫЙ id, новый — id из файла
(свежий ULID, если id занят). Через карту проходят:

- контейнеры обоих видов: папки (совпадение по `id`, затем по имени) и
  подписки (совпадение по `url`);
- ссылки всех видов в записях, приехавших файлом: detour узлов, общий detour
  контейнеров, `hops[]`, `group.members[]`.

Ссылка следует и за переименованием, которое делает само слияние. Член
контейнера файла, легший в совпавшую папку под другим тегом (уникализация
`X` → `X-2`) или узнанный по телу в уже стоящем узле с другим тегом, получает
ссылки файла по паре `(локальный id, локальный тег)`. Оставить тег файла
значит молча увести ссылку на тёзку или повесить её — подстановка узла с
другим именем запрещена (`IDENTITY.md` §2.1).

Ссылка на контейнер, которого в файле нет, ввозится КАК ЕСТЬ, без
предупреждения; разбирает её сборка (`BACKUP.md` §6). Лаунчер на сборке
называет её `the referenced source is gone` и выбрасывает носителя detour,
цепочку или члена группы (§5.1 № 2). Кейс — `v10_node_links` (`orphan`).

Лаунчер: перепись — `core/backup/merge.go:482-510`, вызов после слияния —
`core/backup/import.go:388-392`; папка в карте — `merge.go:786-788`.
⚠ Не сделано (§9.3 п. 5): подписка в карту не попадает
(`merge.go:669-702`) — ссылки на члены совпавшей по URL подписки остаются с id
файла и висят; `group.members[]` не переписываются (`merge.go:500-509`); за
уникализацией (`merge.go:726`, `837`) и узнаванием по телу (`merge.go:716-725`,
`827-831`) ссылки не следуют.

### 7.3 Терпимость читателя: `{tag}` на члена контейнера

Старые и чужие файлы несут `{tag}` там, где цель — член папки или подписки
(`BACKUP.md` §4, §6). Это правило ЧТЕНИЯ: форма записи остаётся парой (§2), и
писатель после чтения пишет уже пару.

- **Файл 1.0.** В такой ссылке лежит ФИНАЛЬНЫЙ тег члена. После слияния и
  переписи id (§7.2) читатель поднимает её до пары `{id контейнера здесь,
  сырой тег}`, если тег (а) не занят корнем результата — корневой узел,
  Направление, тег свёртки, известная цель приёмника, зарезервированный
  литерал: корень сильнее члена папки, как и в резолве; (б) совпал с
  `prefix + сырой тег + postfix` РОВНО ОДНОГО члена папки или подписки
  результата — без раскрытия переменных и суффикса уникализации. Совпало
  несколько или ни одного — ссылка остаётся как есть и без предупреждения;
  разбирает её сборка (§5.1 № 6). Поднимаются приехавшие этим файлом `detour`
  узлов, общий `detour` контейнеров и `hops[]`.
- **Файл 0.x.** Позиции корневых цепочек приходят строками и сопоставляются
  с СЫРЫМ тегом члена контейнера: тег не корневое имя и найден ровно в одном
  контейнере; неразобранные записи не адресуются.

Лаунчер: 1.0 — `normalizeMemberLinks10` (`core/backup/import10.go:467-554`),
финальный тег — `TagPolicy.FinalTag` (`core/state/sources_v7.go:78-92`), вызов
— `core/backup/import.go:394-407`, тест — `core/backup/purity_test.go`,
подтест `tag-only links to folder members`; 0.x — `resolveImportedHops`
(`core/backup/convert_v7.go:222-308`). Члены групп без `folder_id` импорт не
поднимает — внутри контейнера их трактует как свои сборка (§5.1 № 8).

### 7.4 Legacy 0.12

- Четвёрка `detour_tag` / `detour_node_source_id` / `detour_node_tag` /
  `detour_node_label` → один NodeLink (`core/backup/convert_v7.go:116-132`):
  есть тег узла (или подпись запасным тегом) → `{folder_id:
  detour_node_source_id, tag}`; только `detour_tag` → `{tag: detour_tag}`.
- `chains[].chain.hops[]` строками → `{tag}`, затем подъём до пары по §7.3
  (`convert_v7.go:134-152`, `222-308`).
- `detour_node_hash` не читается (`BACKUP.md` §6).
- В 0.12 `detour_node_source_id` мог указывать на КОРНЕВОЙ сервер
  (`servers[].id`, `IDENTITY.md` §2.1). В модели 1.0 корневой узел адресуется
  `{tag}`, поэтому норма перевода: id корневого узла → `{tag: <его тег>}`.
  ⚠ Лаунчер ввозит `{folder_id: <id сервера>, tag}` — на сборке это № 2
  (§9.3 п. 6); миграция состояния v6→v7 ту же ссылку сплющивает в корневую
  (`core/state/migration_v6_to_v7.go:607-617`), а тест
  `core/backup/backup_test.go:887-981` закрепляет висячую форму.

## 8. Известный разрыв: Направления

Направление ссылается на узлы финальными тегами:

- `addOutbounds[]` ↔ `directions[].include` — теги корня строками
  (`core/config/configtypes/types.go:345`, `core/backup/directions.go:45-52`,
  `80-87`). Форма лаунчера предлагает там только Направления, `direct-out` и
  тег блокировки (`ui/configurator/outbounds_configurator/configurator_helpers.go:231-240`,
  `edit_dialog.go:238-263`), но сырой JSON Направления и файл бэкапа несут
  любой тег, в том числе тег узла;
- `preferredDefault` ↔ `default` и `filters` ↔ `filter` — регулярки по
  финальному тегу.

Это расходится с направлением владельца «ссылки на ноды — NodeLink везде»:
Направление, в которое узел включён явно, теряет его при смене `tag_policy`,
переносе или переименовании узла, а перепись ссылок узла (§6) такие ссылки не
переписывает, а только называет (`node_move.go:200-208`). Фильтры ссылками не
являются (§4.3) и остаются регулярками.

Целевая форма для случая «цель Направления — узел, а не группа и не системный
тег» — предложение, не норма этого документа:

- **А. Отдельный список.** `include_nodes: [NodeLink]` рядом с
  `include: [string]`; в `include` остаются только корневые имена
  (Направления, свёртки, системные теги). Сторона, не знающая поля, по
  `BACKUP.md` §1 игнорирует объявленное поле молча; тип `include` не меняется.
- **Б. Union.** `include: [string | NodeLink]` — строка = корневое имя,
  объект = узел. Один список сохраняет порядок опций, но меняет тип поля в
  `direction.schema.json`, и старый читатель на объекте спотыкается.

Рекомендация — А; умолчание на конкретный узел (`default_node: NodeLink`) —
тем же решением. Это ОТДЕЛЬНОЕ решение владельца и отдельная реализация у обеих
сторон; в D-112 и в этот документ оно не входит.

## 9. Сверка сторон

### 9.1 Места ссылок

LxBox — по ответу сессии lxbox-3d (14.09.2026). LxBox принял перевод
хранения на NodeLink напрямую: их спека 439, волна B.

| Место | Лаунчер (develop) | LxBox сегодня | Цель по D-112 |
|---|---|---|---|
| личный detour члена папки | ✅ `Node.Detour` — NodeLink | `FolderMember.detour` — финальный display-тег строкой (их §237) | NodeLink |
| detour корневого узла | ✅ NodeLink | `DetourPolicy.override_detour` у `UserServer` — строкой | NodeLink |
| общий detour папки и подписки | ✅ `detour` записи контейнера | `DetourPolicy.override_detour` у папки и подписки плюс режим `replace_detour_chain` APPEND / REPLACE — строкой | цель — NodeLink или корневое имя (§5.2); режим APPEND / REPLACE — поле стороны (`ONE_NAMESPACE.md` §1) |
| позиции цепочки | ✅ `hops[]` — NodeLink; ⚠ цепочка позицией не разрешается (§5.2) | `SourceChain.hops` строками: узел, группа подписки, Направление, служебный тег, цепочка (позиция 0, выше по списку) | узел → пара; Направление, служебный тег, свёртка, цепочка корня → `{tag}` |
| члены группы | ✅ `group.members[]` — NodeLink; ⚠ группа адресуется финальным тегом (§5.2) | `lxauto://`: `ExplicitMembers.keys` — ключи `protocol\|server\|port\|credential`, не теги; `RuleMembers include/exclude` — регулярки по финальному тегу | явные члены → NodeLink (ключ содержимого идентичностью не является, SPEC 112); правила-регулярки — не ссылки, остаются полем стороны |
| `default` группы | ⚠ сырой тег строкой (§9.3 п. 4) | сверить: в ответе не назван | — |
| Направление → узел | ⚠ финальный тег (§8) | `Direction.include` — теги Направлений; `node_filter` / `default_filter` — регулярки | вне D-112 (§8) |
| бэкап | ✅ пишет и читает пару; ⚠ перепись §7.2, legacy §7.4 | `hops[]` — `{tag}` без `folder_id` (описание `$defs/nodeLink`), `detour` не применяет (`BACKUP.md` §2) | пара, перепись по §7.2 |
| API и рантайм | `GET /state/full`, `GET /backup/export` — ссылки как лежат; `/chains/{tag}/probe`, выбор в селекторе — финальный тег | Debug API `/folders/{id}/members/{index}` — индекс; Intent `SWITCH_NODE`, выбор узла в Направлении (Clash API / cache.db) — финальный тег | адрес узла хранения — NodeLink; рантайм — финальный тег |

Переход LxBox «было → стало» по пунктам — `TASKS_LXBOX.md` `## 17`.

### 9.2 Не ссылки — обе стороны согласны

- LxBox: `Direction.node_filter` / `default_filter` — регулярки по финальному
  тегу; `Direction.include` — теги Направлений; цель правила, `route_final`,
  `detour` DNS-серверов и outbound DNS-переменных — только Направления,
  `direct` или `reject`; `import_rules` — JSONPath над emit-JSON;
  `disabled_hashes` подписки; секции Tailscale — `@self`.
- Лаунчер: те же классы — финальные теги внутри `body` и регулярки (§4.2,
  §4.3). Целей правил у него больше — Направления, их addOutbounds, теги
  пресетов, глобальные outbound'ы шаблона, литералы `direct` / `reject` /
  `drop` (`SPECS/features/directions.md` §1); узлы целью правила не
  предлагаются ни одной стороной.

### 9.3 Расхождения лаунчера с этой нормой

Вне этого документа, к исправлению отдельными задачами:

1. Цепочка как позиция не разрешается на каноническом пути — вложенные
   цепочки не собираются вовсе (§5.2): `core/config/nodelink_resolve.go:61-150`,
   `core/config/outbound_generator.go:1449-1457`.
2. Две цепочки в одной папке → один outbound с тегом первой и позициями
   последней: `core/config/canonical_emit.go:282-316`,
   `core/config/chain_nodes.go:43-54`.
3. Провайдерская группа адресуется финальным тегом, а не сырым — ссылка на неё
   рвётся при смене `tag_policy`: `canonical_emit.go:242-246`,
   `nodelink_resolve.go:176-181`, `ui/configurator/tabs/source_chain_hops.go:216-220`.
   Исправление требует переписи уже сохранённых ссылок.
4. `default` группы — строка, а не NodeLink; разрешается в контейнере первого
   члена (`nodelink_resolve.go:492-499`). Для провайдерских групп, где все
   члены в одном контейнере, это работает; форма расходится с нормой.
5. Импорт: подписка не входит в карту id; `group.members[]` не
   переписываются; ссылки не следуют за уникализацией и узнаванием по телу
   (§7.2): `core/backup/merge.go:500-509`, `669-702`, `716-726`, `827-837`.
6. Legacy 0.12: id корневого сервера ввозится в `folder_id` (§7.4):
   `core/backup/convert_v7.go:116-132`, тест `core/backup/backup_test.go:887-981`.
7. Операции (§6): корневой узел при переименовании СБРАСЫВАЕТ ссылки, а не
   переписывает, и не все; Направление переписывает только корневые записи;
   свёртка не переписывает ничего; удаление узла контейнера снимает detour и
   укорачивает цепочку, тогда как сборка на висячую ссылку отвечает
   fail-closed.
8. Заливка подписки в папку: detour копий-релеев остаётся на узлах подписки —
   копия выпадет, если подписку выключить или удалить
   (`ui/configurator/business/folder_fill_subscription.go:102-133`).

### 9.4 Что сверить с LxBox

- «Группа подписки» в позициях LxBox — провайдерская группа внутри подписки
  (узел → пара) или группа свёртки (`fold_tag` → `{tag}`)?
- `default` группы и `replace_detour_chain` — где хранятся и в какой форме.
- `disabled_hashes`: по `IDENTITY.md` §5 и статусу зеркала ключ отметки —
  тег с 02.09.2026, в ответе — «по хэшу узла». Какой ключ сейчас?
- Debug API `/folders/{id}/members/{index}` — переходит на NodeLink или
  остаётся индексной рантайм-диагностикой.
- Цепочки внутри папок у LxBox не поддержаны (`v10_sources_union.expected.lxbox.json`:
  `backup_source_kind_unsupported`) — D-112 этого не меняет.

## 10. Кейсы корпуса

| Кейс | Что закрепляет |
|---|---|
| `corpus/backup/v10_node_links` (+ `.pre`) | detour корневого узла и члена папки на члена папки — парой; позиции вперемешку: корневой узел, член папки с префиксом (тег сырой), член подписки; Направление позицией 0 и целью detour — `{tag}`; ссылка на неизвестный контейнер ввозится как есть без предупреждения; перепись `folder_id` папки, совпавшей по ИМЕНИ, — и у `detour`, и у `hops` |
| `corpus/backup/v10_sources_union` (+ `.pre`) | перепись `hops[].folder_id`: папка по имени и по `id` |
| `corpus/backup/chains_roundtrip`, `chain_tag_duplicate`, `chain_disabled_enabled_default` | legacy 0.12: позиции цепочек строками |

Ключи ожиданий — `corpus/backup/README.md`: ссылка в `chains[].hops[]` и в
`detours{}` — `{tag, folder | subscription | folder_id}`.

Корпус бэкапа проверяет импорт; поведение сборки (§5) им не покрывается.

Пока не закреплено — добавляется вместе с исправлениями §9.3: подписка,
совпавшая по URL, с другим id — ссылка на её член; `group.members[]` в папке,
совпавшей по имени; ссылка на члена, которого слияние уникализировало или
узнало по телу; legacy 0.12 detour на корневой сервер по
`detour_node_source_id`. Терпимость §7.3 у лаунчера закреплена тестом
(`core/backup/purity_test.go`), кейсом корпуса — нет.
