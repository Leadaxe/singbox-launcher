# BACKUP — семантика LX Backup v1

Нормативно. Схема — `schema/backup.schema.json`. Конверт и процедуры — эволюция
бэкапа LxBox §040 (категории, preview, merge/replace, default-deny allowlist §159);
лаунчер реализует файловый экспорт/импорт впервые (фаза 4).

## 1. Инварианты

1. **Lossless round-trip:** `import(export(x))` в том же приложении = `x`.
   Непереносимое едет в `extensions.<app>` (top-level или внутри сущности);
   принимающая сторона обязана **сохранить чужой блоб нетронутым** до следующего
   экспорта (лаунчер — новое поле state v6; LxBox — новый ключ allowlist).
2. **Default-deny:** неизвестные ключи вне `extensions` — в предупреждение
   пользователю, не в состояние.
3. **Нет молчаливых потерь:** всё, что не применяется, либо в `extensions`,
   либо предъявлено пользователю warning'ом.

## 2. Переносимые сущности

| Секция | Примечания |
|---|---|
| `subscriptions[]` | url, label, tag-политика, update, disabled-отметки, detour. Mobile-only поля подписки (`import_rules`, `identity_override`) — в `extensions.lxbox` записи |
| `servers[]` | ровно одно из `uri` / `config_json`; папки LxBox — в `extensions.lxbox` |
| `rules[]` | `kind: inline\|srs\|preset\|json`; ось порядка `num` — импортёр перенумеровывает, сохраняя относительный порядок; mobile-only матчеры (`packages`, `wifi`) — в `extensions.lxbox` правила |
| `dns` | servers/rules c kind-дискриминатором `template\|preset\|user`, final, strategy |
| `vars` | только portable-имена из `registry/vars.json` |
| `route.final` | символическая ссылка (см. §3) |
| `warp[]` | WG/MASQUE-аккаунты; имена полей — канонические из схемы (`type: wg\|masque` + snake_case-поля регистрации), маппинг в нативные при импорте. Merge не перетирает живую регистрацию принимающей стороны: адреса Cloudflare привязаны к ключу |
| `chains[]` | цепочки хопов (SPEC 110, схема v1.2): `tag` + канон `chain` (`schema/source_chain.schema.json`); порядок записей нормативен, разбор — после `directions[]`, до `rules[]` |

**Цепочки хопов (SPEC 110, схема v1.2).** Запись `chains[]` — это `tag`,
опциональные `label`/`enabled` и канон цепочки в поле `chain`
(`schema/source_chain.schema.json`), без дублирования его полей.

*Идентичность и merge — по `tag`.* Это тег будущего outbound'а: на него
ссылаются `rules[].outbound`, `route.final`, фильтры Направлений и позиции
других цепочек — у лаунчера он же хранится именем источника (`Label` ≡
`TagMask`), у LxBox это `SourceChain.tag`. Занятый тег не перезаписывается:
у пользователя там свои настройки, и своя одноимённая цепочка сильнее
приехавшей. Пропуск предъявляется warning'ом `backup_chain_exists`
**всегда** — молчаливое «своя победила» скрыло бы случай тёзок (ниже).
Тег применённой цепочки пополняет список известных целей: правило, метящее
в цепочку из того же файла, приходит рабочим (симметрия с §3).

*Тег — не стабильный идентификатор, и это осознанная цена v1.* Он меняется
переименованием и уникализируется на сборке при коллизии. Следствия:

- цепочка, переименованная на другой стороне, приедет ВТОРОЙ записью рядом
  со своей — теги разошлись, и распознать их как одну и ту же нечем;
- две несвязанные цепочки, случайно названные одинаково на разных
  устройствах, при merge считаются одной: приехавшая не применяется
  (warning `backup_chain_exists`).

Оба случая — не баг реализации, а отсутствие стабильного id у цепочки на
обеих сторонах; отдельный `id` в канон не заводится, пока дубли не окажутся
реальной болью. Цепочка без имени получает при экспорте эффективный
позиционный тег (`chain-N`), который импорт фиксирует именем, — нормализация
той же категории, что перенумерация `num` у правил.

*`label`* — отображаемое имя, только если отличается от тега; идентичности
не несёт. Сторона без отдельного понятия подписи (лаунчер) не пишет его и
не применяет, но провозит чужое значение нетронутым до следующего экспорта —
по общему правилу §2 о непонятых полях записи.

*Порядок записей нормативен* и не сортируется ни на экспорте, ни на импорте:
вложенная цепочка вправе стоять позицией только у цепочки, объявленной НИЖЕ
её по списку, — этим исключены циклы (тот же приём, что запрет ссылок
вперёд при сборке). Импорт разбирает `chains[]` ПОСЛЕ `directions[]`
(позиция может ссылаться на Направление) и ДО `rules[]` (правило может
метить в цепочку как в цель).

*Достижимость позиций `hops` на импорте не проверяется:* хоп — чаще всего
узел подписки, который материализуется только после её обновления, и
честной проверки в момент импорта не существует. Цепочка ввозится как есть;
рубеж у обеих сторон один — сборка конфига, где цепочка с недостающей
позицией деградирует целиком с внятной причиной (`chain_hop_missing`), не
роняя остальной конфиг. Изобретать импортное «выключить на всякий случай»
сторонам запрещено — поведение разъедется.

*Legacy (только лаунчер):* его релизы v1.5.0–v1.5.1 писали цепочки блобом
`extensions.launcher` (ключ `chains`, внутренняя структура `state.Source`).
Лаунчер читает это место как fallback при импорте старых файлов; писать в
него больше нельзя. LxBox этот блоб, как и любой чужой, провозит нетронутым
(§1) — понимать его он не обязан.

**Поля записи, которые сторона не понимает** (например, `skip`/`detour`
LxBox для лаунчера), сохраняются вместе с per-entity `extensions.<app>`
и возвращаются на место при re-export — вместо молчаливой потери (§1).

## 3. Символические ссылки на outbound-цели

`rules[].outbound` и `route.final` переносятся как есть.

**Схема v1.1 (SPEC 104).** Цели едут вместе с правилами: корневой ключ
`directions[]` содержит Направления в канонической форме
(`schema/direction.schema.json`). На импорте они разбираются ПЕРВЫМИ и
пополняют список известных целей, поэтому правило, чья цель приехала в том
же файле, приходит **рабочим**, а не выключенным.

Отбор узлов переносится ТЕЛОМ регулярного выражения без обёртки и флагов:
язык паттернов у сторон различается (`/re/i` в лаунчере, `RegExp` в LxBox),
а тело одинаково. Регистр не учитывается на обеих сторонах.

Если тег Направления на принимающей стороне уже занят, приехавшее **не
применяется** (warning `backup_direction_exists`): у пользователя там свои
настройки под этим именем, и перезапись стёрла бы их. Правила при этом
находят цель — тег совпадает.

Если целевой тег не существует и не приехал в файле (неизвестный preset id
вне `registry/presets.json`, ссылка на удалённое Направление):

- правило импортируется **выключенным** с warning;
- `route.final` **не применяется** с warning.

Причина именно такая: включённое правило с мёртвой целью заставляет ядро
отвергнуть весь конфиг, то есть одно потерянное правило лишило бы
пользователя VPN целиком.

## 4. Disabled-отметки

Перенос **только по identity-хешу** (`IDENTITY.md`); значения — unix seconds,
импортёр конвертирует в свой нативный формат (Go int64 / Dart ISO-8601).
Честные ограничения: отметки нод, патченных mobile import-rules (§302), и нод,
схлопнутых по SNI, на другой стороне не совпадут — истекают по штатному TTL,
пересопоставление в v1 не строится (D-014).

## 5. Секреты

Открытым текстом — by design (граница доверия — локальная машина, как в
state.json / lxbox_settings.json). Шифрование архива — вне scope v1;
предупреждение при экспорте обязательно.

## 6. Версионирование

`lx_backup: 1` — мажор формата. Импортёр отклоняет бо́льшую мажорную версию
с понятной ошибкой («обновите приложение»), меньшую — читает.
