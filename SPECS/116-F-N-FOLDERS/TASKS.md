# TASKS — SPEC 116, этап 3 (папки в UI)

Чекбоксы по волнам `PLAN.md`. Один агент = одна волна.
Строки в адресах проверять грепом по имени функции.

## Предусловие — решения §O

Вердикты капитана, 2026-08-29: всюду принят рекомендованный вариант —
ни один не вводит нового визуала и не трогает contract/**.

- [x] §O1 = **А** — warning экспорта поднять в UI-отчёт бэкапа; трек
      `folders[]` в контракте (В) — отдельной задачей вне этапа.
- [x] §O2 = **А** — финальные теги через существующий `EmitNodeJSONs`.
- [x] §O3 = **А** — обратную заливку JSON → папка в этап 3 не берём.
- [x] §O4 = **А** — расширить существующий Preview, новых вкладок нет.
- [x] §O5 = **А** — строку папки не декорировать (отличие — нет URL в
      подстроке); метка/иконка — дешёвая доработка позже, если попросят.
- [x] §O6 = **А** — «Add folder» первым пунктом меню ⋮.

## W1 — merge-заливка в папку (data-критично)

- [x] Выделено общее ядро merge из `MergeSubscriptionNodes`
      (`core/state/subscription_merge.go:50`), поведение подписки не изменилось
      (общая половина — `refreshMergedNode:43`; `subscription_merge_test.go`
      зелёный без правок ожиданий)
- [x] `MergeFolderNodesFromSubscription` — ключ (`origin.subUrl`, сырой тег)
- [x] Совпал → body/origin.raw освежены, `enabled`/`detour`/позиция целы
- [x] Новый → добавлен включённым, `origin.subUrl` проставлен
- [x] Исчезнувший → **разыменован**, не удалён; warning «исчез у провайдера»
- [x] `Truncated` → разыменование не выполняется
- [x] `trusted == false` → `nodes[]` не тронуты
- [x] Тест `core/state/folder_merge_test.go` — пять случаев (A5)
- [x] Порядок ключей body не пересобран через `map`; го1.20-гард чист

## W2 — перенос узла между контейнерами

- [x] `business/node_move.go`: `CopyNodeToFolder` (subUrl сохраняется)
- [x] `MoveNodeToFolder` — отказ, когда исходный контейнер подписка
- [x] `ExtractFolderNodesToRoot` — порядок и `enabled` сохранены (узлы встают
      сразу за папкой; папка остаётся пустой — удалять её решает UI)
- [x] `DereferenceNodeOrigin` — общая точка обнуления `origin.subUrl`
      (origin пересаживается на клон, копия не отвязывает оригинал)
- [x] Коллизия сырого тега в целевом контейнере разрешается суффиксом
      (`-2`, `-3`… — та же форма, что у уникализации на эмиссии)
- [x] Побочки (`BumpRevision`/`InvalidateNodePool`) оставлены вызывающему
- [x] Тест `node_move_test.go` — критерии A3, A7 (8 случаев, зелёные)
- [x] Реестр переписи: `repointNodeLinks` — detour/hops/members/default;
      неперезаписываемые ссылки корня (цели правил, route.final, DNS-detour,
      addOutbounds, preset-vars) НАЗВАНЫ в возврате `MoveNodeToFolder` (A3)

## W3 — создание и удаление папки

- [x] «Add folder» в меню ⋮ (`source_tab.go:271`), место по §O6 — ПЕРВЫМ пунктом
- [x] Вызван существующий `corestate.NewFolderSource` (`sources_v7.go:267`);
      окно правки НЕ открывается (пустая папка — законное состояние, в отличие
      от цепочки)
- [x] Свободное имя по умолчанию — `business.NextFolderName` (`sources.go:196`);
      это ИМЯ контейнера (`Source.Name`), не тег: занятость считается по именам
      папок и подписок
- [x] Мутация через `applySourceMutation` (`source_tab.go:816`)
- [x] Удаление папки — диалог двух исходов `showFolderDeleteDialog`
      (`source_tab.go:909`); «вынести узлы» зовёт `ExtractFolderNodesToRoot` (W2),
      затем сносит опустевшую папку; папка адресуется ULID'ом на клике, не
      индексом строки. Предупреждение о протухшем выборе — существующий
      `showStaleSelectionDialog` (только когда у папки была тег-политика)
- [x] Строка списка: имя из `Source.Name` + счётчик; у пустой папки явное
      «· 0 nodes», пометка «не дал узлов» подавлена (пустота — воля
      пользователя, не сбой)
- [x] Кнопки обновления у папки нет (она и была под `isSubscription`)
- [x] §O5 соблюдён: строка папки не декорирована — ни метки, ни иконки

## W4 — настройки папки в окне источника

- [x] Третья ветка `rebuildSettingsLayout` (`source_edit_window.go:847`):
      имя, prefix, postfix, подсказка переменных, свёртка, detour. Развилка
      переписана с `if isServer/else` на `switch` по kind; общие блоки
      вынесены в `tagPolicyBlock`/`detourBlock` (`:828`, `:842`) — подписка и
      папка делят их дословно, а не копией
- [x] Ни URL, ни интервала, ни max_nodes, ни skip у папки (A8)
- [x] Проверены все употребления `isServerSource`: JSON read-only
      (`:1257` `!isServerSource` — верно для папки, §O3), кнопки Apply/Regen
      (`:1513` — папке не положены), `syncFoldTabVisible` (`:1549` — условие
      вычитанием, папку пропускает само), подсказка вкладки JSON (`:1506` —
      **был текст про провайдера, заведена ветка папки**), пустой контейнер
      (`:1477` — «подписку не обновляли» заменено на «в папке нет узлов»)
- [x] Ветка заголовка окна для `SourceKindFolder` (`:426`); заголовок —
      «Folder — имя», а не «Source — » (A8)
- [x] Имя пишется в `Source.Name` (`nameEntry`, `:520`; OnChanged гейтит по
      kind — у узловых kind'ов Name нелегален, у подписки его даёт fetch);
      `displayName()` и `SourceDisplayName` согласованы — второй теперь
      предпочитает `Name` у контейнеров (`business/detour_refs.go:111`)
- [x] Вкладка «Группа» (Replace) у папки появляется/исчезает по галке
      (`syncFoldTabVisible`, правки не потребовалось); подпись галки своя —
      «Fold this folder into a group», механика общая

## W5 — список узлов и операции над узлом

- [x] Меню узла (`showPreviewNodeContextMenu`, `preview_node_info.go:39`)
      получает СЫРОЙ тег отдельным параметром (`identities[id]` вызывающего,
      не `node.Tag` — тот финальный) и контекст операций `*previewNodeOps`
- [x] Пункты Move to / Copy to / Rename / Delete (`preview_node_ops.go`)
- [x] У подписки: только Copy to. `nodeOpsAllowed` = «kind == folder», не
      «kind != subscription»: у server/chain/auto СОСТАВА нет — их единственный
      узел это сам Source, он переименовывается полем формы и удаляется
      кнопкой строки Sources, и второй путь к тем же действиям разъехался бы
      с первым (у формы есть сброс ссылок на Save, у меню его не было бы)
- [x] Операции зовут W2 и затем существующий `applySourceMutation`
      (`source_tab.go:878` — `MarkAsChanged` + `BumpRevision` +
      `InvalidateNodePool` + перерисовки + пересчёт счётчиков)
- [x] Перепись ссылок — реестр W2. **Отклонение от формулировки задачи,
      осознанное**: `resetRefsAfterNodeRename:1655` живёт дальше и правится не
      было — он путь Save для ВЕРХНЕГО узла и зовёт `ResetDetourNodeRefs`,
      который (а) ГАСИТ ссылку, а при переименовании на месте узел никуда не
      делся и её надо ПЕРЕПИСАТЬ, (б) знает только `Source.Detour`, а у узла
      папки ссылок четыре вида. Поэтому rename/delete узла контейнера идут
      реестром W2 — `business.RepointContainerNodeLinks` /
      `ClearContainerNodeLinks` (`node_move.go`), обе поверх `repointNodeLinks`.
      Диалоги при этом СУЩЕСТВУЮЩИЕ, своих не заведено
- [x] Предупреждение о протухшем выборе — существующий
      `showStaleSelectionDialog:1734`; у **copy** его НЕТ (оригинал на месте
      под прежним тегом, пугать сбросом выбора было бы враньём)
- [x] Авторазыменование при правке тега (Rename) — `applyRename` зовёт
      `business.DereferenceNodeOrigin` ДО смены тега
- [x] Авторазыменование при правке body / Regen (`source_body_edit.go:40`,
      `:71`) + уведомление `dialogs.ShowAutoHideInfo`. Признак «связь была»
      снимается ДО материализации: та пересаживает `Origin` целиком, и после
      неё узнать уже негде. Разыменование делает ОБЩАЯ точка, а не побочка
      материализации — иначе правка материализатора тихо отменила бы правило
- [x] Комментарий в коде: шапка `preview_node_ops.go` — почему Move/Copy
      применяются к модели сразу (затрагивают ДВА источника, а scratch знает
      один; буферизация оставила бы два экземпляра узла с одним сырым тегом
      после Cancel), и `reloadScratch` в `source_edit_window.go:929`
- [x] Тест авторазыменования (A4) — `ui/configurator/tabs/node_dereference_test.go`
      (правка тела / Regen снимают связь; узел без `subUrl` не меняется);
      плюс реестр rename/delete — `business/node_rename_delete_test.go`,
      плюс арифметика перестановки — `tabs/node_reorder_test.go`
- [x] Drag-n-drop узлов внутри списка контейнера; через границу нельзя (П5).
      Только у папки: порядок узлов подписки задаёт тело провайдера, и
      перестановка потерялась бы на первом merge. Границу drag не пересекает
      по построению — группа знает только слоты своего списка.
      **Потребовалось расширить `fynewidget.DragReorderGroup`**: список узлов —
      первый drag-список на `widget.List` (у Sources/Rules/DNS строки живут в
      VBox и строятся целиком), а тот ПЕРЕИСПОЛЬЗУЕТ объекты строк. Добавлены
      `RegisterRecycled` (карта индекс→строка держится биекцией — иначе два
      индекса заявляют одну полосу экрана), `DragHandle.SetIndex`
      (перепривязка захвата на каждой привязке строки) и поле `Total` +
      `slots()` (кламп броска по СПИСКУ ДАННЫХ, а не по видимым строкам).
      Поведение VBox-списков не изменилось: `Total == 0` → `slots() == count()`

## W6 — наполнение папки

- [x] Общее ядро «текст → узлы» вынесено из `AppendURLsToSources`
      (`business/sources.go:35`); второго разбора не заведено
      → `business/source_input.go` `parseSourceInput:70`. `AppendURLsToSources`
      сжалась до «куда положить»: разбор, классификатор и материализация у
      корня и у папки теперь физически одни. Страховка — тест
      `TestParseSourceInput_SameNodesForRootAndFolder` (сравнивает тег, тело и
      origin у узла, приехавшего двумя путями).
- [x] `AppendNodesToFolder` кладёт узлы в `Source.Nodes` папки
      → `business/folder_fill.go:54`. Возвращает `FolderFillResult`
      (сколько легло + сколько подписочных строк отброшено): смешанный вход не
      роняется целиком, но и не глотает строку молча.
- [x] Материализация через существующий `config.MaterializeServerNode`
      (единственный вызов — в ядре разбора, `source_input.go:104,129`)
- [x] Уникализация сырого тега в пределах папки — общим `uniqueTagIn`
      (`node_move.go:628`), тем же, что у move/copy: `X`, `X-2`, `X-3`
- [x] «Add nodes…» в окне папки: вставка текста, из файла, Add WARP, Add server
      → `tabs/folder_add_nodes.go`, кнопка в шапке списка узлов вкладки Preview
      (`folderAddNodesHeader`, вшита в `source_edit_window.go:948`).
      **Потребовалось передать окно-владельца в `ShowAddWarpDialog` /
      `ShowAddServerDialog`**: окно источника — отдельное `app.NewWindow`, и
      диалог, прибитый к главному окну визарда, всплывал бы за спиной у
      пользователя. Параметр `owner fyne.Window`, `nil` = прежнее поведение.
- [x] Тег из имени файла при отсутствии `#fragment`
      → `business.TagFromFileName` + флаг `Unnamed[i]` в разборе: имя файла
      достаётся ТОЛЬКО безымянному узлу, свой `#fragment` сильнее.
- [x] Импорт мультивыбором
      **Потребовалось расширить `internal/platform`**: `PickOpenFiles` +
      `pickOpenFilesNative` на все три ОС (AppleScript `with multiple
      selections allowed`, WinForms `Multiselect`, zenity `--multiple`) и общий
      `splitPickedPaths` (перевод строки — единственный разделитель, которого
      не бывает внутри пути). У kdialog мультивыбора без разделителя нет —
      его ветка остаётся одиночной осознанно.

## W7 — заливка подписки в папку

- [x] «Fill from subscription…» в окне папки; выбор из существующих подписок
      (`tabs/folder_fill_from_sub.go:showFillFromSubscriptionDialog`) — пятым
      пунктом того же ⋮-меню «Add nodes…», за разделителем: три первых кладут
      принесённое пользователем, этот — уже добавленное в «Источниках»
- [x] Материал — уже материализованные `sub.Nodes`, тело не парсится заново
      (`business/folder_fill_subscription.go:FillFolderFromSubscription`);
      узлы копируются `cloneCanonicalNodeForMove` — узел папки не делит
      `Body`/`Hops` с живым узлом подписки
- [x] `Truncated` берётся из `sub.UpdateStatus` и передаётся в merge
- [x] Подписка без узлов → сентинел `ErrSubscriptionNotFetched`, диалог с
      предложением обновить существующим `refreshOneSourceFromUI`; заливки нет.
      Дозаливки по завершении обновления НЕТ намеренно — повтор за
      пользователем
- [x] Auto-узлы: members переуказаны на узлы папки по сырому тегу; непопавшие —
      prune + warning (`core/state/subscription_merge.go:repointFolderAutoMembers`,
      вызов в хвосте `MergeFolderNodesFromSubscription`). Копией члена считается
      узел папки ЭТОЙ заливки (`origin.subUrl == subURL`), а не любой
      одноимённый: тег, отбитый коллизией, занят чужим узлом, и увести член на
      него значило бы подменить состав группы. `Group.Default`, выпавший из
      состава, снимается тем же проходом
- [x] Повторная заливка идёт тем же путём (отдельного «обновить папку» нет) —
      merge идемпотентен, второй проход даёт `Changed=false`
- [ ] Сценарий С5 пройден руками (сборка приложения) — **не выполнен**: W10
      запускать сборку запрещено, остаётся за капитаном вместе с С1–С8.
      Data-критичная половина С5 закрыта машинно — `folder_merge_test.go`
      (все пять случаев merge) и `folder_fill_subscription_test.go`

## W8 — «взять всю папку → JSON»

- [x] «Copy nodes as JSON» в окне папки — кнопка на вкладке JSON
      (`folder_copy_json.go:folderCopyJSONButton`, встраивание
      `source_edit_window.go:1624`). Не в «Add nodes…»: там наполнение, здесь
      обратное направление, и текст уезжает ровно тот, что показан на вкладке
- [x] Эмиссия через `config.EmitNodeJSONs` (`outbound_generator.go:1040`),
      второй эмиссии не заведено: общий сборщик документа
      `unpackNodesDoc` (`source_edit_json.go:72`), из него же кормится показ
      вкладки (`renderUnpackedNodes`). Разница одна — лимит: показ режется
      `previewNodeCap`, выгрузка идёт `limit=0`, иначе «взять всю папку»
      отдало бы не всю папку
- [x] Теги по решению §O2 (вариант А, финальные): узлы берутся из
      `config.EmitCanonicalSource` — тег-машина папки отработала (префикс/
      постфикс, `{$num}`, уникализация `-2`); `tagCounts` — свой пустой, чтобы
      документ уникализировался сам в себе, а не в контексте чужих источников
- [x] Результат принимается `sing-box check` как фрагмент конфига (A6) —
      проверено на папке с двумя одноимёнными vless (`F-alpha`/`F-alpha-2`) и
      wireguard (уехал в `endpoints[]`): `bin/sing-box check -c` → exit 0
- [x] Пустой выгрузки в буфер не бывает: 0 эмитируемых узлов — сообщение
      вместо копирования, и причина названа раздельно («все выключены» vs «не
      собрались»); частично отброшенное названо вслух в уведомлении

## W9 — бэкап

- [x] Реализован выбранный вариант §O1 — warning экспорта поднят в UI-отчёт:
      `Warning` получил `Kind`+`Nodes` (`core/backup/import.go:21`), потери
      «целиком» встают в начало списка (`export.go:129`), UI рисует их
      фразой `unsupportedSourceWarnText` (`settings_backup.go:302`)
- [x] Экспорт **никогда** не отдаёт успешный файл, молча потерявший папку (A9)
      — `backup.Export` зовётся ровно из одного места, и после записи файла
      отчёт показывается всегда, когда есть хоть одно предупреждение
- [x] Потеря видна в окне отчёта бэкапа — `showExportReport`
      (`settings_backup_report_window.go:122`) поднимает тот же список в окно
      импортного отчёта БЕЗ обрезки; прежняя модалка резала на 10 строках и
      отсылала за хвостом в отчёт импорта, которого при экспорте не бывает
- [x] `contract/**` не тронут — `Kind`/`Nodes` живут в launcher-локальном
      `backup.Warning`, коды предупреждений прежние

## W10 — прогон

- [x] `go build ./...`, `go vet ./...`, `go test -count=1 ./...` — зелено по
      всему модулю (шум линковщика `-lobjc` не в счёт); `go test ./core/build`
      зелёный отдельно
- [x] `ETALON_V6MIG=1` — **ровно одно** задекларированное расхождение Р2
      (`[P]auto` → `[P]select-auto`, те же три строки); `capture` не
      запускался, эталон не тронут
- [x] Го1.20-гард: греп `slices.` / `maps.` / `errors.Join` / `min(` / `max(` /
      `clear(` / `PathValue` по диффу этапа (база `962fdba`, 9158 строк
      дельты) — 4 попадания, **все в комментариях** вида «go1.20-гард: без
      slices.Insert»; употреблений 0
- [x] Новые строки через `locale.T`/`Tf`; ключи в `bin/locale/ru.json` —
      `l10n_check` hard fails 0, добавлено 14 недостающих ключей
      (`Regen from raw`, `Group tag`, `MTU`, подсказки вкладки JSON, тексты
      отчёта миграции и др.). Оставшиеся 4 missing — в `ui/traffic/toolbar.go`,
      вне границ этапа
- [ ] Сценарии С1–С8 прогнаны на собранном приложении (`build_darwin.sh -i`)
      — **НЕ ВЫПОЛНЕНО**: запуск сборки этой волне запрещён, остаётся за
      капитаном. От него ждут подтверждения A1, A2, A8
- [x] `CODEMAP.md` дополнен новыми адресами — сверены ВСЕ `*.go:NN` карты;
      поправлены три уехавших (`MergeSubscriptionNodes:80→94`,
      `MergeFolderNodesFromSubscription:175→188`,
      `repointFolderAutoMembers:288→301`), остальные попадают в шапку
      док-комментария своей декларации (конвенция карты)
- [x] `SPECS/features/sources.md` сверен с реализацией — исправлено
      расхождение: обещанного пути «создание цепочки (Chain) в папке» нет,
      путей наполнения четыре; записано, как цепочка в папке появляется на
      самом деле (заводится в корне, переносится move to)
- [x] `docs/release_notes/upcoming.md` — папки и Unsupported-узлы, EN и RU
- [x] Итоговый отчёт — `SPECS/116-F-N-FOLDERS/IMPLEMENTATION_REPORT.md`
      (файлы по волнам W1–W12, решения, A1–A10 со статусом, судьба W11/W12)

## W11 — Unsupported-узлы и диагностика источника (утверждено Сашей 2026-08-30)

Класс: `Unsupported extends Node` — reason:String; body нет, detour
неприменим; origin ОБЯЗАТЕЛЕН (raw = запись как пришла); tag из записи,
иначе позиционный "unsupported-N"; enabled всегда false, включение
запрещено. Меняет логику → править SPECS/features/sources.md тем же
коммитом.

- [x] Fetch: отбракованная запись материализуется Unsupported-узлом на
      своей позиции nodes[] — `subscription.RejectedBodyRecord`
      (`parse_body.go`, `bodyParseState.reject`) + вставка на позицию в
      `config.MaterializeSubscriptionBody` (`fetch_materialize.go`).
      Записи без пофрагментного исходника узлом не становятся (показывать
      нечего) — остаются warning'ом, как раньше. Достоверность ответа
      считается по новому `SubscriptionFetchMaterial.Supported`, а не по
      длине Nodes: иначе HTML-страница вместо подписки объявилась бы
      достоверной и снесла бы весь состав. Оранжевый блок «Why nodes were
      rejected» из Preview убран — роль взяли ⚠-строки; остальная
      диагностика (кап, merge, потерянные члены) переехала в Overview
- [x] Эмиссия: пропуск ДО тег-машины — структурно, в проекции
      (`state.(*Source).canonicalProjection`): Unsupported в сборочную форму
      не едет вовсе, поэтому ни {$num}, ни слот уникализации не потребляет.
      Целью NodeLink быть не может (среди узлов сборки её нет — резолв
      отрабатывает как на отсутствующую). В бэкап не попадает ни узлом, ни
      отметкой выключенного (`exportDisabledMap`, `resolveImportedHops`);
      выгрузка JSON идёт той же эмиссией, поэтому исключена автоматически
- [x] Merge: ключ — сырой тег (общий `refreshMergedNode`); починенная запись
      оживает обычным узлом на том же месте и ВКЛЮЧЁННОЙ — `enabled=false`
      неразобранной записи это не выбор пользователя. Обратный переход
      (узел сломался) выключает её и снимает detour с warning
- [x] Preview: строка Unsupported — чекбокс пустой и задизейблен, подстрока
      «⚠ причина» цветом Warning вместо протокола, полный текст + исходник —
      тултип по наведению (`SecondaryTapWrap` получил `ttwidget.ToolTipWidget`:
      строка списка собрана из canvas-объектов, у которых своего hover нет)
- [x] Preview: чекбоксы групп (Auto) работают — строки теперь строятся по
      составу `nodes[]` (`buildPreviewRows`), а не по эмиссии, и сырой тег
      группы берётся из состояния; `config.NodeIdentity` у групп пуст по
      построению (SPEC 112), из-за чего чекбокс и был задизейблен. Тем же
      куском починен выключенный узел: он проходил эмиссию не всегда и
      исчезал из списка — вернуть галку было нечем
- [x] Preview: клик по узлу — название + исходник (`showPreviewRowInfoWindow`,
      `preview_row_view.go`); полный разбор («Node info…») остался в меню
- [x] Overview: поузловой блок `nodeOriginList` убран (дублировал Preview
      хуже неё самой); счётчик — `sourceNodesHeader` («Nodes: 38 + 5
      unsupported»); кнопка Reload с галками base64 / urldecode /
      pretty-print и копированием — `appendRawBodySection`
      (`source_edit_raw_body.go`): скачивает тело ЗАНОВО и только показывает,
      nodes[] и updateStatus не трогает. Storage record и вкладка JSON не
      тронуты
- [x] Тесты: `core/config/unsupported_node_test.go` (эмиссионный инвариант —
      {$num} и слот уникализации; форма узла на Load; merge в обе стороны;
      позиция записи в материализации), `ui/configurator/tabs/preview_rows_test.go`
      (сопоставление строки и узла по сырому тегу). `ETALON_V6MIG=1` —
      по-прежнему РОВНО одно задекларированное расхождение Р2, те же три строки
- [x] `SPECS/features/sources.md` — раздел «Unsupported: запись, которую не
      разобрали» + обещание пользователю

## W12 — UI-фиксы этапа 2 по обкатке этапа 3

Шесть правок существующих виджетов и текстов; ничего нового не заводится.

- [x] **Фикс 1 — ложный конфликт «Направление + его авто-группа».**
      `BuildTagGuard` (`core/config/tag_guard.go:148`) строится по форме
      ПОСЛЕ `ExpandDirectionTwins`, где обе половины пары лежат отдельными
      записями. Наивная формула `d.Tag+twinSuffix` объявляла вторым
      претендентом ту же самую сущность, и КАЖДОЕ Направление с автовыбором
      давало «тег занят дважды: Направление и авто-группа Направления»; тот
      же случай — шаблонная отдельно стоящая `proxy-out-auto`, из-за
      которой твин вовсе не разворачивается (`direction_twins.go:98`).
      Дедупликация по владельцу: твин-запись (`TwinOf != ""`) claim'ится
      как `TagOwnerTwin`, производная формула не применяется к тегу, за
      которым уже стоит своя запись списка (`twinTags`). Настоящий дубль —
      по-прежнему конфликт
- [x] **Фикс 2 — строки эмиссии на механизм локали.** Английский текст в
      коде константами (`core/config/emission_warning.go:31–51`), перевод в
      `bin/locale/ru.json`. Тем же куском переведены значения
      `TagOwnerKind` (они подставляются в текст столкновения) и причины
      `NodeLinkTargets.Resolve`. `l10n_check`: hard fails 0
- [x] **Фикс 3 — у предупреждения эмиссии есть адресат.**
      `OutboundGenerationResult.EmissionWarnings` — `[]EmissionWarning`
      (`{Text, SourceID, SourceLabel, DirectionTag}`), а не `[]string`.
      Адресат берётся у САМОГО узла (`ParsedNode.SourceIndex`), у цепочки —
      у её источника, у столкновения тегов — по владельцу спорного тега
      (`sourceOfNodeTag` / `directionOwningTag`). Отчёт кладёт ULID в
      `SourceID` (`core/build_report_feed.go:77`), строка Sources ставит ⚠
      через новый `config.EmitWarningsForSource:316` — как у подписок
- [x] **Фикс 4 — явный статус сборки над списком «Итога».**
      `finalBuildStatusText` (`final_report_model.go:168`) — чистая
      функция: «Build OK» / «Build OK, N warnings» / «Build FAILED:
      причина». Провал больше не рисуется красной строкой ВНУТРИ списка
      (один факт дважды); `fetch_degraded`/`emit_degraded` получили место в
      порядке видов и собственную формулировку
- [x] **Фикс 5 — Copy на «Итоге».** `NewButtonWithIcon("Copy config",
      theme.ContentCopyIcon())` — иконка и стиль «Copy token»
      (`ui/settings_tab.go:594`). Кнопка копирует СОБРАННЫЙ конфиг: подпись
      обязана совпадать с тем, что уезжает в буфер
- [x] **Фикс 6 — размеры полей по содержимому.** «Default update interval»
      и «Default max nodes» — GridWrap 90px + спейсер (в них живут «4h» и
      «300», а Border растягивал ввод на всю ширину); «Device ID (HWID)» —
      наоборот, 120px → 340px: ID виден целиком, его сверяют с
      зарегистрированным у провайдера и диктуют в поддержку
- [x] Тест только на гард — `core/config/tag_guard_twin_test.go`:
      шаблонная пара селектор+авто конфликта не даёт, настоящий дубль даёт
- [x] Приёмка: `go build ./...`, `go vet ./core/... ./ui/...`,
      `go test ./core/... ./ui/...` — зелено; го1.20-гард по изменённым
      файлам чист
- [x] `CODEMAP.md` — §1.3 (гард, резолв, новый `emission_warning.go`), §1.4
      (адресат записи, статус, кнопка), §3.3/3.4 (пара ≠ коллизия)
