# TASKS — SPEC 116, этап 3 (папки в UI)

Чекбоксы по волнам `PLAN.md`. Один агент = одна волна.
Строки в адресах проверять грепом по имени функции.

## Предусловие — решения §O

Вердикты капитана, 2026-08-29: всюду принят рекомендованный вариант —
ни один не вводит нового визуала и не трогает contract/**.

- [x] §O1 = **А** — warning экспорта поднять в UI-отчёт бэкапа; трек
      `folders[]` в контракте (В) — отдельной задачей вне этапа.
- [x] §O2 = **А** — финальные теги через существующий `EmitNodeJSONs`.
- [x] §O3 = **А** — обратную заливку JSON → папка в этап 3 не берём.
- [x] §O4 = **А** — расширить существующий Preview, новых вкладок нет.
- [x] §O5 = **А** — строку папки не декорировать (отличие — нет URL в
      подстроке); метка/иконка — дешёвая доработка позже, если попросят.
- [x] §O6 = **А** — «Add folder» первым пунктом меню ⋮.

## W1 — merge-заливка в папку (data-критично)

- [ ] Выделено общее ядро merge из `MergeSubscriptionNodes`
      (`core/state/subscription_merge.go:50`), поведение подписки не изменилось
- [ ] `MergeFolderNodesFromSubscription` — ключ (`origin.subUrl`, сырой тег)
- [ ] Совпал → body/origin.raw освежены, `enabled`/`detour`/позиция целы
- [ ] Новый → добавлен включённым, `origin.subUrl` проставлен
- [ ] Исчезнувший → **разыменован**, не удалён; warning «исчез у провайдера»
- [ ] `Truncated` → разыменование не выполняется
- [ ] `trusted == false` → `nodes[]` не тронуты
- [ ] Тест `core/state/folder_merge_test.go` — пять случаев (A5)
- [ ] Порядок ключей body не пересобран через `map`; го1.20-гард чист

## W2 — перенос узла между контейнерами

- [ ] `business/node_move.go`: `CopyNodeToFolder` (subUrl сохраняется)
- [ ] `MoveNodeToFolder` — отказ, когда исходный контейнер подписка
- [ ] `ExtractFolderNodesToRoot` — порядок и `enabled` сохранены
- [ ] `DereferenceNodeOrigin` — общая точка обнуления `origin.subUrl`
- [ ] Коллизия сырого тега в целевом контейнере разрешается суффиксом
- [ ] Побочки (`BumpRevision`/`InvalidateNodePool`) оставлены вызывающему
- [ ] Тест `node_move_test.go` — критерии A3, A7

## W3 — создание и удаление папки

- [ ] «Add folder» в меню ⋮ (`source_tab.go:271`), место по §O6
- [ ] Вызван существующий `corestate.NewFolderSource` (`sources_v7.go:267`)
- [ ] Свободное имя по умолчанию (образец `NextChainLabel`, `sources.go:181`)
- [ ] Мутация через `applySourceMutation` (`source_tab.go:816`)
- [ ] Удаление папки — диалог двух исходов; «вынести узлы» зовёт W2
- [ ] Строка списка: имя + счётчик узлов; пустая папка без пометки «не дал узлов»
- [ ] Кнопки обновления у папки нет

## W4 — настройки папки в окне источника

- [ ] Третья ветка `rebuildSettingsLayout` (`source_edit_window.go:776`):
      имя, prefix, postfix, подсказка переменных, свёртка, detour
- [ ] Ни URL, ни интервала, ни max_nodes, ни skip у папки (A8)
- [ ] Проверены все употребления `isServerSource` (`:779`, `:1166`, `:1453`)
- [ ] Ветка заголовка окна для `SourceKindFolder` (`:416`)
- [ ] Имя пишется в `Source.Name`; `displayName()` и `SourceDisplayName` согласованы
- [ ] Вкладка «Группа» (Replace) у папки появляется/исчезает по галке

## W5 — список узлов и операции над узлом

- [ ] Меню узла (`preview_node_info.go:44`) получает сырой тег и вид контейнера
- [ ] Пункты Move to / Copy to / Rename / Delete
- [ ] У подписки: только Copy to (Move/Rename/Delete скрыты)
- [ ] Операции зовут W2 и затем `BumpRevision` + `InvalidateNodePool` + `MarkAsChanged`
- [ ] Перепись ссылок — существующий `resetRefsAfterNodeRename:1571`
- [ ] Предупреждение о протухшем выборе — существующий `showStaleSelectionDialog:1638`
- [ ] Авторазыменование при правке тега (Rename)
- [ ] Авторазыменование при правке body / Regen
      (`source_body_edit.go:40`, `:71`) + уведомление `ShowAutoHideInfo`
- [ ] Комментарий в коде: почему Move/Copy применяются к модели сразу,
      а не буферизуются в scratch; окно перечитывает scratch после операции
- [ ] Тест авторазыменования (A4)
- [ ] Drag-n-drop узлов внутри списка контейнера; через границу нельзя (П5)

## W6 — наполнение папки

- [ ] Общее ядро «текст → узлы» вынесено из `AppendURLsToSources`
      (`business/sources.go:33`); второго разбора не заведено
- [ ] `AppendNodesToFolder` кладёт узлы в `Source.Nodes` папки
- [ ] Материализация через существующий `config.MaterializeServerNode`
- [ ] Уникализация сырого тега в пределах папки
- [ ] «Add nodes…» в окне папки: вставка текста, из файла, Add WARP, Add server
- [ ] Тег из имени файла при отсутствии `#fragment`
- [ ] Импорт мультивыбором

## W7 — заливка подписки в папку

- [ ] «Fill from subscription…» в окне папки; выбор из существующих подписок
- [ ] Материал — уже материализованные `sub.Nodes`, тело не парсится заново
- [ ] `Truncated` берётся из `sub.UpdateStatus` и передаётся в merge
- [ ] Подписка без узлов → сообщение + предложение обновить, заливки нет
- [ ] Auto-узлы: members переуказаны на узлы папки по сырому тегу; непопавшие — prune + warning
- [ ] Повторная заливка идёт тем же путём (отдельного «обновить папку» нет)
- [ ] Сценарий С5 пройден руками

## W8 — «взять всю папку → JSON»

- [ ] «Copy nodes as JSON» в окне папки
- [ ] Эмиссия через `config.EmitNodeJSONs` (`outbound_generator.go:1040`),
      второй эмиссии не заведено
- [ ] Теги по решению §O2
- [ ] Результат принимается `sing-box check` как фрагмент конфига (A6)

## W9 — бэкап

- [ ] Реализован выбранный вариант §O1
- [ ] Экспорт **никогда** не отдаёт успешный файл, молча потерявший папку (A9)
- [ ] Потеря видна в окне отчёта бэкапа
- [ ] `contract/**` не тронут

## W10 — прогон

- [ ] `go build ./...`, `go vet ./...`, `go test ./...`
- [ ] Го1.20-гард: греп `slices.` / `maps.` / `errors.Join` / `min(` / `max(` /
      `clear(` / `PathValue` в изменённых файлах — 0 вхождений
- [ ] Новые строки через `locale.T`/`Tf`; ключи в `bin/locale/ru.json`
- [ ] Сценарии С1–С8 прогнаны на собранном приложении (`build_darwin.sh -i`)
- [ ] `CODEMAP.md` дополнен новыми адресами
