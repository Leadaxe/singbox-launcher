# TASKS 104 — Каналы в лаунчере

## Сделано

- [x] Модель `corestate.Channel` + `ChannelAuto`: тег `vpn-N` (неизменяем),
      метка, фильтр узлов с инверсией, предпочитаемый узел, include
      direct/block, автовыбор
- [x] Шаблонная часть: `group_templates` (тип групп, магические опции,
      параметры urltest) и `default_channels` — каналы настраиваются
      ШАБЛОНОМ, а не зашиты в код
- [x] Материализация `BuildChannelGroups`: selector + парный urltest, с
      частностями LxBox (узел автовыбора не идёт в urltest канала; пустой
      канал получает [block, direct] с default=block; предупреждение
      только когда виноват фильтр; битый regex = пустой фильтр)
- [x] Хранение: совместимое расширение v6, `channels[]`; nil = «секции не
      было» → сидирование из шаблона ровно один раз
- [x] Каналы в обоих путях сборки (превью из модели, сохранение из состояния)
- [x] Каналы как цели правил (`GetAvailableOutbounds`)
- [x] UI: список и форма канала на вкладке Outbounds
- [x] Тесты: 12 на материализацию, 6 на модель и хранение, 3 на сборку
- [x] Локализация EN+RU (32 ключа)

## Осталось

- [ ] Миграция существующих ручных селекторов «vpn ①/②» шаблона в каналы —
      сейчас сосуществуют, конфликта тегов нет (`vpn ①` против `vpn-1`)
- [ ] Bump схемы LX Backup до v1.1: снятие политики «импорт выключенным»
      для канальных ссылок, когда каналы появятся на обеих сторонах
- [ ] Живая проверка на рабочем конфиге (за пользователем)
