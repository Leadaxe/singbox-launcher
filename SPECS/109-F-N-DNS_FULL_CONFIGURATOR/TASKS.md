# TASKS 109 — Полный конфигуратор DNS

Нормативка: [`SPEC.md`](SPEC.md). Порядок фаз обязателен: формы → детур →
шаблон → D&D → подписи → контракт. Каждая фаза заканчивается зелёной
сборкой; тесты — в конце по решению оператора.

## Фаза 0 — подтвердить открытый вопрос
- [x] Объём переноса шаблона: все десять серверов LxBox или только
      недостающие. Предложение — все, включёнными оставить нынешние

## Фаза 1 — формы серверов (D1)
- [x] Форма UDP: адрес, порт, детур, резолвер имени
- [x] Форма TCP (та же, порт 53)
- [x] Форма DoT (`tls`): + SNI, порт 853
- [x] Форма DoH (`https`): + путь, SNI, порт 443
- [x] Форма Group: состав, режим, `error_ttl`, `win_ttl` — БЕЗ детура (T1)
- [x] Кнопка `+` вместо диалога с JSON-заготовками
- [x] Вкладка JSON остаётся запасным путём для типов без формы
- [x] Обработчики после установки значений (ловушка рекурсии SPEC 104)

## Фаза 2 — детур (D2, D3)
Формы серверов включили детур сразу — фаза сделана вместе с фазой 1.
- [x] Поле детура — список из `business.GetAvailableOutbounds`
- [x] У группы поля нет (T1)
- [x] `domain_resolver` — список DNS-серверов, без самоссылки и циклов (T5)
- [ ] Серверы `kind=template`: форма правит USER-патч, не тело (T4)

## Фаза 3 — перенос шаблона (D4, D5)
- [x] Серверы из `LxBox/app/assets/wizard_template.json` → наш шаблон
- [x] DNS Shield (`group`, девять участников, `mode: fastest`)
- [x] Параметризация `vars` внутри записей серверов
- [x] `dns_servers` / `dns_server` — принимать оба, писать одно (T3) —
      сделано до фазы 1, `canonicalVarType`
- [x] Включёнными оставить те серверы, что включены сейчас

## Фаза 4 — D&D (D6)
- [x] Sources: ручка `NewDragHandle` в строке + коммит порядка
- [x] Directions: то же + проверка, что `addOutbounds` не уехал вперёд
      своей цели (T2)

## Фаза 5 — подписи (D7)
- [x] `wizard.rules.label_outbound`, `wizard.add_rule.label_outbound` →
      «Направление» / "Direction" в обеих локалях

## Фаза 6 — контракт и документация
- [ ] Типы DNS-серверов и их поля в `contract/registry` (T6)
- [ ] `docs/WIZARD_STATE{,.ru}.md` — формы серверов; `upcoming.md` EN+RU

## Проверка
- [ ] `go build ./... && go vet ./...`, линт 0 issues
- [ ] Тесты модуля
- [ ] Живая проверка на рабочем конфиге (за оператором)
