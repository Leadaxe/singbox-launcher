# QUIRKS — журнал странностей старого кода (SPEC 133)

Указание владельца 19.09.2026: «делаем, протоколируем странности, чтобы их не
потерять».

Сюда попадает **каждая** странность, всплывшая на сверке, — не только та, что
стала дельтой. Смысл журнала: рукописные парсеры будут удалены, и вместе с
ними исчезнут комментарии, в которых живёт обоснование. Запись заводится
**до** исправления кейса, а не после.

Судьба записи — одно из:

- **сохранено примитивом `<имя>`** — поведение переехало в данные без изменения;
- **дельта D133-NN** — поведение меняется, запись в `DELTAS.md`;
- **сохранено как есть, кандидат на починку после кампании** — странность
  осознанно не трогаем;
- **открытый вопрос** — ждёт владельца.

---

## Декодирование и `+`

### Q133-01 · четыре заплаты «сохранить `+`» на один корень

**Где:** `node_parser_wireguard.go:353-375` (`queryParamPreservePlus`, ключи
`publickey`/`presharedkey`), `:60` (обратная замена
`ReplaceAll(rawPrivateKey," ","+")`), `awg3.go:235-241`
(`queryParamPreservePlusOrGet`, `header_protection_key`),
`node_parser_masque.go:58` (`publickey`).

**Что делает:** `url.ParseQuery` трактует значение как
`application/x-www-form-urlencoded` и превращает `+` в пробел, ломая base64.
Четыре места чинят это поштучно, тремя разными способами.

**Вход → выход:** `?publickey=aB+cd/e=` → без заплаты `aB cd/e=` (ключ
негоден), с заплатой `aB+cd/e=`.

**Ключевое:** покрыты ровно четыре параметра. **У `pbk` заплаты нет** —
`+` в нём становится пробелом, и реестр снимает поле по `format`, то есть
REALITY деградирует молча.

**Судьба:** дельта **D133-7** — `decode_extra.plus_literal`, выводимый из
`format: base64*`. Одно правило вместо четырёх заплат; заплаты удаляются при
переходе схемы на движок.

### Q133-02 · `path` теряет `+` на ступени, которая его не трогает

**Где:** `node_parser_transport.go:755-776` (`decodeResidualPercent`) —
и `url.ParseQuery` ДО него.

**Что делает:** `decodeResidualPercent` честно использует `PathUnescape`
(комментарий `:765-768` прямо объясняет, что `+` в пути литерален), но
значение приходит к нему уже испорченным: `ParseQuery` заменил `+` на пробел
раньше.

**Вход → выход (проверено запуском `ParseNode`, а не чтением):**
`?type=ws&path=/a+b` → `transport.path = "/a b"`.
Отдельно: `ParseQuery("path=/a+b")` = `/a b`, а
`decodeResidualPercent("/a+b")` = `/a+b` — ломает первая ступень.

**Судьба:** дельта **D133-14**. В движке ступени нет: `ParseQueryOrdered` не
заменяет `+`, форм-семантику применяет запись, а у `path` объявлен
`mode: "path"`.

### Q133-03 · `alpn` декодируется до стабилизации, `path` — ровно 2 прохода

**Где:** `node_parser_transport.go:24-33` (`normalizePercentDecodeLoop`,
цикл БЕЗ предела) против `:755-776` (ровно 2 прохода).

**Что делает:** два разных правила percent-декода в одном файле, и оба
осмысленны: у `alpn` — query-семантика без предела, у `path` — path-семантика
с пределом 2 (D-028).

**Вход → выход:** `alpn=http%2525252F1.1` (ТРИ уровня) → `["http/1.1"]`
(фикстура `uri/vless/alpn_multiply_encoded`).

**Ключевое:** черновик `SCHEMES.md` писал обоим `{"percent": 2}` — это
сдвинуло бы identity, кейс покраснел бы.

**Судьба:** сохранено примитивом **`decode_extra{mode,passes,max}`**:
`alpn` = `{query, until_stable, max:16}`, `path` = `{path, 2}`. Предел 16
добавлен как страховка от неограниченного цикла, на корпусе не достигается.

---

## Имена и порядок

### Q133-04 · `queryGetFold` обходит Go-map — победитель недетерминирован

**Где:** `uri_params.go` (`queryGetFold`), `subscription` пакет.

**Что делает:** ищет имя регистронезависимо обходом `url.Values`, то есть
Go-map со случайным порядком обхода.

**Вход → выход:** `?sni=a&SNI=b` → `a` ИЛИ `b`, **по-разному между
запусками**. На корпусе не всплывает, потому что кейса с двумя написаниями
одного имени в одной ссылке нет.

**Судьба:** сохранено примитивом — норма «побеждает точное совпадение с
каноном, иначе первое по порядку появления» (`PRIMITIVES` §0.6, §15.5).
Требует сохранения порядка query, которого `url.Values` не даёт — отсюда
собственный лексер в движке.

### Q133-05 · фолбэк тега строится по НАПИСАНИЮ схемы

**Где:** `node_parser_core.go:640-642` (`generateDefaultTag(scheme,…)`);
обоснование «почему так оставлено» — `node_parser_core.go:308-317` и
`node_parser_socks.go:13-15`.

**Что делает:** `hy2://` даёт `hy2-host-443`, `hysteria2://` — `hysteria2-host-443`;
`socks5://` даёт `socks5-…`, хотя канон схемы — `socks`. Комментарий прямо
предупреждает, что канонизация переименовала бы живые узлы и порвала бы
ссылки detour/цепочек и отметки.

**Судьба:** дельта **D133-6** (решение владельца: `singbox_type`). Затронуто
12 написаний схем, 52 кейса корпуса. Миграция состояния — открытый вопрос
`SPEC.md` §14.5/§15.5.

### Q133-06 · `Registry.MapsTo` заполняется и не вызывается ниоткуда

**Где:** `core/config/registry/registry.go` (`MapsTo`, `collectMapsTo`).

**Что делает:** таблица «параметр ссылки → путь в теле» строится на каждой
загрузке реестра и **не читается ни одним вызовом**. Из всего реестра
ссылочный путь берёт только `QueryAliases`.

**Судьба:** сохранено примитивом — таблицу начинает исполнять движок
(`source`/`maps_to`). По итогам W0 решить: подключить как публичное API или
снять как дубль нового (`TASKS` W0, последний пункт).

---

## Схемные странности

### Q133-07 · trojan режет пароль по первому `:`

**Где:** `node_parser_core.go:455-466`.

**Что делает:** пароль = часть userinfo ДО `:`; хвост уходит в
`Query["password"]` и в тело **не попадает**.

**Вход → выход:** `trojan://pass:word@h:443` → `password: "pass"`, `word`
теряется.

**Судьба:** дельта **D133-8** (не резать, весь userinfo). Узлы с паролем без
`:` — весь сегодняшний корпус trojan — не затронуты.

### Q133-08 · эвристика SNI у trojan: `server` подставляется в `server_name`

**Где:** `node_parser_transport.go:1042-1045` (`trojanTLSFromNode`).

**Что делает:** при отсутствии `sni=` в `tls.server_name` уезжает адрес
сервера.

**Вход → выход:** `trojan://pw@example-1.com:443?security=tls` →
`tls.server_name: "example-1.com"`. Корпус на этом стоит:
`uri/trojan/basic_password`, `default_port_443`.

**Ключевое:** LxBox просит НЕ включать эвристику в норму. Снятие изменило бы
тела **рабочих** узлов, что нарушает общий критерий.

**Судьба:** **открытый вопрос** (`SPEC.md` §15.9). До ответа — сохранено
примитивом `default_from: "host"`.

### Q133-09 · три разных правила SNI-эвристики на три группы схем

**Где:** `node_parser_transport.go:916-919` и `:1042-1045` (простой фолбэк на
server), `:1014-1021` (`tlsServerNameFromQuery` — `sni` принимается ТОЛЬКО
если содержит `.` или `:`), `node_parser_core.go:889-898` (vmess: `sni` →
`peer` → server).

**Что делает:** одна и та же величина вычисляется тремя способами в
зависимости от схемы.

**Судьба:** сохранено примитивами `source` (цепочка) + `default_from` с
предикатом — одна декларация на все три случая, поведение каждой схемы
сохраняется её собственной записью.

### Q133-10 · masque: свой список `insecure`, где `yes` не работает

**Где:** `node_parser_masque.go:141-148`.

**Что делает:** читает `insecure`/`skip_cert_verify`/`allowinsecure` точным
`Get`, истинность — только `1` и `true`. У всех прочих схем набор из 9
написаний и истинность `1|true|yes` (`uri_params.go:75-81`).

**Вход → выход:** `masque://…?insecure=yes` → флаг **не** применяется;
та же ссылка на trojan — применяется.

**Судьба:** дельта **D133-4**.

### Q133-11 · masque `sni` читается точным `Get`, без алиасов

**Где:** `node_parser_masque.go:141`.

**Что делает:** ни реестровых алиасов, ни регистронезависимости, ни фолбэка —
в отличие от всех прочих QUIC-схем.

**Вход → выход:** `?SNI=x.com` → поле не заполняется.

**Судьба:** дельта **D133-5**.

### Q133-12 · ss: SS2022 уходит в безусловный base64-декод

**Где:** `node_parser_core.go:194-224`, дроп `:429-435`.

**Что делает:** userinfo всегда декодируется как base64; открытый
`2022-blake3-aes-128-gcm:key@host` — легальная форма SIP002 — декод не
проходит, и узел **дропается**.

**Ключевое:** кейс корпуса `ss2022_blake3` живёт **случайно** — ключ в нём
оказался валидным base64.

**Судьба:** дельта **D133-2** (`decode: ["percent","base64?"]` — try-decode).

### Q133-13 · ss `plugin`/`plugin_opts` не читаются вовсе

**Где:** нигде. Слова `plugin` нет во всём `core/` (проверено грепом).
Реестр объявляет оба поля и **прямо пишет** «Go молча теряет query целиком»
(`shadowsocks.json:40-52`).

**Судьба:** дельта **D133-1**.

### Q133-14 · `disable_sni` объявлен у двух схем и не читается ни одной

**Где:** объявление — `tuic.json`, `masque.json` (пометка `ext: mobile`);
в Go только эмиттер `outbound_tls_emit.go:84`.

**Судьба:** дельта **D133-3**.

### Q133-15 · `fp` у hysteria/hy2/tuic — странность СНЯТА до кампании

**Где:** `node_parser_hysteria.go:132` → `applyTLSCamouflageFromQuery`
(`node_parser_transport.go:971-977`).

**Что делает:** параметр **читается**; блокирует его реестр
(`forbidden_for` QUIC, код `tls_not_applicable_quic`, контракт 1.1.4), а не
парсер.

**Судьба:** сохранено как есть. Запись заведена, потому что постановка
задачи называла `fp` нечитаемым — это уже неверно, и проверять его повторно
не нужно.

---

## Xray-вход

### Q133-16 · `xrayMapString` склеивает массив через `fmt.Sprint`

**Где:** `xray_outbound_convert.go:26` (ветка `default`), применение
`:363-365`.

**Что делает:** `httpSettings.host` в Xray — массив строк; приведение к
строке даёт `"[a.com b.com]"`, которое затем оборачивается в
`[]string{...}`.

**Вход → выход:** `"host":["a.com","b.com"]` → `transport.host:
["[a.com b.com]"]`. То есть **типовой реальный конфиг даёт мусорный host**.

**Судьба:** сохранено примитивом `list{coerce_scalar:true}` + отказ движка
приводить массив к скаляру (`jsonScalar` возвращает «нет значения», а не
склейку). Проверено тестом `TestLinkmapEngineW0`.

### Q133-17 · неизвестный `network` и `security: xtls` теряются молча

**Где:** `xray_outbound_convert.go:403-405` (`default: return nil`),
`:223-225` (гард security).

**Что делает:** `network: "kcp"/"quic"/"ds"` → транспорта нет, узел
приезжает чистым TCP; `security: "xtls"` → TLS-блока нет. Ни warning, ни
кода — при том что код `transport_unsupported` объявлен и в `warnings.json`
эта дыра прямо описана как РАЗРЫВ.

**Судьба:** сохранено примитивом — `on_invalid{action,code}` у записи
селектора; коды начинают ставиться (волна W8).

### Q133-18 · `vnext[1..]` / `servers[1..]` / `users[1..]` отбрасываются молча

**Где:** `xray_protocols.go:125`, `:141`, `:159`;
`xray_outbound_convert.go:112`, `:128`, `:418`, `:437`.

**Что делает:** берётся строго нулевой элемент, цикла нет.

**Судьба:** сохранено примитивом `on_len_gt{n:1, action:"note", code}` —
поведение то же, но появляется код.

### Q133-19 · выемка `vnext` продублирована дословно

**Где:** `xray_outbound_convert.go:104-131` ≡ `xray_protocols.go:117-144`
(28 строк). VLESS не использует общий `xrayVNextEndpoint`.

**Судьба:** сохранено примитивом `forms[].base` (`$base`-якорь): одна
таблица обслуживает `vnext[0]`, `servers[0]` и плоскую форму.

### Q133-20 · `keyShare` и `tlsSettings.alpn` не читаются с Xray-входа

**Где:** `keyShare` — нигде в `xray_*.go` (грепа нет); `alpn` — только
`xray_hysteria.go:213-225`, в общей TLS-ветке (`:267-283`) отсутствует.

**Что делает:** узел REALITY из Xray-конфига теряет `key_share`;
vless/vmess/trojan из Xray теряют `alpn`.

**Судьба:** кандидаты **D133-C8** и **D133-C9** — поведение НЕ меняется до
решения владельца, заводятся только кейсы корпуса.

### Q133-21 · второй, скрытый диспетчер протокола

**Где:** `xray_json_array.go:789` — `if protocol == "socks"`.

**Что делает:** `socks` не является узлом (в главном switch его нет) и
существует только как звено цепочки `dialerProxy`.

**Судьба:** сохранено примитивом — `detect` секции плюс различение «может
быть узлом» / «может быть хопом» (волна W8).

---

## sing-box вход

### Q133-22 · `SanitizeSingboxOutboundMap` всегда возвращает `nil`

**Где:** `singbox_sanitize.go:62-72`.

**Что делает:** сигнатура возвращает `[]string` кодов, вызывающий их вешает
на узел (`singbox_import.go:377-379`), но источник **пуст**. Все деградации
sing-box-входа остаются в `debuglog`.

**Судьба:** сохранено примитивом — записи секции `mappers.singbox` с
`on_present{code}`/`on_invalid{code}`; коды начинают доезжать до узла
(волна W8.5).

### Q133-23 · masque legacy-ключи стрипаются без кода

**Где:** `singbox_sanitize.go:84-96`.

**Что делает:** у `type: masque` удаляются плоские `network`/`sni`/
`skip_cert_verify` **без переноса значений** и только с `DebugLog`.
Удалять обязательно: плоский `sni` рядом с `tls.server_name` роняет ядро
fail-fast'ом.

**Судьба:** сохранено примитивом `$masque_flat` с `since: "0.8.0"` и
`on_present{action:"drop", code}`.

### Q133-24 · `hysteria.obfs` объект→строка, три ветки — все молча

**Где:** `singbox_sanitize.go:147-173`.

**Что делает:** форма v2 `{type,password}` сводится к плоской строке v1;
пустой пароль и любая другая форма — `delete` с `WarnLog`.

**Судьба:** сохранено примитивом `coerce{object_to_scalar:"password"}` +
`on_invalid{code}`.

### Q133-25 · `singboxTypeIsAddressless` и `singboxCredentialFromMap`

**Где:** `singbox_import.go:429-431` (`t == "wireguard" || t == "tailscale"`)
и `:437-449` (switch по схеме из 8 литералов: какое поле несёт секрет).

**Что делает:** две таблицы, записанные кодом.

**Судьба:** сохранено примитивами — записи секции (`$addressless`,
`$credential`), таблицы уезжают в реестр.

### Q133-26 · vmess `security`: дефолт материализуется — у НАС странности нет

**Где:** `xray_protocols.go:208`, `node_parser_core.go:792` — значение
пишется в тело **всегда**.

**Что делает:** `security` эмитируется даже с дефолтом `auto`, потому что
json-тег ядра без `omitempty`.

**Ключевое:** у LxBox опущенный ключ снимал узел кодом `field_missing` — у
нас этого дефекта нет. Запись заведена, чтобы примитив
`materialize_default` не выглядел заимствованным без причины: он описывает
НАШЕ поведение.

**Судьба:** сохранено примитивом `materialize_default: true`.

---

## `.conf` / ini

### Q133-27 · ini переводится в ТЕКСТ ССЫЛКИ и разбирается заново

**Где:** `node_parser_amnezia.go:508-599` (`wgConfToURI`).

**Что делает:** `.conf` не переводится в тело — он переводится в другой
ИСТОЧНИК (`wireguard://…`), чтобы переиспользовать чужой парсер. На стыке
стоит `q.Encode()`, то есть один источник ходит через второй, набирая
дефекты обоих.

**Судьба:** сохранено примитивом — секция `mappers.conf` адресует `ini.*`
напрямую, стык исчезает (волна W8.6).

### Q133-28 · чтутся поля только ПЕРВОЙ секции `[Peer]`

**Где:** `node_parser_amnezia.go:466-501` — счётчик `peerSections` уже
посчитан, но прочие секции просто не пишутся.

**Вход → выход:** `.conf` с двумя `[Peer]` → второй отброшен **молча**: ни
warning, ни лога.

**Судьба:** дельта-кандидат волны W8.6 — `sections.Peer.repeat: "first_only"`
+ `on_extra{code}`; поведение то же, но появляется код.

### Q133-29 · `Interface.DNS` ставится в query и не читается никогда

**Где:** `node_parser_amnezia.go:555-557` (ставит `q.Set("dns",…)`);
`parseWireGuardURI` параметра `dns` не читает.

**Что делает:** у sing-box endpoint нет поля DNS — лоссы **by design**
(зафиксировано в `wireguard.json:124-131`), но молча. Amnezia при этом
тратит работу на резолв `$PRIMARY_DNS`/`$SECONDARY_DNS`
(`node_parser_amnezia.go:389-403`), результат которой выбрасывается.

**Судьба:** запись с `maps_to: null` + `on_present{action:"note", code}` —
лоссы остаются, молчание уходит.

### Q133-30 · пять независимых реализаций проверки `[Interface]`/`[Peer]`

**Где:** `body_classify.go:137`; `wgconf_text.go:31`, `:90`;
`node_parser_amnezia.go:435`, `:325` — три разных способа (EqualFold строки,
HasPrefix+Trim, Contains).

**Судьба:** сохранено примитивом `detect.ini{sections,keys,keys_any}` —
одна реализация на обоих уровнях.

### Q133-31 · `knownKeys` продублирован литералом в одной функции

**Где:** `node_parser_amnezia.go:329` и `:342-345` — один список
(`config`, `last_config`, `awg`, `wireguard`) записан дважды.

**Что делает:** добавишь ключ в первый список и забудешь во втором — он
будет обойдён повторно.

**Судьба:** сохранено как есть, **кандидат на починку после кампании**:
распаковщик Amnezia в движок не переводится (остаётся обёрткой над текстом).

---

## Найдено сверкой движка (W1, 19.09.2026)

Записи ниже всплыли на прогоне `TestEngineVsFixtures` по корпусу trojan и
vless: движок исполняет секцию реестра, фикстуры сняты старым путём, и каждое
красное расхождение — либо странность кода, либо ошибка секции.

### Q133-32 · `host` у `httpupgrade` читается БЕЗ фолбэка на `sni`

**Где:** `node_parser_transport.go:250-252` — `queryGetFold(q, "host")` и
всё; у соседнего `ws` (там же, `:198-207`) фолбэк цепочкой
`host → sni → obfsParam` есть.

**Что делает:** ссылка `?type=httpupgrade&sni=cdn.example` кладёт
`transport.host` пустым, хотя для того же `ws` то же самое значение
подставилось бы. Реверс-прокси ждёт vhost и отвечает 404.

**Как всплыло:** секция `transports.json` (`blocks.uri.httpupgrade.host`)
была написана по ws-образцу — `["query.host","query.sni"]`, и движок начал
подставлять `sni`. Три кейса корпуса покраснели.

**Судьба:** **секция приведена к коду** — у `httpupgrade` источник один
(`query.host`). Расширение фолбэка меняло бы тела живых узлов и обязано быть
дельтой с решением владельца, а не побочным эффектом волны.

### Q133-33 · `xhttp` берёт `host` из `sni`, а `httpupgrade` — нет

**Где:** `node_parser_transport.go` — `xhttpTransportFromQuery` подставляет
`sni` в `host`, когда плоского `host` нет (ветка vmess, `:812-816`), а
`httpupgrade` рядом — нет.

**Что делает:** одно и то же «имя vhost» читается у трёх транспортов тремя
разными наборами источников: `ws` = host→sni→obfsParam, `xhttp` = host→sni
(и то не везде), `httpupgrade` = host.

**Судьба:** сохранено как есть в секциях (каждому транспорту свой `source`),
**кандидат на унификацию после кампании** — унификация меняет тела.

### Q133-34 · `security=none` снимает блок TLS, но не снимает то, что уже
написали соседи

**Где:** `vlessTLSFromNode`/`trojanTLSFromNode` — ранний `return nil, false`
до того, как хоть что-то положено в `tlsData`. Блок собирается ЛОКАЛЬНОЙ
картой и кладётся в тело целиком, поэтому «снять» нечего.

**Что делает:** в движке тело общее, и запись `sni` со своим `default_from`
успевает написать `tls.server_name` раньше, чем отработает `security`. Итог —
`tls` из одного `server_name` без `enabled`.

**Судьба:** сохранено примитивом — `sets` со значением `null` СНИМАЕТ путь
(G2), и ветка `none` объявлена как `{"tls": null}` вместо пустого набора.
Пустой набор `{}` выражал «ничего не писать», а нужно «снять написанное».

### Q133-35 · плоские `ed`/`eh` объявлены в реестре только прозой

**Где:** `node_parser_transport.go:190-201` — вторая форма early data
(`?ed=N&eh=Header`) читается кодом, но записи в `transports.json` у неё нет:
блок `ws` знал только хвост пути `?ed=N`.

**Что делает:** движок молча терял `max_early_data` на живой форме подписок
(кейс `ws_ed_eh_flat_query`), а `httpupgrade` наоборот обязан их
игнорировать — это различие тоже было только в коде.

**Судьба:** сохранено примитивом — две записи `ed`/`eh` у блока `ws` с
`when: {transport.type: ws}` и `priority` ниже хвоста пути (хвост
побеждает: он адресует один путь, плоская пара — всю ссылку).

### Q133-36 · битый `%zz` в пути снимает не маппер, а санитайзер

**Где:** `decodeResidualPercent` (`:759-776`) оставляет `%zz` как есть
(`url.PathUnescape` возвращает ошибку), и путь доезжает до тела; снимает его
правило реестра `format: url_path`.

**Что делает:** тело маппера и тело узла на этом кейсе РАЗНЫЕ, и сверка
«движок vs фикстура» не может быть зелёной по построению: фикстура снята
после санитайзера.

**Судьба:** сохранено как есть — **разрешённая дельта раннера**
(`deltas_allowed.json`), а не дельта поведения. Граница «маппер не судит
значения» соблюдена обеими сторонами.

---

### Q133-37 · истинность `tls` у vmess зависит от формы ссылки

**Где:** `node_parser_vmess.go:288` (v2rayN JSON) против `:167-170`
(legacy cleartext).

**Что делает:** `tls=1` включает TLS в legacy-форме и **не включает** в
JSON-форме, где истина — строго строка `"tls"`. Там же нет ни `TrimSpace`,
ни `ToLower`, поэтому `"TLS"` и булев `true` из JSON тоже не срабатывают.

**Судьба:** **сохранено примитивом** — `when` по `$form` (D133-16).
Асимметрия воспроизведена намеренно: сведение форм в одну таблицу изменило
бы тела рабочих узлов.

### Q133-38 · vmess-JSON — единственный вход мимо `url.Parse`

**Где:** `node_parser_vmess.go:209-210` (`node.Server = add` дословно)
против общего пути `node_parser_core.go:415` (`parsedURL.Hostname()`).

**Что делает:** снятие скобок у IPv6 у всех остальных схем — **побочный
эффект** `net/url`, а не осознанное правило. Поэтому дыра ровно одна:
`add: "[::1]"` уезжает в тело со скобками и ядру негоден. Функции вида
`stripBrackets` в core нет вовсе.

**Судьба:** **дельта** D133-17 — чинится JSON-форма; legacy
(`splitVMessHostPort`, `:91-97`) уже целевой.

### Q133-39 · общий разбор заголовков, разные имена параметров

**Где:** `node_parser_http.go:115` (`headers`) и
`node_parser_naive.go:137` (`extra-headers`) при **общем**
`parseNaiveExtraHeaders` (`node_parser_naive.go:81`).

**Что делает:** разбор один, имена входа разные, ключи выхода тоже
(`headers` против `extra_headers`). Ссылка с `?extra-headers=` на
http-прокси теряет заголовки, причём код сюда не ставится **намеренно**
(`node_parser_http.go:116-118`).

**Судьба:** **дельта** D133-18 — `extra-headers` становится алиасом,
каноническим для эмита остаётся `headers`.

### Q133-40 · ssh: заплата `+` существует, но ssh её не зовёт

**Где:** `queryParamPreservePlus` (`node_parser_wireguard.go:353-368`)
зовётся из wireguard (`:93`, `:178`), masque (`node_parser_masque.go:58`)
и awg3 (`awg3.go:186`, `:237-239`) — и **не** зовётся из
`node_parser_ssh.go:32`, где `private_key` берётся из `Query()`.

**Что делает:** тело PEM-ключа портится на чужой ссылке с сырым `+`.
Комментарий `node_parser_ssh.go:12-15` явно отказывается от второго
`QueryUnescape`, но не замечает, что `Query()` уже съел плюсы.
Round-trip-тест зелёный, потому что наш эмиттер пишет `+` как `%2B`
(`shareuri_ssh.go:125`) — ломается только чужая ссылка.

**Судьба:** **дельта** D133-15 — заменяется `format: "pem"`, заплаты
удаляются при переходе схемы на движок.

### Q133-41 · имя заголовка проверяется ЧУЖИМ алфавитом, а не `[^:]`

**Где:** `naiveHeaderNameCharset` (`node_parser_naive.go:44-51`) и
`isValidNaiveHeaderName` (`:55`), зовётся из `parseNaiveExtraHeaders`
(`:99`).

**Что делает:** пара отбраковывается не по «нет двоеточия», а по алфавиту
имени: `X User:bad` (с пробелом) выброшена, `X-Good:ok` остался
(`uri/naive/extra_headers_bad_name_dropped`). Алфавит — подмножество RFC
7230 tchar, взятое из гиста DuckSoft ДОСЛОВНО и сознательно уже нужного.
Регулярка `^(?P<k>[^:]+):\s*(?P<v>.*)$`, записанная в секции реестра,
этого не выражает: пробел в имени она принимает.

**Судьба:** **сохранено примитивом** — алфавит переезжает в саму
регулярку записи (`[!#$%&'*+\-.0-9A-Z\\^_`a-z|~]+`), проверка значения на
CR/LF/NUL — в неё же. Кода реализации не остаётся, поведение то же.

### Q133-42 · `list` + `extract` = карта пар, и это НЕ выражено

**Где:** `parseNaiveExtraHeaders` (`node_parser_naive.go:81-115`) —
единственное место, где один параметр ссылки несёт СПИСОК пар и едет в
объект тела.

**Что делает:** записи `extra-headers` (naive) и `headers` (http)
объявлены в реестре как `type:"object"` + `list.sep:"\r\n"` + `extract`
с группами `$key`/`$value`. Движок же применял `extract` к ОДНОМУ
значению, и `list` перед ним превращал значение в срез: регулярка ловила
первую пару и клала её строкой не туда. Заголовки терялись целиком на
четырёх кейсах naive и одном http.

**Судьба:** **сохранено примитивом** — `$key`/`$value` становятся
исполняемыми: `extract` при объявленном `list` идёт ПО ЭЛЕМЕНТАМ, пара
кладётся в объект по `maps_to`. Добавление к FROZEN, §0.10.

### Q133-43 · `single_into` не снимает разобранный userinfo

**Где:** `applyUserInfo` (`linkmap/exec.go:143`) против лексера
(`linkmap/parse.go`, ветка без `:` кладёт весь userinfo в `UserName`).

**Что делает:** `naive+https://onlypass@host` по конвенции DuckSoft даёт
`password=onlypass` и ПУСТОЙ `username` (`node_parser_naive.go:123-126`
пишет `username` только из `node.UUID`, который у одиночного userinfo не
заполняется). Движок исполнял `single_into` верно, но запись `username`
отдельно читала источник `userinfo.user`, куда лексер уже положил тот же
текст, — и узел получал обоих.

**Судьба:** **сохранено примитивом** — `single_into` перенаправляет само
ПРОСТРАНСТВО: `userinfo.user` пустеет, `userinfo.pass` получает значение.
Источник — одна истина для всех записей, иначе конвенция держится на том,
чтобы никто не объявил запись над `userinfo.user`.

### Q133-44 · дефолт порта у http задаёт написание схемы

**Где:** `node_parser_http.go:68-71` (`defaultPort := 80`, у https —
443).

**Что делает:** дефолт порта — не свойство схемы реестра (у http он
ОДИН), а свойство написания: `proxy-http` → 80, `proxy-https` → 443.
`scheme_sets` умеет писать только пути тела, и служебный ключ
`$default_port` уезжал в тело литералом — узел терял `server_port` и
падал у санитайзера с `field_missing`.

**Судьба:** **сохранено примитивом** — `$default_port` становится
объявленным служебным ключом `scheme_sets`: он не пишется в тело, а
питает `materialize_default` записи порта. Добавление к FROZEN, §0.10.

### Q133-45 · у http TLS-блок полный, а `uri_with_host` — только `sni`

**Где:** `node_parser_http.go:140-146` зовёт `trojanTLSFromNode(node,
"http")` — ВЕСЬ блок trojan-конвенций (sni/peer/host, fp, alpn, insecure,
utls), а не одну запись.

**Что делает:** блок реестра `tls.blocks.uri_with_host` заведён с
припиской «подключается вместо `tls#uri`, не вместе с ним» и несёт ровно
одну запись — цепочку `sni`. Один `include` давал узлу
`tls.server_name` и терял `alpn`/`insecure`/`utls`
(`uri/http/proxy_https_full_tls`).

**Судьба:** **сохранено примитивом** — подключаются ОБА блока
(`tls#uri`, затем `tls#uri_with_host`): позиция объявления перекрывает
одноимённую запись `sni`, ровно тот же приём, на котором стоят `vless.fp`
и `anytls.fp`. Приписка блока исправлена. Селектор `security` при этом
перекрывается СВОЕЙ записью http: у http TLS включает написание схемы, а
`sets[""]` общего блока включал бы его и простому прокси.

### Q133-46 · «обязателен» проверяется у пяти схем из десяти

**Где:** `node_parser_core.go:403-405` — список
`vless|trojan|ssh|tuic|anytls` проверяет `Hostname()` и `User`;
`node_parser_core.go:413` (hysteria) и `:417` (socks) проверяют только
хост; naive, http и ss в списках отсутствуют.

**Что делает:** узел без адреса роняют ОБЕ дороги, но называют причину
по-разному. У пяти перечисленных схем это `parse_error` — разбор
отказался. У naive тот же узел доезжает до санитайзера и роняется его
правилом `body.fields.server` с кодом `field_missing`, то есть
`emit_error`. Причина входит в контракт (корпус пишет и `reason`, и
`code`), поэтому списки нельзя ни расширить, ни сократить молча.

**Судьба:** **сохранено примитивом** — `required` у записи означает
отказ РАЗБОРА, и объявлен он ровно там, где прежний путь отказывался.
У naive.server снят: отбраковку делает санитайзер, и это разные коды, а
не разные слова об одном.

### Q133-47 · REALITY строит ОДНА из двух функций TLS

**Где:** `vlessTLSFromNode` (`node_parser_transport.go:904`, ветка `pbk`
на `:932-945`) против `trojanTLSFromNode` (`:1036`), где ветки `pbk` нет
вовсе. Вторую зовут trojan (`node_parser_core.go:936`) и http
(`node_parser_http.go:136`).

**Что делает:** `pbk=` в ссылке trojan или http **игнорируется** — блок
`reality` не появляется. `trojan.json` пишет это прямо: «REALITY у
trojan-URI не разбирается ни одним проектом (pbk игнорируется; reality
приезжает только через sing-box/Xray-импорт)».

**Как всплыло:** не корпусом — ни одна фикстура trojan и http не несёт
`pbk`, и переключение схем прошло зелёным. Нашла сверка LxBox на своих
фикстурах (пункт 1), подтверждено живым прогоном.

**Судьба:** **сохранено примитивом** — записи `pbk`/`sid`/`key_share`
вынесены из `tls#uri` в отдельный блок `tls#uri_reality`. Подключают его
vless и anytls (у anytls REALITY по ссылке есть, корпус
`uri/anytls/reality_pbk_sid`); trojan и http — нет. Общий блок отдавал
REALITY всем четырём.

### Q133-48 · целое из реестра приезжает float64

**Где:** `applyDefaults` (`linkmap/exec.go`) и любой путь, кладущий в тело
значение `defaults` / `sets` / `scheme_sets`: они разобраны
`encoding/json`, а он делает из числа `float64`.

**Что делает:** `server_port: 22` в `defaults` кладёт в тело
`float64(22)`. `json.Marshal` печатает его так же, как `int`, поэтому
канон сверки и весь корпус разницы **не видят** — а код, читающий тело
ассертом `.(int)`, молча получает ноль. Это та же мина, на которой у
hysteria v1 терялись `up_mbps` и `server_ports`, и ядро не стартовало
(память проекта `json-map-type-assert-trap`).

**Судьба:** **сохранено примитивом** — `setPath` приводит целое к `int`
на записи в тело. Дробное остаётся `float64`: превращать `1.5` в `1`
значило бы судить значение, а маппер значений не судит.

### Q133-49 · `?password=` у ssh — побочный след воронки, а не написание

**Где:** `node_parser_core.go:470-474` — userinfo-пароль пяти схем (ssh,
trojan, socks, naive, tuic) старый путь клал в `node.Query["password"]`, и
дальше `buildSSHOutbound` (`node_parser_ssh.go:27`) читал его ОТТУДА.

**Что делает:** воронка сливает два разных места ссылки в один слот,
поэтому `ssh://user@host?password=x` разбирался наравне с
`ssh://user:x@host` — хотя `?password=` не объявлен ни в `uri.query`
секции (там шесть имён, `password` среди них нет), ни в корпусе (все
кейсы — `user:pass@`), ни у эмиттера (`shareuri_ssh.go:118` пишет пароль
ТОЛЬКО в userinfo, `url.UserPassword`). То есть ссылку с `?password=`
не отдаёт никто, а читал её только наш парсер — написание, которое
никогда не round-trip'илось.

**Как всплыло:** `TestGenerateNodeJSON_SSH` — единственное место во всём
дереве, где встречается `ssh://…?password=`, синтетический smoke-URI.
На движке тест покраснел: движок читает объявленный `userinfo.pass`.

**Судьба:** **сохранено как есть** — реестр не расширяем, написание не
вводим. Тест переведён на контрактную форму `user:pass@`. Тот же вопрос
у trojan/socks/naive/tuic снимается сам: у них `?password=` живой ссылкой
тоже не встречается, а userinfo объявлен.

### Q133-50 · форма опознавалась по тексту, который она ещё не разобрала

**Где:** `UnwrapURI` (`linkmap/parse.go`) — `SelectForm` получал ИСХОДНЫЙ
текст ссылки, а `decode` формы применялся уже после выбора.

**Что делает:** там, где формы различает ПЕЙЛОАД, предикат не срабатывал
никогда. У vmess обе формы — «base64 на authority», и что под ней —
объект JSON v2rayN или cleartext `method:uuid@host:port` — видно только
после декодирования; `detect: {json: {type_of: {$root: object}}}`
смотрел на текст `vmess://eyJ2…` и давал ложь, а всё забирала
`default`-ветка `legacy`. Прежний путь решает ровно наоборот: декодирует,
пробует `json.Unmarshal`, откатывается (`node_parser_vmess.go:78-82`).

**Судьба:** **сохранено примитивом** — форма опознаётся по своему же
распакованному телу: сначала её конвейер `decode`, потом её `detect`.
Формам, различимым по оболочке (`regex` у ss), это ничего не меняет.

### Q133-51 · лексер резал base64 по его же алфавиту

**Где:** `splitAuthorityPart` (`linkmap/parse.go`) — резал текст по
первому из `#`, `?`, `/`.

**Что делает:** `?` и `/` — синтаксис ССЫЛКИ, которой на этом шаге ещё
нет: под base64 лежит неразобранный блоб, где `/` и `+` это законные
63-е и 62-е значения стандартного алфавита (RFC 4648 §4). Блоб
legacy-формы vmess (`method:uuid@host:port?type=ws&path=%2Fws`) содержит
`/` в теле кодировки — разрез отдавал декодеру ПЕРВУЮ ПОЛОВИНУ, base64
доедал её без ошибки, и узел молча терял весь query: транспорт, TLS,
путь. Тело при этом оставалось валидным, то есть отказа не было ни в
одном месте.

**Как всплыло:** корпус `legacy_cleartext_userinfo` на сверке тел.

**Судьба:** **сохранено примитивом** — режется только по `#`. Метка в
обеих формах стоит снаружи base64 открытым текстом и в алфавит не
входит.

### Q133-52 · «источник по формам» исполнялся склейкой всех форм

**Где:** `SourceRef.All()` — метод для линтера («перечисли всё, что
секция называет»), которым пользовалось и ИСПОЛНЕНИЕ метки
(`applyLabel`).

**Что делает:** `All()` склеивает ветки всех форм и сортирует по имени
формы. У vmess метка объявлена `{"v2rayn": "json.ps", "legacy":
"fragment"}` — то есть `fragment` оказывается РАНЬШЕ `json.ps`, и узел
формы-контейнера получал имя из фрагмента ссылки, хотя секция прямо
пишет, что фрагмент у неё не читается вовсе. Записи таблицы от этого не
страдали: у них ветка по форме выбиралась руками на месте
(`lookupSource`), то есть верное правило существовало в одном
экземпляре и не применялось во втором.

**Как всплыло:** корпус `fragment_after_base64` — ждётся `frag` из `ps`,
приходило `MyNodeName` из `#`.

**Судьба:** **сохранено примитивом** — `SourceRef.ForForm(id)`, и оба
места (метка и записи) зовут его. `All()` остаётся линтеру.

---

## Сводка

| Судьба | Записей |
|---|---|
| сохранено примитивом | 29 |
| дельта `DELTAS.md` | 12 |
| кандидат / открытый вопрос | 7 |
### Q133-53 · `tcp_keep_alive`: ответ LxBox устарел через волну

**Симптом.** В `TASKS_LXBOX` §24.29 записано: «параметры одинаково не
читаются всеми девятью, просто у двух схем они ещё и дают
`uri_param_unknown`». Факт был снят по коду ДО удаления рукописного пути
(`git show 80989841^`) и был верен ровно до того момента, когда семь схем
переехали на движок: у них есть `include dialer#uri`, а у блока записи с
`maps_to` — то есть параметр стал ЧИТАТЬСЯ в тело.

**Перемерено 20.09.2026:**

| | `?tcp_keep_alive=30s` | `?tcp_keep_alive=30` |
|---|---|---|
| семь схем с include | в теле `"tcp_keep_alive": "30s"` | `type_invalid` (тип `duration`, голое число ядру не годится) |
| vless, trojan | `uri_param_unknown` | то же |

**Обе ветки корректны каждая по себе**, и чинить здесь нечего: поле
объявлено в теле через `__dialer`, голое число отвергается по делу,
незаявленный параметр честно зовётся неизвестным. Настоящий вопрос —
подключать ли `dialer#uri` к vless/trojan, и он **меняет тела живых узлов**,
так что ждёт владельца. Не подключено.

**Урок.** Факт, снятый по коду, живёт до следующей волны того же кода.
Ответ стороне, содержащий измерение, стоит перемерять перед тем, как на него
сослаться, — особенно если между ответом и ссылкой кампания что-то
переносила. Поправка отправлена параграфом §24.33.

**ЗАКРЫТО** (`a0c403ab`, дельта D133-26): владелец подключил `dialer#uri` к
vless/trojan, а нормализация голого числа переехала в `body.fields` — обе
ветки сошлись в одну. Попутно нашлось, что keep-alive не пишет НИ ОДИН
эмиттер share-URI: узел, прочитавший параметр, теряет его при Copy link.
Чинится волной эмита.

---

### Q133-54 · переопределение записи блока живёт ДВУМЯ несовместимыми приёмами

**Где:** `contract/registry/protocols/http.json` `params.security` против
`anytls.json` `params.security`; норма — `GRAMMAR_SYNC` §2.

Норма §2 говорит: совпало имя записи И полный набор `source` — запись блока
**изымается** из плана, исполняется только запись секции. Причина названа
верно: переопределяющая запись обычно ведёт в ДРУГОЙ путь тела, и исполнение
обеих оставляет в теле оба пути.

Но в живом реестре на этот момент **два разных приёма** под одним видом:

| Приём | Кто | Как работает |
|---|---|---|
| **замещение** | `anytls.security`, `anytls.fp`, `tuic.disable_sni`, `hysteria2.sni` | запись секции ведёт в ТОТ ЖЕ путь и перекрывает блочную через `priority` + `merge: overwrite`. Изъятие безвредно: результат тот же |
| **гейт по условию** | `http.security` (Q133-45) | запись секции гейтится `when.scheme.not_in: [proxy-https]`: на http-суффиксе она снимает блок TLS, а на https-суффиксе **молчит**, и истину там ведёт запись БЛОКА |

Изъятие, написанное буквально по §2, ломает второй приём: блочная запись
исчезает всегда, и на https-суффиксе `security=none` перестаёт снимать TLS
(`TestParseNode_HTTPProxy_SecurityNoneDisablesTLS` покраснел сразу).

**Судьба:** норма **уточнена**, а не реестр переписан: запись ПОД УСЛОВИЕМ
переопределением не считается. Это не послабление — условная запись
исполняется лишь в части случаев, и изъять за неё блочную значит оставить
остальные случаи без правила вовсе.

**Урок.** «Совпало имя и источник» — не то же самое, что «делает то же
самое». Прежде чем изымать чужую запись за совпадение объявления, надо
спросить, ВСЕГДА ли исполняется та, ради которой изымаем. Приём с гейтом
выглядит как дубль ровно до момента, когда условие не выполнено.

### Q133-55 · `transports#xray`: у ВСЕХ путей не хватало `streamSettings.`

**Где:** `contract/registry/transports.json`, `blocks.xray` — все записи
подблоков `ws`, `httpupgrade`, `http`, `grpc`, `xhttp`.

**Что делает:** источники записаны как `json.wsSettings.path`,
`json.grpcSettings.serviceName`, `json.xhttpSettings.mode` — путь от КОРНЯ
элемента. В Xray-конфиге эти контейнеры лежат внутри `streamSettings`.
Полный путь стоял у ОДНОГО `$selector.network`, поэтому транспорт
ОПОЗНАВАЛСЯ (`transport.type` писался), а все его поля терялись молча.

**Почему дожило:** секции `mappers.xray` не исполнялись — блок был написан,
но не подключён. **Данные без исполнителя не проверяются ничем**, и
никакой линтер этого поймать не мог: путь синтаксически безупречен.

**Судьба:** исправлено (контракт 1.1.27), поймано раннером
`TestEngineVsXrayCorpus` на первом же прогоне. Отдельного имени правка не
заводит — это опечатка, а не решение.

### Q133-56 · `xhttp` у `transports#xray` был на четыре записи вместо 29

**Где:** там же, `blocks.xray.xhttp`.

**Что делает:** лежали `$settings` (с `flatten`, но БЕЗ `maps_to` — то есть
читала в никуда), `mode`, `path`, `host`. Остальные ~25 полей XHTTP и
вложенный `xmux` не были объявлены вовсе, тогда как у `blocks.uri.xhttp`
таблица полная. Один и тот же транспорт описан двумя источниками с разной
полнотой.

**Почему важно:** ровно на этом месте у LxBox при переезде молча уехало
~25 полей — это и есть предупреждение, вынесенное в ТЗ волны.

**Судьба:** таблица приведена к `blocks.uri.xhttp` запись в запись
(контракт 1.1.27). Диалект Xray выражен `source`: контейнер под двумя
именами (`xhttpSettings` ∥ `splithttpSettings`), `xmux` — настоящий
вложенный объект, живущий и внутри `extra`, и рядом с ним.

---

| сохранено как есть (починка после кампании) | 4 |

Журнал пополняется **на каждом красном кейсе сверки, до его исправления**.

### Q133-57 · `_…` в присваиваниях отсекался только у `scheme_sets`

**Где:** `core/config/linkmap/exec.go`, `applyAssigns` и `applyDefaults`.

**Что делает:** служебное имя ключа (`$default_port` у http, прозаическая
сноска `_why` рядом с присваиванием) — НЕ путь тела, и писать его туда
нельзя. Отсечение стояло ровно в одном месте — у `scheme_sets`, — и стояло
там потому, что однажды выстрелило: ключ уехал в тело литералом, и узел
потерял `server_port` (Q133-44). Остальные приёмы присваивания (`sets`
селектора, `implies`, `defaults` секции) шли той же дорогой и ждали
первого такого ключа.

**Почему дожило:** ни одна живая запись реестра служебного имени в них не
кладёт — проверено грепом по всем `mappers`/`blocks`: `$`-ключ ровно один
(`$default_port` в `scheme_sets` http), `_`-ключей нет. Дефект был
потенциальным, а не живым, и поймать его мог только тот, кто напишет такой
ключ первым. Написать его собирается LxBox (заявка 21.09.2026).

**Судьба:** проверка сведена в одну функцию `isReservedAssignKey` и
вызывается у ВСЕХ приёмов: вопрос про ИМЯ, а не про то, каким приёмом имя
попало в карту. `scheme_sets` сохраняет свою ветку — там `$`-ключ не
просто отбрасывается, а запоминается для записей таблицы.

### Q133-58 · Незнакомый ключ `.conf` отбрасывается МОЛЧА

**Где:** `core/config/subscription/node_parser_amnezia.go`, `wgConfToURI`.

**Что делает:** боевой путь `.conf` идёт не через секцию `mappers.conf`, а
через конвертер `.conf` → ссылка с последующим разбором секцией
`mappers.uri`. Конвертер переносит в query ТОЛЬКО перечисленные у него
ключи, и всё остальное (`FwMark`, `Table`, `PreUp`, любая опечатка в имени)
исчезает между форматами: ни кода, ни строки в логе.

**Проверено фактом** (21.09.2026): `.conf` с `BogusKey`, `FwMark` и
`AnotherJunk` даёт чистое тело без единого лишнего ключа и **без единого
предупреждения**. Опасения «незнакомый ключ уронит ядро» не подтвердились:
до тела он не доходит вовсе. Настоящая цена другая — человек не узнаёт,
что часть его файла не прочитана.

**Судьба:** не чинится этой волной. Код `wgconf_param_unknown` осмыслен
только тогда, когда `.conf` разбирает СЕКЦИЯ: у конвертера нет ни таблицы
объявленных имён, ни места, куда код прицепить. Это вход задачи «секция
`mappers.conf` — в бой», где `unknown_key` секции заработает вместе с
`ini_dialect`.

### Q133-59 · `mappers.conf` написана и не исполняется никем

**Где:** `contract/registry/protocols/wireguard.json`, секция `mappers.conf`.

**Что делает:** секция полная — `detect` по ini, `kind_when` для awg/awg3,
38 записей, `label` с цепочкой `ini.$comment.Peer`. Не зовёт её НИКТО:
боевой путь ведёт `wgConfToURI`, раннера сверки у вида `conf` нет, корпус
`wgconf` (один кейс) проходит через конвертер.

**Почему важно:** это та же конфигурация, в которой у `transports#xray`
пережили несколько ревизий неверные пути у ВСЕХ записей (Q133-55).
**Данные без исполнителя не проверяются ничем**, и объявлять в такой секции
`ini_dialect` — значит писать вторую непроверенную страницу поверх первой.

**Судьба:** порядок работ развёрнут: сперва исполнитель (раннер сверки +
перевод боевого пути на секцию), потом диалект данными. Заготовка раннера
`TestEngineVsConfCorpus` написана.

### Q133-60 · Движок резал `.conf` по ПЕРВОМУ комментарию

**Где:** `core/config/linkmap/parse.go`, `unwrapBody` → `stripURIWrapper` и
ветка `ini` у `lexSpace`.

**Что делало:** оба места обращались с текстом `.conf` как с пейлоадом
ССЫЛКИ и снимали «метку-фрагмент» по первому `#`. У ссылки это верно
(`vmess://<base64>#Имя`), у ini-документа — разрушительно: `#` там
законный синтаксис тела, и именно им провайдеры пишут имя узла сразу под
`[Peer]` («# US-FREE#137»). Всё, что стояло НИЖЕ первого комментария,
отрезалось: файл терял `PublicKey` и `Endpoint` и отвергался как
«обязательное поле "peers[].public_key" пусто». Вторым эффектом в
`s.Fragment` уезжал весь остаток файла целиком.

**Как нашлось:** кейсом корпуса `ini_comment_label` на первом же прогоне
раннера `TestEngineVsConfCorpus` по РАСШИРЕННОМУ корпусу. На исходном
корпусе (один кейс `ini_basic`, комментариев внутри тела нет) дефект был
невидим — ровно предсказание Q133-59 о непроверяемых данных.

**Почему не всплывало:** боевой путь `.conf` шёл через `wgConfToURI`, и в
секцию `mappers.conf` текст не попадал никогда.

**Судьба:** исправлено. `ini` исключено из снятия оболочки (`#` и `://`
принадлежат ТЕЛУ, а не обёртке), фрагмент у ini не выставляется вовсе —
имя узла даёт объявленная цепочка `ini.$comment.<Секция>`.
