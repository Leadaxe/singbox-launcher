# SPEC 142 — вычистка рукописных правил об узлах вне реестра

Курс владельца (25.09.2026): реестр `contract/registry/**` важнее локальных
правил. Любое правило о полях узла (допустимость, значения, связи, дефолты,
деградации) живёт только в реестре; в Go остаётся общий движок
(`core/config/linkmap`, `core/config/nodeflow`) без имён схем. Рукописные
копии переносятся в примитив реестра (с кодом предупреждения, если узел
меняется) и удаляются вместе с тестами, которые проверяли копию.

Находки и их разбор — [AUDIT.md](AUDIT.md) (группы A–D, номера находок ниже
ссылаются на него).

## Волны

| Волна | Находки | Контракт |
|-------|---------|----------|
| 1 | A1–A11 (удаление копий, мёртвый код) | без бампа, если данные реестра не меняются |
| 2 | B1 + C1 → снять `SanitizeSingboxOutboundMap` целиком | бамп |
| 3 | B2–B10 | бамп |
| 4 | C2 — роль «главный секрет» узла | бамп |
| 5 | C3 — гейты возможностей ядра (naive/tailscale/AWG3) из `build_tag`/`min_core` | бамп |
| 6 | C4 — REALITY ↔ uTLS: одно правило в реестре, с кодом | бамп |
| 7 | C5–C10 | бамп |
| 8 | D1–D4 + проверка в `contract/tools`, что refs/impl/go указывают на существующее | бамп |

## Правила для каждой волны

- Работа в git worktree от `develop`; коммиты явным списком путей; в develop —
  только `git merge --ff-only`. Ветки основной копии не переключать.
- Изменение `contract/registry` = бамп `contract/VERSION` + changelog +
  параграф в `contract/TASKS_LXBOX.md` (что меняется для LxBox).
- В движке — ни одного имени схемы/протокола; нужен частный случай — ищи
  общий примитив реестра.
- Поведение для пользователя не ухудшать: узел, который сейчас работает, после
  волны работает так же; любая правка узла — с кодом из `warnings.json`
  (+ `bin/locale/ru.json` для новых ключей).
- Проверки локально: `go build ./...` и только свой новый тест через `-run`;
  раннер корпуса контракта — по имени, если менялись реестр/корпус. Полный
  прогон — CI после волны.
- В конце волны — строка в `docs/release_notes/upcoming.md` при изменении
  поведения и отметка в таблице ниже.

## Статус

| Волна | Коммит | Контракт | Итог |
|-------|--------|----------|------|
| 1 | 4f98a370 | без бампа (1.1.56) | A1–A11 закрыты; данные реестра не менялись. В Go остались: запасная схема `socks` у пустого `ParsedJump.Scheme` (формат старого состояния) и префикс контейнера `vpn://` (detect в containers.json — проза); refs/impl реестра на снятые функции — волна 8 (D) |
| 2 | 9547f74c | 1.1.57 | B1 + C1 закрыты, `SanitizeSingboxOutboundMap` и `singbox_sanitize.go` сняты целиком (хелперы `IsSingbox{Service,Group}Type`/`mapString` переехали в `singbox_import.go`). Новый примитив `on_invalid: unwrap` (`key`, `else_code`) — hysteria v1 `obfs` объектом → строка-пароль с `obfs_object_flattened` / снят с `obfs_password_missing`; masque `network`/`sni`/`skip_cert_verify` — в `body.skipped`, снимаются общим `unknown_key`. Все входы тела, с кодом. Корпус +3 (`hysteria_obfs_object_*`, `masque_legacy_flat_keys`), Go-тесты копии сняты. Хвосты на волну 8: протухшие `go`/`refs` на `singbox_sanitize.go` в `tls.json`, `vless.json`, `warnings.json`; UI `deriveTransport` читает плоский `network` у masque (B8) |
| 3 | 4bb75e8e | 1.1.58 | B2, B3, B5, B6, B8 (транспорт), B9, B10 закрыты; B7 частично. Данные: `chain.strip` `order`+`fields` (каталог ядра, `default` = снимается при strip_evasion; ключи с точкой), wireguard `ip` `requires id when ip=quic`, `id` `pattern` LDH ядра, tailscale `exit_node_allow_lan_access` `requires exit_node`. Registry API: `Field.AllowedForScheme`, `FieldAllowed`, `FieldStrings`, `FieldsWithBuildTag`. B2 — masque теперь получает глобальную фрагментацию (реестр её разрешает, ядро исполняет на h2). B10 — параметр значения = первый `params` кода; `reason` отказа — общий текст (заголовок `transport_unsupported` описывает замену-fallback Dart, а не отказ). Перенесено: **B4** → волна 6 (C4: проверка по `requires` reality→utls зависит от смены этого правила); **B5** `jmin ≤ jmax` → волна 7 (C8, форма держит проверку до примитива), стартовый профиль 4/40/70 — подсказка формы, в реестре такого понятия нет; `clearRangedKeepalive` → волна 5 (C3); **B7** `auth_key` обязателен — решение владельца; запрет дефолтного маршрута и маскирование префикса в `advertise_routes` → волна 7 (C9 + нормализация), `format: cidr` пропускает голый IP; **B8** уровень AWG → волна 5 (C3); **B9** `masqueVHTTPValues` в миграции оставлен (разовая). Найдено в движке (волна 7): поле, снятое правилом значения/связи, не помечается `removed`, и `requires` следующих соседей видит его в исходной карте (ip снят за пустой id → ib всё равно остаётся) |
| 4 | 18fb2f11 | 1.1.59 | C2 закрыт, C6 закрыт попутно (перенесён из волны 7). Новый атрибут поля `role` (`credential` \| `private_key`), только верхний уровень тела протокола, одна роль — одно поле на схему (линтер `checkFieldRoles` в `TestRegistryBodyStructure`). Registry API: `FieldWithRole`, `Credential`, `RoleCredential`/`RolePrivateKey`. Сняты `singboxCredentialFromMap`, `canonicalCredential`, `credentialFromBody` и ставший мёртвым `linkmap.Result.HadUserInfo`; таблица `shareURISecretCarrierFields` заменена ролью `private_key`. Семантика: credential = поле учётной записи в слоте userinfo (uuid / пароль / auth_str / имя пользователя), шифр ss/vmess не в счёт, у wireguard/masque/tailscale роли нет. Изменения `UUID`: JSON ssh/naive/socks/http "" → имя пользователя; ссылка ss шифр → пароль; ссылка wireguard/masque приватный ключ → ""; ссылка без userinfo с полем учётной записи в query теперь его несёт. Читателей `ParsedNode.UUID` в рабочем коде нет (skip-фильтры и `getNodeValue` ключа `uuid` не знают, аудит C2 здесь неточен) — поле читают только тесты и копирование в звенья цепочки; кандидат на снятие отдельным решением |
| 5 | 38fba8f0 | 1.1.60 | C3 закрыт, хвосты волны 3 (B5 `clearRangedKeepalive`, B8 уровень AWG) закрыты. Новые атрибуты: `on_core_unsupported {drop_node, code}` (тело протокола, поле, `range_form`), `range_form {min_core, build_tag, level, on_core_unsupported}` (только `awg_range`), `levels` тела + `level`/`level_mark` поля. Движок: `nodeflow.NodeCoreRefusal` (CoreInfo += `Tags`, `TagIssues`), Registry API `WalkPresent`, `Level`, `StripBuildTag`, `IsRangeValue`. Приложение даёт только теги сборки (`config.CoreBuildTagsProbe` ← `CoreBuildTags`; libcronet у purego = проблема тега `with_naive_outbound`). Сняты: `Naive/Tailscale/AWG3SupportProbe`, `CoreSupportsNaive/Tailscale/AWG3` с тремя кэшами и вердиктами, `subscription/awg3.go` целиком (`HasAWG3Fields`, `AWG3RootKeys`, мёртвые `Param`-таблицы), константы `WarnTailscaleCoreUnsupported`/`WarnAWG3CoreUnsupported`, `deriveAWGLevel`+таблицы ключей UI, `clearRangedKeepalive`, три вида отчёта (`naive/tailscale/awg3_degraded` → `core_unsupported`, одна запись на код+схему, заголовок из реестра), шесть полей `Skipped*` результата (→ `CoreSkips`). Поведение: снимаются те же узлы с теми же кодами (naive теперь с кодом `naive_unavailable` в логе); тексты причин общие («built without <tag>», «core X does not support <scheme.path> (need Y or newer)»); ядро ровно `1.14.0` без `-lx` с `with_awg` теперь считается старше lx.32 (было «не гадаем»). Хвосты: body-гейт не объявлен у wireguard/quic/masque/chain (нужна сверка `Tags:` реальных сборок); цепочки — свой `ChainSupportProbe`; протухшие `go`-ссылки warnings.json на `awg3.go` (awg3_field_invalid и соседи) — волна 8 |
| 6 | 47e163af | 1.1.61 | C4 и B4 закрыты. Новые примитивы: `requires[].set` (сосед материализуется с кодом вместо снятия поля), `coerce_when` у поля (условная замена годного значения) — оба судятся по готовому телу после обхода (`deferred`, режим `final`: условия читают только чистое тело), коды встают на место по `body.order`; `on_hop_required {unstrip}` у ключа каталога strip цепочки. Registry API: `Relation.Set`, `Field.CoerceWhen`, `Field.OnHopRequired`, `configtypes.ChainStripOnHopRequired`; nodeflow: `Result.Implied/Repaired/RepairWarnings`, `StripBlocked`, `Repairs`. Данные: `tls.reality.enabled` requires `tls.utls.enabled` set true (`reality_utls_enabled`), `tls.utls.fingerprint.coerce_when` random → chrome при REALITY (`reality_fp_random_pinned`), chain strip `tls.utls` `on_hop_required` (`chain_strip_utls_on_reality` → warning). Сняты: `EnforceRealityFingerprint` (файл `node_parser_transport.go`), `HealRealityFingerprints` и шаг сборки, `NodeUsesReality`/`ChainStripsUTLS`/`ChainRealityConflict` + их тесты, `reality_fingerprint_test.go`. Поведение: REALITY без uTLS больше не снимается (был голый uTLS на REALITY-сервер после сборочной починки); random под REALITY → chrome с кодом на всех входах, узел с негодным pbk остаётся с random; цепочка со strip tls.utls и REALITY-звеном собирается (uTLS у всех звеньев) вместо исключения; замороженные тела состояния и ручной config_json получают только правила-починки на сборке (`nodeflow.Repairs`, код в лог). Факты ядра lx.3: reality_client.go:61, utls_client.go:363-370/384, u_common.go:649,657, transform.go:228-232. Хвосты: код цепочки — только в логе (у цепочки нет слота warnings); неявный D-009 random под REALITY получает info-код наравне с явным (маппер не менялся); протухшие `impl` tls.json на `node_parser_transport.go` (файл удалён) — волна 8 |
| 8 | 8fa95fc3, 697894fd, 687da4be | 1.1.62 | D1–D4 закрыты. Данные реестра не менялись — только строки-заметки. `refs.go` 141 → 53: у 11 протоколов, целиком исполняемых движком, ключ `go` в `refs` снят, у остальных — код, специфичный для схемы, в форме `путь.go[:Имя]`; ~335 строк `impl`/`go`/`note`/описаний переведены на место исполнения сейчас, ложная проза (masque sni/skip_cert_verify, vless flow, hysteria up_mbps, эвристика SNI anytls и др.) поправлена по факту. Формат ссылки — норма `contract/README.md`. Линтер `TestRegistryCodeRefsResolve` (`core/config/registry_refs_test.go`): все строки реестра кроме `dart` — путь от корня существует, `:Имя` объявлено в файле (func/type/var/const, `Тип.Метод`, `Тип.Поле`), номер строки и голое имя файла — ошибка. Джоба Contract теперь идёт на любой `.go` (переименование функции ломает реестр). Хвост: `ParsedNode.UUID` (волна 4) и `refs.dart` вне волны |
| 7 | a7377d1f | 1.1.63 | C5, C7, C8, C9, C10 закрыты; хвосты волны 3 (B5 `jmin ≤ jmax`, B7 запрет дефолтного маршрута и маскирование префикса, `format: cidr` пропускал голый адрес) и волны 1 (префикс `vpn://` в коде) закрыты; пробел движка закрыт. Примитивы тела: `relations.kind: ordered` + action `drop` (только у ordered), `item_forbidden {values, code}`, normalize `cidr_masked`, `exit_capable_when` у тела протокола. Примитивы маппера: источники `context.<путь>` (от вызывающего) и `ref.<as>.<путь>`, `deref {key, as}` у записи (сосед по документу, слой до `when`), `substitute {sep, join, tokens}`, оператор `when` `type_of`. Движок: `sanitizer.dropped` — поле, снятое правилом значения/связи, для последующих связей и условий отсутствует (PARSING_PRINCIPLES §6.2); `requires` к обязательному полю своего объекта, снятому своим правилом, снимает зависимое без второго кода (REALITY short_id/key_share при негодном pbk — коды прежние); `requires … set` к снятому соседу его материализует. API: `registry.Registry.ExitCapable`, `Condition.HoldsOn`, `linkmap.ParseElementInDoc`, `ParseURIContext`, `MatchesUnwrap`, `subscription.ParseWGConfByEngineContext`. Данные: wireguard ordered jmin/jmax (`fields_order_invalid`), wireguard conf `mtu_container` + `dns.substitute`, tailscale advertise_routes `cidr_masked` + `item_forbidden` (`tailscale_default_route_advertised`), tailscale `exit_capable_when {any_set: [exit_node]}`, dialer.json xray `fragment_via_dialer`. Сняты: `applyXrayFreedomFragment`/`xrayFreedomFragmentSpec`/`nodeOutboundTLSEnabled`, `amneziaPrepareConf`/`amneziaMTUValue`/`amneziaSetInterfaceLine`/`amneziaReplaceDNSLine`/`parseWGConfSections`, литерал `vpn://` (ParseNode, IsDirectLink, ClassifySubscriptionBody), проверка jmin ≤ jmax формы AWG, `parseTailscalePrefixList` + тест, три ключа locale. Поведение: jmin > jmax — оба поля сняты с кодом (было: отказ всего конфига на любом входе, кроме формы); advertise_routes приводятся на всех входах, дефолтный маршрут снимается элементом с кодом; фрагментация Xray у звена цепочки ставится звену, а не владельцу (прежде `return` до `SyncJumpFromChain` — теперь `break`); `VPN://` опознаётся (prefix_fold реестра); ib/allow_lan/plugin_opts и т. п., требующие снятого соседа, теперь снимаются с field_requires. Корпус +4. Не сделано: вперёд-смотрящие связи (поле раньше по order судит соседа, снятого позже) — порядок обхода, без неподвижной точки; `i1` conflicts id/ip/ib остаётся по исходной карте |
