# TASKS 105 — DNS-группы в лаунчере

## Сделано

- [x] Проверено, что ядро форка принимает `{"type":"group","servers":[…]}`
      (`sing-box check` проходит; выдуманный тип отвергается)
- [x] Хранение: группа — обычный `kind=user` с произвольным телом,
      отдельного вида записи не нужно; round-trip покрыт тестом
- [x] Эмиссия: тело доезжает до конфига целиком (`servers`, `mode`,
      `error_ttl`), поля визарда вычищаются
- [x] **Найдена и закрыта дыра**: `stripDNSWizardOnlyFields` чистила легаси
      `if`/`if_or`, но не канон SPEC 107 — `#enable` уезжал ядру как
      неизвестное поле. Тот же изъян в `preset_merge` (гейт считался
      match-полем, и пустое правило признавалось содержательным)
- [x] UI: выбор заготовки в диалоге добавления DNS-сервера — одиночный
      сервер или группа, с подстановкой тегов существующих серверов
- [x] Локализация EN+RU

## Осталось

- [ ] Выравнивание формы `dns_options` обеих сторон под обменную схему
      (PLAN §9.D7) — после появления DNS-секции в LX Backup v1.1
- [ ] Живая проверка группы на рабочем конфиге (за пользователем)
