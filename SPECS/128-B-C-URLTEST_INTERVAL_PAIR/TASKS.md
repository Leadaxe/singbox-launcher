# TASKS 128 · Пара interval/idle_timeout

## Этап 1. Диагностика

- [x] Воспроизвести по issue #118; установить настоящую причину — не «3h много»,
      а отсутствие парного `idle_timeout` при дефолте ядра 30m
- [x] Подтвердить, что `sing-box check` класс не ловит: `check` exit=0,
      `run` → FATAL (ядро 1.14.0-lx.33)
- [x] Найти ВСЕ входы `interval` в конфиг — их четыре, а не один (SPEC §1.4)
- [x] Отдельная находка: штатный вариант `1h` в `wizard_template.json:486-494`
      ломает старт без всякой подписки
- [x] Установить, за что отвечает `idle_timeout` (таймер засыпания `loopCheck`),
      и обосновать асимметрию «тянуть вверх idle, не вниз interval»

## Этап 2. Решение

- [x] Выбрать точку — графовый санитайзер, одна вместо четырёх (PLAN §1)
- [x] Согласовать с владельцем коэффициент ×1 и границы задачи (только
      санитайзер; шаблон и форма — отдельно)

## Этап 3. Реализация

- [x] `core/build/outbound_graph_urltest.go` — правило + разбор длительностей
      ядра (суффикс `d`)
- [x] `outbound_graph_sanitize.go` — правило 6 в шапку, вызов в ветке `isGroup()`
      до разбора состава
- [x] WARN на каждое вмешательство, с обеими величинами и причиной

## Этап 4. Тесты

- [x] `interval 3h` без пары → `idle_timeout 3h`, `interval` нетронут
- [x] заданы оба, пара не сходится → вверх тянется `idle_timeout`
- [x] валидные записи не переписываются (короткий interval; сходящаяся пара)
- [x] `selector` не затронут
- [x] разбор `1d`, `0.5d`, `1d12h`, `90m`; мусор (`3`, `abc`, `1x`) отвергается
- [x] мусорный `interval` не подменяется и не порождает `idle_timeout`
- [x] сквозной: `1d` из подписки доезжает до правила

## Этап 5. Приёмка

- [x] `go build ./...`
- [x] `go vet ./core/build/`
- [x] `go test ./core/build/` — пакет целиком зелёный
- [x] **Живое ядро**: `run` на конфиге из issue — до FATAL, после стартует
- [x] `docs/release_notes/upcoming.md` — EN + RU
- [x] IMPLEMENTATION_REPORT.md
- [x] Развёрнутый ответ в issue #118
