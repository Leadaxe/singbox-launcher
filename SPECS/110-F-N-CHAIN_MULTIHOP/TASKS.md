# TASKS 110 — Цепочка хопов (`chain`)

Нормативка: [`SPEC.md`](SPEC.md).

**Первая реализация (фазы 1–6) сделана в модели Направления и оказалась
неверной** — разбор в SPEC, раздел «Ревизия». Ниже — задачи переноса в
источники. Что переносится как есть, помечено «↩».

## Фаза 0 — решения
- [x] Вариант (а) деградации: реализуем, не дожидаясь ядра с тегом
- [x] Цепочка = третий тип источника, а не Направление (ревизия)

## Фаза 7 — модель переезжает в источник
- [ ] `SourceChain` в `core/state` (поля те же, что были у `DirectionChain`)
- [ ] `Source.Type == "chain"`, `Source.Chain *SourceChain`
- [ ] Синхронизация `Connections ↔ ParserConfig` в обе стороны
- [ ] `Direction.Chain` и `DirectionTypeChain` убрать
- [ ] Миграция не нужна: тип в релиз не выходил

## Фаза 8 — эмиссия узлом
- [ ] Цепочка → `ParsedNode{EmitRaw: true}` по образцу `manual_config.go`
- [ ] Эмиттер `GenerateChainJSON` ↩ (форма объекта неизменна)
- [ ] Проверка позиций: все теги должны дойти до конфига
- [ ] Убрать врезки в три прохода генератора (проходы 1–3 снова про группы)

## Фаза 9 — T9, цикл группа↔цепочка
- [ ] Направление не берёт цепочку, которая идёт через это же Направление
      (транзитивно, с учётом цепочки-через-цепочку)
- [ ] Кейс корпуса на самый частый сценарий `[proxy-out, exit]`

## Фаза 10 — UI переезжает в Sources
- [ ] Третий тип в кнопке добавления источника
- [ ] Форма цепочки в окне Source (список позиций ↩, `+` ↩, D&D ↩,
      «Дополнительно» с `strip` ↩)
- [ ] Пометка в списке Sources; предупреждение о ядре без тега ↩
- [ ] Убрать вкладку «Цепочка» и переключатель типа из редактора Направления

## Фаза 11 — валидация ↩
- [ ] Инварианты ядра ↩, `utls`+`reality` ↩, вложенность ↩, `<tag>#<i>` ↩
- [ ] Цепочка и `detour` не смешиваются ↩

## Фаза 12 — контракт
- [ ] Схема: `chain` уезжает из `direction.schema.json` в источник/узел
- [ ] Кейсы корпуса: переписать `chain_*` под новую модель + кейс T9
- [ ] Коды предупреждений ↩ (тексты уточнить: речь не о Направлении)

## Фаза 13 — документация
- [ ] `ParserConfig.md`, `WIZARD_STATE{,.ru}.md` — переписать под источник
- [ ] `upcoming.md` EN+RU — поправить формулировки

## Проверка
- [ ] `go build ./... && go vet ./...`, линт 0 issues
- [ ] Тесты модуля, корпус контракта
- [ ] Живая проверка на ядре 1.14.0-lx.27-rc.5
