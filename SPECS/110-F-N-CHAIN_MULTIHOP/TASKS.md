# TASKS 110 — Цепочка хопов (`chain`)

Нормативка: [`SPEC.md`](SPEC.md). Порядок фаз обязателен: модель → проба →
UI → валидация → контракт. Каждая фаза заканчивается зелёной сборкой.

## Фаза 0 — подтвердить открытый вопрос
- [x] Вариант (а): реализовано с деградацией. Ядро при этом успело
      обновиться — установленный бандл несёт 1.14.0-lx.27-rc.5 с
      `with_lx_chain`, а `bin/sing-box` репозитория (rc.3-dev) — без него,
      так что оба пути пробы проверяемы вживую

## Фаза 1 — модель и эмиссия
- [x] `DirectionChain{Hops, IdleTimeout, StripEvasion, Strip, Rewrite}`
- [x] `Direction.Chain *DirectionChain`, json-ключ `chain`
- [x] Эмиссия `{"type":"chain","tag":…,"outbounds":[…]}` — поле ядра
      называется `outbounds`, наше `Hops` (T3: порядок = порядок пакета)
- [x] Цепочка не получает состав селектора и парную auto-группу

## Фаза 2 — проба ядра и деградация (T1)
- [x] `ChainSupportProbe` по образцу `NaiveSupportProbe`
- [x] Без поддержки: запись не эмитится, правила на неё → `direct`,
      предупреждение в лог с версией ядра и нужным тегом
- [x] Пометка неработающей записи в списке Направлений

## Фаза 3 — UI
- [x] Тип записи в редакторе Направления: селектор | цепочка
- [x] Вкладка «Цепочка»: список позиций, `+` с выбором, перетаскивание
- [x] Подпись направления пакета над списком (T3)
- [x] Настройки: `idle_timeout`, `strip_evasion`, четыре ключа `strip`
- [x] При типе «цепочка» вкладка «Автовыбор» скрыта

## Фаза 4 — валидация (T2, T4, T5, T6, T7)
- [x] Минимум две позиции
- [x] `tls.utls` нельзя снять, если на пути узел с `reality`
- [x] Вложенная цепочка — только на позиции 0
- [x] Внутренние теги `<tag>#<i>` не попадают в цели правил
- [x] Цепочка и `detour` подписки не смешиваются

## Фаза 5 — контракт
- [x] Схема цепочки в `contract/schema/`
- [x] Кейсы `chain_*` в корпусе Направлений

## Фаза 6 — документация
- [x] `docs/ParserConfig.md` — секция про цепочку
- [x] `docs/WIZARD_STATE{,.ru}.md` — поле `chain`
- [x] `upcoming.md` EN+RU

## Проверка
- [x] `go build ./... && go vet ./...`, линт 0 issues
- [x] Тесты модуля
- [ ] Живая проверка (ядро 1.14.0-lx.27-rc.5 с `with_lx_chain` установлено)
