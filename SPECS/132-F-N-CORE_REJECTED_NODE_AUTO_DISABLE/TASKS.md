# SPEC 132 · Волны реализации

Порядок волн — порядок зависимостей. Волна не начинается, пока предыдущая не
закрыта тестами.

**Состояние 19.09.2026: волны 1–6 закрыты**, 6А частично (Save remote есть,
Deploy 422 — нет), 7–8 открыты. Что именно сделано и где живёт — SPEC.md §10;
остатки — §11 там же и волна 6А ниже.

Тестов на форматирование строк и вёрстку НЕ писать (память «Без тестов на
UI-формат»). Тесты — комплексные, по данным, в конце волны; полный прогон
один раз.

---

## Волна 1 · Разбор строки ошибки ядра — ЗАКРЫТА

Норма — `contract/docs/PARSING_PRINCIPLES.md` §9.1-9.2.

- [x] Разборщик строки — `core/corereject/parse.go`, `Parse(out, TagSet)`.
      Многострочный вывод `check` обходится построчно, привязки к началу
      строки нет (ядро заворачивает отказ в свою рамку `FATAL[0000] …`).
- [x] Индекс `<i>` читается только в диагностику (`Rejection.Index`).
- [x] Обратное отображение «финальный тег → узел состояния» — строит СБОРКА:
      `ParsedNode.CanonicalLink` → `OutboundGenerationResult.NodeLinks` →
      `stateNodeLinks`. Подробно — SPEC.md §10.3. Узлы с переменными в
      тег-политике сопоставляются нормально (карту строит эмиссия, а не
      предсказание).
- [x] Вход разборщика один на оба движка: текст `check` (classic и daemon).
      Тексты daemon'а с РЕАЛЬНОГО старта (`ApplyError.Message`,
      `ServiceStatus.ErrorMessage`) к разборщику применимы, но точка
      встраивания не сделана — остаток, SPEC.md §11.

**Тесты:** `core/corereject/parse_test.go` — таблица PARSING_PRINCIPLES §9.2 целиком плюс
эмодзи, пробелы, двоеточия, `]` в теге, `endpoint`, форма без тега, `inbound`
/ `dns` / `route`, оба кандидата `]: ` при разных тегах, многострочный вывод,
пустые входы.

---

## Волна 2 · Причина = запись `core_rejected` в `warnings[]` — ЗАКРЫТА

**Решение владельца 19.09.2026 отменило отдельное поле.** `disabled_reason` не
заводится нигде; причина живёт записью существующего списка предупреждений
узла. Обоснование и цена — SPEC.md §4.4.

- [x] Хранение: `{code: "core_rejected", params: {reason}}` в `Node.Warnings`.
      Модель не меняется вовсе — ни поля, ни структуры.
      `core/state/node_enabled.go`: `SetCoreRejected` / `CoreRejectedReason` /
      `ClearCoreRejected`.
- [x] **Одно место-правило пересчёта** — `Node.ReplaceDerivedWarnings(fresh)`
      вместо прямого присваивания списка. Переведены ВСЕ шесть точек
      пересчёта (таблица в SPEC.md §4.4): миграция при загрузке state,
      миграция masque, правка тела, regen из исходника, форма AmneziaWG,
      вставка записи. Новый авторитетный код вносится в
      `authoritativeWarningCodes` одной строкой.
- [x] Ловушка `recountOneNode`: условие «коды уже считали» было
      `Warnings != nil`, и узел с ОДНОЙ записью вердикта навсегда остался бы
      без кодов своего тела — а они и объясняют, за что ядро его не приняло.
      Заменено на `hasDerivedWarnings` (список из одних авторитетных записей
      = «не считали»).
- [x] **Вердикт привязан к ТЕЛУ** (норма PARSING_PRINCIPLES §9.4):
      `CarryCoreVerdict(old)` при refetch (то же тело — держится, другое —
      снят, узел включён обратно) и
      `RevalidateCoreVerdictAfterBodyChange(before)` в трёх точках правки
      тела. Сравнение — `state.BodiesEquivalent` (семантическое, не
      побайтовое). Оживает только узел, выключенный СТРАХОВКОЙ.
- [x] **Единый сеттер включения** `Node.SetNodeEnabled(bool)`; переведены все
      четыре точки UI, включая «включить все» drilldown'а. Включение и
      выключение рукой стирают вердикт.
- [x] Бэкап: правок не потребовалось — `warnings[]` уже едут в оба конца.
      Схема: `disabled_reason` снят из обоих `$defs`, описание `warnings[]`
      получило оговорку про авторитетность `core_rejected` (контракт 1.1.10).
- [x] Миграция: не нужна, запись аддитивная.

**Тесты:** `core/state/node_enabled_test.go` — жизненный цикл вердикта
(выключение, замещение дубля, порядок в списке, `kind=unsupported`), пересчёт
производных кодов его не стирает, смена тела снимает и включает узел, то же
тело в другом написании держит, refetch во всех трёх ветках, round-trip
`state.json` + миграция при загрузке.

**Хвост (владелец не заказывал, записано):** у ПОДПИСКИ состав в бэкап не
едет вовсе — вместо `nodes[]` экспортируется карта `disabled: {tag: 0}`
(`core/backup/convert_v7.go` ↔ `core/backup/import10.go`). Причина
выключения узла подписки через бэкап поэтому не переносится. У папок и ручных
серверов состав едет целиком, и запись переносится.

---

## Волна 3 · check кандидата ДО записи config.json — ЗАКРЫТА

Требование §5А SPEC.md. Реализация — `core/rebuild.go` Step 5 +
`core/rebuild_corereject.go`; схема порядка в SPEC.md §10.1.

- [x] Кандидат `config.json.candidate` — в каталоге `config.json`. Имя НЕ
      `.tmp`: так зовётся промежуточный файл `atomicWriteConfig`, и уборка
      одного снесла бы другой.
- [x] `check` по кандидату; чисто → `os.Rename` поверх `config.json`
      (в пределах каталога атомарен).
- [x] Не чисто и узел не назван → `config.json` НЕ заменяется, на диске
      остаётся предыдущий рабочий; диалог, `ConfigStale` остаётся,
      `ConfigBuilt{OK:false}`.
- [x] `refreshOwnTunNames` и orphan GC — строго ПОСЛЕ реальной замены.
- [x] Кандидат убирается при любом исходе.
- [x] Graceful skip: бинаря нет → `check` отдаёт `nil`, кандидат сразу
      становится `config.json`, страховка не действует.
- [x] Текст диалога переписан: «config.json was NOT replaced — the previous
      working config is still on disk» вместо прежней неправды про «Connect
      won't work until this is fixed».
- [x] Daemon: правок не потребовалось. `applyCurrentConfig` начинается с
      `rebuildConfigBeforeStart`, то есть получает кандидата и `check` даром;
      демон читает не диск, а тело запроса.
- [x] Remote — вынесен отдельной волной 6А (владелец включил его в объём
      фичи; причина переноса — нужен адаптер ЧЕРНОВИКА, см. ниже).

**Тесты:** `core/rebuild_corereject_test.go` —
`TestCoreRejectLoopChecksCandidate` (проверяется кандидат, не `config.json`),
сценарии «не заменён при отказе» и «кандидат убран» внутри
`TestCoreRejectLoopScenarios`.

---

## Волна 4 · Цикл страховки в ОБЩЕЙ функции сборки — ЗАКРЫТА

Один механизм на все входы. Живёт в `coreRejectLoop.run`
(`core/rebuild_corereject.go`), зовётся из `RebuildConfigIfDirty`.

- [x] Цикл: кандидат → `check` → назван узел → выключить + причина →
      пересобрать из ТОГО ЖЕ состояния в памяти → круг; чисто → замена.
- [x] Прерывание: ошибка не про узел / без тега / тег не сопоставлен / тот же
      тег назван повторно / круг не выключил ничего нового.
- [x] Предел `coreRejectAskAfter = 10` → колбэк
      `decide(disabled int) bool`; `true` снимает предел до конца прохода,
      `false` завершает без замены. Фоновые входы (`decide == nil`) идут
      молча до `coreRejectHardCap = 200` — **решение по умолчанию**,
      обоснование в SPEC.md §10.4. Сегодня `decide` возвращает `nil` для всех
      входов: диалог приезжает волной 5.
- [x] Колбэк хода `progress(disabled int)` объявлен (для строки §6.3), пока
      `nil`.
- [x] **Адаптер выключателя** — интерфейс `nodeDisabler`
      (`Disable/Commit/Label`). Реализация этой волны —
      `savedStateDisabler` над сохранённым state: правки копятся в памяти,
      `state.json` пишется один раз в `Commit`, независимо от исхода
      (нажатый `Stop` выключенные узлы не возвращает). Шов для черновика
      Конфигуратора оставлен — волна 6.
- [x] Итог прохода — `events.ConfigBuiltPayload.DisabledNodes`
      (`{SourceLabel, Tag, Reason}`), и при `OK:true`, и при `OK:false`.
- [x] Входы, получившие цикл БЕЗ своего кода: pre-start обоих движков, кнопка
      Rebuild, автообновление подписок, API `/action/rebuild-config`,
      пересборка после Save Конфигуратора, смена `log_level`.
- [x] Реальный старт как второй источник сигнала — **только daemon**
      (решение владельца, §5 SPEC.md): `ApplyError` 422 и
      `ServiceStatus FATAL`. Точка встраивания — `core/backend_daemon_darwin.go`
      (`applyOnce` / `retryAfterCoreFatal`), потолок захода
      `daemonRejectStartCap`. У classic второго источника НЕ будет —
      сознательное ограничение, он текста ошибки старта не видит вовсе.

**Тесты:** `core/rebuild_corereject_test.go` — таблица сценариев (чисто с
первого раза; один негодный; два негодных; ошибка не про узел; тег не
сопоставлен; форма без тега; тот же тег дважды; тег с `]: ` внутри),
`TestCoreRejectLoopLimitDialog` (`Stop` / `Keep checking`),
`TestCoreRejectLoopBackgroundHardCap`, `TestSavedStateDisablerAddressesNodes`
(узел контейнера, корневой узел, ссылка в никуда, повтор той же причины).

Граф (цепочка с выключенным хопом, опустевшая группа, detour fail-closed) —
поведение существующее, новым кодом не тронуто (§4.6 SPEC.md); отдельных
тестов не писали, есть готовые
`core/build/outbound_graph_detour_failclosed_test.go`.

---

## Волна 5 · UI: плашка, warning, «Уведомления» — ЗАКРЫТА

Реализация — `ui/core_rejected_notice.go` (все три поверхности) плюс новый
виджет `internal/fynewidget/notice_bar.go`. Готовой плашки-уведомления в
проекте не было: сообщения показывались либо модальным диалогом, либо строкой
статуса.

- [x] Плашка на вкладке Local, НАД списком узлов (§6.1 SPEC.md): число первым
      словом, до трёх имён, хвост `+%d more`, кнопка `Show`, крестик
      закрывает. Контейнер отдаётся пустым и скрытым, поэтому до первого
      выключения раскладка вкладки не меняется вовсе. Только Local:
      страховка выключает узлы ЛОКАЛЬНОГО состояния, и на Remote эта полоса
      говорила бы о чужой машине.
- [x] `Show` → отдельное окно (`Application.NewWindow`, не модальный попап)
      со списком выключенных: имя жирным, источник приглушённо, ниже текст
      ядра мелким с переносом; высота по содержимому с потолком, скролл
      `components.WrapInScrollWithGutter`. Стиль — раздела «Уведомления»
      (`internal/nodewarn/section.go`).
- [x] Warning `core_rejected` (severity error) на строке узла и в шторке —
      **уже работает без единой правки UI**: причина хранится записью
      `Node.Warnings`, а существующие поверхности (`business/node_warnings.go`,
      `tabs/preview_rows.go`, `nodewarn.Section`) читают этот список и
      расшифровывают коды по реестру. Прямое следствие решения владельца
      19.09.2026 — SPEC.md §10.5.
- [x] **Подстрока превью выключенного узла** — правка нашлась при проверке
      предыдущего пункта: `previewRowSubtitle`
      (`ui/configurator/tabs/preview_row_view.go`) отдавала выключенному узлу
      голое «off» РАНЬШЕ, чем доходила до веток с кодами, — то есть узел,
      выключенный ядром, был неотличим от выключенного рукой ровно в том
      списке, где состав источника и смотрят. Теперь у такого узла подстрока
      = `✖ <заголовок кода>` (`nodewarn.Subtitle` по `r.Warnings`, которые
      берутся из СОСТОЯНИЯ и потому есть и у строки, не дошедшей до эмиссии);
      «off» остаётся у выключенного рукой.
- [x] Все чекбоксы включения узла зовут единый сеттер `SetNodeEnabled`
      (стирает вердикт) — проверено: строка источника `source_tab.go:871`,
      окно правки `source_edit_window.go:108/113`, drilldown
      `source_folder_drilldown.go:1111` и «включить все» `:1199`.
- [x] Состояние во время цикла (§6.3 SPEC.md): `progress(disabled)` пишет
      `Checking servers… (%d disabled)` в строку статуса списка вкладки Local
      — ту же, где живут «Sing-box is stopped.» и «Pinging 5/9…». Возврат к
      обычному тексту делает следующее обновление списка, как и после
      «Pinging…».
- [x] Диалог предела (§6.2) для входов С ОКНОМ: `dialog.NewCustomConfirm`,
      `Keep checking` основная кнопка, оба абзаца отдельными `Label` с
      `TextWrapWord`. Ожидание ответа — НЕ на UI-потоке: цикл идёт в фоне,
      диалог показывается через `fyne.Do`, ответ приезжает каналом.
- [x] Фоновые входы (автообновление, debug API) и **окно в трее** остаются
      без диалога — идут молча до жёсткого потолка 200 (SPEC.md §10.4). Гейт
      стоит ВНУТРИ колбэка: цикл один на все входы и различить их не может, а
      UI может — по видимости главного окна.
- [x] Колбэки ставятся из UI (`SetCoreRejectDecider`/`SetCoreRejectProgress`
      на `AppController`, под мьютексом: ставит UI-поток, читает фоновая
      горутина сборки). nil у обоих = прежнее поведение фонового входа, то
      есть headless-сборка и тесты не меняются.
- [x] Плашка кормится `events.ConfigBuiltPayload.DisabledNodes` — и при
      `OK:true`, и при `OK:false`: цикл мог выключить несколько узлов и
      упереться в ошибку не про узел.
- [x] Кнопки «включить всё обратно» НЕТ; отдельной строки про выбранный
      пользователем сервер НЕТ (решения владельца).
- [x] Локализация §6 — `bin/locale/ru.json`, дословно из SPEC.
- [x] **Плашка перезапуск приложения НЕ переживает** — решение владельца
      19.09.2026: **не восстанавливать**. Состояние видно на самом узле
      (значок и причина `core_rejected`). `DisabledNodes` — итог прохода
      сборки, не состояние; класть его в `settings.json` не заказывали.

---

## Волна 6 · Конфигуратор: цикл на вкладке Final — ЗАКРЫТА

- [x] Цикл выполняется на последнем шаге Конфигуратора ДО разблокировки
      Save: вход в Final уже собирает конфиг для предпросмотра → кандидат →
      `check` → выключение названных → пересборка.
- [x] Save блокируется СУЩЕСТВУЮЩИМ гейтом — отдельного механизма не
      заводить: `guiState.SaveGateAllows` / `updateGlobalSaveGate`
      (`ui/configurator/tabs/final_tab.go`) уже ФУНКЦИЯ
      состояния (SPEC 115 §1), один предикат на кнопку вкладки и на
      глобальную. Цикл просто держит предикат закрытым (`running=true`).
      Статус — §6.3 SPEC.md (существующая строка хода).
- [x] **Выключение пишется в ЧЕРНОВИК** (`SetCoreRejected` через
      `draftDisabler` / `FindNodeByLink`). Commit — no-op: черновик закрепит
      Save. `setNodeEnabled` из окна правки не переиспользован.
- [x] Соблюсти контракт потоков Final: сборка в горутине, `onStart`/`onDone`
      только через `fyne.Do`; ход цикла — тоже через `fyne.Do`.
- [x] По окончании Save разблокируется; список выключенных кормит существующую
      плашку/`Show` событием `ConfigBuilt` (новых плашек не делали).
- [x] Диалог предела — тот же колбэк, что у локальной сборки.
      **`Stop` → Save остаётся ДОСТУПНОЙ** (вариант А).
- [x] После Save сборка `config.json` гоняет один `check`: узлы уже выключены
      в сохранённом state, повторного круга выключений нет. Отдельный признак
      «уже проверено» не заводили — одного check достаточно.

---

## Волна 6А · Remote-цели: тот же цикл (SPEC 097/098) — частично

**Решение владельца 19.09.2026: страховка действует на remote ТАК ЖЕ, как
локально** — узлы выключаются по вердикту `check`. Оговорка принята
сознательно: конфиг чужой машины проверяет ЛОКАЛЬНЫЙ бинарь, и его версия
может отличаться от ядра той машины («обновят»).

Зависит от волны 6: путь сохранения remote собирает из ЧЕРНОВИКА
(`BuildRemoteConfig(p.model)`), а не из сохранённого состояния, — значит
выключать надо в черновике, тем же адаптером.

- [x] `writeRemoteConfig` — «кандидат → `check` → замена» тем же циклом,
      что на Final (`runDraftRejectLoop`, `Promote` в файл машины).
- [x] Цикл с адаптером черновика (волна 6).
- [x] Локальные dirty-маркеры по-прежнему НЕ поднимаются.
- [ ] Отказ демона на `Deploy` (`ApplyError.Rejected`) — **не сделано**.
      Нужен отдельный rebuild remote-конфига из `state.json` машины
      (не из черновика визарда): большого нового конвейера в этом заходе
      нет. Локальный `check` на Save закрывает типичный случай; 422 из-за
      другой версии ядра на машине остаётся на решение владельца.

---

## Волна 7 · Локализация и доки

- [ ] `bin/locale/ru.json` — все ключи §6 SPEC.md (переводы на мне, память
      «Переводы — на мне»).
- [ ] `README.md` — что значит «выключено ядром» и как вернуть узел.
- [ ] `RELEASE_NOTES.md`.
- [ ] `contract/README.md` — строка версии контракта.

---

## Волна 8 · Полный прогон

- [ ] Один прогон затронутых пакетов целиком.
- [ ] Ревизия: закрыть SPEC 132, отметить паритет с LxBox 478 в
      `contract/TASKS_LXBOX.md`.
