# DECISIONS 103 — протокол решений программы LX Shared Contract

Формат: `D-NNN | дата | решение | кто | обоснование`. Нормативные решения
дублируются в `contract/docs/` при реализации. Решения не переписываются —
отменённое получает новую запись со ссылкой на старую.

| № | Дата | Решение | Кто | Обоснование |
|---|------|---------|-----|-------------|
| D-001 | 2026-08-18 | Унификация контрактная, не кодовая: две реализации (Go/Dart), общими делаются форматы данных, блок-схемы, тесты | Пользователь | «Работать через Go-сущность на мобиле — неправильно»; gomobile/FFI-мост отвергнут |
| D-002 | 2026-08-18 | Шаблоны унифицируются на уровне JSON-структур, не кодовой базы | Пользователь | Прямое указание |
| D-003 | 2026-08-18 | Канонический узел — JSON-конверт с `kind: outbound\|endpoint\|group` | Пользователь + план | «Учти, что бывают endpoint»; sing-box ≥1.11 |
| D-004 | 2026-08-19 | План SPEC 103 утверждён целиком, рекомендации таблицы §9 приняты как рабочие решения | Пользователь | «В целом мне нравится, я могу это утвердить. Давай сделаем» |
| D-005 | 2026-08-19 | Программа расширена: каналы в лаунчере (SPEC 104), DNS-группы (SPEC 105), архитектурное ревью по ходу, лучшие решения обоих проектов, протокол решений | Пользователь | Список конечных целей 1–11 |
| D-006 | 2026-08-19 | ECH: оба проекта вычищают `tls.ech` с warning-кодом (модель LxBox §320) | План (факт) | `with_ech` отсутствует в `LX_TAGS` ядра (`sing-box-lx/Makefile.lx:37`, `ech_tag_stub.go`) — пропущенный ech уронил бы конфиг целиком |
| D-007 | 2026-08-19 | Канонический JSON identity-хеша — без HTML/юникод-escaping (сырой UTF-8); Go выравнивается к Dart | План §9.A1 | Dart `jsonEncode` не умеет Go-escaping; менять проще Go |
| D-008 | 2026-08-19 | ws `?ed=N` без `eh` → эмитится и `early_data_header_name: "Sec-WebSocket-Protocol"` (поведение Go); Dart добавляет дефолт | План §9.A2 | Конвенция v2ray-экосистемы: ed = заголовок Sec-WebSocket-Protocol |
| D-009 | 2026-08-19 | anytls без `fp=` → дефолт `utls random` (vless-конвенция); Go добавляет | План §9.A3 | Оба проекта уже дефолтят `random` для vless — anytls выравнивается по ней |
| D-010 | 2026-08-19 | WG endpoint: дефолтные `name`/`system` не эмитятся; Go убирает | План §9.A4 | Канон без дефолтных полей |
| D-011 | 2026-08-19 | Unresolved var: модель Dart (необъявленная → плейсхолдер остаётся; объявленная null → Dropped-каскад); Go мигрирует в фазе 3 | План §9.C4 | Видимый плейсхолдер ловит опечатки; Dropped даёт пофрагментную деградацию вместо потери пресета целиком |
| D-012 | 2026-08-19 | Теги пресетов: автопрефикс `<preset_id>:<tag>` (модель Go); LxBox мигрирует с heal-редиректами | План §9.C7 | Переносимость пресетов требует детерминированных тегов |
| D-013 | 2026-08-19 | Лимиты: URI 65536 (Go поднимает), нод 3000 и тело 10 MB (Dart добавляет) | План §9.B9 | Защита от CIDR-подписок на 500+ нод и стампида сетевого стека |
| D-014 | 2026-08-19 | disabled-ноды переносятся только identity-хешем (unix seconds в бэкапе); несовпавшие истекают по TTL, пересопоставление в v1 не строится | План §7 (после критики) | Хеш необратим, nodeIdentityKey в Go отсутствует by design |
| D-015 | 2026-08-19 | Контракт живёт в `contract/` репо лаунчера; LxBox вендорит с пином `contract.lock`; вынос в отдельный репо — решение фазы 5 | План §1 | Паттерн `libbox.version` уже обкатан |
| D-016 | 2026-08-19 | Принципы решений по расхождениям §9.E (по-фикстурно, фазы 1–2): (а) ядро поддерживает → расширяем allowlist, не режем; (б) секреты в share-URI не пишем; (в) канон — без дефолтных полей; (г) деградируй ноду, не конфиг | План (фаза 0) | Обобщение уже принятых D-006…D-010 и философии обоих проектов |
| D-017 | 2026-08-19 | masque в каноне — `kind: outbound`, не endpoint | Факт кода | Оба проекта эмитят masque в `outbounds[]` (Go `GenerateNodeJSON`, ядро `outbound.Register`; Dart `MasqueSpec → Outbound`); endpoint — только wireguard |
| D-018 | 2026-08-19 | Вложенные `and`/`or` внутри предикатов `#if` — вне ядра v1, запрещены спекой | Факт кода | Ни один движок их не поддерживает (Go warn+false, Dart TemplateIfError на load) |
| D-019 | 2026-08-19 | Неоднозначная семантика URI/подписочных параметров сверяется с тем, как их парсит **Happ**; порядок арбитража: поведение ядра sing-box-lx → Happ → конвенции Xray/v2rayN | Пользователь | «Если не понятно как парсить параметры — смотри как парсит happ подписки» |
| D-020 | 2026-08-19 | Актуализация документации парсеров и правил обоих проектов — сквозной DoD каждой фазы (лаунчер: ParserConfig/DATA_FLOW/TEMPLATE_REFERENCE/WIZARD_STATE + .ru; LxBox: PROTOCOLS/TEMPLATE/STORAGE) | Пользователь | «Документацию по парсерам и правилам привести в порядок» |
| D-021 | 2026-08-19 | WG private key принимается и из query (`privatekey`/`private_key`), не только из userinfo; Go добавляет fallback по модели Dart | Прогон корпуса (фаза 1) | 21 из 49 WG-фикстур дропалась на десктопе; клиенты в дикой природе пишут ключ в query — деградировать всю ноду нельзя |
| D-022 | 2026-08-19 | WG без `allowedips` — дефолт `0.0.0.0/0,::/0`, а не отказ ноды; Go выравнивается к Dart | Прогон корпуса (фаза 1) | Отсутствие allowed_ips во всех WG-клиентах значит «весь трафик»; отказ давал разный набор нод из одной подписки |
| D-023 | 2026-08-19 | Мусорные WG-ключи (private/public/preshared невалидного base64 или длины) → нода отбрасывается с warning; канон = поведение Go, LxBox добавляет валидацию | Прогон корпуса (фаза 1) | Мусорный ключ уезжает в config.json и валит `sing-box check` целиком (класс `pbk=enabled`, v1.1.7) — принцип D-016(г): деградируй ноду, не конфиг |
| D-024 | 2026-08-19 | Длительности в sing-box-map эмитятся строго с единицей измерения (`"30s"`, модель Go `normalizeTuicHeartbeat`); LxBox чинит эмиссию `idle_session_timeout`/`idle_session_check_interval` | Прогон корпуса + эмпирика | Проверено запуском `badoption.Duration`: `"30"` → `time: missing unit in duration` → падает ВЕСЬ конфиг; голое число → `cannot unmarshal number`. Живой баг мобилки на anytls-нодах с session-полями |
| D-025 | 2026-08-19 | hysteria2 `obfs=gecko` поддержан (плюс поля `min_packet_size`/`max_packet_size` из `obfs-min/max-packet-size`); obfs без пароля — блок отбрасывается, нода живёт | Прогон корпуса (фаза 1) | Ядро форка реализует gecko (`protocol/hysteria2/outbound.go`), Go его резал зря; там же `missing obfs password` — fatal всего конфига, поэтому obfs без пароля эмитить нельзя. Правка в 3 местах: allowlist, URI-парсер, эмиттер (ловушка emitter-parser-pairing) |
| D-026 | 2026-08-19 | Plain-WG без `mtu=` в URI — поле `mtu` не эмитится вовсе (ядро само ставит 1408; при AWG — 1280). Go убрал свой дефолт 1420, LxBox убирает 1408 | Прогон корпуса + код ядра | `transport/wireguard/endpoint.go:152-159`: ядро выставляет 1408, а при AWG-junk 1280. Свой дефолт спорил с ядром и ломал identity-хеш всех WG-нод. AWG-клампы (1280) сохранены — защита от EMSGSIZE |
| D-027 | 2026-08-19 | XHTTP `xmux` остаётся реализованным (отклонение SPEC 102 R7 признано верным); LxBox добавляет его поддержку — сейчас поле только у лаунчера | Факт кода ядра | Ядро форка несёт `option/v2ray_xhttp_xmux_range.go` (тип `XmuxRange`, диапазоны «min-max») — R7 откладывал xmux «до подтверждения поддержки ядром», подтверждение получено. Тесты фикстур stream-one с полным xmux зелёные |
| D-028 | 2026-08-19 | Остаточное percent-кодирование пути снимается до срезки `?ed=`-хвоста (модель LxBox §320): `path=%2F%252Fassignment` → `//assignment`. Go добавляет `decodeResidualPercent` | Прогон корпуса + эмпирика | Проверено `net/url`: путь `/%2Fassignment` (нынешний результат Go) уезжает на сервер как `/%252Fassignment` — сервер такого пути не знает, ws-нода даёт 404 при живом TLS. Ядро кладёт путь как есть (`transport/v2raywebsocket/client.go:50-51`, `URLSetPath` = `net/url.setPath`). Панели в дикой природе кодируют путь дважды |
| D-029 | 2026-08-19 | Единая junk-политика uTLS: известный xray-псевдоним канонизируется молча; **неопознанный мусор → `chrome` + warning `utls_fp_unknown`, нода живёт** (модель LxBox §281). Go выравнивается на обоих путях: URI-путь (сейчас молча `random`) и sing-box-импорт (сейчас снимает блок utls целиком) | Прогон корпуса + код ядра | Три разных поведения на трёх путях = разные конфиги и разные identity-хеши из одной подписки. `chrome` вместо `random`: `random` в ядре выбирает отпечаток случайно на каждом старте (`common/tls/utls_client.go:355-361`) — недетерминированная маскировка там, где подписка просила конкретную; fp — клиентская маскировка, сервер её не проверяет, поэтому нода почти наверняка рабочая и выбрасывать её нельзя (D-016(г)). Дефолт «fp не задан вовсе» остаётся `random` (D-009) — это другой случай |
| D-030 | 2026-08-19 | WG-ключи канонизируются через decode→encode base64 (модель Go `normalizeWGKey`); LxBox добавляет ту же нормализацию | Прогон корпуса + эмпирика | Ядро принимает и неканоническую форму (`…ccC=` и `…ccA=` оба декодируются в 32 байта), но в конфиг они уезжают по-разному → разные identity-хеши у одной и той же ноды. Канон = каноническая форма base64; заодно это ловит мусорные ключи (D-023) |
| D-031 | 2026-08-19 | AWG-диапазоны (`h1`…`h4`) нормализуются к возрастающему порядку: `300-200` → `200-300` (модель Go); LxBox выравнивается | Прогон корпуса | Обратный порядок границ — не «другое значение», а та же пара; без нормализации одна нода даёт два разных конфига и два хеша |
| D-032 | 2026-08-19 | reality `short_id` нечётной длины или >16 символов: значение отбрасывается целиком (`""`, модель LxBox), а не обрезается | Прогон корпуса + код ядра | Ядро декодирует sid через `encoding/hex`; нечётная длина = ошибка декода = отказ всего конфига. Обрезка Go («abc» → «abc») оставляет невалидную длину — чинится на стороне Go |
| D-033 | 2026-08-19 | uTLS на QUIC-протоколах (hysteria2/tuic): блок `utls` не эмитится вовсе, даже при явном `fp=` (модель LxBox §282) | Прогон корпуса | QUIC не использует uTLS-отпечатки; `fp` на hy2/tuic — мусор подписок. Go эмитил его на URI-пути, но сам же срезал на Xray-пути — внутреннее противоречие |
| D-034 | 2026-08-19 | Валидация WG-ключей применяется на ВСЕХ путях ввода (URI, INI, sing-box-JSON), а не только на URI | Находка при починке тестов (фаза 1) | В LxBox `normalizeWGKey` звался только из URI/INI-парсеров: мусорный ключ из импортированного sing-box-конфига проходил насквозь и валил `sing-box check`. Класс той же ошибки, что D-023 — дыра в покрытии между форматами ввода |
| D-035 | 2026-08-19 | VLESS `encryption` (пост-квантовый слой, lx SPEC 032) читается из URI и эмитится; `none`/пусто = слой выключен, поле опускается | Прогон корпуса + код ядра | Ядро поддерживает поле (`option/vless.go:26`, `Encryption string`), а Go его терял: писал `encryption=none` в share-URI, но из URI не читал и в конфиг не эмитил → нода с пост-квантовым слоем работала на мобиле и не работала на десктопе. Правка в паре парсер+эмиттер |
| D-036 | 2026-08-19 | ssh `client_version` и `private_key_path` — задекларированные desktop-расширения: помечены `ext:desktop` в реестре, кейсы корпуса несут per-app override `<case>.expected.launcher.json` (CANON §7) | Прогон корпуса | Путь к файлу ключа на мобиле бессмысленен (нет файловой системы в том же виде); поля не «расхождение», а осознанная разница платформ |
| D-037 | 2026-08-19 | Dart-декодер base64 для WG-ключей — свой лояльный (`_decodeBase64Lenient`), а не `dart:convert` | Реализация D-030 | `dart:convert` отвергает неканонические padding-биты, которые Go `encoding/base64` принимает молча — без своего декодера формы вроде `…ccC=` дропались бы только на мобиле |
| D-038 | 2026-08-19 | AWG: поля masquerade (`id`/`ip`/`ib`) применяются только при отсутствии явного `i1` (модель Go `applyAWGFields`); LxBox выровнен | Прогон корпуса | Явный i1 задаёт пакет целиком — masquerade поверх него давал другой конфиг на мобиле |
| D-039 | 2026-08-19 | XHTTP placement/uplink-поля (`session_placement`, `uplink_data_placement`, `uplink_http_method`) проходят насквозь без нормализации — валидацию делает ядро | Прогон корпуса + `registry/warnings.json` (`xhttp_param_reset: go=null`) | Dart сбрасывал их с warning, Go не трогает; до закрытия SPEC 102 канон — поведение Go. Пересмотреть в фазе 2 |
| D-040 | 2026-08-19 | `normalizeRealityShortID`: проверка валидности (чётная длина И ≤16 hex) выполняется ДО обрезки | Код ядра `common/tls/reality_client.go` | Обрезка 17-символьного значения подставляла другой валидный на вид short_id, которого подписка не задавала — ядро отвергает `decodedLen > 8` и нечётную длину |
| D-041 | 2026-08-19 | WG-нода с `allowed_ips` вида `0.0.0.0/0, ::/0`, но без IPv6-адреса интерфейса — **корректна**: парсер не режет `::/0` и не отбраковывает ноду | Проверка на живой системе + код ядра | Такой конфиг выдаёт Proton и другие провайдеры (IPv6-адрес на бесплатных серверах не назначается) — это норма, а не порча. `allowed_ips` в gVisor-стеке ядром не читается вовсе (только system-режим, `transport/wireguard/device_system.go:97-100`), стек выбирается по адресу назначения — вырезание `::/0` проблему не решает, а round-trip и identity-хеш ломает. Ошибка `missing IPv6 local address` в реальной работе не возникает: IPv6 выключен на уровне TUN (нет `inet6_address`) и DNS (`prefer_ipv4`). Если IPv6 в туннеле включат — правильным местом будет `domain_resolver: {strategy: "ipv4_only"}` на эндпоинте (форма проверена `sing-box check`), но не правка `allowed_ips` |
| D-042 | 2026-08-19 | `on_change.set` перестаёт быть mobile-расширением и входит в **ядро языка шаблонов**: реализован в Go (`core/template/on_change.go`, `EvalIfScalar` в substitute.go, подключён ко всем точкам записи переменных в settings_tab.go) по модели LxBox — рекурсия по цепочке, fixpoint-guard, жёсткий предохранитель от циклов | Пользователь | «on_change сделать надо в лаунчере, мы же к паритету конфигов идём». Закрывает разрыв TEMPLATE_LANG §8.2 |
| D-043 | 2026-08-19 | IPv6 в туннеле — явная галка `ipv6_enabled` (default false) + `tun_address6`; при включении добавляется IPv6-адрес в TUN и стратегии становятся `prefer_ipv4`, при выключении — `ipv4_only`. Модель LxBox (§ipv6_enabled), перенесена в лаунчер | Пользователь | Раньше IPv6 глушился неявно в 4 местах конфига, и сайты с AAAA не открывались без объяснения. Проверено сборкой обеих веток + `sing-box check`: false → один адрес и `ipv4_only`, true → два адреса и `prefer_ipv4` |
| D-044 | 2026-08-19 | `#if` **нельзя** использовать как значение скалярного поля (`"strategy": {"#if": …}`) — только как элемент массива или как ключ среди других полей объекта (map-spread). Значение скалярного поля ведётся через `on_change` | Проверка сборкой (фаза после D-042) | Попытка `"strategy": {"#if": {"and": ["@ipv6_enabled"], "value": "@dns_strategy", "else": "ipv4_only"}}` даёт на выходе пустой объект `{}`: выбранная ветка мержится в родителя (TEMPLATE_LANG §4.3), а не подставляется вместо поля. Ограничение грамматики, а не реализации — одинаково в обоих движках. Вариант «on_change + #if как страховка» в этой форме нереализуем; защита от правки состояния мимо UI, если понадобится, делается на этапе сборки конфига |
| D-045 | 2026-08-19 | Условный ключ `#if` принимает произвольный суффикс: `#if`, `#if1`, `#if 2`, `#if tun-only`. Несколько таких ключей на одном объекте применяются последовательно в порядке сортировки. Реализовано в обоих движках | Пользователь | «в одном объекте не может быть более 1 #if… расширить синтаксис и позволить писать туда имена». Проверено: два одинаковых ключа JSON молча теряют первое условие (в обоих языках выживает последний) — суффикс снимает ограничение и даёт условию имя. Порядок сортировки байт-идентичен (`sort.Strings` ↔ `List.sort()`), проверено кросс-прогоном |
| D-046 | 2026-08-19 | **Шаблоны остаются разными в двух проектах** — унифицируется только движок и корпус, не содержимое. Секции, наборы переменных и пресеты каждого приложения свои; выравнивание имён переменных (N1–N8) снято из целей фазы 3 | Пользователь | «сами шаблоны пусть остаются разными… Шаблоны остаются разными но движок должен быть унифицирован». Шаблон описывает возможности конкретной платформы (у мобилы нет TUN-настроек десктопа и наоборот); принудительное сведение словарей дало бы фиктивную общность при разной семантике |
| D-047 | 2026-08-19 | Паритет движка шаблонов проверяется общим корпусом `contract/corpus/template/`: тройка `<case>.template.json` + `<case>.vars.json` + `<case>.expected.json`, оба движка гоняют один набор — по модели `corpus/uri/` | Пользователь | «фикстуры переменные результат должны быть на общем наборе тестов как с парсером». Корпус URI довёл паритет парсеров с 144/282 до 282/282 — расхождения находятся прогоном, а не чтением кода |
| D-048 | 2026-08-19 | **Каналы**: глубокой переделки нет — в лаунчере уже есть аналог, его переименовывают и подводят логику под модель LxBox. SPEC 104 сокращается с «ввести каналы» до «выровнять терминологию и поведение существующего механизма» | Пользователь | «не будем заниматься глубокой переделкой каналов, в лаунчере уже есть аналог их, просто переименуем, сделаем логику похожей» |
| D-049 | 2026-08-19 | **Модель пресетов канонизируется по LxBox** и мигрирует в лаунчер целиком: (а) один пресет несёт несколько route-правил + DNS-серверы + DNS-правила; (б) **базовые правила** — неотчуждаемая часть конфига (не выключаемый пользователем пресет); (в) правила несут собственные переменные пресета. Разрывы N3/N4/N6/N11 решаются в пользу формы LxBox | Пользователь | «логику пресетов берем из LxBox она там более проработанная… это всё мигрируем в лаунчер». В лаунчере пресет = плоский набор `rules`, DNS живёт отдельной секцией и переменные пресета не поддержаны — из-за этого приём, придуманный на мобиле (правило + его DNS-сервер + его переменная как один переносимый блок), на десктопе нечем выразить |
| D-050 | 2026-08-19 | **Вариант А**: базовые правила лаунчера переезжают из зашитого `config.route.rules` в неотчуждаемый пресет `traffic-processing` (sniff + hijack-dns + resolve) по модели LxBox — с флагами `locked`/`isSortable:false`, якорем позиции и re-seed при каждой сборке | Пользователь | «так что — А». Сегодня в лаунчере из трёх базовых правил есть только hijack-dns, зашитый в шаблон: пользователь его не видит и не настраивает, а sniff/resolve отсутствуют вовсе. Неотчуждаемость по модели LxBox держится re-seed'ом, а не флагом — стёртый из состояния пресет восстанавливается на следующей сборке. **Меняет `route.rules` у всех существующих пользователей** (появляются sniff и resolve) → обязательна проверка на живом конфиге и запись в release notes |
| D-051 | 2026-08-19 | В лаунчер вводится **числовая ось порядка правил** (`orderNum` в состоянии, `ui.num` как стартовое значение в шаблоне) по модели LxBox: стабильная сортировка по возрастанию, шаг 10 между шаблонными якорями, зона пользователя 1000–1100, ленивый сдвиг при drag (двигается только сплошной занятый блок до первой дырки), `isSortable:false` исключает из сдвига | Факт обхода кода + требование D-050 | В лаунчере порядка как данных нет вовсе: ни `num`, ни `orderNum`, ни `weight` — приоритет = позиция в `state.Rules[]`. Для D-050 этого недостаточно: `traffic-processing` обязан быть ПЕРВЫМ (sniff должен отработать до матчинга, иначе домен не извлечён), а позиция в массиве не защищена от drag'а. Ось даёт якоря (0 — traffic-processing, 1150 — unknown-traffic) и вставку между соседями без перенумерации списка |
| D-052 | 2026-08-19 | **Логика сложных пресетов мигрирует из LxBox в лаунчер целиком** — не только базовые правила (D-050) и ось (D-051), но и весь связанный механизм: `ref`-переменные (значение из глобальных vars), глобальные vars как fallback через putIfAbsent, `required` с Dropped-каскадом вместо выброса всего пресета, магические имена (`outbound`-override, `dns_enable`, `dns_server`-фильтр), гейты валидности фрагментов, reject-backstop, нормализация detour у DNS-групп | Пользователь | «вот собственно и логику этих сложных пресетов переносим из LxBox в лаунчер». Разрозненный перенос кусков (правила без ref-vars, ось без гейтов) даёт полурабочую модель: пресет вроде `ru-direct` опирается сразу на несколько механизмов — ref-var, фильтр серверов, даунгрейд rule_set при выключенном geoip, force-ipv4 через DNS-правило |
| D-053 | 2026-08-19 | Инварианты оси порядка, которые запрещено «упрощать» при переносе (каждый — исправленный баг LxBox, а не стиль): (а) каскад сдвига останавливается на **первой дырке**, а не двигает всех с `num >= want` — иначе якорь за сотней свободных номеров всё равно вытесняется (наблюдалось на устройстве); (б) зоны **не перенумеровываются** — иначе `num` шаблона перестаёт быть якорем и новый пресет некуда вписать между соседями; (в) дедуп по preset_id идёт **перед** seed'ом — иначе seed считает задвоенный пресет присутствующим и удаление копии не работает; (г) несортируемые исключены и из перемещения, и из сдвига | Чтение `rule_order.dart` (§370/§398) | Ось заменила более раннее решение `pinned` («всегда первый»), которое умело прибивать к началу, но не упорядочивало остальное. Все три инварианта записаны в коде LxBox с причиной — перенос без них воспроизведёт уже починенные баги |
| D-054 | 2026-08-19 | Канон §5 реализован в Go отдельной точкой входа `SubstituteVarsInJSONCanon` (`core/template/substitute_canon.go`), legacy-режимы (обычный и Strict) остаются до миграции вызывающих | Реализация C4 (фаза 3) | Preset-путь сегодня опирается на strict-семантику «unresolved → выбросить пресет целиком» (SPEC 067 фаза 8); её замена на Dropped-каскад — часть SPEC 106, и делать её одновременно со сменой движка значило бы менять две вещи разом без возможности изолировать регрессию. Движок предикатов (P1–P6) переиспользуется из legacy-обхода целиком: канон меняет политику unresolved, но не грамматику условий — своя копия дала бы два расходящихся движка |
| D-055 | 2026-08-19 | int-каст в Go выполняется по **объявленному типу** (`int`/`number`), список имён `isIntCastVar` понижен до fallback для шаблонов с непроставленным типом; не-число уезжает строкой + warning, значение вне [0,65535] клампится + warning | Прогон корпуса (фаза 3), разрыв C5 | Каст по хардкод-списку из 4 имён означал: любая НОВАЯ числовая переменная шаблона с другим именем уезжает в конфиг строкой, и ядро отвергает конфиг целиком на decode. Прежний фолбэк «не-число → 0» маскировал опечатку валидным портом/MTU — отладить было нечего |
| D-056 | 2026-08-19 | Разрывы движка на стороне Dart закрыты одним заходом: C1/N12 (bool с trim+casefold в `coerceVarValue` и в bare-предикате), C2 (`#notEmpty`/`#isEmpty` по типу значения через `_isNotEmptyTyped`), C6 (тип `text_list` + `splitTextList` + строковая форма `#in`), C8 (@var-подстановка в RHS равенства, элементах `#in`, паттерне `#matches` — `_substRhs`), N10 (warning на неизвестную `#`-директиву через хук `onTemplateWarning`) | Прогон корпуса (фаза 3) | Первое измерение Dart дало 55/65; все 10 расхождений совпали с предсказанными спекой разрывами — корпус меряет то, что задумано. После правок 65/65 на обеих сторонах, полный прогон LxBox (3414 тестов) зелёный |
| D-057 | 2026-08-19 | Warning'и движка шаблонов Dart отдаются через **глобальный хук** `onTemplateWarning`, а не параметром функций | Реализация N10 | `walk` рекурсивен и вызывается из двух движков (`build_config`, `preset_expand`); протаскивание накопителя через все уровни исказило бы их сигнатуры ради диагностики. Хук по умолчанию null — сбор не включён, поведение продакшн-пути не меняется |
| D-058 | 2026-08-19 | Разрыв C3 закрыт с обеих сторон: `#if` с обоими ключами `and`/`or` или без обоих → условие **false** + warning (Go: `selectIfBranchCanon`; Dart: `_evalCondition`); истинное условие без обязательной ветки `value` → пропуск + warning | Прогон корпуса, раздел `grammar/` | Legacy-Go на «ни and, ни or» отвечал **true** — то есть невалидное условие ВКЛЮЧАЛО ветку. Опечатка в условии втаскивала в конфиг блок, которого автор не просил, причём молча; на мобиле та же форма давала false — один шаблон, два разных конфига |
| D-059 | 2026-08-19 | Разрыв G3 закрыт: тело пресета видит **глобальные переменные шаблона** для имён, которых не объявило у себя (`ExpandPresetWithGlobals`, `ResolveRouteWithGlobals`, поле `PresetMergeContext.GlobalVars`). Локальная переменная всегда сильнее глобальной (подмешивание только при отсутствии, модель LxBox `preset_expand.dart:165-167`) | Реализация D-050 | Без этого `traffic-processing` не собирается вовсе: его правила ссылаются на `@tun`/`@enable_proxy_in`/`@resolve_strategy` — настройки со вкладки Settings, дублировать которые в каждом пресете бессмысленно. Все три уходили в unresolved → strict-режим выбрасывал пресет целиком |
| D-060 | 2026-08-19 | `filterActiveVars` применяется **только к переменным, объявленным в самом пресете**: глобали под фильтр if/if_or не попадают | Находка при реализации D-059 | Фильтр вычищал из varsMap все имена без локального объявления — глобали доходили до подстановки как «unknown var» и роняли пресет. Они не «неактивны», а объявлены в другом месте; отсутствие локального `if`/`if_or` не повод их удалять |
| D-061 | 2026-08-19 | `MergePresetsIntoRoute` больше не выходит сразу на пустом списке правил: проверяется ещё и наличие неотчуждаемых пресетов в шаблоне (`hasNonSortablePreset`) | Находка при реализации D-050 | Ранний выход `!hasAnyV6Rule(ctx.Rules)` пропускал re-seed — на чистой установке (правил нет вовсе) `traffic-processing` не появлялся, и конфиг собирался без sniff/hijack-dns/resolve |
| D-062 | 2026-08-21 | Вкладка **Target убрана в локальном режиме** визарда; `gateway_mode` и `gateway_include_interface` для локальной цели показываются на Settings (`settingsVarVisible` получил параметр isRemote). У удалённой машины вкладка остаётся — там задаются её платформа и архитектура | Пользователь | «если я про локальный шаблон то нет вкладки Target», «Gateway mode и LAN interfaces вообще-то можно перенести в Settings для локальной сборки». Выбор цели на вкладке был рудиментом: он задан точкой входа (`ShowConfigWizard` из вкладки Local главного окна vs `ShowConfigWizardForMachine` из Remote), и в коде прямо записано «таргет задаётся здесь и дальше не выбирается». Порядок объявления сохраняется: `gateway_mode` в шаблоне первым, поэтому на Settings он выше зависимых полей — дефолты резолвятся однопроходно |
| D-063 | 2026-08-21 | Подпись неактивной строки Settings приглушается (`newSettingsTitleLabelFor`) | Наблюдение на живом UI | `Disable()` у пустого Entry в тёмной теме почти не меняет вид — строка выглядела рабочей, и связь «галка → зависимое поле» не читалась (пара gateway_mode → LAN interfaces). Блокировка при этом работала верно, проблема была чисто в неразличимости состояния |
| D-064 | 2026-08-21 | Языки интерфейса сокращены до английского (вшит) и русского; восемь машинных переводов удалены из `bin/locale/` и `RemoteLanguages` | Пользователь | «давай все языки уберем пока кроме русского и англ». Каждая новая строка требовала правки в десяти файлах, качество машинных переводов никто не проверял. Вернуть язык = добавить код в список и файл в релиз |
| D-065 | 2026-08-21 | Ядро языка получает **`#enable`** — плоский гейт существования/активности носителя: `"#enable": ["@a", "@b"]` (все истинны) или с оператором первым элементом: `["or", "@a", "@b"]` / `["and", ...]`; объектная форма `{"or": [...]}` — алиас чтения. Носители: фрагменты пресета (эмит), объявления переменных (активность строки/поля), `params` (применение блока). `if`/`if_or` (Go) и `enabled:` (Dart) — легаси-алиасы чтения, бессрочно. Закрывает разрыв N6 | Пользователь | «форма if вообще плохая»: непомеченная управляющая конструкция лежала внутри тела правила вперемешку с полями sing-box (движок её вырезал перед эмиссией) — нарушение конвенции пометок; полная `#if`-обёртка отвергнута как тяжеловесная («выворачивает объект ради одного условия»). Плоская форма сохранена, имя стало помеченным и говорящим |
| D-066 | 2026-08-21 | Условия шаблона — **одна грамматика на весь язык** (`#if` и `#enable` питаются одним движком), вложенные `and`/`or` разрешены на любую глубину (снимает D-018), сахар `["@a"]` ≡ `{"and": ["@a"]}`; в UI — **реактивный пересчёт**: зависимости узла извлекаются статически при загрузке, индекс `переменная → узлы`, пересчёт только затронутых батчем после on_change, точечное обновление виджета. Контракт фиксирует семантику условий и `deps()` (корпус), доставку уведомлений — каждый UI сам. Полное ТЗ — **SPEC 107** | Пользователь | «#enable становится входом в управляющую матрицу»; «каждый узел вычисления должен знать, на изменение каких переменных он реагирует, а не на все подряд»; «мы строим реактивный движок». Форма `["or", "@a"]` из предыдущей итерации отвергнута в пользу объектной грамматики `#if`. Язык декларативен и полностью анализируем — динамический трекинг не нужен, deps берутся одним проходом по дереву |
| D-067 | 2026-08-22 | Легаси-список `if`/`if_or` принимает имя **без** ведущего `@` (`["gateway_mode"]`) — нормализация дописывает префикс сама | Регрессия при реализации SPEC 107 | Старый `EvalIf` делал `TrimPrefix`, то есть голое имя работало; движок предикатов требует канонический `@` и дал бы false. Без совместимости все гейты в шаблонах, писавшихся до канона, молча стали бы ложными, и их фрагменты исчезли из конфига. Поймано падением 4 тестов пресетов |
| D-068 | 2026-08-22 | Вычисление гейта носителя **вырезает из контекста переменные чужой платформы** (`scopeToPlatform`): объявленная для windows/linux переменная на macOS считается ложной, а не берётся из состояния | Регрессия при реализации SPEC 107 | Поведение старого `ParamBoolVarTrue` (`vars_resolve.go:480`); движок предикатов о платформах не знает. Без этого `if_or: [tun_builtin, tun]` оставлял бы строку активной на macOS из-за значения windows-переменной. Реализовано вырезанием из resolved, а не отдельной веткой в движке — одна семантика |
| D-069 | 2026-08-22 | Предикат, записанный **строкой с JSON** (`"{\"@runtime.target\":\"local\"}"`, SPEC 097), разбирается в самом движке — работает в `#enable`, в легаси `if[]` и в `deps()` одинаково | Регрессия при реализации SPEC 107 | Разбор жил только в легаси-пути `if[]`; после миграции `clash_api`/`clash_secret` на `#enable` их условие стало строкой-литералом → `deps` пуст → `VarConditionIsTargetOnly` дал false → переменные пропали с локальной цели. `VarConditionIsTargetOnly` переписан через `CondDeps` — «от чего зависит условие» отвечает одна функция для любой формы |
| D-070 | 2026-08-22 | `if` и `if_or` на одном носителе больше **не ошибка**: части объединяются через `and` | Реализация SPEC 107 §7 | Прежнее «оба заданы → всегда false» (`VarUISatisfiedFor`) молча гасило строку целиком и роняло загрузку в `params`. Объединение через and — то, что и подразумевал автор такого шаблона |
| D-071 | 2026-08-22 | Голая ссылка на список в позиции элемента массива **сплайсится** и в Dart (была только в Go) | Прогон корпуса (SPEC 107) | `["@tun_address", {"#if": …}]` давал `[["10.0.0.1/30"]]` — ядро отвергает конфиг (`cannot unmarshal array into netip.Prefix`). Та же дыра, что чинилась в Go; корпус нашёл её на мобиле |
| D-072 | 2026-08-22 | В Dart `enabled` снимается **только с верхнего уровня** rule_set-фрагмента (обе формы), вложенные объекты не трогаются | Реализация SPEC 107 §11.2 | На верхнем уровне rule_set это мета-конвенция §045 (у sing-box такого поля нет), а вложенный `tls.enabled` / `cache_file.enabled` — настоящее поле ядра. Обход дерева с удалением сломал бы конфиг |
| D-073 | 2026-08-22 | **Все ключевые слова движка помечаются `#`**: канон — `#and`, `#or`, `#value`, `#else`, `#on_change`, `#set` (наряду с уже помеченными `#if`, `#enable`, `#not`, `#in`, `#notIn`, `#matches`, `#notEmpty`, `#isEmpty`). Правило языка одной фразой: **`#` — ключевое слово движка, `@` — ссылка на переменную, всё остальное — данные.** Непомеченные формы читаются бессрочно как алиасы | Пользователь | «кажется опять неконсистентность… почему тогда and, or, value, else без #? мы не докрутили синтаксис». После введения рекурсии (D-066) в одной позиции списка `#and`/`#or` оказались три вида объектов с тремя конвенциями: `{"@tun": "true"}` (ссылка), `{"#not": …}` (оператор), `{"and": […]}` (без пометки) — различать их умел только разборщик по контексту. Помеченная форма делает первый символ ключа самодостаточным, а правило «неизвестный `#`-ключ → warning» начинает ловить опечатки вида `#adn` |
| D-074 | 2026-08-22 | `auto_route` TUN-инбаунда становится переменной шаблона (`type: bool`, `#enable: ["@tun"]`, default `true`) вместо зашитой константы — **issue #106** | Пользователь (баг из issue) | «в интерфейсе лаунчера галочки нет, при попытке изменить конфиг самому он сам перезаписывает флаг на true». Соседние `strict_route`/`stack` уже были переменными — `auto_route` просто пропустили. Строка Settings появляется автоматически: интерфейс строится из объявлений шаблона. Побочно проверено: TLS-фрагментация, которую автор issue считал ненастраиваемой, уже имеет три переменные (`tls_fragment`, `tls_record_fragment`, `tls_fragment_fallback_delay`) |
| D-075 | 2026-08-22 | Батч реактивного пересчёта = **сама изменённая переменная + цели каскада on_change**; ранний выход при пустом каскаде убран | Живой баг на UI | «ставлю ip6 не появляется адреса»: `ApplyOnChange` возвращает только цели каскада, и без явного добавления `changedName` поля, чей гейт зависит от переключателя (`tun_address6` ← `ipv6_enabled`), не пересчитывались. Ранний выход при пустом каскаде давал тот же эффект для любой переменной без `on_change` — а таких гейтов большинство (`@tun`, `@gateway_mode`). Тест `TestGateBatchIncludesChangedVarItself` |
| D-076 | 2026-08-22 | Семантика IPv6-переключателя подтверждена владельцем: **выключен → `ipv4_only`, включён → `prefer_ipv4`** (не `prefer_ipv6`) | Пользователь | «IPv6 выключен: ipv4_only; prefer_ipv4 при включённом IPv6». `prefer_ipv4` не регрессирует: dual-stack хосты идут по IPv4 как раньше, AAAA используется только там, где это единственная запись; `prefer_ipv6` давал бы таймауты на каждом дуальном домене при битой v6-связности выше по цепочке (из шести endpoint'ов IPv6-адрес только у WARP). `on_change` — разовый эффект переключения, не форс при сборке (D-043): исторически записанные стратегии в состоянии при снятом флаге не «лечатся» автоматически, пока галку не переключат |
| D-077 | 2026-08-25 | Корпус шаблонов нормирует рубеж **загрузки**: необязательное поле `load` в `<case>.expected.json` — `accept` (дефолт при отсутствии, миграция нулевая) / `reject` (валидатор обязан отвергнуть шаблон на load; рантайм-ожидания того же кейса продолжают проверяться толерантным прогоном — поле не выключает проверку рантайма) / `either` (вердикт намеренно не нормирован). Классификация: `reject` — невалидная форма условия по §4.1 (согласованная четвёрка grammar/enable + тройка D-058: `if_empty_and_is_true`, `if_missing_value_is_skipped`, `nested_empty_or_is_false`); `either` — класс «необъявленное имя» целиком (`unresolved/*` + `enable/unknown_var_is_false`, `enable/false_skips_inner_evaluation`) | Обе сессии (лаунчер + LxBox), добро пользователя | Ни один раннер не звал валидатор — load-поведение сторон расходилось молча (Dart грузил то, что Go отвергал). Go-классификация получена честным прогоном `ValidateWizardTemplate` по всем 94 кейсам: согласованные reject-кейсы совпали с фактическими вердиктами; enable-кейсы с undeclared-переменной отвергаются тем же правилом, что `unresolved/undeclared_*`, поэтому нормировать их отдельно от класса нельзя — either |
| D-078 | 2026-08-25 | Легаси-приём MASQUE снесён (контракт 0.8.0): URI-алиасы `?network=` (vhttp) и `?server_name=` (sni) больше не читаются — игнорируются как неизвестные параметры, узел живёт на канонических именах и дефолтах; плоские `network`/`sni`/`skip_cert_verify` sing-box-импорта СТРИПАЮТСЯ без переноса значений (нормализация снесена, но удаление ключей осталось: плоский `sni` рядом с `tls.server_name` роняет ядро fail-fast'ом). URI-алиасы insecure (`?skip_cert_verify=`, `?allowinsecure=`) не тронуты — другая сущность, директива их не покрывает. Фикстуры: `legacy_network_h2`/`vhttp_wins_over_network`/`canonical_legacy_network_sni` сняты, добавлены `legacy_params_ignored` и `canonical_dual_stack_sni`; parse-дефолт vhttp остаётся явным `h3` в expected (смена на auto ядра lx.28 — отдельное решение вне директивы) | Пользователь (директива через LxBox-сессию): «?network= как алиас vhttp — удаляем, больше не принимаем. ?server_name= как алиас sni — тоже» | Легаси-имена были только совместимостью с ранним диалектом WARP-подписок; `network` в masque означал противоположное `network` всех остальных схем и был deprecated ядром ещё в SPEC 062 |
| D-079 | 2026-09-02 | Бэкап: `label` у `servers[]`/`chains[]` лаунчер НЕ применяет и НЕ пишет; непустой `label`, разошедшийся с тегом, — warning `backup_label_dropped` (у сервера без `node_tag` подпись по-прежнему становится тегом). Golden `corpus/backup/chains_roundtrip.expected.json` приведён. Развилка для LxBox: `extension: mobile` у `label` с side-specific ожиданием либо снос поля в 0.12 по образцу D-0.9.0 для Направлений | Лаунчер (факт), ждёт LxBox | SPEC 112: у узла одно имя — тег; `Source.Label` в v7 — `json:"-"`, подпись умирала на первом Save молча, а golden это маскировал |
| D-080 | 2026-09-02 | Per-source идентификация подписки (`user_agent`, `hwid`, `send_hwid`, `hash_device_model`, `relays_in_directions`) в схему 0.11 не добавляется; потеря называется на экспорте warning `backup_source_identity_dropped` с перечнем заданных полей. Решение о необязательных полях в 0.12 — за LxBox (есть ли у него такая настройка) | Лаунчер (факт) | П6 «нет молчаливых потерь»; односторонние поля вернули бы тайный груз, ради сноса которого убран `extensions` (П1/П3) |
| D-081 | 2026-09-02 | Позиционный дериватив тега замены (`<N>:select`/`<N>:auto`, N — номер записи в секции `subscriptions[]`, считая с 1) — одно правило для экспорта и импорта на обеих сторонах; проверка `backup_replace_tag_derived` на экспорте лаунчера считает по нему же | Лаунчер (факт), сверка LxBox | Экспорт лаунчера считал по позиции среди ВСЕХ источников, импорт — по секции: подписка без префикса после сервера молча меняла имя группы, правила файла повисали |
| D-082 | 2026-09-02 | Бэкап, контракт 0.12: **поле `label` у `servers[]` и `chains[]` снесено из схемы** (вариант Б развилки D-079 — по образцу 0.9.0 для Направлений), решение ЗАКРЫВАЕТ D-079. Чтение оставлено legacy-входом для файлов 0.11 и раньше: у сервера без `node_tag` подпись становится тегом, разошедшаяся — warning `backup_label_dropped`; экспорт не пишет поле никогда. У `subscriptions[]` `label` ОСТАЁТСЯ — это имя источника | Лаунчер + LxBox | У канона v7 имя узла одно — тег (SPEC 112), и на него ссылаются правила, фильтры и позиции цепочек. Вариант А (`extension: mobile` + side-specific ожидание) сохранил бы у одной сущности два имени на одной стороне — ровно ту болезнь, которую 0.9.0 лечил у Направлений. У подписки такой пары нет: ссылочного тега рядом с её подписью не существует, переименовать источник можно ничего не сломав |
| D-083 | 2026-09-02 | Идентификация подписки перед провайдером едет **вложенным объектом `subscriptions[].identity`** с семью необязательными ключами (`user_agent`, `send_hwid`, `hwid`, `device_os`, `ver_os`, `device_model`, `hash_device_model`); отсутствие ключа значит «как в системе», а не «выключено» (у булевых — три состояния, потому `*bool`). Неприменённые ключи дают ОДИН код `backup_source_identity_dropped` на подписку с перечнем — код общий на оба направления и обе стороны. `relays_in_directions` в схему НЕ входит (у LxBox такой развилки нет) и уезжает кодом `backup_local_only_dropped`. Закрывает D-080 | Лаунчер + LxBox | Вложенный объект, а не плоские ключи: это одна настройка из нескольких частей, и по корню записи они расползлись бы, смешавшись с полями самого источника. Один код вместо двух — потеря для пользователя одна («подписка спросит провайдера не тем, чем спрашивала»), и разные коды заставили бы UI объяснять два раза одно. Провайдеры ВЕТВЯТ выдачу по UA: без переноса та же ссылка на новой машине отдаёт другой набор узлов |
| D-084 | 2026-09-02 | `hysteria` (v1) помечен `"extension": "desktop"` в реестре протоколов: парсер есть только у лаунчера. Раннер каждой стороны **пропускает каталоги и кейсы корпуса с чужим extension** (у кейса — `meta.extension` в `expected`) | Лаунчер + LxBox | Правило 3 контракта: расхождение либо чинится, либо помечается — бесхозных различий не бывает. Писать парсер hysteria v1 для мобильной стороны ради конформанса незачем: протокол вытеснен hysteria2, который поддержан обеими. Без пропуска по extension мобильный раннер падал бы на фикстурах протокола, которого не поддерживает |
| D-085 | 2026-09-02 | Канон кейса `body/xray/dialer_proxy` — **вложенный `chain[]` внутри узла-владельца** (CANON §2.1-2.2), порядок хопов нормативен; `label` хопа = собственный тег outbound'а-релея из конфига провайдера (предложение лаунчера, ждёт подтверждения LxBox — сегодня лаунчер копирует label владельца, правится). Материализация в отдельные служебные узлы с `detour` — модель СОСТОЯНИЯ лаунчера, вне контракта разбора. Второй кейс `dialer_proxy_missing`: недостижимая цель `dialerProxy` роняет владельца в отбраковку ЦЕЛИКОМ, а не даёт узел с прямым путём. Ограничение кейсов — один хоп, socks | Лаунчер + LxBox | Корпус тел проверяет РАЗБОР, а канонический узел уже несёт хопы вложенным `chain[]` — так его строят раннеры обеих сторон; `detour`/`service` это понятия состояния и из канона вырезаются. Side-specific ожидание тут не нужно: форма у сторон общая. Прямой путь вместо недостижимого релея — молчаливая подмена маршрута там, где прямой путь как раз зарезан |
| D-086 | 2026-09-02 | Подпись записи подписки для дедупа = **каноническая эмиссия узла без тега и без detour + идентичность пути дозвона** (подпись релея, рекурсивно по хопам). Записи с разной подписью — разные узлы; схлопываются только байтовые дубли с одинаковым путём дозвона. Две записи на один сервер с разным SNI/транспортом — разные узлы. LxBox расширяет свой ключ P4 (`protocol\|server\|port\|credential`) до полной эмиссии + путь дозвона | Лаунчер + LxBox | BYPASS-запись — другой МАРШРУТ, а не резерв: схлопывание = молчаливая потеря там, где прямой путь зарезан (кейс Liberty, хотфикс v1.5.3). Ключ по четвёрке подключения не видит ни транспорта, ни пути дозвона и склеивал бы разные маршруты в один |
| D-087 | 2026-09-02 | Папка = **контейнер одиночных серверов у обеих сторон**, отдельной секции в бэкапе НЕТ: запись `servers[]` носит необязательное поле `folder` (имя папки), и обе стороны собирают контейнер по этому имени. Нормативны порядок членов (порядок записей файла) и «одно имя = одна папка»; `subscriptions[]`/`chains[]` поля `folder` не получают. Настройки самой папки (`tag_policy`, `replace`, `detour`) и члены не-server в файл не едут. Введён код `backup_local_only_dropped` — «настройки своей стороны, которым в схеме дома нет», один на оба направления и обе стороны (у LxBox им же помечаются `import_rules`, настройки папки, политика detour источника). Словарь кодов бэкапа вынесен в `registry/backup_warnings.json` | Лаунчер + LxBox | П1/П3: объявленное поле вместо тайного груза. Папка не несёт собственных данных, кроме имени, поэтому вторая секция потребовала бы держать два места в согласии; сборка по имени даёт обеим сторонам один результат. Прежде папка целиком объявлялась неподдержанной и её СОСТАВ в файл не попадал — та самая молчаливая потеря узлов, из-за которой файл был негоден к восстановлению |
| D-088 | 2026-09-02 | Запись отбраковки в конверте корпуса — `{ref, code, reason}`. Нормативны `ref` (что именно отвергнуто) и `code` (машинная причина из `registry/warnings.json`); `reason` — человеческий текст стороны, раннеры его **не сравнивают** (при `-update` пишется текущий текст). `code` необязателен: в старых URI-golden его нет, там нормативен только `ref`, коды им заводятся отдельным проходом. Заведён код `dialer_proxy_unusable` (severity `error`, params `target`/`cause`; `cause ∈ not_found \| service_outbound \| group \| not_convertible \| cycle \| depth`) — лаунчер сегодня даёт `not_found`, LxBox все шесть | Лаунчер + LxBox | Тексты причин у сторон свои: у Go это формат `fmt.Errorf`, у Dart свой, — побайтовая нормативность заставила бы LxBox копировать формат ошибки Go в свои сообщения пользователю. Пары `ref`+`code` достаточно, чтобы контракт отвечал на оба вопроса: ЧТО отвергнуто и ПОЧЕМУ; человеческая формулировка — дело UI каждой стороны |
| D-089 | 2026-09-02 | **ЗАКРЫТО решением D-095 (импорт = слияние у обеих сторон).** Было: Режим импорта бэкапа расходится с записанной нормой: `docs/BACKUP.md` §9 говорит «режим один — replace», и лаунчер так и делает (разделы источников, правил, DNS и warp замещаются целиком), а LxBox сегодня **сливает по URL подписки** — локальные источники, которых нет в файле, остаются; совпавшая по URL подписка получает настройки файла (identity, имя, префикс, интервал), отметки disabled объединяются; правила и DNS замещаются. Вариант 1 — переписать §9 как «replace у лаунчера, слияние у LxBox», пометив режим `extension: mobile`; вариант 2 — перевести LxBox на replace. До решения норма §9 НЕ меняется, факт записан в §9 пометкой «ждёт решения владельца» | LxBox (факт) + Лаунчер | Расхождение поведения при одном и том же файле: у пользователя с обоими приложениями «восстановить из бэкапа» даёт разный итог — на мобиле остаётся то, чего в файле нет. Молча оставить нельзя (правило 3: расхождение либо чинится, либо помечается), но и выбрать за владельца нельзя — это вопрос модели, а не дефект: слияние сохраняет локально заведённые источники, replace даёт воспроизводимое состояние «файл = истина» |
| D-090 | 2026-09-02 | By-design различие (класс A, расхождение СОХРАНЯЕТСЯ): **у LxBox безымянных узлов не бывает** — парсер синтезирует тег `<scheme>-<host>-<port>` и для URI без `#`-фрагмента, и для пустого `tag` в JSON-теле, поэтому такие узлы имеют идентичность (`docs/IDENTITY.md` §1) и выключаются поштучно. У лаунчера безымянный узел подписки получает тег по своему правилу, поэтому **отметки выключения безымянных узлов между сторонами не переносятся**; именованных узлов различие не касается. Записано в `docs/IDENTITY.md` §4 рядом с различием по `nodeIdentityKey` | LxBox + Лаунчер | Идентичность = тег (SPEC 112), а тег безымянного узла каждая сторона синтезирует своим правилом — общего ключа у такого узла просто нет, и выравнивание правил синтеза стоило бы сброса отметок у всех уже живущих безымянных узлов на одной из сторон. Цена расхождения ограничена и известна: теряются отметки только у узлов, которым провайдер не дал имени |
| D-091 | 2026-09-02 | Поля MASQUE-аккаунта `warp[].sni` и `warp[].idle_timeout` **объявлены в `schema/backup.schema.json` с пометкой `extension: mobile`** (в самой схеме механизма пометки нет — она стоит словами «extension: mobile» в `description` поля и колонкой «Поддержка» в `docs/BACKUP.md` §2). Семантика у сторон разная: у LxBox оба значения хранятся НА аккаунте, у лаунчера на аккаунте их нет — SNI он берёт из пула на сборке конфига, `idle_timeout` ставит UI. Лаунчер приехавшие значения не применяет, но провозит сырым JSON (раздел `warp[]` он переносит как есть). Этим закрыт последний карман `extensions.lxbox` | LxBox + Лаунчер | Пометить «обе стороны» невозможно — поле значит у сторон разное и у лаунчера дома на аккаунте не имеет; оставить его в теневом кармане `extensions.lxbox` тоже нельзя: механизм `extensions` упразднён вместе с обязанностью провозить чужой блоб (П1 «файл = сериализация состояния», П3 «непонятое отбрасывается»). Остаётся третий путь — объявить поле в схеме и назвать сторону: бесхозных различий не бывает (правило 3 `contract/README.md`) |
| D-092 | 2026-09-02 | Общее правило `docs/BACKUP.md` §1: **поле, объявленное в схеме с пометкой `extension` чужой стороны, принимающая сторона игнорирует МОЛЧА** — это не «непонятое» (П3), а чужое объявленное, и предупреждение давало бы шум на каждом импорте. Провоз таких полей насквозь (до следующего экспорта) допустим, но не обязателен. Правило действует только на поля, которые схема называет прямо: незнакомый ключ, которого в схеме нет, по-прежнему даёт warning (П3/П6) | Лаунчер (ждёт молчаливого согласия LxBox) | Без этого правила D-091 конфликтует с П3/П6: каждый импорт мобильного файла давал бы warning на `sni`/`idle_timeout` — предупреждение о том, что работает штатно и потерей не является. П6 требует не молчать о ПОТЕРЯХ; здесь потери нет: поле объявлено, сторона названа, поведение задокументировано в §2 |
| D-093 | 2026-09-02 | Ещё три поля WARP-аккаунта — `warp[].keep_alive` (MASQUE, период keep-alive, строка длительности как `idle_timeout`), `warp[].endpoint` (wg-аккаунт, `host:port`) и `warp[].awg` (wg-аккаунт, объект параметров AmneziaWG, состав открытый) — **объявлены в `schema/backup.schema.json` с пометкой `extension: mobile`** по правилу D-091 (пометка словами в `description` плюс колонка «Поддержка» в `docs/BACKUP.md` §2). Семантика у сторон разная: у LxBox все три хранятся НА аккаунте, у лаунчера на аккаунте их нет — `endpoint` и параметры AWG он берёт из пула `warp_endpoints.json` на сборке конфига, keep-alive ставит UI. Лаунчер приехавшие значения не применяет (молча, D-092), но провозит сырым JSON. `endpoint` LxBox пишет только если он не дефолтный. Этим карман `extensions.lxbox` закрыт целиком | LxBox + Лаунчер | Тот же выбор, что в D-091, и тем же способом: «обе стороны» не поставить — у лаунчера этих значений на аккаунте нет вовсе, они приходят из пула на сборке и из UI; вернуть их в теневой карман `extensions.lxbox` нельзя, механизм `extensions` упразднён вместе с обязанностью провозить чужой блоб (П1/П3). Остаётся объявить поле и назвать сторону — бесхозных различий не бывает (правило 3 `contract/README.md`) |
| D-094 | 2026-09-02 | **Поле `label` у `directions[]` и `chains[]` остаётся у LxBox**: он подпись Направления и цепочки пишет и читает, лаунчер её игнорирует МОЛЧА — имя у него одно, тег (SPEC 112), — и на экспорте не пишет. Поле объявлено в схемах (`direction.schema.json` — необязательное `label` при `additionalProperties:false`, чтобы запись LxBox проходила валидацию; `backup.schema.json` — у `chains[]` и у `$defs.direction`). Предупреждений об этих полях нет ни у одной стороны: `backup_label_dropped` остаётся ровно за `servers[]` (legacy-вход, подпись, разошедшаяся с тегом), а `directions[].label` перестал быть неизвестным ключом и `backup_unknown_field` не даёт. Решение **перекрывает D-082 в части `chains[]`** и встречную правку 0.9.0 (снос `label` у Направлений) **для LxBox**; `servers[].label` не тронут. Вторая часть решения: **термин «extension» для ПОЛЕЙ схемы бэкапа отменён как мёртвый**. Новая формулировка правила `docs/BACKUP.md` §1 — «поле объявлено в схеме; каждая сторона применяет то, что умеет, остальное молча игнорирует и по возможности провозит сырым JSON; кто что применяет — колонка «Поддержка» (лаунчер / LxBox / обе)». D-091 и D-092 остаются в силе ПО СУЩЕСТВУ, их «extension: mobile» читать как «Поддержка: LxBox». Маркер `extension` в `registry/protocols/*.json` и `meta.extension` в корпусе не тронуты: это механизм пропуска кейсов раннером, другая граница. Side-specific golden `corpus/backup/chains_roundtrip.expected.lxbox.json` (класс различия B по `docs/IDENTITY.md` §4a) | Владелец | У LxBox подпись Направления и цепочки — живая часть модели и интерфейса, и снос её ради «одного имени» стоил бы там реальной функции, тогда как проблема 0.9.0 (пользователь видит в списке одно имя, а в выпадашке целей другое) — проблема ЛАУНЧЕРА, где второе имя и заводили. Объявленное поле решает обе задачи разом: LxBox своё имя сохраняет, лаунчер называет Направление тегом и чужую подпись молча пропускает, а схема не отвергает файл LxBox. Слово «extension» при этом только мешало: механизм `extensions` упразднён (П1/П3), и пометка полем с тем же корнем читалась как воскресший карман — разговор идёт про то, КТО поле применяет, а это ровно колонка «Поддержка», которая в таблицах уже есть |
| D-095 | 2026-09-02 | **Импорт бэкапа — СЛИЯНИЕ по идентичности для ВСЕХ источников; лаунчер повторяет логику LxBox.** Владелец: «хочу, чтобы лаунчер работал как LxBox, у LxBox более здоровая логика». Норма одна на обе стороны, записана единым списком в `docs/BACKUP.md` §9: (1) подписки — по `url` БАЙТ В БАЙТ без нормализации; совпавшая держит локальные `id`, состав узлов и историю обновлений, настройки берёт из файла (`label` применяется только непустой, `identity` — слепком целиком, и отсутствие объекта = сброс на «как в системе»), отметки `disabled` ОБЪЕДИНЯЮТСЯ; (2) одиночные серверы без `folder` — дедуп по ТЕЛУ, тег в сравнении не участвует, и тело берётся из ИСХОДНИКА записи, а не из материализованного outbound'а (у живущей записи материализованное тело уже есть, у приехавшего uri-сервера ещё нет — сравнение одного с другим не совпало бы никогда и удваивало сервер на каждом импорте): `uri` после TrimSpace и БЕЗ фрагмента `#имя` (имя узла телом не является), `config_json` — в канонической форме identity-хэша (D-007: сняты верхнеуровневые `tag` и `detour`, ключи отсортированы рекурсивно, без пробелов, сырой UTF-8 без HTML/юникод-экранирования); совпало → пропуск БЕЗ warning, иначе тег уникализируется суффиксом `-2`; (3) серверы с `folder` — имя папки сравнивается КАК ЕСТЬ, без trim и с учётом регистра, члены дедупятся по телу внутри папки, новые в конец; (4) цепочки и Направления — по тегу, совпадение ТОЧНОЕ с учётом регистра (теги ядра регистрозависимы), пространство общее с тегами замен, занятый не перезаписывается; (5) DNS — слияние «СВОЁ СИЛЬНЕЕ», а не замена: серверы по `kind`+`tag`, dns-правила по `kind`+`ref`+телу, совпавшие остаются локальными, новые в конец, `dns.final`/`dns.strategy` замещаются; `vars` — только переносимые имена (`backup_var_skipped` на прочие), `route.final` — замещает только при известной цели (иначе `backup_final_dropped`); (6) `warp[]` — добавление, дубли по ключу аккаунта (`device_id`, иначе приватный ключ) пропускаются молча; (7) `rules[]` — ЕДИНСТВЕННАЯ секция полной замены; (8) порядок: совпавшие держат локальную позицию, новые в конец в порядке файла; `id` в файле необязателен — совпавшая держит локальный, новая берёт из файла, если он есть и не занят, иначе свежий ULID. Режима «заменить всё» нет, флага режима нет. **Перекрывает D-089** (закрыт этим решением) **и часть 0.11.0 про replace** (строка сшита пометкой «уточнено в 0.12.5»). Контракт поднят до 0.12.5; конвенция корпуса `<case>.pre.backup.json`; кейс `merge_sources_by_url` | Владелец | Расхождение поведения при одном и том же файле было недопустимо: у пользователя с обоими приложениями «восстановить из бэкапа» давало разный итог. Из двух моделей выбрана та, что не уничтожает работу пользователя: replace ради воспроизводимого «файл = истина» стирал источники и резолверы, заведённые на этой машине и в файл не попавшие, — цену платил пользователь, а не формат. Слияние даёт обычное состояние (П1 не нарушен), а идентичность у каждого вида записи выбрана та, по которой две стороны узнают одно и то же: адрес подписки, тело сервера без его имени, тег ссылочной сущности |
